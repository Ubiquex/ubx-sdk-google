# Code generated by `ubx sdk gen` from google_dataproc_dataproc_autoscaling_policy. DO NOT EDIT.
from __future__ import annotations

import dataclasses
from typing import Any

import ubx_sdk as ubx

@dataclasses.dataclass
class DataprocAutoscalingPolicy_BasicAlgorithm_SparkStandaloneConfig:
    # Required. Timeout for Spark graceful decommissioning of spark workers. Specifies the duration to wait for spark worker to complete spark decommissioning tasks before forcefully removing workers. Only applicable to downscaling operations.Bounds: 0s, 1d.
    graceful_decommission_timeout: Any = None
    # Optional. Remove only idle workers when scaling down cluster
    remove_only_idle_workers: Any = None
    # Required. Fraction of required executors to remove from Spark Serverless clusters. A scale-down factor of 1.0 will result in scaling down so that there are no more executors for the Spark Job.(more aggressive scaling). A scale-down factor closer to 0 will result in a smaller magnitude of scaling donw (less aggressive scaling).Bounds: 0.0, 1.0.
    scale_down_factor: Any = None
    # Optional. Minimum scale-down threshold as a fraction of total cluster size before scaling occurs. For example, in a 20-worker cluster, a threshold of 0.1 means the autoscaler must recommend at least a 2 worker scale-down for the cluster to scale. A threshold of 0 means the autoscaler will scale down on any recommended change.Bounds: 0.0, 1.0. Default: 0.0.
    scale_down_min_worker_fraction: Any = None
    # Required. Fraction of required workers to add to Spark Standalone clusters. A scale-up factor of 1.0 will result in scaling up so that there are no more required workers for the Spark Job (more aggressive scaling). A scale-up factor closer to 0 will result in a smaller magnitude of scaling up (less aggressive scaling).Bounds: 0.0, 1.0.
    scale_up_factor: Any = None
    # Optional. Minimum scale-up threshold as a fraction of total cluster size before scaling occurs. For example, in a 20-worker cluster, a threshold of 0.1 means the autoscaler must recommend at least a 2-worker scale-up for the cluster to scale. A threshold of 0 means the autoscaler will scale up on any recommended change.Bounds: 0.0, 1.0. Default: 0.0.
    scale_up_min_worker_fraction: Any = None

@dataclasses.dataclass
class DataprocAutoscalingPolicy_BasicAlgorithm_YarnConfig:
    # Required. Timeout for YARN graceful decommissioning of Node Managers. Specifies the duration to wait for jobs to complete before forcefully removing workers (and potentially interrupting jobs). Only applicable to downscaling operations.Bounds: 0s, 1d.
    graceful_decommission_timeout: Any = None
    # Required. Fraction of average YARN pending memory in the last cooldown period for which to remove workers. A scale-down factor of 1 will result in scaling down so that there is no available memory remaining after the update (more aggressive scaling). A scale-down factor of 0 disables removing workers, which can be beneficial for autoscaling a single job. See How autoscaling works (https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/autoscaling#how_autoscaling_works) for more information.Bounds: 0.0, 1.0.
    scale_down_factor: Any = None
    # Optional. Minimum scale-down threshold as a fraction of total cluster size before scaling occurs. For example, in a 20-worker cluster, a threshold of 0.1 means the autoscaler must recommend at least a 2 worker scale-down for the cluster to scale. A threshold of 0 means the autoscaler will scale down on any recommended change.Bounds: 0.0, 1.0. Default: 0.0.
    scale_down_min_worker_fraction: Any = None
    # Required. Fraction of average YARN pending memory in the last cooldown period for which to add workers. A scale-up factor of 1.0 will result in scaling up so that there is no pending memory remaining after the update (more aggressive scaling). A scale-up factor closer to 0 will result in a smaller magnitude of scaling up (less aggressive scaling). See How autoscaling works (https://cloud.google.com/dataproc/docs/concepts/configuring-clusters/autoscaling#how_autoscaling_works) for more information.Bounds: 0.0, 1.0.
    scale_up_factor: Any = None
    # Optional. Minimum scale-up threshold as a fraction of total cluster size before scaling occurs. For example, in a 20-worker cluster, a threshold of 0.1 means the autoscaler must recommend at least a 2-worker scale-up for the cluster to scale. A threshold of 0 means the autoscaler will scale up on any recommended change.Bounds: 0.0, 1.0. Default: 0.0.
    scale_up_min_worker_fraction: Any = None

@dataclasses.dataclass
class DataprocAutoscalingPolicy_BasicAlgorithm:
    # Optional. Duration between scaling events. A scaling period starts after the update operation from the previous event has completed.Bounds: 2m, 1d. Default: 2m.
    cooldown_period: Any = None
    # Basic autoscaling configurations for Spark Standalone.
    spark_standalone_config: Any = None
    # Basic autoscaling configurations for YARN.
    yarn_config: Any = None

@dataclasses.dataclass
class DataprocAutoscalingPolicy_SecondaryWorkerConfig:
    # Required. Maximum number of instances for this group. Required for primary workers. Note that by default, clusters will not use secondary workers. Required for secondary workers if the minimum secondary instances is set.Primary workers - Bounds: [min_instances, ). Secondary workers - Bounds: [min_instances, ). Default: 0.
    max_instances: Any = None
    # Optional. Minimum number of instances for this group.Primary workers - Bounds: 2, max_instances. Default: 2. Secondary workers - Bounds: 0, max_instances. Default: 0.
    min_instances: Any = None
    # Optional. Weight for the instance group, which is used to determine the fraction of total workers in the cluster from this instance group. For example, if primary workers have weight 2, and secondary workers have weight 1, the cluster will have approximately 2 primary workers for each secondary worker.The cluster may not reach the specified balance if constrained by min/max bounds or other autoscaling settings. For example, if max_instances for secondary workers is 0, then only primary workers will be added. The cluster can also be out of balance when created.If weight is not set on any instance group, the cluster will default to equal weight for all groups: the cluster will attempt to maintain an equal number of workers in each group within the configured size bounds for each group. If weight is set for one group only, the cluster will default to zero weight on the unset group. For example if weight is set only on primary workers, the cluster will use primary workers only and no secondary workers.
    weight: Any = None

_DataprocAutoscalingPolicy_BasicAlgorithm_SparkStandaloneConfigFields = {
    "graceful_decommission_timeout": ubx.FieldSpec(wire_name="graceful_decommission_timeout"),
    "remove_only_idle_workers": ubx.FieldSpec(wire_name="remove_only_idle_workers"),
    "scale_down_factor": ubx.FieldSpec(wire_name="scale_down_factor"),
    "scale_down_min_worker_fraction": ubx.FieldSpec(wire_name="scale_down_min_worker_fraction"),
    "scale_up_factor": ubx.FieldSpec(wire_name="scale_up_factor"),
    "scale_up_min_worker_fraction": ubx.FieldSpec(wire_name="scale_up_min_worker_fraction"),
}

_DataprocAutoscalingPolicy_BasicAlgorithm_YarnConfigFields = {
    "graceful_decommission_timeout": ubx.FieldSpec(wire_name="graceful_decommission_timeout"),
    "scale_down_factor": ubx.FieldSpec(wire_name="scale_down_factor"),
    "scale_down_min_worker_fraction": ubx.FieldSpec(wire_name="scale_down_min_worker_fraction"),
    "scale_up_factor": ubx.FieldSpec(wire_name="scale_up_factor"),
    "scale_up_min_worker_fraction": ubx.FieldSpec(wire_name="scale_up_min_worker_fraction"),
}

_DataprocAutoscalingPolicy_BasicAlgorithmFields = {
    "cooldown_period": ubx.FieldSpec(wire_name="cooldown_period"),
    "spark_standalone_config": ubx.FieldSpec(
        wire_name="spark_standalone_config",
        kind="object",
        fields=_DataprocAutoscalingPolicy_BasicAlgorithm_SparkStandaloneConfigFields,
    ),
    "yarn_config": ubx.FieldSpec(
        wire_name="yarn_config",
        kind="object",
        fields=_DataprocAutoscalingPolicy_BasicAlgorithm_YarnConfigFields,
    ),
}

_DataprocAutoscalingPolicy_SecondaryWorkerConfigFields = {
    "max_instances": ubx.FieldSpec(wire_name="max_instances"),
    "min_instances": ubx.FieldSpec(wire_name="min_instances"),
    "weight": ubx.FieldSpec(wire_name="weight"),
}

@dataclasses.dataclass
class DataprocAutoscalingPolicyConfig:
    # Basic algorithm for autoscaling.
    basic_algorithm: Any = None
    # Optional. The type of the clusters for which this autoscaling policy is to be configured.
    cluster_type: Any = None
    # Required. The policy id.The id must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), and hyphens (-). Cannot begin or end with underscore or hyphen. Must consist of between 3 and 50 characters.
    id: Any = None
    # Optional. The labels to associate with this autoscaling policy. Label keys must contain 1 to 63 characters, and must conform to RFC 1035 (https://www.ietf.org/rfc/rfc1035.txt). Label values may be empty, but, if present, must contain 1 to 63 characters, and must conform to RFC 1035 (https://www.ietf.org/rfc/rfc1035.txt). No more than 32 labels can be associated with an autoscaling policy.
    labels: Any = None
    # Output only. The "resource name" of the autoscaling policy, as described in https://cloud.google.com/apis/design/resource_names. For projects.regions.autoscalingPolicies, the resource name of the policy has the following format: projects/{project_id}/regions/{region}/autoscalingPolicies/{policy_id} For projects.locations.autoscalingPolicies, the resource name of the policy has the following format: projects/{project_id}/locations/{location}/autoscalingPolicies/{policy_id}
    name: Any = None
    # Configuration for the size bounds of an instance group, including its proportional size to other groups.
    secondary_worker_config: Any = None
    # Configuration for the size bounds of an instance group, including its proportional size to other groups.
    worker_config: Any = None

@dataclasses.dataclass
class DataprocAutoscalingPolicyAttrs:
    # Basic algorithm for autoscaling.
    basic_algorithm: Any = None
    # Optional. The type of the clusters for which this autoscaling policy is to be configured.
    cluster_type: Any = None
    # Required. The policy id.The id must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), and hyphens (-). Cannot begin or end with underscore or hyphen. Must consist of between 3 and 50 characters.
    id: Any = None
    # Optional. The labels to associate with this autoscaling policy. Label keys must contain 1 to 63 characters, and must conform to RFC 1035 (https://www.ietf.org/rfc/rfc1035.txt). Label values may be empty, but, if present, must contain 1 to 63 characters, and must conform to RFC 1035 (https://www.ietf.org/rfc/rfc1035.txt). No more than 32 labels can be associated with an autoscaling policy.
    labels: Any = None
    # Output only. The "resource name" of the autoscaling policy, as described in https://cloud.google.com/apis/design/resource_names. For projects.regions.autoscalingPolicies, the resource name of the policy has the following format: projects/{project_id}/regions/{region}/autoscalingPolicies/{policy_id} For projects.locations.autoscalingPolicies, the resource name of the policy has the following format: projects/{project_id}/locations/{location}/autoscalingPolicies/{policy_id}
    name: Any = None
    # Configuration for the size bounds of an instance group, including its proportional size to other groups.
    secondary_worker_config: Any = None
    # Configuration for the size bounds of an instance group, including its proportional size to other groups.
    worker_config: Any = None

DataprocAutoscalingPolicy = ubx.ResourceBinding(
    wire_type="google_dataproc_dataproc_autoscaling_policy",
    fields={
        "basic_algorithm": ubx.FieldSpec(
            wire_name="basic_algorithm",
            kind="object",
            fields=_DataprocAutoscalingPolicy_BasicAlgorithmFields,
        ),
        "cluster_type": ubx.FieldSpec(wire_name="cluster_type"),
        "id": ubx.FieldSpec(wire_name="id"),
        "labels": ubx.FieldSpec(wire_name="labels"),
        "name": ubx.FieldSpec(wire_name="name"),
        "secondary_worker_config": ubx.FieldSpec(
            wire_name="secondary_worker_config",
            kind="object",
            fields=_DataprocAutoscalingPolicy_SecondaryWorkerConfigFields,
        ),
        "worker_config": ubx.FieldSpec(
            wire_name="worker_config",
            kind="object",
            fields=_DataprocAutoscalingPolicy_SecondaryWorkerConfigFields,
        ),
    },
)
