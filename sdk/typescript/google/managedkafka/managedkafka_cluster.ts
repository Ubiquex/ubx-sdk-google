// Code generated by `ubx sdk gen` from google_managedkafka_managedkafka_cluster. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface ManagedkafkaCluster_BrokerDetails {
  brokerIndex?: string | Computed<string>;
  nodeId?: string | Computed<string>;
  rack?: string | Computed<string>;
}

export interface ManagedkafkaCluster_CapacityConfig {
  /** Required. The memory to provision for the cluster in bytes. The CPU:memory ratio (vCPU:GiB) must be between 1:1 and 1:8. Minimum: 3221225472 (3 GiB). */
  memoryBytes?: string | Computed<string>;
  /** Required. The number of vCPUs to provision for the cluster. Minimum: 3. */
  vcpuCount?: string | Computed<string>;
}

export interface ManagedkafkaCluster_GcpConfig_AccessConfig_NetworkConfigs {
  subnet?: string | Computed<string>;
}

export interface ManagedkafkaCluster_GcpConfig_AccessConfig {
  /** Required. Virtual Private Cloud (VPC) networks that must be granted direct access to the Kafka cluster. Minimum of 1 network is required. Maximum 10 networks can be specified. */
  networkConfigs?: ManagedkafkaCluster_GcpConfig_AccessConfig_NetworkConfigs[] | Computed<ManagedkafkaCluster_GcpConfig_AccessConfig_NetworkConfigs[]>;
}

export interface ManagedkafkaCluster_GcpConfig {
  /** The configuration of access to the Kafka cluster. */
  accessConfig?: ManagedkafkaCluster_GcpConfig_AccessConfig | Computed<ManagedkafkaCluster_GcpConfig_AccessConfig>;
  /** Optional. Immutable. The Cloud KMS Key name to use for encryption. The key must be located in the same region as the cluster and cannot be changed. Structured like: projects/{project}/locations/{location}/keyRings/{key_ring}/cryptoKeys/{crypto_key}. */
  kmsKey?: string | Computed<string>;
}

export interface ManagedkafkaCluster_RebalanceConfig {
  /** Optional. The rebalance behavior for the cluster. When not specified, defaults to `NO_REBALANCE`. */
  mode?: string | Computed<string>;
}

export interface ManagedkafkaCluster_TlsConfig_TrustConfig_CasConfigs {
  caPool?: string | Computed<string>;
}

export interface ManagedkafkaCluster_TlsConfig_TrustConfig {
  /** Optional. Configuration for the Google Certificate Authority Service. Maximum 10. */
  casConfigs?: ManagedkafkaCluster_TlsConfig_TrustConfig_CasConfigs[] | Computed<ManagedkafkaCluster_TlsConfig_TrustConfig_CasConfigs[]>;
}

export interface ManagedkafkaCluster_TlsConfig {
  /** Optional. A list of rules for mapping from SSL principal names to short names. These are applied in order by Kafka. Refer to the Apache Kafka documentation for `ssl.principal.mapping.rules` for the precise formatting details and syntax. Example: "RULE:^CN=(.*?),OU=ServiceUsers.*$/$1@example.com/,DEFAULT" This is a static Kafka broker configuration. Setting or modifying this field will trigger a rolling restart of the Kafka brokers to apply the change. An empty string means no rules are applied (Kafka default). */
  sslPrincipalMappingRules?: string | Computed<string>;
  /** Sources of CA certificates to install in the broker's truststore. */
  trustConfig?: ManagedkafkaCluster_TlsConfig_TrustConfig | Computed<ManagedkafkaCluster_TlsConfig_TrustConfig>;
}

export interface ManagedkafkaCluster_UpdateOptions {
  /** Optional. If true, allows an update operation that increases the total vCPU and/or memory allocation of the cluster to significantly decrease the per-broker vCPU and/or memory allocation. This can result in reduced performance and availability. By default, the update operation will fail if an upscale request results in a vCPU or memory allocation for the brokers that is smaller than 90% of the current broker size. */
  allowBrokerDownscaleOnClusterUpscale?: boolean | Computed<boolean>;
}

const ManagedkafkaCluster_BrokerDetailsFields: FieldMap = {
  brokerIndex: "broker_index",
  nodeId: "node_id",
  rack: "rack",
};

const ManagedkafkaCluster_CapacityConfigFields: FieldMap = {
  memoryBytes: "memory_bytes",
  vcpuCount: "vcpu_count",
};

const ManagedkafkaCluster_GcpConfig_AccessConfig_NetworkConfigsFields: FieldMap = {
  subnet: "subnet",
};

const ManagedkafkaCluster_GcpConfig_AccessConfigFields: FieldMap = {
  networkConfigs: {
    wireName: "network_configs",
    kind: "list",
    fields: ManagedkafkaCluster_GcpConfig_AccessConfig_NetworkConfigsFields,
  },
};

const ManagedkafkaCluster_GcpConfigFields: FieldMap = {
  accessConfig: {
    wireName: "access_config",
    kind: "object",
    fields: ManagedkafkaCluster_GcpConfig_AccessConfigFields,
  },
  kmsKey: "kms_key",
};

const ManagedkafkaCluster_RebalanceConfigFields: FieldMap = {
  mode: "mode",
};

const ManagedkafkaCluster_TlsConfig_TrustConfig_CasConfigsFields: FieldMap = {
  caPool: "ca_pool",
};

const ManagedkafkaCluster_TlsConfig_TrustConfigFields: FieldMap = {
  casConfigs: {
    wireName: "cas_configs",
    kind: "list",
    fields: ManagedkafkaCluster_TlsConfig_TrustConfig_CasConfigsFields,
  },
};

const ManagedkafkaCluster_TlsConfigFields: FieldMap = {
  sslPrincipalMappingRules: "ssl_principal_mapping_rules",
  trustConfig: {
    wireName: "trust_config",
    kind: "object",
    fields: ManagedkafkaCluster_TlsConfig_TrustConfigFields,
  },
};

const ManagedkafkaCluster_UpdateOptionsFields: FieldMap = {
  allowBrokerDownscaleOnClusterUpscale: "allow_broker_downscale_on_cluster_upscale",
};

export interface ManagedkafkaClusterConfig {
  /** Output only. Only populated when FULL view is requested. Details of each broker in the cluster. */
  brokerDetails?: ManagedkafkaCluster_BrokerDetails[] | Computed<ManagedkafkaCluster_BrokerDetails[]>;
  /** A capacity configuration of a Kafka cluster. */
  capacityConfig?: ManagedkafkaCluster_CapacityConfig | Computed<ManagedkafkaCluster_CapacityConfig>;
  /** Output only. The time when the cluster was created. */
  createTime?: string | Computed<string>;
  /** Configuration properties for a Kafka cluster deployed to Google Cloud Platform. */
  gcpConfig?: ManagedkafkaCluster_GcpConfig | Computed<ManagedkafkaCluster_GcpConfig>;
  /** Output only. Only populated when FULL view is requested. The Kafka version of the cluster. */
  kafkaVersion?: string | Computed<string>;
  /** Optional. Labels as key value pairs. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Identifier. The name of the cluster. Structured like: projects/{project_number}/locations/{location}/clusters/{cluster_id} */
  name?: string | Computed<string>;
  /** Defines rebalancing behavior of a Kafka cluster. */
  rebalanceConfig?: ManagedkafkaCluster_RebalanceConfig | Computed<ManagedkafkaCluster_RebalanceConfig>;
  /** Output only. Reserved for future use. */
  satisfiesPzi?: boolean | Computed<boolean>;
  /** Output only. Reserved for future use. */
  satisfiesPzs?: boolean | Computed<boolean>;
  /** Output only. The current state of the cluster. */
  state?: string | Computed<string>;
  /** The TLS configuration for the Kafka cluster. */
  tlsConfig?: ManagedkafkaCluster_TlsConfig | Computed<ManagedkafkaCluster_TlsConfig>;
  /** UpdateOptions specifies options that influence how a cluster update is applied. These options control the behavior of the update process, rather than defining the desired end-state of a cluster. */
  updateOptions?: ManagedkafkaCluster_UpdateOptions | Computed<ManagedkafkaCluster_UpdateOptions>;
  /** Output only. The time when the cluster was last updated. */
  updateTime?: string | Computed<string>;
}

export interface ManagedkafkaClusterAttrs {
  /** Output only. Only populated when FULL view is requested. Details of each broker in the cluster. */
  brokerDetails: ManagedkafkaCluster_BrokerDetails[];
  /** A capacity configuration of a Kafka cluster. */
  capacityConfig: ManagedkafkaCluster_CapacityConfig;
  /** Output only. The time when the cluster was created. */
  createTime: string;
  /** Configuration properties for a Kafka cluster deployed to Google Cloud Platform. */
  gcpConfig: ManagedkafkaCluster_GcpConfig;
  /** Output only. Only populated when FULL view is requested. The Kafka version of the cluster. */
  kafkaVersion: string;
  /** Optional. Labels as key value pairs. */
  labels: Record<string, string>;
  /** Identifier. The name of the cluster. Structured like: projects/{project_number}/locations/{location}/clusters/{cluster_id} */
  name: string;
  /** Defines rebalancing behavior of a Kafka cluster. */
  rebalanceConfig: ManagedkafkaCluster_RebalanceConfig;
  /** Output only. Reserved for future use. */
  satisfiesPzi: boolean;
  /** Output only. Reserved for future use. */
  satisfiesPzs: boolean;
  /** Output only. The current state of the cluster. */
  state: string;
  /** The TLS configuration for the Kafka cluster. */
  tlsConfig: ManagedkafkaCluster_TlsConfig;
  /** UpdateOptions specifies options that influence how a cluster update is applied. These options control the behavior of the update process, rather than defining the desired end-state of a cluster. */
  updateOptions: ManagedkafkaCluster_UpdateOptions;
  /** Output only. The time when the cluster was last updated. */
  updateTime: string;
}

export const ManagedkafkaCluster: ResourceBinding<ManagedkafkaClusterConfig, ManagedkafkaClusterAttrs> = {
  wireType: "google_managedkafka_managedkafka_cluster",
  fields: {
    brokerDetails: {
      wireName: "broker_details",
      kind: "list",
      fields: ManagedkafkaCluster_BrokerDetailsFields,
    },
    capacityConfig: {
      wireName: "capacity_config",
      kind: "object",
      fields: ManagedkafkaCluster_CapacityConfigFields,
    },
    createTime: "create_time",
    gcpConfig: {
      wireName: "gcp_config",
      kind: "object",
      fields: ManagedkafkaCluster_GcpConfigFields,
    },
    kafkaVersion: "kafka_version",
    labels: "labels",
    name: "name",
    rebalanceConfig: {
      wireName: "rebalance_config",
      kind: "object",
      fields: ManagedkafkaCluster_RebalanceConfigFields,
    },
    satisfiesPzi: "satisfies_pzi",
    satisfiesPzs: "satisfies_pzs",
    state: "state",
    tlsConfig: {
      wireName: "tls_config",
      kind: "object",
      fields: ManagedkafkaCluster_TlsConfigFields,
    },
    updateOptions: {
      wireName: "update_options",
      kind: "object",
      fields: ManagedkafkaCluster_UpdateOptionsFields,
    },
    updateTime: "update_time",
  },
};
