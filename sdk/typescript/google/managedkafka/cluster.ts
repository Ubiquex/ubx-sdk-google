// Code generated by `ubx sdk gen` from google_managedkafka_cluster. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Cluster_BrokerCapacityConfig {
  /** Optional. The disk to provision for each broker in Gibibytes. Minimum: 100 GiB. */
  diskSizeGib?: string | Computed<string>;
}

export interface Cluster_BrokerDetails {
  /** Output only. This broker's own index within the cluster. (AI-inferred) */
  brokerIndex?: string | Computed<string>;
  /** Output only. This broker's own Kafka node ID. (AI-inferred) */
  nodeId?: string | Computed<string>;
  /** Output only. The availability zone (rack) this broker runs in, used for rack-aware replica placement. (AI-inferred) */
  rack?: string | Computed<string>;
}

export interface Cluster_CapacityConfig {
  /** Required. The memory to provision for the cluster in bytes. The CPU:memory ratio (vCPU:GiB) must be between 1:1 and 1:8. Minimum: 3221225472 (3 GiB). */
  memoryBytes?: string | Computed<string>;
  /** Required. The number of vCPUs to provision for the cluster. Minimum: 3. */
  vcpuCount?: string | Computed<string>;
}

export interface Cluster_EffectiveCapacityConfig {
  /** Output only. The number of brokers in the cluster. */
  brokerCount?: string | Computed<string>;
  /** Output only. The disk assigned to each broker in Gibibytes. */
  brokerDiskSizeGib?: string | Computed<string>;
}

export interface Cluster_GcpConfig_AccessConfig_NetworkConfigs {
  /** The subnet this cluster is reachable from. (AI-inferred) */
  subnet?: string | Computed<string>;
}

export interface Cluster_GcpConfig_AccessConfig_PublicClusterConfig {
  /** Required. The list of IPv4 ranges in CIDR notation that are allowed to connect to the public Kafka broker endpoints. The Kafka cluster should only be exposed to trusted external ranges. A maximum of 500 IP ranges can be specified and no single range can be larger than a `/16`. This field is required if PublicClusterConfig is specified. */
  allowedSourceIpRanges?: string[] | Computed<string[]>;
}

export interface Cluster_GcpConfig_AccessConfig {
  /** Required. Virtual Private Cloud (VPC) networks that must be granted direct access to the Kafka cluster. Minimum of 1 network is required. Maximum 10 networks can be specified. */
  networkConfigs?: Cluster_GcpConfig_AccessConfig_NetworkConfigs[] | Computed<Cluster_GcpConfig_AccessConfig_NetworkConfigs[]>;
  /** The configuration for a public Kafka cluster */
  publicClusterConfig?: Cluster_GcpConfig_AccessConfig_PublicClusterConfig | Computed<Cluster_GcpConfig_AccessConfig_PublicClusterConfig>;
}

export interface Cluster_GcpConfig {
  /** The configuration of access to the Kafka cluster. */
  accessConfig?: Cluster_GcpConfig_AccessConfig | Computed<Cluster_GcpConfig_AccessConfig>;
  /** Optional. Immutable. The Cloud KMS Key name to use for encryption. The key must be located in the same region as the cluster and cannot be changed. Structured like: projects/{project}/locations/{location}/keyRings/{key_ring}/cryptoKeys/{crypto_key}. */
  kmsKey?: string | Computed<string>;
}

export interface Cluster_PublicClusterDetails {
  /** Output only. DNS discovery records that resolve to all of the external IP addresses associated with the public cluster. Used for configuring DNS-based egress firewall rules to a public cluster. discovery_dns_record can be added to this list if the cluster is scaled up. Must configure DNS based firewalls to resolve ALL DNS records in this list as large clusters have IP addresses sharded across records. Each record contains a maximum of 30 IP addresses. */
  discoveryDnsRecords?: string[] | Computed<string[]>;
  /** Output only. All of the external IP addresses associated with the public cluster used for configuring egress firewall rules to a public cluster. external_ip_address can be added to this list if the cluster is scaled up. */
  externalIpAddresses?: string[] | Computed<string[]>;
}

export interface Cluster_RebalanceConfig {
  /** Optional. The rebalance behavior for the cluster. When not specified, defaults to `NO_REBALANCE`. */
  mode?: string | Computed<string>;
}

export interface Cluster_TlsConfig_TrustConfig_CasConfigs {
  /** The Certificate Authority Service CA pool trusted for validating client TLS certificates. (AI-inferred) */
  caPool?: string | Computed<string>;
}

export interface Cluster_TlsConfig_TrustConfig {
  /** Optional. Configuration for the Google Certificate Authority Service. Maximum 10. */
  casConfigs?: Cluster_TlsConfig_TrustConfig_CasConfigs[] | Computed<Cluster_TlsConfig_TrustConfig_CasConfigs[]>;
}

export interface Cluster_TlsConfig {
  /** Optional. A list of rules for mapping from SSL principal names to short names. These are applied in order by Kafka. Refer to the Apache Kafka documentation for `ssl.principal.mapping.rules` for the precise formatting details and syntax. Example: "RULE:^CN=(.*?),OU=ServiceUsers.*$/$1@example.com/,DEFAULT" This is a static Kafka broker configuration. Setting or modifying this field will trigger a rolling restart of the Kafka brokers to apply the change. An empty string means no rules are applied (Kafka default). */
  sslPrincipalMappingRules?: string | Computed<string>;
  /** Sources of CA certificates to install in the broker's truststore. */
  trustConfig?: Cluster_TlsConfig_TrustConfig | Computed<Cluster_TlsConfig_TrustConfig>;
}

export interface Cluster_UpdateOptions {
  /** Optional. If true, allows an update operation that increases the total vCPU and/or memory allocation of the cluster to significantly decrease the per-broker vCPU and/or memory allocation. This can result in reduced performance and availability. By default, the update operation will fail if an upscale request results in a vCPU or memory allocation for the brokers that is smaller than 90% of the current broker size. */
  allowBrokerDownscaleOnClusterUpscale?: boolean | Computed<boolean>;
}

const Cluster_BrokerCapacityConfigFields: FieldMap = {
  diskSizeGib: "disk_size_gib",
};

const Cluster_CapacityConfigFields: FieldMap = {
  memoryBytes: "memory_bytes",
  vcpuCount: "vcpu_count",
};

const Cluster_EffectiveCapacityConfigFields: FieldMap = {
  brokerCount: "broker_count",
  brokerDiskSizeGib: "broker_disk_size_gib",
};

const Cluster_GcpConfig_AccessConfig_NetworkConfigsFields: FieldMap = {
  subnet: "subnet",
};

const Cluster_GcpConfig_AccessConfig_PublicClusterConfigFields: FieldMap = {
  allowedSourceIpRanges: "allowed_source_ip_ranges",
};

const Cluster_GcpConfig_AccessConfigFields: FieldMap = {
  networkConfigs: {
    wireName: "network_configs",
    kind: "list",
    fields: Cluster_GcpConfig_AccessConfig_NetworkConfigsFields,
  },
  publicClusterConfig: {
    wireName: "public_cluster_config",
    kind: "object",
    fields: Cluster_GcpConfig_AccessConfig_PublicClusterConfigFields,
  },
};

const Cluster_GcpConfigFields: FieldMap = {
  accessConfig: {
    wireName: "access_config",
    kind: "object",
    fields: Cluster_GcpConfig_AccessConfigFields,
  },
  kmsKey: "kms_key",
};

const Cluster_PublicClusterDetailsFields: FieldMap = {
  discoveryDnsRecords: "discovery_dns_records",
  externalIpAddresses: "external_ip_addresses",
};

const Cluster_RebalanceConfigFields: FieldMap = {
  mode: "mode",
};

const Cluster_TlsConfig_TrustConfig_CasConfigsFields: FieldMap = {
  caPool: "ca_pool",
};

const Cluster_TlsConfig_TrustConfigFields: FieldMap = {
  casConfigs: {
    wireName: "cas_configs",
    kind: "list",
    fields: Cluster_TlsConfig_TrustConfig_CasConfigsFields,
  },
};

const Cluster_TlsConfigFields: FieldMap = {
  sslPrincipalMappingRules: "ssl_principal_mapping_rules",
  trustConfig: {
    wireName: "trust_config",
    kind: "object",
    fields: Cluster_TlsConfig_TrustConfigFields,
  },
};

const Cluster_UpdateOptionsFields: FieldMap = {
  allowBrokerDownscaleOnClusterUpscale: "allow_broker_downscale_on_cluster_upscale",
};

export interface ClusterConfig {
  /** Capacity configuration at a per-broker level within the Kafka cluster. The config will be appled to each broker in the cluster. */
  brokerCapacityConfig?: Cluster_BrokerCapacityConfig | Computed<Cluster_BrokerCapacityConfig>;
  /** A capacity configuration of a Kafka cluster. */
  capacityConfig?: Cluster_CapacityConfig | Computed<Cluster_CapacityConfig>;
  /** Describes the effective capacity configuration of a Kafka cluster, both cluster-wide and per-broker. */
  effectiveCapacityConfig?: Cluster_EffectiveCapacityConfig | Computed<Cluster_EffectiveCapacityConfig>;
  /** Configuration properties for a Kafka cluster deployed to Google Cloud Platform. */
  gcpConfig?: Cluster_GcpConfig | Computed<Cluster_GcpConfig>;
  /** Optional. Labels as key value pairs. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Identifier. The name of the cluster. Structured like: projects/{project_number}/locations/{location}/clusters/{cluster_id} */
  name?: string | Computed<string>;
  /** Details of the public cluster feature for the Kafka cluster. */
  publicClusterDetails?: Cluster_PublicClusterDetails | Computed<Cluster_PublicClusterDetails>;
  /** Defines rebalancing behavior of a Kafka cluster. */
  rebalanceConfig?: Cluster_RebalanceConfig | Computed<Cluster_RebalanceConfig>;
  /** The TLS configuration for the Kafka cluster. */
  tlsConfig?: Cluster_TlsConfig | Computed<Cluster_TlsConfig>;
  /** UpdateOptions specifies options that influence how a cluster update is applied. These options control the behavior of the update process, rather than defining the desired end-state of a cluster. */
  updateOptions?: Cluster_UpdateOptions | Computed<Cluster_UpdateOptions>;
}

export interface ClusterAttrs {
  /** Output only. The bootstrap address of the Kafka cluster. The returned address format is: `bootstrap-...managedkafka.s.cloud.goog` or `bootstrap...managedkafka..cloud.goog` (legacy format). ## Examples: `bootstrap-nol2mecj8p94jhx2ge2rg54579a.c0aad26f.europe-west1.managedkafka.s.cloud.goog` - `bootstrap.my-cluster.us-central1.managedkafka.my-project.cloud.goog` The port number is omitted so clients can connect to their target listener (for example, `:9092` for TLS or `:9094` for mTLS). */
  bootstrapAddress: string;
  /** Capacity configuration at a per-broker level within the Kafka cluster. The config will be appled to each broker in the cluster. */
  brokerCapacityConfig: Cluster_BrokerCapacityConfig;
  /** Output only. Only populated when FULL view is requested. Details of each broker in the cluster. */
  brokerDetails: Cluster_BrokerDetails[];
  /** A capacity configuration of a Kafka cluster. */
  capacityConfig: Cluster_CapacityConfig;
  /** Output only. The time when the cluster was created. */
  createTime: string;
  /** Describes the effective capacity configuration of a Kafka cluster, both cluster-wide and per-broker. */
  effectiveCapacityConfig: Cluster_EffectiveCapacityConfig;
  /** Configuration properties for a Kafka cluster deployed to Google Cloud Platform. */
  gcpConfig: Cluster_GcpConfig;
  /** Output only. Only populated when FULL view is requested. The Kafka version of the cluster. */
  kafkaVersion: string;
  /** Optional. Labels as key value pairs. */
  labels: Record<string, string>;
  /** Identifier. The name of the cluster. Structured like: projects/{project_number}/locations/{location}/clusters/{cluster_id} */
  name: string;
  /** Details of the public cluster feature for the Kafka cluster. */
  publicClusterDetails: Cluster_PublicClusterDetails;
  /** Defines rebalancing behavior of a Kafka cluster. */
  rebalanceConfig: Cluster_RebalanceConfig;
  /** Output only. Reserved for future use. */
  satisfiesPzi: boolean;
  /** Output only. Reserved for future use. */
  satisfiesPzs: boolean;
  /** Output only. The current state of the cluster. */
  state: string;
  /** The TLS configuration for the Kafka cluster. */
  tlsConfig: Cluster_TlsConfig;
  /** UpdateOptions specifies options that influence how a cluster update is applied. These options control the behavior of the update process, rather than defining the desired end-state of a cluster. */
  updateOptions: Cluster_UpdateOptions;
  /** Output only. The time when the cluster was last updated. */
  updateTime: string;
}

export const Cluster: ResourceBinding<ClusterConfig, ClusterAttrs> = {
  wireType: "google_managedkafka_cluster",
  fields: {
    brokerCapacityConfig: {
      wireName: "broker_capacity_config",
      kind: "object",
      fields: Cluster_BrokerCapacityConfigFields,
    },
    capacityConfig: {
      wireName: "capacity_config",
      kind: "object",
      fields: Cluster_CapacityConfigFields,
    },
    effectiveCapacityConfig: {
      wireName: "effective_capacity_config",
      kind: "object",
      fields: Cluster_EffectiveCapacityConfigFields,
    },
    gcpConfig: {
      wireName: "gcp_config",
      kind: "object",
      fields: Cluster_GcpConfigFields,
    },
    labels: "labels",
    name: "name",
    publicClusterDetails: {
      wireName: "public_cluster_details",
      kind: "object",
      fields: Cluster_PublicClusterDetailsFields,
    },
    rebalanceConfig: {
      wireName: "rebalance_config",
      kind: "object",
      fields: Cluster_RebalanceConfigFields,
    },
    tlsConfig: {
      wireName: "tls_config",
      kind: "object",
      fields: Cluster_TlsConfigFields,
    },
    updateOptions: {
      wireName: "update_options",
      kind: "object",
      fields: Cluster_UpdateOptionsFields,
    },
  },
};
