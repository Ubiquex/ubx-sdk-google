// Code generated by `ubx sdk gen` from google_managedkafka_managedkafka_schema_registry. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface ManagedkafkaSchemaRegistry_SchemaRegistry {
  /** Output only. The contexts of the schema registry instance. */
  contexts?: string[] | Computed<string[]>;
  /** Identifier. The name of the schema registry instance. Structured like: `projects/{project}/locations/{location}/schemaRegistries/{schema_registry}` The instance name {schema_registry} can contain the following: * Up to 255 characters. * Letters (uppercase or lowercase), numbers, and underscores. */
  name?: string | Computed<string>;
}

const ManagedkafkaSchemaRegistry_SchemaRegistryFields: FieldMap = {
  contexts: "contexts",
  name: "name",
};

export interface ManagedkafkaSchemaRegistryConfig {
  /** SchemaRegistry is a schema registry instance. */
  schemaRegistry?: ManagedkafkaSchemaRegistry_SchemaRegistry | Computed<ManagedkafkaSchemaRegistry_SchemaRegistry>;
  /** Required. The schema registry instance ID to use for this schema registry. The ID must contain only letters (a-z, A-Z), numbers (0-9), and underscores (-). The maximum length is 63 characters. The ID must not start with a number. */
  schemaRegistryId?: string | Computed<string>;
}

export interface ManagedkafkaSchemaRegistryAttrs {
  /** Output only. The contexts of the schema registry instance. */
  contexts: string[];
  /** Identifier. The name of the schema registry instance. Structured like: `projects/{project}/locations/{location}/schemaRegistries/{schema_registry}` The instance name {schema_registry} can contain the following: * Up to 255 characters. * Letters (uppercase or lowercase), numbers, and underscores. */
  name: string;
  /** SchemaRegistry is a schema registry instance. */
  schemaRegistry: ManagedkafkaSchemaRegistry_SchemaRegistry;
  /** Required. The schema registry instance ID to use for this schema registry. The ID must contain only letters (a-z, A-Z), numbers (0-9), and underscores (-). The maximum length is 63 characters. The ID must not start with a number. */
  schemaRegistryId: string;
}

export const ManagedkafkaSchemaRegistry: ResourceBinding<ManagedkafkaSchemaRegistryConfig, ManagedkafkaSchemaRegistryAttrs> = {
  wireType: "google_managedkafka_managedkafka_schema_registry",
  fields: {
    schemaRegistry: {
      wireName: "schema_registry",
      kind: "object",
      fields: ManagedkafkaSchemaRegistry_SchemaRegistryFields,
    },
    schemaRegistryId: "schema_registry_id",
  },
};
