// Code generated by `ubx sdk gen` from google_workloadmanager_evaluation. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Evaluation_BigQueryDestination {
  /** Optional. Determines if a new results table will be created when an Execution is created. */
  createNewResultsTable?: boolean | Computed<boolean>;
  /** Optional. Destination dataset to save evaluation results. */
  destinationDataset?: string | Computed<string>;
}

export interface Evaluation_ResourceFilter_GceInstanceFilter {
  /** If non-empty, only Compute Engine instances associated with at least one of the provided service accounts will be included in the evaluation. */
  serviceAccounts?: string[] | Computed<string[]>;
}

export interface Evaluation_ResourceFilter {
  /** A filter for matching Compute Engine instances. */
  gceInstanceFilter?: Evaluation_ResourceFilter_GceInstanceFilter | Computed<Evaluation_ResourceFilter_GceInstanceFilter>;
  /** Labels to filter resources by. Each key-value pair in the map must exist on the resource for it to be included (e.g. VM instance labels). For example, specifying `{ "env": "prod", "database": "nosql" }` will only include resources that have labels `env=prod` and `database=nosql`. */
  inclusionLabels?: Record<string, string> | Computed<Record<string, string>>;
  /** The pattern to filter resources by their id For example, a pattern of ".*prod-cluster.*" will match all resources that contain "prod-cluster" in their ID. */
  resourceIdPatterns?: string[] | Computed<string[]>;
  /** The scopes of evaluation resource. Format: * `projects/{project_id}` * `folders/{folder_id}` * `organizations/{organization_id}` */
  scopes?: string[] | Computed<string[]>;
}

export interface Evaluation_ResourceStatus {
  /** State of the Evaluation resource. */
  state?: string | Computed<string>;
}

const Evaluation_BigQueryDestinationFields: FieldMap = {
  createNewResultsTable: "create_new_results_table",
  destinationDataset: "destination_dataset",
};

const Evaluation_ResourceFilter_GceInstanceFilterFields: FieldMap = {
  serviceAccounts: "service_accounts",
};

const Evaluation_ResourceFilterFields: FieldMap = {
  gceInstanceFilter: {
    wireName: "gce_instance_filter",
    kind: "object",
    fields: Evaluation_ResourceFilter_GceInstanceFilterFields,
  },
  inclusionLabels: "inclusion_labels",
  resourceIdPatterns: "resource_id_patterns",
  scopes: "scopes",
};

const Evaluation_ResourceStatusFields: FieldMap = {
  state: "state",
};

export interface EvaluationConfig {
  /** BigQuery destination for evaluation results. */
  bigQueryDestination?: Evaluation_BigQueryDestination | Computed<Evaluation_BigQueryDestination>;
  /** Output only. [Output only] Create time stamp. */
  createTime?: string | Computed<string>;
  /** The Cloud Storage bucket name for custom rules. */
  customRulesBucket?: string | Computed<string>;
  /** Description of the Evaluation. */
  description?: string | Computed<string>;
  /** Evaluation type. */
  evaluationType?: string | Computed<string>;
  /** Optional. Immutable. Customer-managed encryption key name, in the format projects/* /locations/* /keyRings/* /cryptoKeys/*. The key will be used for CMEK encryption of the evaluation resource. */
  kmsKey?: string | Computed<string>;
  /** Labels as key value pairs. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Name of resource that has the form `projects/{project_id}/locations/{location_id}/evaluations/{evaluation_id}`. */
  name?: string | Computed<string>;
  /** Resource filter for an evaluation defining the scope of resources to be evaluated. */
  resourceFilter?: Evaluation_ResourceFilter | Computed<Evaluation_ResourceFilter>;
  /** The lifecycle status of an Evaluation resource. */
  resourceStatus?: Evaluation_ResourceStatus | Computed<Evaluation_ResourceStatus>;
  /** The names of the rules used for this evaluation. */
  ruleNames?: string[] | Computed<string[]>;
  /** Crontab format schedule for scheduled evaluation, currently only supports the following fixed schedules: * `0 * /1 * * *` # Hourly * `0 * /6 * * *` # Every 6 hours * `0 * /12 * * *` # Every 12 hours * `0 0 * /1 * *` # Daily * `0 0 * /7 * *` # Weekly * `0 0 * /14 * *` # Every 14 days * `0 0 1 * /1 *` # Monthly */
  schedule?: string | Computed<string>;
  /** Output only. [Output only] Update time stamp. */
  updateTime?: string | Computed<string>;
}

export interface EvaluationAttrs {
  /** BigQuery destination for evaluation results. */
  bigQueryDestination: Evaluation_BigQueryDestination;
  /** Output only. [Output only] Create time stamp. */
  createTime: string;
  /** The Cloud Storage bucket name for custom rules. */
  customRulesBucket: string;
  /** Description of the Evaluation. */
  description: string;
  /** Evaluation type. */
  evaluationType: string;
  /** Optional. Immutable. Customer-managed encryption key name, in the format projects/* /locations/* /keyRings/* /cryptoKeys/*. The key will be used for CMEK encryption of the evaluation resource. */
  kmsKey: string;
  /** Labels as key value pairs. */
  labels: Record<string, string>;
  /** Name of resource that has the form `projects/{project_id}/locations/{location_id}/evaluations/{evaluation_id}`. */
  name: string;
  /** Resource filter for an evaluation defining the scope of resources to be evaluated. */
  resourceFilter: Evaluation_ResourceFilter;
  /** The lifecycle status of an Evaluation resource. */
  resourceStatus: Evaluation_ResourceStatus;
  /** The names of the rules used for this evaluation. */
  ruleNames: string[];
  /** Crontab format schedule for scheduled evaluation, currently only supports the following fixed schedules: * `0 * /1 * * *` # Hourly * `0 * /6 * * *` # Every 6 hours * `0 * /12 * * *` # Every 12 hours * `0 0 * /1 * *` # Daily * `0 0 * /7 * *` # Weekly * `0 0 * /14 * *` # Every 14 days * `0 0 1 * /1 *` # Monthly */
  schedule: string;
  /** Output only. [Output only] Update time stamp. */
  updateTime: string;
}

export const Evaluation: ResourceBinding<EvaluationConfig, EvaluationAttrs> = {
  wireType: "google_workloadmanager_evaluation",
  fields: {
    bigQueryDestination: {
      wireName: "big_query_destination",
      kind: "object",
      fields: Evaluation_BigQueryDestinationFields,
    },
    createTime: "create_time",
    customRulesBucket: "custom_rules_bucket",
    description: "description",
    evaluationType: "evaluation_type",
    kmsKey: "kms_key",
    labels: "labels",
    name: "name",
    resourceFilter: {
      wireName: "resource_filter",
      kind: "object",
      fields: Evaluation_ResourceFilterFields,
    },
    resourceStatus: {
      wireName: "resource_status",
      kind: "object",
      fields: Evaluation_ResourceStatusFields,
    },
    ruleNames: "rule_names",
    schedule: "schedule",
    updateTime: "update_time",
  },
};
