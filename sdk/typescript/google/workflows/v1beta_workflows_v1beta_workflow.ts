// Code generated by `ubx sdk gen` from google_workflows_v1beta_workflows_v1beta_workflow. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1betaWorkflowsV1betaWorkflowConfig {
  /** Output only. The timestamp of when the workflow was created. */
  createTime?: string | Computed<string>;
  /** Description of the workflow provided by the user. Must be at most 1000 unicode characters long. */
  description?: string | Computed<string>;
  /** Labels associated with this workflow. Labels can contain at most 64 entries. Keys and values can be no longer than 63 characters and can only contain lowercase letters, numeric characters, underscores and dashes. Label keys must start with a letter. International characters are allowed. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** The resource name of the workflow. Format: projects/{project}/locations/{location}/workflows/{workflow} */
  name?: string | Computed<string>;
  /** Output only. The timestamp that the latest revision of the workflow was created. */
  revisionCreateTime?: string | Computed<string>;
  /** Output only. The revision of the workflow. A new revision of a workflow is created as a result of updating the following properties of a workflow: - Service account - Workflow code to be executed The format is "000001-a4d", where the first 6 characters define the zero-padded revision ordinal number. They are followed by a hyphen and 3 hexadecimal random characters. */
  revisionId?: string | Computed<string>;
  /** The service account associated with the latest workflow version. This service account represents the identity of the workflow and determines what permissions the workflow has. Format: projects/{project}/serviceAccounts/{account} or {account} Using `-` as a wildcard for the `{project}` or not providing one at all will infer the project from the account. The `{account}` value can be the `email` address or the `unique_id` of the service account. If not provided, workflow will use the project's default service account. Modifying this field for an existing workflow results in a new workflow revision. */
  serviceAccount?: string | Computed<string>;
  /** Workflow code to be executed. The size limit is 128KB. */
  sourceContents?: string | Computed<string>;
  /** Output only. State of the workflow deployment. */
  state?: string | Computed<string>;
  /** Output only. The last update timestamp of the workflow. */
  updateTime?: string | Computed<string>;
}

export interface V1betaWorkflowsV1betaWorkflowAttrs {
  /** Output only. The timestamp of when the workflow was created. */
  createTime: string;
  /** Description of the workflow provided by the user. Must be at most 1000 unicode characters long. */
  description: string;
  /** Labels associated with this workflow. Labels can contain at most 64 entries. Keys and values can be no longer than 63 characters and can only contain lowercase letters, numeric characters, underscores and dashes. Label keys must start with a letter. International characters are allowed. */
  labels: Record<string, string>;
  /** The resource name of the workflow. Format: projects/{project}/locations/{location}/workflows/{workflow} */
  name: string;
  /** Output only. The timestamp that the latest revision of the workflow was created. */
  revisionCreateTime: string;
  /** Output only. The revision of the workflow. A new revision of a workflow is created as a result of updating the following properties of a workflow: - Service account - Workflow code to be executed The format is "000001-a4d", where the first 6 characters define the zero-padded revision ordinal number. They are followed by a hyphen and 3 hexadecimal random characters. */
  revisionId: string;
  /** The service account associated with the latest workflow version. This service account represents the identity of the workflow and determines what permissions the workflow has. Format: projects/{project}/serviceAccounts/{account} or {account} Using `-` as a wildcard for the `{project}` or not providing one at all will infer the project from the account. The `{account}` value can be the `email` address or the `unique_id` of the service account. If not provided, workflow will use the project's default service account. Modifying this field for an existing workflow results in a new workflow revision. */
  serviceAccount: string;
  /** Workflow code to be executed. The size limit is 128KB. */
  sourceContents: string;
  /** Output only. State of the workflow deployment. */
  state: string;
  /** Output only. The last update timestamp of the workflow. */
  updateTime: string;
}

export const V1betaWorkflowsV1betaWorkflow: ResourceBinding<V1betaWorkflowsV1betaWorkflowConfig, V1betaWorkflowsV1betaWorkflowAttrs> = {
  wireType: "google_workflows_v1beta_workflows_v1beta_workflow",
  fields: {
    createTime: "create_time",
    description: "description",
    labels: "labels",
    name: "name",
    revisionCreateTime: "revision_create_time",
    revisionId: "revision_id",
    serviceAccount: "service_account",
    sourceContents: "source_contents",
    state: "state",
    updateTime: "update_time",
  },
};
