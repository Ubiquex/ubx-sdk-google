// Code generated by `ubx sdk gen` from google_cloudsupport_v2beta_cloudsupport_v2beta_case. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V2betaCloudsupportV2betaCase_Classification_Product {
  /** The product line of the Product. */
  productLine?: string | Computed<string>;
}

export interface V2betaCloudsupportV2betaCase_Classification {
  /** A display name for the classification. The display name is not static and can change. To uniquely and consistently identify classifications, use the `CaseClassification.id` field. */
  displayName?: string | Computed<string>;
  /** The unique ID for a classification. Must be specified for case creation. To retrieve valid classification IDs for case creation, use `caseClassifications.search`. Classification IDs returned by `caseClassifications.search` are guaranteed to be valid for at least 6 months. If a given classification is deactiveated, it will immediately stop being returned. After 6 months, `case.create` requests using the classification ID will fail. */
  id?: string | Computed<string>;
  /** The product a case may be associated with. */
  product?: V2betaCloudsupportV2betaCase_Classification_Product | Computed<V2betaCloudsupportV2betaCase_Classification_Product>;
}

export interface V2betaCloudsupportV2betaCase_Creator {
  /** The name to display for the actor. If not provided, it is inferred from credentials supplied during case creation. When an email is provided, a display name must also be provided. This will be obfuscated if the user is a Google Support agent. */
  displayName?: string | Computed<string>;
  /** The email address of the actor. If not provided, it is inferred from the credentials supplied during case creation. When a name is provided, an email must also be provided. If the user is a Google Support agent, this is obfuscated. This field is deprecated. Use `username` instead. */
  email?: string | Computed<string>;
  /** Output only. Whether the actor is a Google support actor. */
  googleSupport?: boolean | Computed<boolean>;
  /** Output only. The username of the actor. It may look like an email or other format provided by the identity provider. If not provided, it is inferred from the credentials supplied. When a name is provided, a username must also be provided. If the user is a Google Support agent, this will not be set. */
  username?: string | Computed<string>;
}

const V2betaCloudsupportV2betaCase_Classification_ProductFields: FieldMap = {
  productLine: "product_line",
};

const V2betaCloudsupportV2betaCase_ClassificationFields: FieldMap = {
  displayName: "display_name",
  id: "id",
  product: {
    wireName: "product",
    kind: "object",
    fields: V2betaCloudsupportV2betaCase_Classification_ProductFields,
  },
};

const V2betaCloudsupportV2betaCase_CreatorFields: FieldMap = {
  displayName: "display_name",
  email: "email",
  googleSupport: "google_support",
  username: "username",
};

export interface V2betaCloudsupportV2betaCaseConfig {
  /** A Case Classification represents the topic that a case is about. It's very important to use accurate classifications, because they're used to route your cases to specialists who can help you. A classification always has an ID that is its unique identifier. A valid ID is required when creating a case. */
  classification?: V2betaCloudsupportV2betaCase_Classification | Computed<V2betaCloudsupportV2betaCase_Classification>;
  /** A user-supplied email address to send case update notifications for. This should only be used in BYOID flows, where we cannot infer the user's email address directly from their EUCs. */
  contactEmail?: string | Computed<string>;
  /** Output only. The time this case was created. */
  createTime?: string | Computed<string>;
  /** An Actor represents an entity that performed an action. For example, an actor could be a user who posted a comment on a support case, a user who uploaded an attachment, or a service account that created a support case. */
  creator?: V2betaCloudsupportV2betaCase_Creator | Computed<V2betaCloudsupportV2betaCase_Creator>;
  /** A broad description of the issue. */
  description?: string | Computed<string>;
  /** The short summary of the issue reported in this case. */
  displayName?: string | Computed<string>;
  /** Whether the case is currently escalated. */
  escalated?: boolean | Computed<boolean>;
  /** The language the user has requested to receive support in. This should be a BCP 47 language code (e.g., `"en"`, `"zh-CN"`, `"zh-TW"`, `"ja"`, `"ko"`). If no language or an unsupported language is specified, this field defaults to English (en). Language selection during case creation may affect your available support options. For a list of supported languages and their support working hours, see: https://cloud.google.com/support/docs/language-working-hours */
  languageCode?: string | Computed<string>;
  /** Identifier. The resource name for the case. */
  name?: string | Computed<string>;
  /** The priority of this case. */
  priority?: string | Computed<string>;
  /** REMOVED. The severity of this case. Use priority instead. */
  severity?: string | Computed<string>;
  /** Output only. The current status of the support case. */
  state?: string | Computed<string>;
  /** The email addresses to receive updates on this case. */
  subscriberEmailAddresses?: string[] | Computed<string[]>;
  /** Whether this case was created for internal API testing and should not be acted on by the support team. */
  testCase?: boolean | Computed<boolean>;
  /** The timezone of the user who created the support case. It should be in a format IANA recognizes: https://www.iana.org/time-zones. There is no additional validation done by the API. */
  timeZone?: string | Computed<string>;
  /** Output only. The time this case was last updated. */
  updateTime?: string | Computed<string>;
}

export interface V2betaCloudsupportV2betaCaseAttrs {
  /** A Case Classification represents the topic that a case is about. It's very important to use accurate classifications, because they're used to route your cases to specialists who can help you. A classification always has an ID that is its unique identifier. A valid ID is required when creating a case. */
  classification: V2betaCloudsupportV2betaCase_Classification;
  /** A user-supplied email address to send case update notifications for. This should only be used in BYOID flows, where we cannot infer the user's email address directly from their EUCs. */
  contactEmail: string;
  /** Output only. The time this case was created. */
  createTime: string;
  /** An Actor represents an entity that performed an action. For example, an actor could be a user who posted a comment on a support case, a user who uploaded an attachment, or a service account that created a support case. */
  creator: V2betaCloudsupportV2betaCase_Creator;
  /** A broad description of the issue. */
  description: string;
  /** The short summary of the issue reported in this case. */
  displayName: string;
  /** Whether the case is currently escalated. */
  escalated: boolean;
  /** The language the user has requested to receive support in. This should be a BCP 47 language code (e.g., `"en"`, `"zh-CN"`, `"zh-TW"`, `"ja"`, `"ko"`). If no language or an unsupported language is specified, this field defaults to English (en). Language selection during case creation may affect your available support options. For a list of supported languages and their support working hours, see: https://cloud.google.com/support/docs/language-working-hours */
  languageCode: string;
  /** Identifier. The resource name for the case. */
  name: string;
  /** The priority of this case. */
  priority: string;
  /** REMOVED. The severity of this case. Use priority instead. */
  severity: string;
  /** Output only. The current status of the support case. */
  state: string;
  /** The email addresses to receive updates on this case. */
  subscriberEmailAddresses: string[];
  /** Whether this case was created for internal API testing and should not be acted on by the support team. */
  testCase: boolean;
  /** The timezone of the user who created the support case. It should be in a format IANA recognizes: https://www.iana.org/time-zones. There is no additional validation done by the API. */
  timeZone: string;
  /** Output only. The time this case was last updated. */
  updateTime: string;
}

export const V2betaCloudsupportV2betaCase: ResourceBinding<V2betaCloudsupportV2betaCaseConfig, V2betaCloudsupportV2betaCaseAttrs> = {
  wireType: "google_cloudsupport_v2beta_cloudsupport_v2beta_case",
  fields: {
    classification: {
      wireName: "classification",
      kind: "object",
      fields: V2betaCloudsupportV2betaCase_ClassificationFields,
    },
    contactEmail: "contact_email",
    createTime: "create_time",
    creator: {
      wireName: "creator",
      kind: "object",
      fields: V2betaCloudsupportV2betaCase_CreatorFields,
    },
    description: "description",
    displayName: "display_name",
    escalated: "escalated",
    languageCode: "language_code",
    name: "name",
    priority: "priority",
    severity: "severity",
    state: "state",
    subscriberEmailAddresses: "subscriber_email_addresses",
    testCase: "test_case",
    timeZone: "time_zone",
    updateTime: "update_time",
  },
};
