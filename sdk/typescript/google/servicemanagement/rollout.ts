// Code generated by `ubx sdk gen` from google_servicemanagement_rollout. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Rollout_TrafficPercentStrategy {
  /** Maps service configuration IDs to their corresponding traffic percentage. Key is the service configuration ID, Value is the traffic percentage which must be greater than 0.0 and the sum must equal to 100.0. */
  percentages?: Record<string, number> | Computed<Record<string, number>>;
}

const Rollout_TrafficPercentStrategyFields: FieldMap = {
  percentages: "percentages",
};

export interface RolloutConfig {
  /** Creation time of the rollout. Readonly. */
  createTime?: string | Computed<string>;
  /** The user who created the Rollout. Readonly. */
  createdBy?: string | Computed<string>;
  /** Strategy used to delete a service. This strategy is a placeholder only used by the system generated rollout to delete a service. */
  deleteServiceStrategy?: unknown | Computed<unknown>;
  /** Optional. Unique identifier of this Rollout. Must be no longer than 63 characters and only lower case letters, digits, '.', '_' and '-' are allowed. If not specified by client, the server will generate one. The generated id will have the form of , where "date" is the create date in ISO 8601 format. "revision number" is a monotonically increasing positive number that is reset every day for each service. An example of the generated rollout_id is '2016-02-16r1' */
  rolloutId?: string | Computed<string>;
  /** The name of the service associated with this Rollout. */
  serviceName?: string | Computed<string>;
  /** The status of this rollout. Readonly. In case of a failed rollout, the system will automatically rollback to the current Rollout version. Readonly. */
  status?: string | Computed<string>;
  /** Strategy that specifies how clients of Google Service Controller want to send traffic to use different config versions. This is generally used by API proxy to split traffic based on your configured percentage for each config version. One example of how to gradually rollout a new service configuration using this strategy: Day 1 Rollout { id: "example.googleapis.com/rollout_20160206" traffic_percent_strategy { percentages: { "example.googleapis.com/20160201": 70.00 "example.googleapis.com/20160206": 30.00 } } } Day 2 Rollout { id: "example.googleapis.com/rollout_20160207" traffic_percent_strategy: { percentages: { "example.googleapis.com/20160206": 100.00 } } } */
  trafficPercentStrategy?: Rollout_TrafficPercentStrategy | Computed<Rollout_TrafficPercentStrategy>;
}

export interface RolloutAttrs {
  /** Creation time of the rollout. Readonly. */
  createTime: string;
  /** The user who created the Rollout. Readonly. */
  createdBy: string;
  /** Strategy used to delete a service. This strategy is a placeholder only used by the system generated rollout to delete a service. */
  deleteServiceStrategy: unknown;
  /** Optional. Unique identifier of this Rollout. Must be no longer than 63 characters and only lower case letters, digits, '.', '_' and '-' are allowed. If not specified by client, the server will generate one. The generated id will have the form of , where "date" is the create date in ISO 8601 format. "revision number" is a monotonically increasing positive number that is reset every day for each service. An example of the generated rollout_id is '2016-02-16r1' */
  rolloutId: string;
  /** The name of the service associated with this Rollout. */
  serviceName: string;
  /** The status of this rollout. Readonly. In case of a failed rollout, the system will automatically rollback to the current Rollout version. Readonly. */
  status: string;
  /** Strategy that specifies how clients of Google Service Controller want to send traffic to use different config versions. This is generally used by API proxy to split traffic based on your configured percentage for each config version. One example of how to gradually rollout a new service configuration using this strategy: Day 1 Rollout { id: "example.googleapis.com/rollout_20160206" traffic_percent_strategy { percentages: { "example.googleapis.com/20160201": 70.00 "example.googleapis.com/20160206": 30.00 } } } Day 2 Rollout { id: "example.googleapis.com/rollout_20160207" traffic_percent_strategy: { percentages: { "example.googleapis.com/20160206": 100.00 } } } */
  trafficPercentStrategy: Rollout_TrafficPercentStrategy;
}

export const Rollout: ResourceBinding<RolloutConfig, RolloutAttrs> = {
  wireType: "google_servicemanagement_rollout",
  fields: {
    createTime: "create_time",
    createdBy: "created_by",
    deleteServiceStrategy: "delete_service_strategy",
    rolloutId: "rollout_id",
    serviceName: "service_name",
    status: "status",
    trafficPercentStrategy: {
      wireName: "traffic_percent_strategy",
      kind: "object",
      fields: Rollout_TrafficPercentStrategyFields,
    },
  },
};
