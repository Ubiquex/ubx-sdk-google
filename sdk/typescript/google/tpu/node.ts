// Code generated by `ubx sdk gen` from google_tpu_node. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Node_AcceleratorConfig {
  /** Required. Topology of TPU in chips. */
  topology?: string | Computed<string>;
  /** Required. Type of TPU. */
  type?: string | Computed<string>;
}

export interface Node_BootDiskConfig_CustomerEncryptionKey {
  /** The name of the encryption key that is stored in Google Cloud KMS. For example: "kmsKeyName": "projects/KMS_PROJECT_ID/locations/REGION/keyRings/KEY_REGION/cryptoKeys/KEY The fully-qualifed key name may be returned for resource GET requests. For example: "kmsKeyName": "projects/KMS_PROJECT_ID/locations/REGION/keyRings/KEY_REGION/cryptoKeys/KEY/cryptoKeyVersions/1 */
  kmsKeyName?: string | Computed<string>;
}

export interface Node_BootDiskConfig {
  /** Defines the customer encryption key for disk encryption. */
  customerEncryptionKey?: Node_BootDiskConfig_CustomerEncryptionKey | Computed<Node_BootDiskConfig_CustomerEncryptionKey>;
}

export interface Node_DataDisks {
  /** The real access mode (`READ_WRITE` or `READ_ONLY`) this data disk is attached to the TPU node with. (AI-inferred) */
  mode?: string | Computed<string>;
  /** The real Compute Engine disk this TPU node's own data disk is created from or attached to. (AI-inferred) */
  sourceDisk?: string | Computed<string>;
}

export interface Node_NetworkConfig {
  /** Allows the TPU node to send and receive packets with non-matching destination or source IPs. This is required if you plan to use the TPU workers to forward routes. */
  canIpForward?: boolean | Computed<boolean>;
  /** Indicates that external IP addresses would be associated with the TPU workers. If set to false, the specified subnetwork or network should have Private Google Access enabled. */
  enableExternalIps?: boolean | Computed<boolean>;
  /** The name of the network for the TPU node. It must be a preexisting Google Compute Engine network. If none is provided, "default" will be used. */
  network?: string | Computed<string>;
  /** Optional. Specifies networking queue count for TPU VM instance's network interface. */
  queueCount?: number | Computed<number>;
  /** The name of the subnetwork for the TPU node. It must be a preexisting Google Compute Engine subnetwork. If none is provided, "default" will be used. */
  subnetwork?: string | Computed<string>;
}

export interface Node_NetworkEndpoints_AccessConfig {
  /** The real, assigned external (public) IP address for this TPU node's own network endpoint. (AI-inferred) */
  externalIp?: string | Computed<string>;
}

export interface Node_NetworkEndpoints {
  /** Real, external-access configuration (such as the assigned `external_ip`) for this TPU node's own network endpoint. (AI-inferred) */
  accessConfig?: Node_NetworkEndpoints_AccessConfig | Computed<Node_NetworkEndpoints_AccessConfig>;
  /** An IP address, in IPv4 or IPv6 format. (AI-inferred) */
  ipAddress?: string | Computed<string>;
  /** The real network port this TPU node's own network endpoint listens on. (AI-inferred) */
  port?: number | Computed<number>;
}

export interface Node_SchedulingConfig {
  /** Defines whether the node is preemptible. */
  preemptible?: boolean | Computed<boolean>;
  /** Whether the node is created under a reservation. */
  reserved?: boolean | Computed<boolean>;
  /** Optional. Defines whether the node is Spot VM. */
  spot?: boolean | Computed<boolean>;
}

export interface Node_ServiceAccount {
  /** Email address of the service account. If empty, default Compute service account will be used. */
  email?: string | Computed<string>;
  /** The list of scopes to be made available for this service account. If empty, access to all Cloud APIs will be allowed. */
  scope?: string[] | Computed<string[]>;
}

export interface Node_ShieldedInstanceConfig {
  /** Defines whether the instance has Secure Boot enabled. */
  enableSecureBoot?: boolean | Computed<boolean>;
}

export interface Node_Symptoms {
  /** Output only. The timestamp when this resource was created. (AI-inferred) */
  createTime?: string | Computed<string>;
  /** Real, additional detail text describing this detected TPU node symptom. (AI-inferred) */
  details?: string | Computed<string>;
  /** The real, detected category of issue (e.g. `OUT_OF_MEMORY`, `HBM_OUT_OF_MEMORY`) affecting this TPU node. (AI-inferred) */
  symptomType?: string | Computed<string>;
  /** The real, specific TPU worker this symptom was detected on, within a multi-worker TPU node. (AI-inferred) */
  workerId?: string | Computed<string>;
}

export interface Node_UpcomingMaintenance {
  /** Indicates if the maintenance can be customer triggered. */
  canReschedule?: boolean | Computed<boolean>;
  /** The latest time for the planned maintenance window to start. This timestamp value is in RFC3339 text format. */
  latestWindowStartTime?: string | Computed<string>;
  /** The status of the maintenance. */
  maintenanceStatus?: string | Computed<string>;
  /** Defines the type of maintenance. */
  type?: string | Computed<string>;
  /** The time by which the maintenance disruption will be completed. This timestamp value is in RFC3339 text format. */
  windowEndTime?: string | Computed<string>;
  /** The current start time of the maintenance window. This timestamp value is in RFC3339 text format. */
  windowStartTime?: string | Computed<string>;
}

const Node_AcceleratorConfigFields: FieldMap = {
  topology: "topology",
  type: "type",
};

const Node_BootDiskConfig_CustomerEncryptionKeyFields: FieldMap = {
  kmsKeyName: "kms_key_name",
};

const Node_BootDiskConfigFields: FieldMap = {
  customerEncryptionKey: {
    wireName: "customer_encryption_key",
    kind: "object",
    fields: Node_BootDiskConfig_CustomerEncryptionKeyFields,
  },
};

const Node_DataDisksFields: FieldMap = {
  mode: "mode",
  sourceDisk: "source_disk",
};

const Node_NetworkConfigFields: FieldMap = {
  canIpForward: "can_ip_forward",
  enableExternalIps: "enable_external_ips",
  network: "network",
  queueCount: "queue_count",
  subnetwork: "subnetwork",
};

const Node_SchedulingConfigFields: FieldMap = {
  preemptible: "preemptible",
  reserved: "reserved",
  spot: "spot",
};

const Node_ServiceAccountFields: FieldMap = {
  email: "email",
  scope: "scope",
};

const Node_ShieldedInstanceConfigFields: FieldMap = {
  enableSecureBoot: "enable_secure_boot",
};

const Node_UpcomingMaintenanceFields: FieldMap = {
  canReschedule: "can_reschedule",
  latestWindowStartTime: "latest_window_start_time",
  maintenanceStatus: "maintenance_status",
  type: "type",
  windowEndTime: "window_end_time",
  windowStartTime: "window_start_time",
};

export interface NodeConfig {
  /** A TPU accelerator configuration. */
  acceleratorConfig?: Node_AcceleratorConfig | Computed<Node_AcceleratorConfig>;
  /** Optional. The type of hardware accelerators associated with this node. */
  acceleratorType?: string | Computed<string>;
  /** Sets the boot disk configuration for the TPU node. */
  bootDiskConfig?: Node_BootDiskConfig | Computed<Node_BootDiskConfig>;
  /** The CIDR block that the TPU node will use when selecting an IP address. This CIDR block must be a /29 block; the Compute Engine networks API forbids a smaller block, and using a larger block would be wasteful (a node can only consume one IP address). Errors will occur if the CIDR block has already been used for a currently existing TPU node, the CIDR block conflicts with any subnetworks in the user's provided network, or the provided network is peered with another network that is using that CIDR block. */
  cidrBlock?: string | Computed<string>;
  /** The additional data disks for the Node. */
  dataDisks?: Node_DataDisks[] | Computed<Node_DataDisks[]>;
  /** The user-supplied description of the TPU. Maximum of 512 characters. */
  description?: string | Computed<string>;
  /** The health status of the TPU node. */
  health?: string | Computed<string>;
  /** Resource labels to represent user-provided metadata. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Custom metadata to apply to the TPU Node. Can set startup-script and shutdown-script */
  metadata?: Record<string, string> | Computed<Record<string, string>>;
  /** Network related configurations. */
  networkConfig?: Node_NetworkConfig | Computed<Node_NetworkConfig>;
  /** Optional. Repeated network configurations for the TPU node. This field is used to specify multiple networks configs for the TPU node. network_config and network_configs are mutually exclusive, you can only specify one of them. If both are specified, an error will be returned. */
  networkConfigs?: Node_NetworkConfig[] | Computed<Node_NetworkConfig[]>;
  /** Required. The runtime version running in the Node. */
  runtimeVersion?: string | Computed<string>;
  /** Sets the scheduling options for this node. */
  schedulingConfig?: Node_SchedulingConfig | Computed<Node_SchedulingConfig>;
  /** A service account. */
  serviceAccount?: Node_ServiceAccount | Computed<Node_ServiceAccount>;
  /** A set of Shielded Instance options. */
  shieldedInstanceConfig?: Node_ShieldedInstanceConfig | Computed<Node_ShieldedInstanceConfig>;
  /** Tags to apply to the TPU Node. Tags are used to identify valid sources or targets for network firewalls. */
  tags?: string[] | Computed<string[]>;
  /** Upcoming Maintenance notification information. */
  upcomingMaintenance?: Node_UpcomingMaintenance | Computed<Node_UpcomingMaintenance>;
}

export interface NodeAttrs {
  /** A TPU accelerator configuration. */
  acceleratorConfig: Node_AcceleratorConfig;
  /** Optional. The type of hardware accelerators associated with this node. */
  acceleratorType: string;
  /** Output only. The API version that created this Node. */
  apiVersion: string;
  /** Sets the boot disk configuration for the TPU node. */
  bootDiskConfig: Node_BootDiskConfig;
  /** The CIDR block that the TPU node will use when selecting an IP address. This CIDR block must be a /29 block; the Compute Engine networks API forbids a smaller block, and using a larger block would be wasteful (a node can only consume one IP address). Errors will occur if the CIDR block has already been used for a currently existing TPU node, the CIDR block conflicts with any subnetworks in the user's provided network, or the provided network is peered with another network that is using that CIDR block. */
  cidrBlock: string;
  /** Output only. The time when the node was created. */
  createTime: string;
  /** The additional data disks for the Node. */
  dataDisks: Node_DataDisks[];
  /** The user-supplied description of the TPU. Maximum of 512 characters. */
  description: string;
  /** The health status of the TPU node. */
  health: string;
  /** Output only. If this field is populated, it contains a description of why the TPU Node is unhealthy. */
  healthDescription: string;
  /** Output only. The unique identifier for the TPU Node. */
  id: string;
  /** Resource labels to represent user-provided metadata. */
  labels: Record<string, string>;
  /** Custom metadata to apply to the TPU Node. Can set startup-script and shutdown-script */
  metadata: Record<string, string>;
  /** Output only. Whether the Node belongs to a Multislice group. */
  multisliceNode: boolean;
  /** Output only. Immutable. The name of the TPU. */
  name: string;
  /** Network related configurations. */
  networkConfig: Node_NetworkConfig;
  /** Optional. Repeated network configurations for the TPU node. This field is used to specify multiple networks configs for the TPU node. network_config and network_configs are mutually exclusive, you can only specify one of them. If both are specified, an error will be returned. */
  networkConfigs: Node_NetworkConfig[];
  /** Output only. The network endpoints where TPU workers can be accessed and sent work. It is recommended that runtime clients of the node reach out to the 0th entry in this map first. */
  networkEndpoints: Node_NetworkEndpoints[];
  /** Output only. The qualified name of the QueuedResource that requested this Node. */
  queuedResource: string;
  /** Required. The runtime version running in the Node. */
  runtimeVersion: string;
  /** Sets the scheduling options for this node. */
  schedulingConfig: Node_SchedulingConfig;
  /** A service account. */
  serviceAccount: Node_ServiceAccount;
  /** A set of Shielded Instance options. */
  shieldedInstanceConfig: Node_ShieldedInstanceConfig;
  /** Output only. The current state for the TPU Node. */
  state: string;
  /** Output only. The Symptoms that have occurred to the TPU Node. */
  symptoms: Node_Symptoms[];
  /** Tags to apply to the TPU Node. Tags are used to identify valid sources or targets for network firewalls. */
  tags: string[];
  /** Upcoming Maintenance notification information. */
  upcomingMaintenance: Node_UpcomingMaintenance;
}

export const Node: ResourceBinding<NodeConfig, NodeAttrs> = {
  wireType: "google_tpu_node",
  fields: {
    acceleratorConfig: {
      wireName: "accelerator_config",
      kind: "object",
      fields: Node_AcceleratorConfigFields,
    },
    acceleratorType: "accelerator_type",
    bootDiskConfig: {
      wireName: "boot_disk_config",
      kind: "object",
      fields: Node_BootDiskConfigFields,
    },
    cidrBlock: "cidr_block",
    dataDisks: {
      wireName: "data_disks",
      kind: "list",
      fields: Node_DataDisksFields,
    },
    description: "description",
    health: "health",
    labels: "labels",
    metadata: "metadata",
    networkConfig: {
      wireName: "network_config",
      kind: "object",
      fields: Node_NetworkConfigFields,
    },
    networkConfigs: {
      wireName: "network_configs",
      kind: "list",
      fields: Node_NetworkConfigFields,
    },
    runtimeVersion: "runtime_version",
    schedulingConfig: {
      wireName: "scheduling_config",
      kind: "object",
      fields: Node_SchedulingConfigFields,
    },
    serviceAccount: {
      wireName: "service_account",
      kind: "object",
      fields: Node_ServiceAccountFields,
    },
    shieldedInstanceConfig: {
      wireName: "shielded_instance_config",
      kind: "object",
      fields: Node_ShieldedInstanceConfigFields,
    },
    tags: "tags",
    upcomingMaintenance: {
      wireName: "upcoming_maintenance",
      kind: "object",
      fields: Node_UpcomingMaintenanceFields,
    },
  },
};
