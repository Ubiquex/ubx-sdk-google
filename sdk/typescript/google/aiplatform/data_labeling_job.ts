// Code generated by `ubx sdk gen` from google_aiplatform_data_labeling_job. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface DataLabelingJob_ActiveLearningConfig_SampleConfig {
  /** The percentage of data needed to be labeled in each following batch (except the first batch). */
  followingBatchSamplePercentage?: number | Computed<number>;
  /** The percentage of data needed to be labeled in the first batch. */
  initialBatchSamplePercentage?: number | Computed<number>;
  /** Field to choose sampling strategy. Sampling strategy will decide which data should be selected for human labeling in every batch. */
  sampleStrategy?: string | Computed<string>;
}

export interface DataLabelingJob_ActiveLearningConfig_TrainingConfig {
  /** The timeout hours for the CMLE training job, expressed in milli hours i.e. 1,000 value in this field means 1 hour. */
  timeoutTrainingMilliHours?: string | Computed<string>;
}

export interface DataLabelingJob_ActiveLearningConfig {
  /** Max number of human labeled DataItems. */
  maxDataItemCount?: string | Computed<string>;
  /** Max percent of total DataItems for human labeling. */
  maxDataItemPercentage?: number | Computed<number>;
  /** Active learning data sampling config. For every active learning labeling iteration, it will select a batch of data based on the sampling strategy. */
  sampleConfig?: DataLabelingJob_ActiveLearningConfig_SampleConfig | Computed<DataLabelingJob_ActiveLearningConfig_SampleConfig>;
  /** CMLE training config. For every active learning labeling iteration, system will train a machine learning model on CMLE. The trained model will be used by data sampling algorithm to select DataItems. */
  trainingConfig?: DataLabelingJob_ActiveLearningConfig_TrainingConfig | Computed<DataLabelingJob_ActiveLearningConfig_TrainingConfig>;
}

export interface DataLabelingJob_CurrentSpend {
  /** The three-letter currency code defined in ISO 4217. */
  currencyCode?: string | Computed<string>;
  /** Number of nano (10^-9) units of the amount. The value must be between -999,999,999 and +999,999,999 inclusive. If `units` is positive, `nanos` must be positive or zero. If `units` is zero, `nanos` can be positive, zero, or negative. If `units` is negative, `nanos` must be negative or zero. For example $-1.75 is represented as `units`=-1 and `nanos`=-750,000,000. */
  nanos?: number | Computed<number>;
  /** The whole units of the amount. For example if `currencyCode` is `"USD"`, then 1 unit is one US dollar. */
  units?: string | Computed<string>;
}

export interface DataLabelingJob_EncryptionSpec {
  /** Required. Resource name of the Cloud KMS key used to protect the resource. The Cloud KMS key must be in the same region as the resource. It must have the format `projects/{project}/locations/{location}/keyRings/{key_ring}/cryptoKeys/{crypto_key}`. */
  kmsKeyName?: string | Computed<string>;
}

export interface DataLabelingJob_Error {
  /** The status code, which should be an enum value of google.rpc.Code. */
  code?: number | Computed<number>;
  /** A list of messages that carry the error details. There is a common set of message types for APIs to use. */
  details?: Record<string, unknown>[] | Computed<Record<string, unknown>[]>;
  /** A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. */
  message?: string | Computed<string>;
}

const DataLabelingJob_ActiveLearningConfig_SampleConfigFields: FieldMap = {
  followingBatchSamplePercentage: "following_batch_sample_percentage",
  initialBatchSamplePercentage: "initial_batch_sample_percentage",
  sampleStrategy: "sample_strategy",
};

const DataLabelingJob_ActiveLearningConfig_TrainingConfigFields: FieldMap = {
  timeoutTrainingMilliHours: "timeout_training_milli_hours",
};

const DataLabelingJob_ActiveLearningConfigFields: FieldMap = {
  maxDataItemCount: "max_data_item_count",
  maxDataItemPercentage: "max_data_item_percentage",
  sampleConfig: {
    wireName: "sample_config",
    kind: "object",
    fields: DataLabelingJob_ActiveLearningConfig_SampleConfigFields,
  },
  trainingConfig: {
    wireName: "training_config",
    kind: "object",
    fields: DataLabelingJob_ActiveLearningConfig_TrainingConfigFields,
  },
};

const DataLabelingJob_CurrentSpendFields: FieldMap = {
  currencyCode: "currency_code",
  nanos: "nanos",
  units: "units",
};

const DataLabelingJob_EncryptionSpecFields: FieldMap = {
  kmsKeyName: "kms_key_name",
};

const DataLabelingJob_ErrorFields: FieldMap = {
  code: "code",
  details: "details",
  message: "message",
};

export interface DataLabelingJobConfig {
  /** Parameters that configure the active learning pipeline. Active learning will label the data incrementally by several iterations. For every iteration, it will select a batch of data based on the sampling strategy. */
  activeLearningConfig?: DataLabelingJob_ActiveLearningConfig | Computed<DataLabelingJob_ActiveLearningConfig>;
  /** Labels to assign to annotations generated by this DataLabelingJob. Label keys and values can be no longer than 64 characters (Unicode codepoints), can only contain lowercase letters, numeric characters, underscores and dashes. International characters are allowed. See https://goo.gl/xmQnxf for more information and examples of labels. System reserved label keys are prefixed with "aiplatform.googleapis.com/" and are immutable. */
  annotationLabels?: Record<string, string> | Computed<Record<string, string>>;
  /** Represents an amount of money with its currency type. */
  currentSpend?: DataLabelingJob_CurrentSpend | Computed<DataLabelingJob_CurrentSpend>;
  /** Required. Dataset resource names. Right now we only support labeling from a single Dataset. Format: `projects/{project}/locations/{location}/datasets/{dataset}` */
  datasets?: string[] | Computed<string[]>;
  /** Required. The user-defined name of the DataLabelingJob. The name can be up to 128 characters long and can consist of any UTF-8 characters. Display name of a DataLabelingJob. */
  displayName?: string | Computed<string>;
  /** Represents a customer-managed encryption key specification that can be applied to a Vertex AI resource. */
  encryptionSpec?: DataLabelingJob_EncryptionSpec | Computed<DataLabelingJob_EncryptionSpec>;
  /** The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). */
  error?: DataLabelingJob_Error | Computed<DataLabelingJob_Error>;
  /** Required. Input config parameters for the DataLabelingJob. */
  inputs?: unknown | Computed<unknown>;
  /** Required. Points to a YAML file stored on Google Cloud Storage describing the config for a specific type of DataLabelingJob. The schema files that can be used here are found in the https://storage.googleapis.com/google-cloud-aiplatform bucket in the /schema/datalabelingjob/inputs/ folder. */
  inputsSchemaUri?: string | Computed<string>;
  /** Required. The Google Cloud Storage location of the instruction pdf. This pdf is shared with labelers, and provides detailed description on how to label DataItems in Datasets. */
  instructionUri?: string | Computed<string>;
  /** Required. Number of labelers to work on each DataItem. */
  labelerCount?: number | Computed<number>;
  /** The labels with user-defined metadata to organize your DataLabelingJobs. Label keys and values can be no longer than 64 characters (Unicode codepoints), can only contain lowercase letters, numeric characters, underscores and dashes. International characters are allowed. See https://goo.gl/xmQnxf for more information and examples of labels. System reserved label keys are prefixed with "aiplatform.googleapis.com/" and are immutable. Following system labels exist for each DataLabelingJob: * "aiplatform.googleapis.com/schema": output only, its value is the inputs_schema's title. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** The SpecialistPools' resource names associated with this job. */
  specialistPools?: string[] | Computed<string[]>;
}

export interface DataLabelingJobAttrs {
  /** Parameters that configure the active learning pipeline. Active learning will label the data incrementally by several iterations. For every iteration, it will select a batch of data based on the sampling strategy. */
  activeLearningConfig: DataLabelingJob_ActiveLearningConfig;
  /** Labels to assign to annotations generated by this DataLabelingJob. Label keys and values can be no longer than 64 characters (Unicode codepoints), can only contain lowercase letters, numeric characters, underscores and dashes. International characters are allowed. See https://goo.gl/xmQnxf for more information and examples of labels. System reserved label keys are prefixed with "aiplatform.googleapis.com/" and are immutable. */
  annotationLabels: Record<string, string>;
  /** Output only. Timestamp when this DataLabelingJob was created. */
  createTime: string;
  /** Represents an amount of money with its currency type. */
  currentSpend: DataLabelingJob_CurrentSpend;
  /** Required. Dataset resource names. Right now we only support labeling from a single Dataset. Format: `projects/{project}/locations/{location}/datasets/{dataset}` */
  datasets: string[];
  /** Required. The user-defined name of the DataLabelingJob. The name can be up to 128 characters long and can consist of any UTF-8 characters. Display name of a DataLabelingJob. */
  displayName: string;
  /** Represents a customer-managed encryption key specification that can be applied to a Vertex AI resource. */
  encryptionSpec: DataLabelingJob_EncryptionSpec;
  /** The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). */
  error: DataLabelingJob_Error;
  /** Required. Input config parameters for the DataLabelingJob. */
  inputs: unknown;
  /** Required. Points to a YAML file stored on Google Cloud Storage describing the config for a specific type of DataLabelingJob. The schema files that can be used here are found in the https://storage.googleapis.com/google-cloud-aiplatform bucket in the /schema/datalabelingjob/inputs/ folder. */
  inputsSchemaUri: string;
  /** Required. The Google Cloud Storage location of the instruction pdf. This pdf is shared with labelers, and provides detailed description on how to label DataItems in Datasets. */
  instructionUri: string;
  /** Required. Number of labelers to work on each DataItem. */
  labelerCount: number;
  /** Output only. Current labeling job progress percentage scaled in interval [0, 100], indicating the percentage of DataItems that has been finished. */
  labelingProgress: number;
  /** The labels with user-defined metadata to organize your DataLabelingJobs. Label keys and values can be no longer than 64 characters (Unicode codepoints), can only contain lowercase letters, numeric characters, underscores and dashes. International characters are allowed. See https://goo.gl/xmQnxf for more information and examples of labels. System reserved label keys are prefixed with "aiplatform.googleapis.com/" and are immutable. Following system labels exist for each DataLabelingJob: * "aiplatform.googleapis.com/schema": output only, its value is the inputs_schema's title. */
  labels: Record<string, string>;
  /** Output only. Resource name of the DataLabelingJob. */
  name: string;
  /** The SpecialistPools' resource names associated with this job. */
  specialistPools: string[];
  /** Output only. The detailed state of the job. */
  state: string;
  /** Output only. Timestamp when this DataLabelingJob was updated most recently. */
  updateTime: string;
}

export const DataLabelingJob: ResourceBinding<DataLabelingJobConfig, DataLabelingJobAttrs> = {
  wireType: "google_aiplatform_data_labeling_job",
  fields: {
    activeLearningConfig: {
      wireName: "active_learning_config",
      kind: "object",
      fields: DataLabelingJob_ActiveLearningConfigFields,
    },
    annotationLabels: "annotation_labels",
    currentSpend: {
      wireName: "current_spend",
      kind: "object",
      fields: DataLabelingJob_CurrentSpendFields,
    },
    datasets: "datasets",
    displayName: "display_name",
    encryptionSpec: {
      wireName: "encryption_spec",
      kind: "object",
      fields: DataLabelingJob_EncryptionSpecFields,
    },
    error: {
      wireName: "error",
      kind: "object",
      fields: DataLabelingJob_ErrorFields,
    },
    inputs: "inputs",
    inputsSchemaUri: "inputs_schema_uri",
    instructionUri: "instruction_uri",
    labelerCount: "labeler_count",
    labels: "labels",
    specialistPools: "specialist_pools",
  },
};
