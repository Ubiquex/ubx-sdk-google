// Code generated by `ubx sdk gen` from google_networkconnectivity_hub. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Hub_RoutingVpcs {
  /** Whether this filter is required for new site-to-site data transfer spokes. (AI-inferred) */
  requiredForNewSiteToSiteDataTransferSpokes?: boolean | Computed<boolean>;
  /** A resource URI. (AI-inferred) */
  uri?: string | Computed<string>;
}

export interface Hub_SpokeSummary_SpokeStateCounts {
  /** How many of this item there are. (AI-inferred) */
  count?: string | Computed<string>;
  /** The current lifecycle state of this resource. (AI-inferred) */
  state?: string | Computed<string>;
}

export interface Hub_SpokeSummary_SpokeStateReasonCounts {
  /** How many of this item there are. (AI-inferred) */
  count?: string | Computed<string>;
  /** A machine-readable code explaining why this resource is in its own current state. (AI-inferred) */
  stateReasonCode?: string | Computed<string>;
}

export interface Hub_SpokeSummary_SpokeTypeCounts {
  /** How many of this item there are. (AI-inferred) */
  count?: string | Computed<string>;
  /** The kind of resource this spoke represents, e.g. VPN tunnel or VPC network. (AI-inferred) */
  spokeType?: string | Computed<string>;
}

export interface Hub_SpokeSummary {
  /** Output only. Counts the number of spokes that are in each state and associated with a given hub. */
  spokeStateCounts?: Hub_SpokeSummary_SpokeStateCounts[] | Computed<Hub_SpokeSummary_SpokeStateCounts[]>;
  /** Output only. Counts the number of spokes that are inactive for each possible reason and associated with a given hub. */
  spokeStateReasonCounts?: Hub_SpokeSummary_SpokeStateReasonCounts[] | Computed<Hub_SpokeSummary_SpokeStateReasonCounts[]>;
  /** Output only. Counts the number of spokes of each type that are associated with a specific hub. */
  spokeTypeCounts?: Hub_SpokeSummary_SpokeTypeCounts[] | Computed<Hub_SpokeSummary_SpokeTypeCounts[]>;
}

const Hub_SpokeSummary_SpokeStateCountsFields: FieldMap = {
  count: "count",
  state: "state",
};

const Hub_SpokeSummary_SpokeStateReasonCountsFields: FieldMap = {
  count: "count",
  stateReasonCode: "state_reason_code",
};

const Hub_SpokeSummary_SpokeTypeCountsFields: FieldMap = {
  count: "count",
  spokeType: "spoke_type",
};

const Hub_SpokeSummaryFields: FieldMap = {
  spokeStateCounts: {
    wireName: "spoke_state_counts",
    kind: "list",
    fields: Hub_SpokeSummary_SpokeStateCountsFields,
  },
  spokeStateReasonCounts: {
    wireName: "spoke_state_reason_counts",
    kind: "list",
    fields: Hub_SpokeSummary_SpokeStateReasonCountsFields,
  },
  spokeTypeCounts: {
    wireName: "spoke_type_counts",
    kind: "list",
    fields: Hub_SpokeSummary_SpokeTypeCountsFields,
  },
};

export interface HubConfig {
  /** Optional. An optional description of the hub. */
  description?: string | Computed<string>;
  /** Optional. Whether Private Service Connect connection propagation is enabled for the hub. If true, Private Service Connect endpoints in VPC spokes attached to the hub are made accessible to other VPC spokes attached to the hub. The default value is false. */
  exportPsc?: boolean | Computed<boolean>;
  /** Optional labels in key-value pair format. For more information about labels, see [Requirements for labels](https://cloud.google.com/resource-manager/docs/creating-managing-labels#requirements). */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Immutable. The name of the hub. Hub names must be unique. They use the following form: `projects/{project_number}/locations/global/hubs/{hub_id}` */
  name?: string | Computed<string>;
  /** Optional. The policy mode of this hub. This field can be either PRESET or CUSTOM. If unspecified, the policy_mode defaults to PRESET. */
  policyMode?: string | Computed<string>;
  /** Optional. The topology implemented in this hub. Currently, this field is only used when policy_mode = PRESET. The available preset topologies are MESH and STAR. If preset_topology is unspecified and policy_mode = PRESET, the preset_topology defaults to MESH. When policy_mode = CUSTOM, the preset_topology is set to PRESET_TOPOLOGY_UNSPECIFIED. */
  presetTopology?: string | Computed<string>;
  /** Summarizes information about the spokes associated with a hub. The summary includes a count of spokes according to type and according to state. If any spokes are inactive, the summary also lists the reasons they are inactive, including a count for each reason. */
  spokeSummary?: Hub_SpokeSummary | Computed<Hub_SpokeSummary>;
}

export interface HubAttrs {
  /** Output only. The time the hub was created. */
  createTime: string;
  /** Optional. An optional description of the hub. */
  description: string;
  /** Optional. Whether Private Service Connect connection propagation is enabled for the hub. If true, Private Service Connect endpoints in VPC spokes attached to the hub are made accessible to other VPC spokes attached to the hub. The default value is false. */
  exportPsc: boolean;
  /** Optional labels in key-value pair format. For more information about labels, see [Requirements for labels](https://cloud.google.com/resource-manager/docs/creating-managing-labels#requirements). */
  labels: Record<string, string>;
  /** Immutable. The name of the hub. Hub names must be unique. They use the following form: `projects/{project_number}/locations/global/hubs/{hub_id}` */
  name: string;
  /** Optional. The policy mode of this hub. This field can be either PRESET or CUSTOM. If unspecified, the policy_mode defaults to PRESET. */
  policyMode: string;
  /** Optional. The topology implemented in this hub. Currently, this field is only used when policy_mode = PRESET. The available preset topologies are MESH and STAR. If preset_topology is unspecified and policy_mode = PRESET, the preset_topology defaults to MESH. When policy_mode = CUSTOM, the preset_topology is set to PRESET_TOPOLOGY_UNSPECIFIED. */
  presetTopology: string;
  /** Output only. The route tables that belong to this hub. They use the following form: `projects/{project_number}/locations/global/hubs/{hub_id}/routeTables/{route_table_id}` This field is read-only. Network Connectivity Center automatically populates it based on the route tables nested under the hub. */
  routeTables: string[];
  /** Output only. The VPC networks associated with this hub's spokes. This field is read-only. Network Connectivity Center automatically populates it based on the set of spokes attached to the hub. */
  routingVpcs: Hub_RoutingVpcs[];
  /** Summarizes information about the spokes associated with a hub. The summary includes a count of spokes according to type and according to state. If any spokes are inactive, the summary also lists the reasons they are inactive, including a count for each reason. */
  spokeSummary: Hub_SpokeSummary;
  /** Output only. The current lifecycle state of this hub. */
  state: string;
  /** Output only. The Google-generated UUID for the hub. This value is unique across all hub resources. If a hub is deleted and another with the same name is created, the new hub is assigned a different unique_id. */
  uniqueId: string;
  /** Output only. The time the hub was last updated. */
  updateTime: string;
}

export const Hub: ResourceBinding<HubConfig, HubAttrs> = {
  wireType: "google_networkconnectivity_hub",
  fields: {
    description: "description",
    exportPsc: "export_psc",
    labels: "labels",
    name: "name",
    policyMode: "policy_mode",
    presetTopology: "preset_topology",
    spokeSummary: {
      wireName: "spoke_summary",
      kind: "object",
      fields: Hub_SpokeSummaryFields,
    },
  },
};
