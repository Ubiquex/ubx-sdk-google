// Code generated by `ubx sdk gen` from google_workstations_v1beta_workstation. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1betaWorkstation_BoostConfigs {
  id?: string | Computed<string>;
  running?: boolean | Computed<boolean>;
}

export interface V1betaWorkstation_Conditions {
  code?: number | Computed<number>;
  details?: Record<string, unknown>[] | Computed<Record<string, unknown>[]>;
  message?: string | Computed<string>;
}

export interface V1betaWorkstation_PersistentDirectories {
  mountPath?: string | Computed<string>;
  sizeGb?: number | Computed<number>;
}

export interface V1betaWorkstation_RuntimeHost_GceInstanceHost {
  /** Optional. Output only. The ID of the Compute Engine instance. */
  id?: string | Computed<string>;
  /** Optional. Output only. The name of the Compute Engine instance. */
  name?: string | Computed<string>;
  /** Optional. Output only. The zone of the Compute Engine instance. */
  zone?: string | Computed<string>;
}

export interface V1betaWorkstation_RuntimeHost {
  /** The Compute Engine instance host. */
  gceInstanceHost?: V1betaWorkstation_RuntimeHost_GceInstanceHost | Computed<V1betaWorkstation_RuntimeHost_GceInstanceHost>;
}

const V1betaWorkstation_BoostConfigsFields: FieldMap = {
  id: "id",
  running: "running",
};

const V1betaWorkstation_ConditionsFields: FieldMap = {
  code: "code",
  details: "details",
  message: "message",
};

const V1betaWorkstation_PersistentDirectoriesFields: FieldMap = {
  mountPath: "mount_path",
  sizeGb: "size_gb",
};

const V1betaWorkstation_RuntimeHost_GceInstanceHostFields: FieldMap = {
  id: "id",
  name: "name",
  zone: "zone",
};

const V1betaWorkstation_RuntimeHostFields: FieldMap = {
  gceInstanceHost: {
    wireName: "gce_instance_host",
    kind: "object",
    fields: V1betaWorkstation_RuntimeHost_GceInstanceHostFields,
  },
};

export interface V1betaWorkstationConfig {
  /** Optional. Client-specified annotations. */
  annotations?: Record<string, string> | Computed<Record<string, string>>;
  /** Output only. List of available boost configuration IDs that this workstation can be boosted up to. */
  boostConfigs?: V1betaWorkstation_BoostConfigs[] | Computed<V1betaWorkstation_BoostConfigs[]>;
  /** Output only. Status conditions describing the workstation's current state. */
  conditions?: V1betaWorkstation_Conditions[] | Computed<V1betaWorkstation_Conditions[]>;
  /** Output only. Time when this workstation was created. */
  createTime?: string | Computed<string>;
  /** Output only. Whether this workstation is in degraded mode, in which case it may require user action to restore full functionality. The conditions field contains detailed information about the status of the workstation. */
  degraded?: boolean | Computed<boolean>;
  /** Output only. Time when this workstation was soft-deleted. */
  deleteTime?: string | Computed<string>;
  /** Optional. Human-readable name for this workstation. */
  displayName?: string | Computed<string>;
  /** Optional. Environment variables passed to the workstation container's entrypoint. */
  env?: Record<string, string> | Computed<Record<string, string>>;
  /** Optional. Checksum computed by the server. May be sent on update and delete requests to make sure that the client has an up-to-date value before proceeding. */
  etag?: string | Computed<string>;
  /** Output only. Host to which clients can send HTTPS traffic that will be received by the workstation. Authorized traffic will be received to the workstation as HTTP on port 80. To send traffic to a different port, clients may prefix the host with the destination port in the format `{port}-{host}`. */
  host?: string | Computed<string>;
  /** Output only. The name of the Google Cloud KMS encryption key used to encrypt this workstation. The KMS key can only be configured in the WorkstationConfig. The expected format is `projects/* /locations/* /keyRings/* /cryptoKeys/*`. */
  kmsKey?: string | Computed<string>;
  /** Optional. [Labels](https://cloud.google.com/workstations/docs/label-resources) that are applied to the workstation and that are also propagated to the underlying Compute Engine resources. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Identifier. Full name of this workstation. */
  name?: string | Computed<string>;
  /** Optional. Directories to persist across workstation sessions. */
  persistentDirectories?: V1betaWorkstation_PersistentDirectories[] | Computed<V1betaWorkstation_PersistentDirectories[]>;
  /** Output only. Indicates whether this workstation is currently being updated to match its intended state. */
  reconciling?: boolean | Computed<boolean>;
  /** Runtime host for the workstation. */
  runtimeHost?: V1betaWorkstation_RuntimeHost | Computed<V1betaWorkstation_RuntimeHost>;
  /** Output only. Reserved for future use. */
  satisfiesPzi?: boolean | Computed<boolean>;
  /** Output only. Reserved for future use. */
  satisfiesPzs?: boolean | Computed<boolean>;
  /** Optional. The source workstation from which this workstation's persistent directories were cloned on creation. */
  sourceWorkstation?: string | Computed<string>;
  /** Output only. Time when this workstation was most recently successfully started, regardless of the workstation's initial state. */
  startTime?: string | Computed<string>;
  /** Output only. Current state of the workstation. */
  state?: string | Computed<string>;
  /** Output only. A system-assigned unique identifier for this workstation. */
  uid?: string | Computed<string>;
  /** Output only. Time when this workstation was most recently updated. */
  updateTime?: string | Computed<string>;
}

export interface V1betaWorkstationAttrs {
  /** Optional. Client-specified annotations. */
  annotations: Record<string, string>;
  /** Output only. List of available boost configuration IDs that this workstation can be boosted up to. */
  boostConfigs: V1betaWorkstation_BoostConfigs[];
  /** Output only. Status conditions describing the workstation's current state. */
  conditions: V1betaWorkstation_Conditions[];
  /** Output only. Time when this workstation was created. */
  createTime: string;
  /** Output only. Whether this workstation is in degraded mode, in which case it may require user action to restore full functionality. The conditions field contains detailed information about the status of the workstation. */
  degraded: boolean;
  /** Output only. Time when this workstation was soft-deleted. */
  deleteTime: string;
  /** Optional. Human-readable name for this workstation. */
  displayName: string;
  /** Optional. Environment variables passed to the workstation container's entrypoint. */
  env: Record<string, string>;
  /** Optional. Checksum computed by the server. May be sent on update and delete requests to make sure that the client has an up-to-date value before proceeding. */
  etag: string;
  /** Output only. Host to which clients can send HTTPS traffic that will be received by the workstation. Authorized traffic will be received to the workstation as HTTP on port 80. To send traffic to a different port, clients may prefix the host with the destination port in the format `{port}-{host}`. */
  host: string;
  /** Output only. The name of the Google Cloud KMS encryption key used to encrypt this workstation. The KMS key can only be configured in the WorkstationConfig. The expected format is `projects/* /locations/* /keyRings/* /cryptoKeys/*`. */
  kmsKey: string;
  /** Optional. [Labels](https://cloud.google.com/workstations/docs/label-resources) that are applied to the workstation and that are also propagated to the underlying Compute Engine resources. */
  labels: Record<string, string>;
  /** Identifier. Full name of this workstation. */
  name: string;
  /** Optional. Directories to persist across workstation sessions. */
  persistentDirectories: V1betaWorkstation_PersistentDirectories[];
  /** Output only. Indicates whether this workstation is currently being updated to match its intended state. */
  reconciling: boolean;
  /** Runtime host for the workstation. */
  runtimeHost: V1betaWorkstation_RuntimeHost;
  /** Output only. Reserved for future use. */
  satisfiesPzi: boolean;
  /** Output only. Reserved for future use. */
  satisfiesPzs: boolean;
  /** Optional. The source workstation from which this workstation's persistent directories were cloned on creation. */
  sourceWorkstation: string;
  /** Output only. Time when this workstation was most recently successfully started, regardless of the workstation's initial state. */
  startTime: string;
  /** Output only. Current state of the workstation. */
  state: string;
  /** Output only. A system-assigned unique identifier for this workstation. */
  uid: string;
  /** Output only. Time when this workstation was most recently updated. */
  updateTime: string;
}

export const V1betaWorkstation: ResourceBinding<V1betaWorkstationConfig, V1betaWorkstationAttrs> = {
  wireType: "google_workstations_v1beta_workstation",
  fields: {
    annotations: "annotations",
    boostConfigs: {
      wireName: "boost_configs",
      kind: "list",
      fields: V1betaWorkstation_BoostConfigsFields,
    },
    conditions: {
      wireName: "conditions",
      kind: "list",
      fields: V1betaWorkstation_ConditionsFields,
    },
    createTime: "create_time",
    degraded: "degraded",
    deleteTime: "delete_time",
    displayName: "display_name",
    env: "env",
    etag: "etag",
    host: "host",
    kmsKey: "kms_key",
    labels: "labels",
    name: "name",
    persistentDirectories: {
      wireName: "persistent_directories",
      kind: "list",
      fields: V1betaWorkstation_PersistentDirectoriesFields,
    },
    reconciling: "reconciling",
    runtimeHost: {
      wireName: "runtime_host",
      kind: "object",
      fields: V1betaWorkstation_RuntimeHostFields,
    },
    satisfiesPzi: "satisfies_pzi",
    satisfiesPzs: "satisfies_pzs",
    sourceWorkstation: "source_workstation",
    startTime: "start_time",
    state: "state",
    uid: "uid",
    updateTime: "update_time",
  },
};
