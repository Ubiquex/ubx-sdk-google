// Code generated by `ubx sdk gen` from google_dlp_dlp_connection. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface DlpConnection_CloudSql_UsernamePassword {
  /** Required. The name of the Secret Manager resource that stores the password, in the form `projects/project-id/secrets/secret-name/versions/version`. */
  passwordSecretVersionName?: string | Computed<string>;
  /** Required. The username. */
  username?: string | Computed<string>;
}

export interface DlpConnection_CloudSql {
  /** Use IAM authentication to connect. This requires the Cloud SQL IAM feature to be enabled on the instance, which is not the default for Cloud SQL. See https://docs.cloud.google.com/sql/docs/postgres/authentication and https://docs.cloud.google.com/sql/docs/mysql/authentication. */
  cloudSqlIam?: unknown | Computed<unknown>;
  /** Optional. Immutable. The Cloud SQL instance for which the connection is defined. Only one connection per instance is allowed. This can only be set at creation time, and cannot be updated. It is an error to use a connection_name from different project or region than the one that holds the connection. For example, a Connection resource for Cloud SQL connection_name `project-id:us-central1:sql-instance` must be created under the parent `projects/project-id/locations/us-central1` */
  connectionName?: string | Computed<string>;
  /** Required. The database engine used by the Cloud SQL instance that this connection configures. */
  databaseEngine?: string | Computed<string>;
  /** Required. The DLP API will limit its connections to max_connections. Must be 2 or greater. */
  maxConnections?: number | Computed<number>;
  /** A credential consisting of a username and password, where the password is stored in a Secret Manager resource. Note: Secret Manager [charges apply](https://cloud.google.com/secret-manager/pricing). */
  usernamePassword?: DlpConnection_CloudSql_UsernamePassword | Computed<DlpConnection_CloudSql_UsernamePassword>;
}

export interface DlpConnection_Connection_Errors_Details {
  code?: number | Computed<number>;
  details?: Record<string, unknown>[] | Computed<Record<string, unknown>[]>;
  message?: string | Computed<string>;
}

export interface DlpConnection_Connection_Errors {
  details?: DlpConnection_Connection_Errors_Details | Computed<DlpConnection_Connection_Errors_Details>;
  extraInfo?: string | Computed<string>;
  timestamps?: string[] | Computed<string[]>;
}

export interface DlpConnection_Connection {
  /** Cloud SQL connection properties. */
  cloudSql?: DlpConnection_CloudSql | Computed<DlpConnection_CloudSql>;
  /** Output only. Set if status == ERROR, to provide additional details. Will store the last 10 errors sorted with the most recent first. */
  errors?: DlpConnection_Connection_Errors[] | Computed<DlpConnection_Connection_Errors[]>;
  /** Output only. Name of the connection: `projects/{project}/locations/{location}/connections/{name}`. */
  name?: string | Computed<string>;
  /** Required. The connection's state in its lifecycle. */
  state?: string | Computed<string>;
}

const DlpConnection_CloudSql_UsernamePasswordFields: FieldMap = {
  passwordSecretVersionName: "password_secret_version_name",
  username: "username",
};

const DlpConnection_CloudSqlFields: FieldMap = {
  cloudSqlIam: "cloud_sql_iam",
  connectionName: "connection_name",
  databaseEngine: "database_engine",
  maxConnections: "max_connections",
  usernamePassword: {
    wireName: "username_password",
    kind: "object",
    fields: DlpConnection_CloudSql_UsernamePasswordFields,
  },
};

const DlpConnection_Connection_Errors_DetailsFields: FieldMap = {
  code: "code",
  details: "details",
  message: "message",
};

const DlpConnection_Connection_ErrorsFields: FieldMap = {
  details: {
    wireName: "details",
    kind: "object",
    fields: DlpConnection_Connection_Errors_DetailsFields,
  },
  extraInfo: "extra_info",
  timestamps: "timestamps",
};

const DlpConnection_ConnectionFields: FieldMap = {
  cloudSql: {
    wireName: "cloud_sql",
    kind: "object",
    fields: DlpConnection_CloudSqlFields,
  },
  errors: {
    wireName: "errors",
    kind: "list",
    fields: DlpConnection_Connection_ErrorsFields,
  },
  name: "name",
  state: "state",
};

export interface DlpConnectionConfig {
  /** A data connection to allow the DLP API to profile data in locations that require additional configuration. */
  connection?: DlpConnection_Connection | Computed<DlpConnection_Connection>;
}

export interface DlpConnectionAttrs {
  /** Cloud SQL connection properties. */
  cloudSql: DlpConnection_CloudSql;
  /** A data connection to allow the DLP API to profile data in locations that require additional configuration. */
  connection: DlpConnection_Connection;
  /** Output only. Set if status == ERROR, to provide additional details. Will store the last 10 errors sorted with the most recent first. */
  errors: DlpConnection_Connection_Errors[];
  /** Output only. Name of the connection: `projects/{project}/locations/{location}/connections/{name}`. */
  name: string;
  /** Required. The connection's state in its lifecycle. */
  state: string;
}

export const DlpConnection: ResourceBinding<DlpConnectionConfig, DlpConnectionAttrs> = {
  wireType: "google_dlp_dlp_connection",
  fields: {
    connection: {
      wireName: "connection",
      kind: "object",
      fields: DlpConnection_ConnectionFields,
    },
  },
};
