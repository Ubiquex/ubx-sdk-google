// Code generated by `ubx sdk gen` from google_monitoring_service. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Service_AppEngine {
  /** The ID of the App Engine module underlying this service. Corresponds to the module_id resource label in the gae_app monitored resource (https://cloud.google.com/monitoring/api/resources#tag_gae_app). */
  moduleId?: string | Computed<string>;
}

export interface Service_BasicService {
  /** Labels that specify the resource that emits the monitoring data which is used for SLO reporting of this Service. Documentation and valid values for given service types here (https://cloud.google.com/stackdriver/docs/solutions/slo-monitoring/api/api-structures#basic-svc-w-basic-sli). */
  serviceLabels?: Record<string, string> | Computed<Record<string, string>>;
  /** The type of service that this basic service defines, e.g. APP_ENGINE service type. Documentation and valid values here (https://cloud.google.com/stackdriver/docs/solutions/slo-monitoring/api/api-structures#basic-svc-w-basic-sli). */
  serviceType?: string | Computed<string>;
}

export interface Service_CloudEndpoints {
  /** The name of the Cloud Endpoints service underlying this service. Corresponds to the service resource label in the api monitored resource (https://cloud.google.com/monitoring/api/resources#tag_api). */
  service?: string | Computed<string>;
}

export interface Service_CloudRun {
  /** The location the service is run. Corresponds to the location resource label in the cloud_run_revision monitored resource (https://cloud.google.com/monitoring/api/resources#tag_cloud_run_revision). */
  location?: string | Computed<string>;
  /** The name of the Cloud Run service. Corresponds to the service_name resource label in the cloud_run_revision monitored resource (https://cloud.google.com/monitoring/api/resources#tag_cloud_run_revision). */
  serviceName?: string | Computed<string>;
}

export interface Service_ClusterIstio {
  /** The name of the Kubernetes cluster in which this Istio service is defined. Corresponds to the cluster_name resource label in k8s_cluster resources. */
  clusterName?: string | Computed<string>;
  /** The location of the Kubernetes cluster in which this Istio service is defined. Corresponds to the location resource label in k8s_cluster resources. */
  location?: string | Computed<string>;
  /** The name of the Istio service underlying this service. Corresponds to the destination_service_name metric label in Istio metrics. */
  serviceName?: string | Computed<string>;
  /** The namespace of the Istio service underlying this service. Corresponds to the destination_service_namespace metric label in Istio metrics. */
  serviceNamespace?: string | Computed<string>;
}

export interface Service_GkeNamespace {
  /** The name of the parent cluster. */
  clusterName?: string | Computed<string>;
  /** The location of the parent cluster. This may be a zone or region. */
  location?: string | Computed<string>;
  /** The name of this namespace. */
  namespaceName?: string | Computed<string>;
  /** Output only. The project this resource lives in. For legacy services migrated from the Custom type, this may be a distinct project from the one parenting the service itself. */
  projectId?: string | Computed<string>;
}

export interface Service_GkeService {
  /** The name of the parent cluster. */
  clusterName?: string | Computed<string>;
  /** The location of the parent cluster. This may be a zone or region. */
  location?: string | Computed<string>;
  /** The name of the parent namespace. */
  namespaceName?: string | Computed<string>;
  /** Output only. The project this resource lives in. For legacy services migrated from the Custom type, this may be a distinct project from the one parenting the service itself. */
  projectId?: string | Computed<string>;
  /** The name of this service. */
  serviceName?: string | Computed<string>;
}

export interface Service_GkeWorkload {
  /** The name of the parent cluster. */
  clusterName?: string | Computed<string>;
  /** The location of the parent cluster. This may be a zone or region. */
  location?: string | Computed<string>;
  /** The name of the parent namespace. */
  namespaceName?: string | Computed<string>;
  /** Output only. The project this resource lives in. For legacy services migrated from the Custom type, this may be a distinct project from the one parenting the service itself. */
  projectId?: string | Computed<string>;
  /** The name of this workload. */
  topLevelControllerName?: string | Computed<string>;
  /** The type of this workload (for example, "Deployment" or "DaemonSet") */
  topLevelControllerType?: string | Computed<string>;
}

export interface Service_IstioCanonicalService {
  /** The name of the canonical service underlying this service. Corresponds to the destination_canonical_service_name metric label in label in Istio metrics (https://cloud.google.com/monitoring/api/metrics_istio). */
  canonicalService?: string | Computed<string>;
  /** The namespace of the canonical service underlying this service. Corresponds to the destination_canonical_service_namespace metric label in Istio metrics (https://cloud.google.com/monitoring/api/metrics_istio). */
  canonicalServiceNamespace?: string | Computed<string>;
  /** Identifier for the Istio mesh in which this canonical service is defined. Corresponds to the mesh_uid metric label in Istio metrics (https://cloud.google.com/monitoring/api/metrics_istio). */
  meshUid?: string | Computed<string>;
}

export interface Service_MeshIstio {
  /** Identifier for the mesh in which this Istio service is defined. Corresponds to the mesh_uid metric label in Istio metrics. */
  meshUid?: string | Computed<string>;
  /** The name of the Istio service underlying this service. Corresponds to the destination_service_name metric label in Istio metrics. */
  serviceName?: string | Computed<string>;
  /** The namespace of the Istio service underlying this service. Corresponds to the destination_service_namespace metric label in Istio metrics. */
  serviceNamespace?: string | Computed<string>;
}

export interface Service_Telemetry {
  /** The full name of the resource that defines this service. Formatted as described in https://cloud.google.com/apis/design/resource_names. */
  resourceName?: string | Computed<string>;
}

const Service_AppEngineFields: FieldMap = {
  moduleId: "module_id",
};

const Service_BasicServiceFields: FieldMap = {
  serviceLabels: "service_labels",
  serviceType: "service_type",
};

const Service_CloudEndpointsFields: FieldMap = {
  service: "service",
};

const Service_CloudRunFields: FieldMap = {
  location: "location",
  serviceName: "service_name",
};

const Service_ClusterIstioFields: FieldMap = {
  clusterName: "cluster_name",
  location: "location",
  serviceName: "service_name",
  serviceNamespace: "service_namespace",
};

const Service_GkeNamespaceFields: FieldMap = {
  clusterName: "cluster_name",
  location: "location",
  namespaceName: "namespace_name",
  projectId: "project_id",
};

const Service_GkeServiceFields: FieldMap = {
  clusterName: "cluster_name",
  location: "location",
  namespaceName: "namespace_name",
  projectId: "project_id",
  serviceName: "service_name",
};

const Service_GkeWorkloadFields: FieldMap = {
  clusterName: "cluster_name",
  location: "location",
  namespaceName: "namespace_name",
  projectId: "project_id",
  topLevelControllerName: "top_level_controller_name",
  topLevelControllerType: "top_level_controller_type",
};

const Service_IstioCanonicalServiceFields: FieldMap = {
  canonicalService: "canonical_service",
  canonicalServiceNamespace: "canonical_service_namespace",
  meshUid: "mesh_uid",
};

const Service_MeshIstioFields: FieldMap = {
  meshUid: "mesh_uid",
  serviceName: "service_name",
  serviceNamespace: "service_namespace",
};

const Service_TelemetryFields: FieldMap = {
  resourceName: "resource_name",
};

export interface ServiceConfig {
  /** App Engine service. Learn more at https://cloud.google.com/appengine. */
  appEngine?: Service_AppEngine | Computed<Service_AppEngine>;
  /** A well-known service type, defined by its service type and service labels. Documentation and examples here (https://cloud.google.com/stackdriver/docs/solutions/slo-monitoring/api/api-structures#basic-svc-w-basic-sli). */
  basicService?: Service_BasicService | Computed<Service_BasicService>;
  /** Cloud Endpoints service. Learn more at https://cloud.google.com/endpoints. */
  cloudEndpoints?: Service_CloudEndpoints | Computed<Service_CloudEndpoints>;
  /** Cloud Run service. Learn more at https://cloud.google.com/run. */
  cloudRun?: Service_CloudRun | Computed<Service_CloudRun>;
  /** Istio service scoped to a single Kubernetes cluster. Learn more at https://istio.io. Clusters running OSS Istio will have their services ingested as this type. */
  clusterIstio?: Service_ClusterIstio | Computed<Service_ClusterIstio>;
  /** Use a custom service to designate a service that you want to monitor when none of the other service types (like App Engine, Cloud Run, or a GKE type) matches your intended service. */
  custom?: unknown | Computed<unknown>;
  /** Name used for UI elements listing this Service. */
  displayName?: string | Computed<string>;
  /** GKE Namespace. The field names correspond to the resource metadata labels on monitored resources that fall under a namespace (for example, k8s_container or k8s_pod). */
  gkeNamespace?: Service_GkeNamespace | Computed<Service_GkeNamespace>;
  /** GKE Service. The "service" here represents a Kubernetes service object (https://kubernetes.io/docs/concepts/services-networking/service). The field names correspond to the resource labels on k8s_service monitored resources (https://cloud.google.com/monitoring/api/resources#tag_k8s_service). */
  gkeService?: Service_GkeService | Computed<Service_GkeService>;
  /** A GKE Workload (Deployment, StatefulSet, etc). The field names correspond to the metadata labels on monitored resources that fall under a workload (for example, k8s_container or k8s_pod). */
  gkeWorkload?: Service_GkeWorkload | Computed<Service_GkeWorkload>;
  /** Canonical service scoped to an Istio mesh. Anthos clusters running ASM >= 1.6.8 will have their services ingested as this type. */
  istioCanonicalService?: Service_IstioCanonicalService | Computed<Service_IstioCanonicalService>;
  /** Istio service scoped to an Istio mesh. Anthos clusters running ASM < 1.6.8 will have their services ingested as this type. */
  meshIstio?: Service_MeshIstio | Computed<Service_MeshIstio>;
  /** Identifier. Resource name for this Service. The format is: projects/[PROJECT_ID_OR_NUMBER]/services/[SERVICE_ID] */
  name?: string | Computed<string>;
  /** Configuration for how to query telemetry on a Service. */
  telemetry?: Service_Telemetry | Computed<Service_Telemetry>;
  /** Labels which have been used to annotate the service. Label keys must start with a letter. Label keys and values may contain lowercase letters, numbers, underscores, and dashes. Label keys and values have a maximum length of 63 characters, and must be less than 128 bytes in size. Up to 64 label entries may be stored. For labels which do not have a semantic value, the empty string may be supplied for the label value. */
  userLabels?: Record<string, string> | Computed<Record<string, string>>;
}

export interface ServiceAttrs {
  /** App Engine service. Learn more at https://cloud.google.com/appengine. */
  appEngine: Service_AppEngine;
  /** A well-known service type, defined by its service type and service labels. Documentation and examples here (https://cloud.google.com/stackdriver/docs/solutions/slo-monitoring/api/api-structures#basic-svc-w-basic-sli). */
  basicService: Service_BasicService;
  /** Cloud Endpoints service. Learn more at https://cloud.google.com/endpoints. */
  cloudEndpoints: Service_CloudEndpoints;
  /** Cloud Run service. Learn more at https://cloud.google.com/run. */
  cloudRun: Service_CloudRun;
  /** Istio service scoped to a single Kubernetes cluster. Learn more at https://istio.io. Clusters running OSS Istio will have their services ingested as this type. */
  clusterIstio: Service_ClusterIstio;
  /** Use a custom service to designate a service that you want to monitor when none of the other service types (like App Engine, Cloud Run, or a GKE type) matches your intended service. */
  custom: unknown;
  /** Name used for UI elements listing this Service. */
  displayName: string;
  /** GKE Namespace. The field names correspond to the resource metadata labels on monitored resources that fall under a namespace (for example, k8s_container or k8s_pod). */
  gkeNamespace: Service_GkeNamespace;
  /** GKE Service. The "service" here represents a Kubernetes service object (https://kubernetes.io/docs/concepts/services-networking/service). The field names correspond to the resource labels on k8s_service monitored resources (https://cloud.google.com/monitoring/api/resources#tag_k8s_service). */
  gkeService: Service_GkeService;
  /** A GKE Workload (Deployment, StatefulSet, etc). The field names correspond to the metadata labels on monitored resources that fall under a workload (for example, k8s_container or k8s_pod). */
  gkeWorkload: Service_GkeWorkload;
  /** Canonical service scoped to an Istio mesh. Anthos clusters running ASM >= 1.6.8 will have their services ingested as this type. */
  istioCanonicalService: Service_IstioCanonicalService;
  /** Istio service scoped to an Istio mesh. Anthos clusters running ASM < 1.6.8 will have their services ingested as this type. */
  meshIstio: Service_MeshIstio;
  /** Identifier. Resource name for this Service. The format is: projects/[PROJECT_ID_OR_NUMBER]/services/[SERVICE_ID] */
  name: string;
  /** Configuration for how to query telemetry on a Service. */
  telemetry: Service_Telemetry;
  /** Labels which have been used to annotate the service. Label keys must start with a letter. Label keys and values may contain lowercase letters, numbers, underscores, and dashes. Label keys and values have a maximum length of 63 characters, and must be less than 128 bytes in size. Up to 64 label entries may be stored. For labels which do not have a semantic value, the empty string may be supplied for the label value. */
  userLabels: Record<string, string>;
}

export const Service: ResourceBinding<ServiceConfig, ServiceAttrs> = {
  wireType: "google_monitoring_service",
  fields: {
    appEngine: {
      wireName: "app_engine",
      kind: "object",
      fields: Service_AppEngineFields,
    },
    basicService: {
      wireName: "basic_service",
      kind: "object",
      fields: Service_BasicServiceFields,
    },
    cloudEndpoints: {
      wireName: "cloud_endpoints",
      kind: "object",
      fields: Service_CloudEndpointsFields,
    },
    cloudRun: {
      wireName: "cloud_run",
      kind: "object",
      fields: Service_CloudRunFields,
    },
    clusterIstio: {
      wireName: "cluster_istio",
      kind: "object",
      fields: Service_ClusterIstioFields,
    },
    custom: "custom",
    displayName: "display_name",
    gkeNamespace: {
      wireName: "gke_namespace",
      kind: "object",
      fields: Service_GkeNamespaceFields,
    },
    gkeService: {
      wireName: "gke_service",
      kind: "object",
      fields: Service_GkeServiceFields,
    },
    gkeWorkload: {
      wireName: "gke_workload",
      kind: "object",
      fields: Service_GkeWorkloadFields,
    },
    istioCanonicalService: {
      wireName: "istio_canonical_service",
      kind: "object",
      fields: Service_IstioCanonicalServiceFields,
    },
    meshIstio: {
      wireName: "mesh_istio",
      kind: "object",
      fields: Service_MeshIstioFields,
    },
    name: "name",
    telemetry: {
      wireName: "telemetry",
      kind: "object",
      fields: Service_TelemetryFields,
    },
    userLabels: "user_labels",
  },
};
