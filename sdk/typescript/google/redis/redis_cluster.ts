// Code generated by `ubx sdk gen` from google_redis_redis_cluster. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface RedisCluster_AclPolicyInfo_AclPolicyRevisionStatuses {
  aclPolicyRevision?: string | Computed<string>;
  aclPolicyRevisionNumber?: string | Computed<string>;
  errorMessage?: string | Computed<string>;
  state?: string | Computed<string>;
}

export interface RedisCluster_AclPolicyInfo {
  /** Output only. A list of status for various revisions of this ACL policy on the cluster. */
  aclPolicyRevisionStatuses?: RedisCluster_AclPolicyInfo_AclPolicyRevisionStatuses[] | Computed<RedisCluster_AclPolicyInfo_AclPolicyRevisionStatuses[]>;
  /** Output only. The resource name of the applied ACL policy. Format: "projects/{project}/locations/{location}/aclPolicies/{acl_policy}" */
  appliedAclPolicy?: string | Computed<string>;
  /** Output only. The resource name of the applied ACL policy revision. Format: "projects/{project}/locations/{location}/aclPolicies/{acl_policy}/revisions/{revision}" */
  appliedAclPolicyRevision?: string | Computed<string>;
  /** Output only. The revision number of the applied ACL policy revision. */
  appliedAclPolicyRevisionNumber?: string | Computed<string>;
}

export interface RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule_StartTime {
  /** Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value "24:00:00" for scenarios like business closing time. */
  hours?: number | Computed<number>;
  /** Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59. */
  minutes?: number | Computed<number>;
  /** Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999. */
  nanos?: number | Computed<number>;
  /** Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds. */
  seconds?: number | Computed<number>;
}

export interface RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule {
  /** Represents a time of day. The date and time zone are either not significant or are specified elsewhere. An API may choose to allow leap seconds. Related types are google.type.Date and `google.protobuf.Timestamp`. */
  startTime?: RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule_StartTime | Computed<RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule_StartTime>;
}

export interface RedisCluster_AutomatedBackupConfig {
  /** Optional. The automated backup mode. If the mode is disabled, the other fields will be ignored. */
  automatedBackupMode?: string | Computed<string>;
  /** This schedule allows the backup to be triggered at a fixed frequency (currently only daily is supported). */
  fixedFrequencySchedule?: RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule | Computed<RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule>;
  /** Optional. How long to keep automated backups before the backups are deleted. The value should be between 1 day and 365 days. If not specified, the default value is 35 days. */
  retention?: string | Computed<string>;
}

export interface RedisCluster_ClusterEndpoints_Connections_PscAutoConnection {
  address?: string | Computed<string>;
  connectionType?: string | Computed<string>;
  forwardingRule?: string | Computed<string>;
  network?: string | Computed<string>;
  projectId?: string | Computed<string>;
  pscConnectionId?: string | Computed<string>;
  pscConnectionStatus?: string | Computed<string>;
  serviceAttachment?: string | Computed<string>;
}

export interface RedisCluster_ClusterEndpoints_Connections_PscConnection {
  address?: string | Computed<string>;
  connectionType?: string | Computed<string>;
  forwardingRule?: string | Computed<string>;
  network?: string | Computed<string>;
  port?: number | Computed<number>;
  projectId?: string | Computed<string>;
  pscConnectionId?: string | Computed<string>;
  pscConnectionStatus?: string | Computed<string>;
  serviceAttachment?: string | Computed<string>;
}

export interface RedisCluster_ClusterEndpoints_Connections {
  pscAutoConnection?: RedisCluster_ClusterEndpoints_Connections_PscAutoConnection | Computed<RedisCluster_ClusterEndpoints_Connections_PscAutoConnection>;
  pscConnection?: RedisCluster_ClusterEndpoints_Connections_PscConnection | Computed<RedisCluster_ClusterEndpoints_Connections_PscConnection>;
}

export interface RedisCluster_ClusterEndpoints {
  connections?: RedisCluster_ClusterEndpoints_Connections[] | Computed<RedisCluster_ClusterEndpoints_Connections[]>;
}

export interface RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster {
  /** Output only. The full resource path of the remote cluster in the format: projects//locations//clusters/ */
  cluster?: string | Computed<string>;
  /** Output only. The unique identifier of the remote cluster. */
  uid?: string | Computed<string>;
}

export interface RedisCluster_CrossClusterReplicationConfig_Membership {
  /** Details of the remote cluster associated with this cluster in a cross cluster replication setup. */
  primaryCluster?: RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster | Computed<RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster>;
  /** Output only. The list of secondary clusters replicating from the primary cluster. */
  secondaryClusters?: RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster[] | Computed<RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster[]>;
}

export interface RedisCluster_CrossClusterReplicationConfig {
  /** Output only. The role of the cluster in cross cluster replication. */
  clusterRole?: string | Computed<string>;
  /** An output only view of all the member clusters participating in the cross cluster replication. */
  membership?: RedisCluster_CrossClusterReplicationConfig_Membership | Computed<RedisCluster_CrossClusterReplicationConfig_Membership>;
  /** Details of the remote cluster associated with this cluster in a cross cluster replication setup. */
  primaryCluster?: RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster | Computed<RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster>;
  /** List of secondary clusters that are replicating from this primary cluster. This field is only set for a primary cluster. */
  secondaryClusters?: RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster[] | Computed<RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryCluster[]>;
  /** Output only. The last time cross cluster replication config was updated. */
  updateTime?: string | Computed<string>;
}

export interface RedisCluster_DiscoveryEndpoints_PscConfig {
  network?: string | Computed<string>;
}

export interface RedisCluster_DiscoveryEndpoints {
  address?: string | Computed<string>;
  port?: number | Computed<number>;
  pscConfig?: RedisCluster_DiscoveryEndpoints_PscConfig | Computed<RedisCluster_DiscoveryEndpoints_PscConfig>;
}

export interface RedisCluster_EncryptionInfo {
  /** Output only. Type of encryption. */
  encryptionType?: string | Computed<string>;
  /** Output only. The state of the primary version of the KMS key perceived by the system. This field is not populated in backups. */
  kmsKeyPrimaryState?: string | Computed<string>;
  /** Output only. KMS key versions that are being used to protect the data at-rest. */
  kmsKeyVersions?: string[] | Computed<string[]>;
  /** Output only. The most recent time when the encryption info was updated. */
  lastUpdateTime?: string | Computed<string>;
}

export interface RedisCluster_GcsSource {
  /** Optional. URIs of the Cloud Storage objects to import. Example: gs://bucket1/object1, gs://bucket2/folder2/object2 */
  uris?: string[] | Computed<string[]>;
}

export interface RedisCluster_MaintenancePolicy_WeeklyMaintenanceWindow {
  day?: string | Computed<string>;
  startTime?: RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule_StartTime | Computed<RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule_StartTime>;
}

export interface RedisCluster_MaintenancePolicy {
  /** Output only. The time when the policy was created i.e. Maintenance Window or Deny Period was assigned. */
  createTime?: string | Computed<string>;
  /** Output only. The time when the policy was updated i.e. Maintenance Window or Deny Period was updated. */
  updateTime?: string | Computed<string>;
  /** Optional. Maintenance window that is applied to resources covered by this policy. Minimum 1. For the current version, the maximum number of weekly_maintenance_window is expected to be one. */
  weeklyMaintenanceWindow?: RedisCluster_MaintenancePolicy_WeeklyMaintenanceWindow[] | Computed<RedisCluster_MaintenancePolicy_WeeklyMaintenanceWindow[]>;
}

export interface RedisCluster_MaintenanceSchedule {
  /** Output only. The end time of any upcoming scheduled maintenance for this instance. */
  endTime?: string | Computed<string>;
  /** Output only. The start time of any upcoming scheduled maintenance for this instance. */
  startTime?: string | Computed<string>;
}

export interface RedisCluster_ManagedBackupSource {
  /** Optional. Example: //redis.googleapis.com/projects/{project}/locations/{location}/backupCollections/{collection}/backups/{backup} A shorter version (without the prefix) of the backup name is also supported, like projects/{project}/locations/{location}/backupCollections/{collection}/backups/{backup_id} In this case, it assumes the backup is under redis.googleapis.com. */
  backup?: string | Computed<string>;
}

export interface RedisCluster_PersistenceConfig_AofConfig {
  /** Optional. fsync configuration. */
  appendFsync?: string | Computed<string>;
}

export interface RedisCluster_PersistenceConfig_RdbConfig {
  /** Optional. Period between RDB snapshots. */
  rdbSnapshotPeriod?: string | Computed<string>;
  /** Optional. The time that the first snapshot was/will be attempted, and to which future snapshots will be aligned. If not provided, the current time will be used. */
  rdbSnapshotStartTime?: string | Computed<string>;
}

export interface RedisCluster_PersistenceConfig {
  /** Configuration of the AOF based persistence. */
  aofConfig?: RedisCluster_PersistenceConfig_AofConfig | Computed<RedisCluster_PersistenceConfig_AofConfig>;
  /** Optional. The mode of persistence. */
  mode?: string | Computed<string>;
  /** Configuration of the RDB based persistence. */
  rdbConfig?: RedisCluster_PersistenceConfig_RdbConfig | Computed<RedisCluster_PersistenceConfig_RdbConfig>;
}

export interface RedisCluster_PscServiceAttachments {
  connectionType?: string | Computed<string>;
  serviceAttachment?: string | Computed<string>;
}

export interface RedisCluster_StateInfo_UpdateInfo {
  /** Target node type for redis cluster. */
  targetNodeType?: string | Computed<string>;
  /** Target number of replica nodes per shard. */
  targetReplicaCount?: number | Computed<number>;
  /** Target number of shards for redis cluster */
  targetShardCount?: number | Computed<number>;
}

export interface RedisCluster_StateInfo {
  /** Represents information about an updating cluster. */
  updateInfo?: RedisCluster_StateInfo_UpdateInfo | Computed<RedisCluster_StateInfo_UpdateInfo>;
}

export interface RedisCluster_ZoneDistributionConfig {
  /** Optional. The mode of zone distribution. Defaults to MULTI_ZONE, when not specified. */
  mode?: string | Computed<string>;
  /** Optional. When SINGLE ZONE distribution is selected, zone field would be used to allocate all resources in that zone. This is not applicable to MULTI_ZONE, and would be ignored for MULTI_ZONE clusters. */
  zone?: string | Computed<string>;
  /** Optional. Specify the zones of a multi-zone cluster where Redis Cluster allocates resources. This flag isn't applicable for single-zone clusters. */
  zones?: string[] | Computed<string[]>;
}

const RedisCluster_AclPolicyInfo_AclPolicyRevisionStatusesFields: FieldMap = {
  aclPolicyRevision: "acl_policy_revision",
  aclPolicyRevisionNumber: "acl_policy_revision_number",
  errorMessage: "error_message",
  state: "state",
};

const RedisCluster_AclPolicyInfoFields: FieldMap = {
  aclPolicyRevisionStatuses: {
    wireName: "acl_policy_revision_statuses",
    kind: "list",
    fields: RedisCluster_AclPolicyInfo_AclPolicyRevisionStatusesFields,
  },
  appliedAclPolicy: "applied_acl_policy",
  appliedAclPolicyRevision: "applied_acl_policy_revision",
  appliedAclPolicyRevisionNumber: "applied_acl_policy_revision_number",
};

const RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule_StartTimeFields: FieldMap = {
  hours: "hours",
  minutes: "minutes",
  nanos: "nanos",
  seconds: "seconds",
};

const RedisCluster_AutomatedBackupConfig_FixedFrequencyScheduleFields: FieldMap = {
  startTime: {
    wireName: "start_time",
    kind: "object",
    fields: RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule_StartTimeFields,
  },
};

const RedisCluster_AutomatedBackupConfigFields: FieldMap = {
  automatedBackupMode: "automated_backup_mode",
  fixedFrequencySchedule: {
    wireName: "fixed_frequency_schedule",
    kind: "object",
    fields: RedisCluster_AutomatedBackupConfig_FixedFrequencyScheduleFields,
  },
  retention: "retention",
};

const RedisCluster_ClusterEndpoints_Connections_PscAutoConnectionFields: FieldMap = {
  address: "address",
  connectionType: "connection_type",
  forwardingRule: "forwarding_rule",
  network: "network",
  projectId: "project_id",
  pscConnectionId: "psc_connection_id",
  pscConnectionStatus: "psc_connection_status",
  serviceAttachment: "service_attachment",
};

const RedisCluster_ClusterEndpoints_Connections_PscConnectionFields: FieldMap = {
  address: "address",
  connectionType: "connection_type",
  forwardingRule: "forwarding_rule",
  network: "network",
  port: "port",
  projectId: "project_id",
  pscConnectionId: "psc_connection_id",
  pscConnectionStatus: "psc_connection_status",
  serviceAttachment: "service_attachment",
};

const RedisCluster_ClusterEndpoints_ConnectionsFields: FieldMap = {
  pscAutoConnection: {
    wireName: "psc_auto_connection",
    kind: "object",
    fields: RedisCluster_ClusterEndpoints_Connections_PscAutoConnectionFields,
  },
  pscConnection: {
    wireName: "psc_connection",
    kind: "object",
    fields: RedisCluster_ClusterEndpoints_Connections_PscConnectionFields,
  },
};

const RedisCluster_ClusterEndpointsFields: FieldMap = {
  connections: {
    wireName: "connections",
    kind: "list",
    fields: RedisCluster_ClusterEndpoints_ConnectionsFields,
  },
};

const RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryClusterFields: FieldMap = {
  cluster: "cluster",
  uid: "uid",
};

const RedisCluster_CrossClusterReplicationConfig_MembershipFields: FieldMap = {
  primaryCluster: {
    wireName: "primary_cluster",
    kind: "object",
    fields: RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryClusterFields,
  },
  secondaryClusters: {
    wireName: "secondary_clusters",
    kind: "list",
    fields: RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryClusterFields,
  },
};

const RedisCluster_CrossClusterReplicationConfigFields: FieldMap = {
  clusterRole: "cluster_role",
  membership: {
    wireName: "membership",
    kind: "object",
    fields: RedisCluster_CrossClusterReplicationConfig_MembershipFields,
  },
  primaryCluster: {
    wireName: "primary_cluster",
    kind: "object",
    fields: RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryClusterFields,
  },
  secondaryClusters: {
    wireName: "secondary_clusters",
    kind: "list",
    fields: RedisCluster_CrossClusterReplicationConfig_Membership_PrimaryClusterFields,
  },
  updateTime: "update_time",
};

const RedisCluster_DiscoveryEndpoints_PscConfigFields: FieldMap = {
  network: "network",
};

const RedisCluster_DiscoveryEndpointsFields: FieldMap = {
  address: "address",
  port: "port",
  pscConfig: {
    wireName: "psc_config",
    kind: "object",
    fields: RedisCluster_DiscoveryEndpoints_PscConfigFields,
  },
};

const RedisCluster_EncryptionInfoFields: FieldMap = {
  encryptionType: "encryption_type",
  kmsKeyPrimaryState: "kms_key_primary_state",
  kmsKeyVersions: "kms_key_versions",
  lastUpdateTime: "last_update_time",
};

const RedisCluster_GcsSourceFields: FieldMap = {
  uris: "uris",
};

const RedisCluster_MaintenancePolicy_WeeklyMaintenanceWindowFields: FieldMap = {
  day: "day",
  startTime: {
    wireName: "start_time",
    kind: "object",
    fields: RedisCluster_AutomatedBackupConfig_FixedFrequencySchedule_StartTimeFields,
  },
};

const RedisCluster_MaintenancePolicyFields: FieldMap = {
  createTime: "create_time",
  updateTime: "update_time",
  weeklyMaintenanceWindow: {
    wireName: "weekly_maintenance_window",
    kind: "list",
    fields: RedisCluster_MaintenancePolicy_WeeklyMaintenanceWindowFields,
  },
};

const RedisCluster_MaintenanceScheduleFields: FieldMap = {
  endTime: "end_time",
  startTime: "start_time",
};

const RedisCluster_ManagedBackupSourceFields: FieldMap = {
  backup: "backup",
};

const RedisCluster_PersistenceConfig_AofConfigFields: FieldMap = {
  appendFsync: "append_fsync",
};

const RedisCluster_PersistenceConfig_RdbConfigFields: FieldMap = {
  rdbSnapshotPeriod: "rdb_snapshot_period",
  rdbSnapshotStartTime: "rdb_snapshot_start_time",
};

const RedisCluster_PersistenceConfigFields: FieldMap = {
  aofConfig: {
    wireName: "aof_config",
    kind: "object",
    fields: RedisCluster_PersistenceConfig_AofConfigFields,
  },
  mode: "mode",
  rdbConfig: {
    wireName: "rdb_config",
    kind: "object",
    fields: RedisCluster_PersistenceConfig_RdbConfigFields,
  },
};

const RedisCluster_PscServiceAttachmentsFields: FieldMap = {
  connectionType: "connection_type",
  serviceAttachment: "service_attachment",
};

const RedisCluster_StateInfo_UpdateInfoFields: FieldMap = {
  targetNodeType: "target_node_type",
  targetReplicaCount: "target_replica_count",
  targetShardCount: "target_shard_count",
};

const RedisCluster_StateInfoFields: FieldMap = {
  updateInfo: {
    wireName: "update_info",
    kind: "object",
    fields: RedisCluster_StateInfo_UpdateInfoFields,
  },
};

const RedisCluster_ZoneDistributionConfigFields: FieldMap = {
  mode: "mode",
  zone: "zone",
  zones: "zones",
};

export interface RedisClusterConfig {
  /** Optional. The ACL policy to be applied to the cluster. */
  aclPolicy?: string | Computed<string>;
  /** Optional. Output only. Deprecated: Indicates whether the ACL rules applied to the cluster are in sync. */
  aclPolicyInSync?: boolean | Computed<boolean>;
  /** Details of the applied ACL policy. */
  aclPolicyInfo?: RedisCluster_AclPolicyInfo | Computed<RedisCluster_AclPolicyInfo>;
  /** Optional. Immutable. Deprecated, do not use. */
  allowFewerZonesDeployment?: boolean | Computed<boolean>;
  /** Optional. If true, cluster endpoints that are created and registered by customers can be deleted asynchronously. That is, such a cluster endpoint can be de-registered before the forwarding rules in the cluster endpoint are deleted. */
  asyncClusterEndpointsDeletionEnabled?: boolean | Computed<boolean>;
  /** Optional. The authorization mode of the Redis cluster. If not provided, auth feature is disabled for the cluster. */
  authorizationMode?: string | Computed<string>;
  /** The automated backup config for a cluster. */
  automatedBackupConfig?: RedisCluster_AutomatedBackupConfig | Computed<RedisCluster_AutomatedBackupConfig>;
  /** Output only. This field is used to determine the available maintenance versions for the self service update. */
  availableMaintenanceVersions?: string[] | Computed<string[]>;
  /** Optional. Output only. The backup collection full resource name. Example: projects/{project}/locations/{location}/backupCollections/{collection} */
  backupCollection?: string | Computed<string>;
  /** Optional. A list of cluster endpoints. */
  clusterEndpoints?: RedisCluster_ClusterEndpoints[] | Computed<RedisCluster_ClusterEndpoints[]>;
  /** Output only. The timestamp associated with the cluster creation request. */
  createTime?: string | Computed<string>;
  /** Cross cluster replication config. */
  crossClusterReplicationConfig?: RedisCluster_CrossClusterReplicationConfig | Computed<RedisCluster_CrossClusterReplicationConfig>;
  /** Optional. The delete operation will fail when the value is set to true. */
  deletionProtectionEnabled?: boolean | Computed<boolean>;
  /** Output only. Endpoints created on each given network, for Redis clients to connect to the cluster. Currently only one discovery endpoint is supported. */
  discoveryEndpoints?: RedisCluster_DiscoveryEndpoints[] | Computed<RedisCluster_DiscoveryEndpoints[]>;
  /** Output only. This field represents the actual maintenance version of the cluster. */
  effectiveMaintenanceVersion?: string | Computed<string>;
  /** EncryptionInfo describes the encryption information of a cluster or a backup. */
  encryptionInfo?: RedisCluster_EncryptionInfo | Computed<RedisCluster_EncryptionInfo>;
  /** Backups stored in Cloud Storage buckets. The Cloud Storage buckets need to be the same region as the clusters. */
  gcsSource?: RedisCluster_GcsSource | Computed<RedisCluster_GcsSource>;
  /** Optional. The KMS key used to encrypt the at-rest data of the cluster. */
  kmsKey?: string | Computed<string>;
  /** Optional. Labels to represent user-provided metadata. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Maintenance policy per cluster. */
  maintenancePolicy?: RedisCluster_MaintenancePolicy | Computed<RedisCluster_MaintenancePolicy>;
  /** Upcoming maintenance schedule. */
  maintenanceSchedule?: RedisCluster_MaintenanceSchedule | Computed<RedisCluster_MaintenanceSchedule>;
  /** Optional. This field can be used to trigger self service update to indicate the desired maintenance version. The input to this field can be determined by the available_maintenance_versions field. */
  maintenanceVersion?: string | Computed<string>;
  /** Backups that generated and managed by memorystore. */
  managedBackupSource?: RedisCluster_ManagedBackupSource | Computed<RedisCluster_ManagedBackupSource>;
  /** Required. Identifier. Unique name of the resource in this scope including project and location using the form: `projects/{project_id}/locations/{location_id}/clusters/{cluster_id}` */
  name?: string | Computed<string>;
  /** Optional. The type of a redis node in the cluster. NodeType determines the underlying machine-type of a redis node. */
  nodeType?: string | Computed<string>;
  /** Optional. Input only. Ondemand maintenance for the cluster. This field can be used to trigger ondemand critical update on the cluster. */
  ondemandMaintenance?: boolean | Computed<boolean>;
  /** Configuration of the persistence functionality. */
  persistenceConfig?: RedisCluster_PersistenceConfig | Computed<RedisCluster_PersistenceConfig>;
  /** Output only. Precise value of redis memory size in GB for the entire cluster. */
  preciseSizeGb?: number | Computed<number>;
  /** Optional. Each PscConfig configures the consumer network where IPs will be designated to the cluster for client access through Private Service Connect Automation. Currently, only one PscConfig is supported. */
  pscConfigs?: RedisCluster_DiscoveryEndpoints_PscConfig[] | Computed<RedisCluster_DiscoveryEndpoints_PscConfig[]>;
  /** Output only. The list of PSC connections that are auto-created through service connectivity automation. */
  pscConnections?: RedisCluster_ClusterEndpoints_Connections_PscConnection[] | Computed<RedisCluster_ClusterEndpoints_Connections_PscConnection[]>;
  /** Output only. Service attachment details to configure Psc connections */
  pscServiceAttachments?: RedisCluster_PscServiceAttachments[] | Computed<RedisCluster_PscServiceAttachments[]>;
  /** Optional. Key/Value pairs of customer overrides for mutable Redis Configs */
  redisConfigs?: Record<string, string> | Computed<Record<string, string>>;
  /** Optional. The number of replica nodes per shard. */
  replicaCount?: number | Computed<number>;
  /** Optional. Input only. Rotate the server certificates. */
  rotateServerCertificate?: boolean | Computed<boolean>;
  /** Optional. Output only. Reserved for future use. */
  satisfiesPzi?: boolean | Computed<boolean>;
  /** Optional. Output only. Reserved for future use. */
  satisfiesPzs?: boolean | Computed<boolean>;
  /** Optional. Server CA mode for the cluster. */
  serverCaMode?: string | Computed<string>;
  /** Optional. Customer-managed CA pool for the cluster. Only applicable for BYOCA i.e. if server_ca_mode is SERVER_CA_MODE_CUSTOMER_MANAGED_CAS_CA. Format: "projects/{project}/locations/{region}/caPools/{ca_pool}". */
  serverCaPool?: string | Computed<string>;
  /** Optional. Number of shards for the Redis cluster. */
  shardCount?: number | Computed<number>;
  /** Optional. Input only. Simulate a maintenance event. */
  simulateMaintenanceEvent?: boolean | Computed<boolean>;
  /** Output only. Redis memory size in GB for the entire cluster rounded up to the next integer. */
  sizeGb?: number | Computed<number>;
  /** Output only. The current state of this cluster. Can be CREATING, READY, UPDATING, DELETING and SUSPENDED */
  state?: string | Computed<string>;
  /** Represents additional information about the state of the cluster. */
  stateInfo?: RedisCluster_StateInfo | Computed<RedisCluster_StateInfo>;
  /** Optional. The in-transit encryption for the Redis cluster. If not provided, encryption is disabled for the cluster. */
  transitEncryptionMode?: string | Computed<string>;
  /** Output only. System assigned, unique identifier for the cluster. */
  uid?: string | Computed<string>;
  /** Zone distribution config for allocation of cluster resources. */
  zoneDistributionConfig?: RedisCluster_ZoneDistributionConfig | Computed<RedisCluster_ZoneDistributionConfig>;
}

export interface RedisClusterAttrs {
  /** Optional. The ACL policy to be applied to the cluster. */
  aclPolicy: string;
  /** Optional. Output only. Deprecated: Indicates whether the ACL rules applied to the cluster are in sync. */
  aclPolicyInSync: boolean;
  /** Details of the applied ACL policy. */
  aclPolicyInfo: RedisCluster_AclPolicyInfo;
  /** Optional. Immutable. Deprecated, do not use. */
  allowFewerZonesDeployment: boolean;
  /** Optional. If true, cluster endpoints that are created and registered by customers can be deleted asynchronously. That is, such a cluster endpoint can be de-registered before the forwarding rules in the cluster endpoint are deleted. */
  asyncClusterEndpointsDeletionEnabled: boolean;
  /** Optional. The authorization mode of the Redis cluster. If not provided, auth feature is disabled for the cluster. */
  authorizationMode: string;
  /** The automated backup config for a cluster. */
  automatedBackupConfig: RedisCluster_AutomatedBackupConfig;
  /** Output only. This field is used to determine the available maintenance versions for the self service update. */
  availableMaintenanceVersions: string[];
  /** Optional. Output only. The backup collection full resource name. Example: projects/{project}/locations/{location}/backupCollections/{collection} */
  backupCollection: string;
  /** Optional. A list of cluster endpoints. */
  clusterEndpoints: RedisCluster_ClusterEndpoints[];
  /** Output only. The timestamp associated with the cluster creation request. */
  createTime: string;
  /** Cross cluster replication config. */
  crossClusterReplicationConfig: RedisCluster_CrossClusterReplicationConfig;
  /** Optional. The delete operation will fail when the value is set to true. */
  deletionProtectionEnabled: boolean;
  /** Output only. Endpoints created on each given network, for Redis clients to connect to the cluster. Currently only one discovery endpoint is supported. */
  discoveryEndpoints: RedisCluster_DiscoveryEndpoints[];
  /** Output only. This field represents the actual maintenance version of the cluster. */
  effectiveMaintenanceVersion: string;
  /** EncryptionInfo describes the encryption information of a cluster or a backup. */
  encryptionInfo: RedisCluster_EncryptionInfo;
  /** Backups stored in Cloud Storage buckets. The Cloud Storage buckets need to be the same region as the clusters. */
  gcsSource: RedisCluster_GcsSource;
  /** Optional. The KMS key used to encrypt the at-rest data of the cluster. */
  kmsKey: string;
  /** Optional. Labels to represent user-provided metadata. */
  labels: Record<string, string>;
  /** Maintenance policy per cluster. */
  maintenancePolicy: RedisCluster_MaintenancePolicy;
  /** Upcoming maintenance schedule. */
  maintenanceSchedule: RedisCluster_MaintenanceSchedule;
  /** Optional. This field can be used to trigger self service update to indicate the desired maintenance version. The input to this field can be determined by the available_maintenance_versions field. */
  maintenanceVersion: string;
  /** Backups that generated and managed by memorystore. */
  managedBackupSource: RedisCluster_ManagedBackupSource;
  /** Required. Identifier. Unique name of the resource in this scope including project and location using the form: `projects/{project_id}/locations/{location_id}/clusters/{cluster_id}` */
  name: string;
  /** Optional. The type of a redis node in the cluster. NodeType determines the underlying machine-type of a redis node. */
  nodeType: string;
  /** Optional. Input only. Ondemand maintenance for the cluster. This field can be used to trigger ondemand critical update on the cluster. */
  ondemandMaintenance: boolean;
  /** Configuration of the persistence functionality. */
  persistenceConfig: RedisCluster_PersistenceConfig;
  /** Output only. Precise value of redis memory size in GB for the entire cluster. */
  preciseSizeGb: number;
  /** Optional. Each PscConfig configures the consumer network where IPs will be designated to the cluster for client access through Private Service Connect Automation. Currently, only one PscConfig is supported. */
  pscConfigs: RedisCluster_DiscoveryEndpoints_PscConfig[];
  /** Output only. The list of PSC connections that are auto-created through service connectivity automation. */
  pscConnections: RedisCluster_ClusterEndpoints_Connections_PscConnection[];
  /** Output only. Service attachment details to configure Psc connections */
  pscServiceAttachments: RedisCluster_PscServiceAttachments[];
  /** Optional. Key/Value pairs of customer overrides for mutable Redis Configs */
  redisConfigs: Record<string, string>;
  /** Optional. The number of replica nodes per shard. */
  replicaCount: number;
  /** Optional. Input only. Rotate the server certificates. */
  rotateServerCertificate: boolean;
  /** Optional. Output only. Reserved for future use. */
  satisfiesPzi: boolean;
  /** Optional. Output only. Reserved for future use. */
  satisfiesPzs: boolean;
  /** Optional. Server CA mode for the cluster. */
  serverCaMode: string;
  /** Optional. Customer-managed CA pool for the cluster. Only applicable for BYOCA i.e. if server_ca_mode is SERVER_CA_MODE_CUSTOMER_MANAGED_CAS_CA. Format: "projects/{project}/locations/{region}/caPools/{ca_pool}". */
  serverCaPool: string;
  /** Optional. Number of shards for the Redis cluster. */
  shardCount: number;
  /** Optional. Input only. Simulate a maintenance event. */
  simulateMaintenanceEvent: boolean;
  /** Output only. Redis memory size in GB for the entire cluster rounded up to the next integer. */
  sizeGb: number;
  /** Output only. The current state of this cluster. Can be CREATING, READY, UPDATING, DELETING and SUSPENDED */
  state: string;
  /** Represents additional information about the state of the cluster. */
  stateInfo: RedisCluster_StateInfo;
  /** Optional. The in-transit encryption for the Redis cluster. If not provided, encryption is disabled for the cluster. */
  transitEncryptionMode: string;
  /** Output only. System assigned, unique identifier for the cluster. */
  uid: string;
  /** Zone distribution config for allocation of cluster resources. */
  zoneDistributionConfig: RedisCluster_ZoneDistributionConfig;
}

export const RedisCluster: ResourceBinding<RedisClusterConfig, RedisClusterAttrs> = {
  wireType: "google_redis_redis_cluster",
  fields: {
    aclPolicy: "acl_policy",
    aclPolicyInSync: "acl_policy_in_sync",
    aclPolicyInfo: {
      wireName: "acl_policy_info",
      kind: "object",
      fields: RedisCluster_AclPolicyInfoFields,
    },
    allowFewerZonesDeployment: "allow_fewer_zones_deployment",
    asyncClusterEndpointsDeletionEnabled: "async_cluster_endpoints_deletion_enabled",
    authorizationMode: "authorization_mode",
    automatedBackupConfig: {
      wireName: "automated_backup_config",
      kind: "object",
      fields: RedisCluster_AutomatedBackupConfigFields,
    },
    availableMaintenanceVersions: "available_maintenance_versions",
    backupCollection: "backup_collection",
    clusterEndpoints: {
      wireName: "cluster_endpoints",
      kind: "list",
      fields: RedisCluster_ClusterEndpointsFields,
    },
    createTime: "create_time",
    crossClusterReplicationConfig: {
      wireName: "cross_cluster_replication_config",
      kind: "object",
      fields: RedisCluster_CrossClusterReplicationConfigFields,
    },
    deletionProtectionEnabled: "deletion_protection_enabled",
    discoveryEndpoints: {
      wireName: "discovery_endpoints",
      kind: "list",
      fields: RedisCluster_DiscoveryEndpointsFields,
    },
    effectiveMaintenanceVersion: "effective_maintenance_version",
    encryptionInfo: {
      wireName: "encryption_info",
      kind: "object",
      fields: RedisCluster_EncryptionInfoFields,
    },
    gcsSource: {
      wireName: "gcs_source",
      kind: "object",
      fields: RedisCluster_GcsSourceFields,
    },
    kmsKey: "kms_key",
    labels: "labels",
    maintenancePolicy: {
      wireName: "maintenance_policy",
      kind: "object",
      fields: RedisCluster_MaintenancePolicyFields,
    },
    maintenanceSchedule: {
      wireName: "maintenance_schedule",
      kind: "object",
      fields: RedisCluster_MaintenanceScheduleFields,
    },
    maintenanceVersion: "maintenance_version",
    managedBackupSource: {
      wireName: "managed_backup_source",
      kind: "object",
      fields: RedisCluster_ManagedBackupSourceFields,
    },
    name: "name",
    nodeType: "node_type",
    ondemandMaintenance: "ondemand_maintenance",
    persistenceConfig: {
      wireName: "persistence_config",
      kind: "object",
      fields: RedisCluster_PersistenceConfigFields,
    },
    preciseSizeGb: "precise_size_gb",
    pscConfigs: {
      wireName: "psc_configs",
      kind: "list",
      fields: RedisCluster_DiscoveryEndpoints_PscConfigFields,
    },
    pscConnections: {
      wireName: "psc_connections",
      kind: "list",
      fields: RedisCluster_ClusterEndpoints_Connections_PscConnectionFields,
    },
    pscServiceAttachments: {
      wireName: "psc_service_attachments",
      kind: "list",
      fields: RedisCluster_PscServiceAttachmentsFields,
    },
    redisConfigs: "redis_configs",
    replicaCount: "replica_count",
    rotateServerCertificate: "rotate_server_certificate",
    satisfiesPzi: "satisfies_pzi",
    satisfiesPzs: "satisfies_pzs",
    serverCaMode: "server_ca_mode",
    serverCaPool: "server_ca_pool",
    shardCount: "shard_count",
    simulateMaintenanceEvent: "simulate_maintenance_event",
    sizeGb: "size_gb",
    state: "state",
    stateInfo: {
      wireName: "state_info",
      kind: "object",
      fields: RedisCluster_StateInfoFields,
    },
    transitEncryptionMode: "transit_encryption_mode",
    uid: "uid",
    zoneDistributionConfig: {
      wireName: "zone_distribution_config",
      kind: "object",
      fields: RedisCluster_ZoneDistributionConfigFields,
    },
  },
};
