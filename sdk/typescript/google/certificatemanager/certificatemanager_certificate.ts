// Code generated by `ubx sdk gen` from google_certificatemanager_certificatemanager_certificate. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Cname {
  expectedData?: string | Computed<string>;
  name?: string | Computed<string>;
  resolvedData?: string[] | Computed<string[]>;
}

export interface CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Ips {
  resolved?: string[] | Computed<string[]>;
  serving?: string[] | Computed<string[]>;
  servingOnAltPorts?: string[] | Computed<string[]>;
}

export interface CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting {
  cname?: CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Cname | Computed<CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Cname>;
  ips?: CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Ips | Computed<CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Ips>;
  issues?: string[] | Computed<string[]>;
}

export interface CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo {
  attemptTime?: string | Computed<string>;
  details?: string | Computed<string>;
  domain?: string | Computed<string>;
  failureReason?: string | Computed<string>;
  state?: string | Computed<string>;
  troubleshooting?: CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting | Computed<CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting>;
}

export interface CertificatemanagerCertificate_Managed_ProvisioningIssue {
  /** Output only. Human readable explanation about the issue. Provided to help address the configuration issues. Not guaranteed to be stable. For programmatic access use Reason enum. */
  details?: string | Computed<string>;
  /** Output only. Reason for provisioning failures. */
  reason?: string | Computed<string>;
}

export interface CertificatemanagerCertificate_Managed {
  /** Output only. Detailed state of the latest authorization attempt for each domain specified for managed certificate resource. */
  authorizationAttemptInfo?: CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo[] | Computed<CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo[]>;
  /** Optional. Immutable. Authorizations that will be used for performing domain authorization. */
  dnsAuthorizations?: string[] | Computed<string[]>;
  /** Optional. Immutable. The domains for which a managed SSL certificate will be generated. Wildcard domains are only supported with DNS challenge resolution. */
  domains?: string[] | Computed<string[]>;
  /** Optional. Immutable. The resource name for a CertificateIssuanceConfig used to configure private PKI certificates in the format `projects/* /locations/* /certificateIssuanceConfigs/*`. If this field is not set, the certificates will instead be publicly signed as documented at https://cloud.google.com/load-balancing/docs/ssl-certificates/google-managed-certs#caa. */
  issuanceConfig?: string | Computed<string>;
  /** Information about issues with provisioning a Managed Certificate. */
  provisioningIssue?: CertificatemanagerCertificate_Managed_ProvisioningIssue | Computed<CertificatemanagerCertificate_Managed_ProvisioningIssue>;
  /** Output only. State of the managed certificate resource. */
  state?: string | Computed<string>;
}

export interface CertificatemanagerCertificate_ManagedIdentity {
  /** Required. Immutable. SPIFFE ID of the Managed Identity used for this certificate. */
  identity?: string | Computed<string>;
  /** Information about issues with provisioning a Managed Certificate. */
  provisioningIssue?: CertificatemanagerCertificate_Managed_ProvisioningIssue | Computed<CertificatemanagerCertificate_Managed_ProvisioningIssue>;
  /** Output only. State of the managed certificate resource. */
  state?: string | Computed<string>;
}

export interface CertificatemanagerCertificate_SelfManaged {
  /** Optional. Input only. The PEM-encoded certificate chain. Leaf certificate comes first, followed by intermediate ones if any. */
  pemCertificate?: string | Computed<string>;
  /** Optional. Input only. The PEM-encoded private key of the leaf certificate. */
  pemPrivateKey?: string | Computed<string>;
}

export interface CertificatemanagerCertificate_UsedBy {
  name?: string | Computed<string>;
}

const CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_CnameFields: FieldMap = {
  expectedData: "expected_data",
  name: "name",
  resolvedData: "resolved_data",
};

const CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_IpsFields: FieldMap = {
  resolved: "resolved",
  serving: "serving",
  servingOnAltPorts: "serving_on_alt_ports",
};

const CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_TroubleshootingFields: FieldMap = {
  cname: {
    wireName: "cname",
    kind: "object",
    fields: CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_CnameFields,
  },
  ips: {
    wireName: "ips",
    kind: "object",
    fields: CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_Troubleshooting_IpsFields,
  },
  issues: "issues",
};

const CertificatemanagerCertificate_Managed_AuthorizationAttemptInfoFields: FieldMap = {
  attemptTime: "attempt_time",
  details: "details",
  domain: "domain",
  failureReason: "failure_reason",
  state: "state",
  troubleshooting: {
    wireName: "troubleshooting",
    kind: "object",
    fields: CertificatemanagerCertificate_Managed_AuthorizationAttemptInfo_TroubleshootingFields,
  },
};

const CertificatemanagerCertificate_Managed_ProvisioningIssueFields: FieldMap = {
  details: "details",
  reason: "reason",
};

const CertificatemanagerCertificate_ManagedFields: FieldMap = {
  authorizationAttemptInfo: {
    wireName: "authorization_attempt_info",
    kind: "list",
    fields: CertificatemanagerCertificate_Managed_AuthorizationAttemptInfoFields,
  },
  dnsAuthorizations: "dns_authorizations",
  domains: "domains",
  issuanceConfig: "issuance_config",
  provisioningIssue: {
    wireName: "provisioning_issue",
    kind: "object",
    fields: CertificatemanagerCertificate_Managed_ProvisioningIssueFields,
  },
  state: "state",
};

const CertificatemanagerCertificate_ManagedIdentityFields: FieldMap = {
  identity: "identity",
  provisioningIssue: {
    wireName: "provisioning_issue",
    kind: "object",
    fields: CertificatemanagerCertificate_Managed_ProvisioningIssueFields,
  },
  state: "state",
};

const CertificatemanagerCertificate_SelfManagedFields: FieldMap = {
  pemCertificate: "pem_certificate",
  pemPrivateKey: "pem_private_key",
};

const CertificatemanagerCertificate_UsedByFields: FieldMap = {
  name: "name",
};

export interface CertificatemanagerCertificateConfig {
  /** Output only. The creation timestamp of a Certificate. */
  createTime?: string | Computed<string>;
  /** Optional. One or more paragraphs of text description of a certificate. */
  description?: string | Computed<string>;
  /** Output only. The expiry timestamp of a Certificate. */
  expireTime?: string | Computed<string>;
  /** Optional. Set of labels associated with a Certificate. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Configuration and state of a Managed Certificate. Certificate Manager provisions and renews Managed Certificates automatically, for as long as it's authorized to do so. */
  managed?: CertificatemanagerCertificate_Managed | Computed<CertificatemanagerCertificate_Managed>;
  /** Configuration and state of a Managed Identity Certificate. Certificate Manager provisions and renews Managed Identity Certificates automatically, for as long as it's authorized to do so. */
  managedIdentity?: CertificatemanagerCertificate_ManagedIdentity | Computed<CertificatemanagerCertificate_ManagedIdentity>;
  /** Identifier. A user-defined name of the certificate. Certificate names must be unique globally and match pattern `projects/* /locations/* /certificates/*`. */
  name?: string | Computed<string>;
  /** Output only. The PEM-encoded certificate chain. */
  pemCertificate?: string | Computed<string>;
  /** Output only. The list of Subject Alternative Names of dnsName type defined in the certificate (see RFC 5280 4.2.1.6). Managed certificates that haven't been provisioned yet have this field populated with a value of the managed.domains field. */
  sanDnsnames?: string[] | Computed<string[]>;
  /** Optional. Immutable. The scope of the certificate. */
  scope?: string | Computed<string>;
  /** Certificate data for a SelfManaged Certificate. SelfManaged Certificates are uploaded by the user. Updating such certificates before they expire remains the user's responsibility. */
  selfManaged?: CertificatemanagerCertificate_SelfManaged | Computed<CertificatemanagerCertificate_SelfManaged>;
  /** Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" */
  tags?: Record<string, string> | Computed<Record<string, string>>;
  /** Output only. The last update timestamp of a Certificate. */
  updateTime?: string | Computed<string>;
  /** Output only. The list of resources that use this Certificate. */
  usedBy?: CertificatemanagerCertificate_UsedBy[] | Computed<CertificatemanagerCertificate_UsedBy[]>;
}

export interface CertificatemanagerCertificateAttrs {
  /** Output only. The creation timestamp of a Certificate. */
  createTime: string;
  /** Optional. One or more paragraphs of text description of a certificate. */
  description: string;
  /** Output only. The expiry timestamp of a Certificate. */
  expireTime: string;
  /** Optional. Set of labels associated with a Certificate. */
  labels: Record<string, string>;
  /** Configuration and state of a Managed Certificate. Certificate Manager provisions and renews Managed Certificates automatically, for as long as it's authorized to do so. */
  managed: CertificatemanagerCertificate_Managed;
  /** Configuration and state of a Managed Identity Certificate. Certificate Manager provisions and renews Managed Identity Certificates automatically, for as long as it's authorized to do so. */
  managedIdentity: CertificatemanagerCertificate_ManagedIdentity;
  /** Identifier. A user-defined name of the certificate. Certificate names must be unique globally and match pattern `projects/* /locations/* /certificates/*`. */
  name: string;
  /** Output only. The PEM-encoded certificate chain. */
  pemCertificate: string;
  /** Output only. The list of Subject Alternative Names of dnsName type defined in the certificate (see RFC 5280 4.2.1.6). Managed certificates that haven't been provisioned yet have this field populated with a value of the managed.domains field. */
  sanDnsnames: string[];
  /** Optional. Immutable. The scope of the certificate. */
  scope: string;
  /** Certificate data for a SelfManaged Certificate. SelfManaged Certificates are uploaded by the user. Updating such certificates before they expire remains the user's responsibility. */
  selfManaged: CertificatemanagerCertificate_SelfManaged;
  /** Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" */
  tags: Record<string, string>;
  /** Output only. The last update timestamp of a Certificate. */
  updateTime: string;
  /** Output only. The list of resources that use this Certificate. */
  usedBy: CertificatemanagerCertificate_UsedBy[];
}

export const CertificatemanagerCertificate: ResourceBinding<CertificatemanagerCertificateConfig, CertificatemanagerCertificateAttrs> = {
  wireType: "google_certificatemanager_certificatemanager_certificate",
  fields: {
    createTime: "create_time",
    description: "description",
    expireTime: "expire_time",
    labels: "labels",
    managed: {
      wireName: "managed",
      kind: "object",
      fields: CertificatemanagerCertificate_ManagedFields,
    },
    managedIdentity: {
      wireName: "managed_identity",
      kind: "object",
      fields: CertificatemanagerCertificate_ManagedIdentityFields,
    },
    name: "name",
    pemCertificate: "pem_certificate",
    sanDnsnames: "san_dnsnames",
    scope: "scope",
    selfManaged: {
      wireName: "self_managed",
      kind: "object",
      fields: CertificatemanagerCertificate_SelfManagedFields,
    },
    tags: "tags",
    updateTime: "update_time",
    usedBy: {
      wireName: "used_by",
      kind: "list",
      fields: CertificatemanagerCertificate_UsedByFields,
    },
  },
};
