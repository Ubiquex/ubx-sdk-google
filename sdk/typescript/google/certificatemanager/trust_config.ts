// Code generated by `ubx sdk gen` from google_certificatemanager_trust_config. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface TrustConfig_AllowlistedCertificates {
  pemCertificate?: string | Computed<string>;
}

export interface TrustConfig_SpiffeTrustStores {
  intermediateCas?: TrustConfig_AllowlistedCertificates[] | Computed<TrustConfig_AllowlistedCertificates[]>;
  trustAnchors?: TrustConfig_AllowlistedCertificates[] | Computed<TrustConfig_AllowlistedCertificates[]>;
}

const TrustConfig_AllowlistedCertificatesFields: FieldMap = {
  pemCertificate: "pem_certificate",
};

const TrustConfig_SpiffeTrustStoresFields: FieldMap = {
  intermediateCas: {
    wireName: "intermediate_cas",
    kind: "list",
    fields: TrustConfig_AllowlistedCertificatesFields,
  },
  trustAnchors: {
    wireName: "trust_anchors",
    kind: "list",
    fields: TrustConfig_AllowlistedCertificatesFields,
  },
};

export interface TrustConfigConfig {
  /** Optional. A certificate matching an allowlisted certificate is always considered valid as long as the certificate is parseable, proof of private key possession is established, and constraints on the certificate's SAN field are met. */
  allowlistedCertificates?: TrustConfig_AllowlistedCertificates[] | Computed<TrustConfig_AllowlistedCertificates[]>;
  /** Output only. The creation timestamp of a TrustConfig. */
  createTime?: string | Computed<string>;
  /** Optional. One or more paragraphs of text description of a TrustConfig. */
  description?: string | Computed<string>;
  /** This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. */
  etag?: string | Computed<string>;
  /** Optional. Set of labels associated with a TrustConfig. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Identifier. A user-defined name of the trust config. TrustConfig names must be unique globally and match pattern `projects/* /locations/* /trustConfigs/*`. */
  name?: string | Computed<string>;
  /** Optional. Defines a mapping from a trust domain to a TrustStore. This is used for SPIFFE certificate validation. */
  spiffeTrustStores?: Record<string, TrustConfig_SpiffeTrustStores> | Computed<Record<string, TrustConfig_SpiffeTrustStores>>;
  /** Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" */
  tags?: Record<string, string> | Computed<Record<string, string>>;
  /** Optional. Set of trust stores to perform validation against. This field is supported when TrustConfig is configured with Load Balancers, currently not supported for SPIFFE certificate validation. Only one TrustStore specified is currently allowed. */
  trustStores?: TrustConfig_SpiffeTrustStores[] | Computed<TrustConfig_SpiffeTrustStores[]>;
  /** Output only. The last update timestamp of a TrustConfig. */
  updateTime?: string | Computed<string>;
}

export interface TrustConfigAttrs {
  /** Optional. A certificate matching an allowlisted certificate is always considered valid as long as the certificate is parseable, proof of private key possession is established, and constraints on the certificate's SAN field are met. */
  allowlistedCertificates: TrustConfig_AllowlistedCertificates[];
  /** Output only. The creation timestamp of a TrustConfig. */
  createTime: string;
  /** Optional. One or more paragraphs of text description of a TrustConfig. */
  description: string;
  /** This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. */
  etag: string;
  /** Optional. Set of labels associated with a TrustConfig. */
  labels: Record<string, string>;
  /** Identifier. A user-defined name of the trust config. TrustConfig names must be unique globally and match pattern `projects/* /locations/* /trustConfigs/*`. */
  name: string;
  /** Optional. Defines a mapping from a trust domain to a TrustStore. This is used for SPIFFE certificate validation. */
  spiffeTrustStores: Record<string, TrustConfig_SpiffeTrustStores>;
  /** Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" */
  tags: Record<string, string>;
  /** Optional. Set of trust stores to perform validation against. This field is supported when TrustConfig is configured with Load Balancers, currently not supported for SPIFFE certificate validation. Only one TrustStore specified is currently allowed. */
  trustStores: TrustConfig_SpiffeTrustStores[];
  /** Output only. The last update timestamp of a TrustConfig. */
  updateTime: string;
}

export const TrustConfig: ResourceBinding<TrustConfigConfig, TrustConfigAttrs> = {
  wireType: "google_certificatemanager_trust_config",
  fields: {
    allowlistedCertificates: {
      wireName: "allowlisted_certificates",
      kind: "list",
      fields: TrustConfig_AllowlistedCertificatesFields,
    },
    createTime: "create_time",
    description: "description",
    etag: "etag",
    labels: "labels",
    name: "name",
    spiffeTrustStores: {
      wireName: "spiffe_trust_stores",
      kind: "map",
      fields: TrustConfig_SpiffeTrustStoresFields,
    },
    tags: "tags",
    trustStores: {
      wireName: "trust_stores",
      kind: "list",
      fields: TrustConfig_SpiffeTrustStoresFields,
    },
    updateTime: "update_time",
  },
};
