// Code generated by `ubx sdk gen` from google_certificatemanager_certificate. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Cname {
  /** The record data expected for this validation to succeed. (AI-inferred) */
  expectedData?: string | Computed<string>;
  /** The resource name or identifier of this object. (AI-inferred) */
  name?: string | Computed<string>;
  /** The record data actually observed for this domain. (AI-inferred) */
  resolvedData?: string[] | Computed<string[]>;
}

export interface Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Ips {
  /** Whether this DNS record currently resolves to the expected value. (AI-inferred) */
  resolved?: string[] | Computed<string[]>;
  /** Whether this certificate is actively serving traffic. (AI-inferred) */
  serving?: string[] | Computed<string[]>;
  /** Whether this certificate is served on non-standard port(s). (AI-inferred) */
  servingOnAltPorts?: string[] | Computed<string[]>;
}

export interface Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting {
  /** The CNAME target this DNS record must point to. (AI-inferred) */
  cname?: Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Cname | Computed<Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Cname>;
  /** The IP address(es) this applies to. (AI-inferred) */
  ips?: Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Ips | Computed<Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_Ips>;
  /** The issue(s) detected during troubleshooting. (AI-inferred) */
  issues?: string[] | Computed<string[]>;
}

export interface Certificate_Managed_AuthorizationAttemptInfo {
  /** When this validation attempt was made. (AI-inferred) */
  attemptTime?: string | Computed<string>;
  /** Additional detail about this result. (AI-inferred) */
  details?: string | Computed<string>;
  /** The domain this applies to. (AI-inferred) */
  domain?: string | Computed<string>;
  /** Why this validation attempt failed. (AI-inferred) */
  failureReason?: string | Computed<string>;
  /** The current status of this resource. (AI-inferred) */
  state?: string | Computed<string>;
  /** Diagnostic detail helping to explain this resource's own current state. (AI-inferred) */
  troubleshooting?: Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting | Computed<Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting>;
}

export interface Certificate_Managed_ProvisioningIssue {
  /** Output only. Human readable explanation about the issue. Provided to help address the configuration issues. Not guaranteed to be stable. For programmatic access use Reason enum. */
  details?: string | Computed<string>;
  /** Output only. Reason for provisioning failures. */
  reason?: string | Computed<string>;
}

export interface Certificate_Managed {
  /** Output only. Detailed state of the latest authorization attempt for each domain specified for managed certificate resource. */
  authorizationAttemptInfo?: Certificate_Managed_AuthorizationAttemptInfo[] | Computed<Certificate_Managed_AuthorizationAttemptInfo[]>;
  /** Optional. Immutable. Authorizations that will be used for performing domain authorization. */
  dnsAuthorizations?: string[] | Computed<string[]>;
  /** Optional. Immutable. The domains for which a managed SSL certificate will be generated. Wildcard domains are only supported with DNS challenge resolution. */
  domains?: string[] | Computed<string[]>;
  /** Optional. Immutable. The resource name for a CertificateIssuanceConfig used to configure private PKI certificates in the format `projects/* /locations/* /certificateIssuanceConfigs/*`. If this field is not set, the certificates will instead be publicly signed as documented at https://cloud.google.com/load-balancing/docs/ssl-certificates/google-managed-certs#caa. */
  issuanceConfig?: string | Computed<string>;
  /** Information about issues with provisioning a Managed Certificate. */
  provisioningIssue?: Certificate_Managed_ProvisioningIssue | Computed<Certificate_Managed_ProvisioningIssue>;
  /** Output only. State of the managed certificate resource. */
  state?: string | Computed<string>;
}

export interface Certificate_ManagedIdentity {
  /** Required. Immutable. SPIFFE ID of the Managed Identity used for this certificate. */
  identity?: string | Computed<string>;
  /** Information about issues with provisioning a Managed Certificate. */
  provisioningIssue?: Certificate_Managed_ProvisioningIssue | Computed<Certificate_Managed_ProvisioningIssue>;
  /** Output only. State of the managed certificate resource. */
  state?: string | Computed<string>;
}

export interface Certificate_SelfManaged {
  /** Optional. Input only. The PEM-encoded certificate chain. Leaf certificate comes first, followed by intermediate ones if any. */
  pemCertificate?: string | Computed<string>;
  /** Optional. Input only. The PEM-encoded private key of the leaf certificate. */
  pemPrivateKey?: string | Computed<string>;
}

export interface Certificate_UsedBy {
  /** The resource name or identifier of this object. (AI-inferred) */
  name?: string | Computed<string>;
}

const Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_CnameFields: FieldMap = {
  expectedData: "expected_data",
  name: "name",
  resolvedData: "resolved_data",
};

const Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_IpsFields: FieldMap = {
  resolved: "resolved",
  serving: "serving",
  servingOnAltPorts: "serving_on_alt_ports",
};

const Certificate_Managed_AuthorizationAttemptInfo_TroubleshootingFields: FieldMap = {
  cname: {
    wireName: "cname",
    kind: "object",
    fields: Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_CnameFields,
  },
  ips: {
    wireName: "ips",
    kind: "object",
    fields: Certificate_Managed_AuthorizationAttemptInfo_Troubleshooting_IpsFields,
  },
  issues: "issues",
};

const Certificate_Managed_AuthorizationAttemptInfoFields: FieldMap = {
  attemptTime: "attempt_time",
  details: "details",
  domain: "domain",
  failureReason: "failure_reason",
  state: "state",
  troubleshooting: {
    wireName: "troubleshooting",
    kind: "object",
    fields: Certificate_Managed_AuthorizationAttemptInfo_TroubleshootingFields,
  },
};

const Certificate_Managed_ProvisioningIssueFields: FieldMap = {
  details: "details",
  reason: "reason",
};

const Certificate_ManagedFields: FieldMap = {
  authorizationAttemptInfo: {
    wireName: "authorization_attempt_info",
    kind: "list",
    fields: Certificate_Managed_AuthorizationAttemptInfoFields,
  },
  dnsAuthorizations: "dns_authorizations",
  domains: "domains",
  issuanceConfig: "issuance_config",
  provisioningIssue: {
    wireName: "provisioning_issue",
    kind: "object",
    fields: Certificate_Managed_ProvisioningIssueFields,
  },
  state: "state",
};

const Certificate_ManagedIdentityFields: FieldMap = {
  identity: "identity",
  provisioningIssue: {
    wireName: "provisioning_issue",
    kind: "object",
    fields: Certificate_Managed_ProvisioningIssueFields,
  },
  state: "state",
};

const Certificate_SelfManagedFields: FieldMap = {
  pemCertificate: "pem_certificate",
  pemPrivateKey: "pem_private_key",
};

export interface CertificateConfig {
  /** Optional. One or more paragraphs of text description of a certificate. */
  description?: string | Computed<string>;
  /** Optional. Set of labels associated with a Certificate. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Configuration and state of a Managed Certificate. Certificate Manager provisions and renews Managed Certificates automatically, for as long as it's authorized to do so. */
  managed?: Certificate_Managed | Computed<Certificate_Managed>;
  /** Configuration and state of a Managed Identity Certificate. Certificate Manager provisions and renews Managed Identity Certificates automatically, for as long as it's authorized to do so. */
  managedIdentity?: Certificate_ManagedIdentity | Computed<Certificate_ManagedIdentity>;
  /** Identifier. A user-defined name of the certificate. Certificate names must be unique globally and match pattern `projects/* /locations/* /certificates/*`. */
  name?: string | Computed<string>;
  /** Optional. Immutable. The scope of the certificate. */
  scope?: string | Computed<string>;
  /** Certificate data for a SelfManaged Certificate. SelfManaged Certificates are uploaded by the user. Updating such certificates before they expire remains the user's responsibility. */
  selfManaged?: Certificate_SelfManaged | Computed<Certificate_SelfManaged>;
  /** Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" */
  tags?: Record<string, string> | Computed<Record<string, string>>;
}

export interface CertificateAttrs {
  /** Output only. The creation timestamp of a Certificate. */
  createTime: string;
  /** Optional. One or more paragraphs of text description of a certificate. */
  description: string;
  /** Output only. The expiry timestamp of a Certificate. */
  expireTime: string;
  /** Optional. Set of labels associated with a Certificate. */
  labels: Record<string, string>;
  /** Configuration and state of a Managed Certificate. Certificate Manager provisions and renews Managed Certificates automatically, for as long as it's authorized to do so. */
  managed: Certificate_Managed;
  /** Configuration and state of a Managed Identity Certificate. Certificate Manager provisions and renews Managed Identity Certificates automatically, for as long as it's authorized to do so. */
  managedIdentity: Certificate_ManagedIdentity;
  /** Identifier. A user-defined name of the certificate. Certificate names must be unique globally and match pattern `projects/* /locations/* /certificates/*`. */
  name: string;
  /** Output only. The PEM-encoded certificate chain. */
  pemCertificate: string;
  /** Output only. The list of Subject Alternative Names of dnsName type defined in the certificate (see RFC 5280 4.2.1.6). Managed certificates that haven't been provisioned yet have this field populated with a value of the managed.domains field. */
  sanDnsnames: string[];
  /** Optional. Immutable. The scope of the certificate. */
  scope: string;
  /** Certificate data for a SelfManaged Certificate. SelfManaged Certificates are uploaded by the user. Updating such certificates before they expire remains the user's responsibility. */
  selfManaged: Certificate_SelfManaged;
  /** Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: "123/environment": "production", "123/costCenter": "marketing" */
  tags: Record<string, string>;
  /** Output only. The last update timestamp of a Certificate. */
  updateTime: string;
  /** Output only. The list of resources that use this Certificate. */
  usedBy: Certificate_UsedBy[];
}

export const Certificate: ResourceBinding<CertificateConfig, CertificateAttrs> = {
  wireType: "google_certificatemanager_certificate",
  fields: {
    description: "description",
    labels: "labels",
    managed: {
      wireName: "managed",
      kind: "object",
      fields: Certificate_ManagedFields,
    },
    managedIdentity: {
      wireName: "managed_identity",
      kind: "object",
      fields: Certificate_ManagedIdentityFields,
    },
    name: "name",
    scope: "scope",
    selfManaged: {
      wireName: "self_managed",
      kind: "object",
      fields: Certificate_SelfManagedFields,
    },
    tags: "tags",
  },
};
