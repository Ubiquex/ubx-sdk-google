// Code generated by `ubx sdk gen` from google_storage_object. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Object_Acl_ProjectTeam {
  projectNumber?: string | Computed<string>;
  team?: string | Computed<string>;
}

export interface Object_Acl {
  bucket?: string | Computed<string>;
  domain?: string | Computed<string>;
  email?: string | Computed<string>;
  entity?: string | Computed<string>;
  entityId?: string | Computed<string>;
  etag?: string | Computed<string>;
  generation?: string | Computed<string>;
  id?: string | Computed<string>;
  kind?: string | Computed<string>;
  object?: string | Computed<string>;
  projectTeam?: Object_Acl_ProjectTeam | Computed<Object_Acl_ProjectTeam>;
  role?: string | Computed<string>;
  selfLink?: string | Computed<string>;
}

export interface Object_Contexts_Custom {
  createTime?: string | Computed<string>;
  updateTime?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface Object_Contexts {
  /** User-defined object contexts. */
  custom?: Record<string, Object_Contexts_Custom> | Computed<Record<string, Object_Contexts_Custom>>;
}

export interface Object_CustomerEncryption {
  /** The encryption algorithm. */
  encryptionAlgorithm?: string | Computed<string>;
  /** SHA256 hash value of the encryption key. */
  keySha256?: string | Computed<string>;
}

export interface Object_Owner {
  /** The entity, in the form user-userId. */
  entity?: string | Computed<string>;
  /** The ID for the entity. */
  entityId?: string | Computed<string>;
}

export interface Object_Retention {
  /** The bucket's object retention mode, can only be Unlocked or Locked. */
  mode?: string | Computed<string>;
  /** A time in RFC 3339 format until which object retention protects this object. */
  retainUntilTime?: string | Computed<string>;
}

const Object_Acl_ProjectTeamFields: FieldMap = {
  projectNumber: "project_number",
  team: "team",
};

const Object_AclFields: FieldMap = {
  bucket: "bucket",
  domain: "domain",
  email: "email",
  entity: "entity",
  entityId: "entity_id",
  etag: "etag",
  generation: "generation",
  id: "id",
  kind: "kind",
  object: "object",
  projectTeam: {
    wireName: "project_team",
    kind: "object",
    fields: Object_Acl_ProjectTeamFields,
  },
  role: "role",
  selfLink: "self_link",
};

const Object_Contexts_CustomFields: FieldMap = {
  createTime: "create_time",
  updateTime: "update_time",
  value: "value",
};

const Object_ContextsFields: FieldMap = {
  custom: {
    wireName: "custom",
    kind: "map",
    fields: Object_Contexts_CustomFields,
  },
};

const Object_CustomerEncryptionFields: FieldMap = {
  encryptionAlgorithm: "encryption_algorithm",
  keySha256: "key_sha256",
};

const Object_OwnerFields: FieldMap = {
  entity: "entity",
  entityId: "entity_id",
};

const Object_RetentionFields: FieldMap = {
  mode: "mode",
  retainUntilTime: "retain_until_time",
};

export interface ObjectConfig {
  /** Access controls on the object. */
  acl?: Object_Acl[] | Computed<Object_Acl[]>;
  /** The name of the bucket containing this object. */
  bucket?: string | Computed<string>;
  /** Cache-Control directive for the object data. If omitted, and the object is accessible to all anonymous users, the default will be public, max-age=3600. */
  cacheControl?: string | Computed<string>;
  /** Number of underlying components that make up this object. Components are accumulated by compose operations. */
  componentCount?: number | Computed<number>;
  /** Content-Disposition of the object data. */
  contentDisposition?: string | Computed<string>;
  /** Content-Encoding of the object data. */
  contentEncoding?: string | Computed<string>;
  /** Content-Language of the object data. */
  contentLanguage?: string | Computed<string>;
  /** Content-Type of the object data. If an object is stored without a Content-Type, it is served as application/octet-stream. */
  contentType?: string | Computed<string>;
  /** User-defined or system-defined object contexts. Each object context is a key-payload pair, where the key provides the identification and the payload holds the associated value and additional metadata. */
  contexts?: Object_Contexts | Computed<Object_Contexts>;
  /** CRC32c checksum, as described in RFC 4960, Appendix B; encoded using base64 in big-endian byte order. For more information about using the CRC32c checksum, see [Data Validation and Change Detection](https://cloud.google.com/storage/docs/data-validation). */
  crc32c?: string | Computed<string>;
  /** A timestamp in RFC 3339 format specified by the user for an object. */
  customTime?: string | Computed<string>;
  /** Metadata of customer-supplied encryption key, if the object is encrypted by such a key. */
  customerEncryption?: Object_CustomerEncryption | Computed<Object_CustomerEncryption>;
  /** HTTP 1.1 Entity tag for the object. */
  etag?: string | Computed<string>;
  /** Whether an object is under event-based hold. Event-based hold is a way to retain objects until an event occurs, which is signified by the hold's release (i.e. this value is set to false). After being released (set to false), such objects will be subject to bucket-level retention (if any). One sample use case of this flag is for banks to hold loan documents for at least 3 years after loan is paid in full. Here, bucket-level retention is 3 years and the event is the loan being paid in full. In this example, these objects will be held intact for any number of years until the event has occurred (event-based hold on the object is released) and then 3 more years after that. That means retention duration of the objects begins from the moment event-based hold transitioned from true to false. */
  eventBasedHold?: boolean | Computed<boolean>;
  /** The content generation of this object. Used for object versioning. */
  generation?: string | Computed<string>;
  /** This is the time (in the future) when the soft-deleted object will no longer be restorable. It is equal to the soft delete time plus the current soft delete retention duration of the bucket. */
  hardDeleteTime?: string | Computed<string>;
  /** The ID of the object, including the bucket name, object name, and generation number. */
  id?: string | Computed<string>;
  /** The kind of item this is. For objects, this is always storage#object. */
  kind?: string | Computed<string>;
  /** Not currently supported. Specifying the parameter causes the request to fail with status code 400 - Bad Request. */
  kmsKeyName?: string | Computed<string>;
  /** MD5 hash of the data; encoded using base64. For more information about using the MD5 hash, see [Data Validation and Change Detection](https://cloud.google.com/storage/docs/data-validation). */
  md5Hash?: string | Computed<string>;
  /** Media download link. */
  mediaLink?: string | Computed<string>;
  /** User-provided metadata, in key/value pairs. */
  metadata?: Record<string, string> | Computed<Record<string, string>>;
  /** The version of the metadata for this object at this generation. Used for preconditions and for detecting changes in metadata. A metageneration number is only meaningful in the context of a particular generation of a particular object. */
  metageneration?: string | Computed<string>;
  /** The name of the object. Required if not specified by URL parameter. */
  name?: string | Computed<string>;
  /** The owner of the object. This will always be the uploader of the object. */
  owner?: Object_Owner | Computed<Object_Owner>;
  /** Restore token used to differentiate deleted objects with the same name and generation. This field is only returned for deleted objects in hierarchical namespace buckets. */
  restoreToken?: string | Computed<string>;
  /** A collection of object level retention parameters. */
  retention?: Object_Retention | Computed<Object_Retention>;
  /** A server-determined value that specifies the earliest time that the object's retention period expires. This value is in RFC 3339 format. Note 1: This field is not provided for objects with an active event-based hold, since retention expiration is unknown until the hold is removed. Note 2: This value can be provided even when temporary hold is set (so that the user can reason about policy without having to first unset the temporary hold). */
  retentionExpirationTime?: string | Computed<string>;
  /** The link to this object. */
  selfLink?: string | Computed<string>;
  /** Content-Length of the data in bytes. */
  size?: string | Computed<string>;
  /** The time at which the object became soft-deleted in RFC 3339 format. */
  softDeleteTime?: string | Computed<string>;
  /** Storage class of the object. */
  storageClass?: string | Computed<string>;
  /** Whether an object is under temporary hold. While this flag is set to true, the object is protected against deletion and overwrites. A common use case of this flag is regulatory investigations where objects need to be retained while the investigation is ongoing. Note that unlike event-based hold, temporary hold does not impact retention expiration time of an object. */
  temporaryHold?: boolean | Computed<boolean>;
  /** The creation time of the object in RFC 3339 format. */
  timeCreated?: string | Computed<string>;
  /** The time at which the object became noncurrent in RFC 3339 format. Will be returned if and only if this version of the object has been deleted. */
  timeDeleted?: string | Computed<string>;
  /** The time when the object was finalized. */
  timeFinalized?: string | Computed<string>;
  /** The time at which the object's storage class was last changed. When the object is initially created, it will be set to timeCreated. */
  timeStorageClassUpdated?: string | Computed<string>;
  /** The modification time of the object metadata in RFC 3339 format. Set initially to object creation time and then updated whenever any metadata of the object changes. This includes changes made by a requester, such as modifying custom metadata, as well as changes made by Cloud Storage on behalf of a requester, such as changing the storage class based on an Object Lifecycle Configuration. */
  updated?: string | Computed<string>;
}

export interface ObjectAttrs {
  /** Access controls on the object. */
  acl: Object_Acl[];
  /** The name of the bucket containing this object. */
  bucket: string;
  /** Cache-Control directive for the object data. If omitted, and the object is accessible to all anonymous users, the default will be public, max-age=3600. */
  cacheControl: string;
  /** Number of underlying components that make up this object. Components are accumulated by compose operations. */
  componentCount: number;
  /** Content-Disposition of the object data. */
  contentDisposition: string;
  /** Content-Encoding of the object data. */
  contentEncoding: string;
  /** Content-Language of the object data. */
  contentLanguage: string;
  /** Content-Type of the object data. If an object is stored without a Content-Type, it is served as application/octet-stream. */
  contentType: string;
  /** User-defined or system-defined object contexts. Each object context is a key-payload pair, where the key provides the identification and the payload holds the associated value and additional metadata. */
  contexts: Object_Contexts;
  /** CRC32c checksum, as described in RFC 4960, Appendix B; encoded using base64 in big-endian byte order. For more information about using the CRC32c checksum, see [Data Validation and Change Detection](https://cloud.google.com/storage/docs/data-validation). */
  crc32c: string;
  /** A timestamp in RFC 3339 format specified by the user for an object. */
  customTime: string;
  /** Metadata of customer-supplied encryption key, if the object is encrypted by such a key. */
  customerEncryption: Object_CustomerEncryption;
  /** HTTP 1.1 Entity tag for the object. */
  etag: string;
  /** Whether an object is under event-based hold. Event-based hold is a way to retain objects until an event occurs, which is signified by the hold's release (i.e. this value is set to false). After being released (set to false), such objects will be subject to bucket-level retention (if any). One sample use case of this flag is for banks to hold loan documents for at least 3 years after loan is paid in full. Here, bucket-level retention is 3 years and the event is the loan being paid in full. In this example, these objects will be held intact for any number of years until the event has occurred (event-based hold on the object is released) and then 3 more years after that. That means retention duration of the objects begins from the moment event-based hold transitioned from true to false. */
  eventBasedHold: boolean;
  /** The content generation of this object. Used for object versioning. */
  generation: string;
  /** This is the time (in the future) when the soft-deleted object will no longer be restorable. It is equal to the soft delete time plus the current soft delete retention duration of the bucket. */
  hardDeleteTime: string;
  /** The ID of the object, including the bucket name, object name, and generation number. */
  id: string;
  /** The kind of item this is. For objects, this is always storage#object. */
  kind: string;
  /** Not currently supported. Specifying the parameter causes the request to fail with status code 400 - Bad Request. */
  kmsKeyName: string;
  /** MD5 hash of the data; encoded using base64. For more information about using the MD5 hash, see [Data Validation and Change Detection](https://cloud.google.com/storage/docs/data-validation). */
  md5Hash: string;
  /** Media download link. */
  mediaLink: string;
  /** User-provided metadata, in key/value pairs. */
  metadata: Record<string, string>;
  /** The version of the metadata for this object at this generation. Used for preconditions and for detecting changes in metadata. A metageneration number is only meaningful in the context of a particular generation of a particular object. */
  metageneration: string;
  /** The name of the object. Required if not specified by URL parameter. */
  name: string;
  /** The owner of the object. This will always be the uploader of the object. */
  owner: Object_Owner;
  /** Restore token used to differentiate deleted objects with the same name and generation. This field is only returned for deleted objects in hierarchical namespace buckets. */
  restoreToken: string;
  /** A collection of object level retention parameters. */
  retention: Object_Retention;
  /** A server-determined value that specifies the earliest time that the object's retention period expires. This value is in RFC 3339 format. Note 1: This field is not provided for objects with an active event-based hold, since retention expiration is unknown until the hold is removed. Note 2: This value can be provided even when temporary hold is set (so that the user can reason about policy without having to first unset the temporary hold). */
  retentionExpirationTime: string;
  /** The link to this object. */
  selfLink: string;
  /** Content-Length of the data in bytes. */
  size: string;
  /** The time at which the object became soft-deleted in RFC 3339 format. */
  softDeleteTime: string;
  /** Storage class of the object. */
  storageClass: string;
  /** Whether an object is under temporary hold. While this flag is set to true, the object is protected against deletion and overwrites. A common use case of this flag is regulatory investigations where objects need to be retained while the investigation is ongoing. Note that unlike event-based hold, temporary hold does not impact retention expiration time of an object. */
  temporaryHold: boolean;
  /** The creation time of the object in RFC 3339 format. */
  timeCreated: string;
  /** The time at which the object became noncurrent in RFC 3339 format. Will be returned if and only if this version of the object has been deleted. */
  timeDeleted: string;
  /** The time when the object was finalized. */
  timeFinalized: string;
  /** The time at which the object's storage class was last changed. When the object is initially created, it will be set to timeCreated. */
  timeStorageClassUpdated: string;
  /** The modification time of the object metadata in RFC 3339 format. Set initially to object creation time and then updated whenever any metadata of the object changes. This includes changes made by a requester, such as modifying custom metadata, as well as changes made by Cloud Storage on behalf of a requester, such as changing the storage class based on an Object Lifecycle Configuration. */
  updated: string;
}

export const Object: ResourceBinding<ObjectConfig, ObjectAttrs> = {
  wireType: "google_storage_object",
  fields: {
    acl: {
      wireName: "acl",
      kind: "list",
      fields: Object_AclFields,
    },
    bucket: "bucket",
    cacheControl: "cache_control",
    componentCount: "component_count",
    contentDisposition: "content_disposition",
    contentEncoding: "content_encoding",
    contentLanguage: "content_language",
    contentType: "content_type",
    contexts: {
      wireName: "contexts",
      kind: "object",
      fields: Object_ContextsFields,
    },
    crc32c: "crc32c",
    customTime: "custom_time",
    customerEncryption: {
      wireName: "customer_encryption",
      kind: "object",
      fields: Object_CustomerEncryptionFields,
    },
    etag: "etag",
    eventBasedHold: "event_based_hold",
    generation: "generation",
    hardDeleteTime: "hard_delete_time",
    id: "id",
    kind: "kind",
    kmsKeyName: "kms_key_name",
    md5Hash: "md5_hash",
    mediaLink: "media_link",
    metadata: "metadata",
    metageneration: "metageneration",
    name: "name",
    owner: {
      wireName: "owner",
      kind: "object",
      fields: Object_OwnerFields,
    },
    restoreToken: "restore_token",
    retention: {
      wireName: "retention",
      kind: "object",
      fields: Object_RetentionFields,
    },
    retentionExpirationTime: "retention_expiration_time",
    selfLink: "self_link",
    size: "size",
    softDeleteTime: "soft_delete_time",
    storageClass: "storage_class",
    temporaryHold: "temporary_hold",
    timeCreated: "time_created",
    timeDeleted: "time_deleted",
    timeFinalized: "time_finalized",
    timeStorageClassUpdated: "time_storage_class_updated",
    updated: "updated",
  },
};
