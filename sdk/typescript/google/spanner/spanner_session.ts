// Code generated by `ubx sdk gen` from google_spanner_spanner_session. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface SpannerSession_Session {
  /** Output only. The approximate timestamp when the session is last used. It's typically earlier than the actual last use time. */
  approximateLastUseTime?: string | Computed<string>;
  /** Output only. The timestamp when the session is created. */
  createTime?: string | Computed<string>;
  /** The database role which created this session. */
  creatorRole?: string | Computed<string>;
  /** The labels for the session. * Label keys must be between 1 and 63 characters long and must conform to the following regular expression: `[a-z]([-a-z0-9]*[a-z0-9])?`. * Label values must be between 0 and 63 characters long and must conform to the regular expression `([a-z]([-a-z0-9]*[a-z0-9])?)?`. * No more than 64 labels can be associated with a given session. See https://goo.gl/xmQnxf for more information on and examples of labels. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Optional. If `true`, specifies a multiplexed session. Use a multiplexed session for multiple, concurrent operations including any combination of read-only and read-write transactions. Use `sessions.create` to create multiplexed sessions. Don't use BatchCreateSessions to create a multiplexed session. You can't delete or list multiplexed sessions. */
  multiplexed?: boolean | Computed<boolean>;
  /** Output only. The name of the session. This is always system-assigned. */
  name?: string | Computed<string>;
}

const SpannerSession_SessionFields: FieldMap = {
  approximateLastUseTime: "approximate_last_use_time",
  createTime: "create_time",
  creatorRole: "creator_role",
  labels: "labels",
  multiplexed: "multiplexed",
  name: "name",
};

export interface SpannerSessionConfig {
  /** A session in the Cloud Spanner API. */
  session?: SpannerSession_Session | Computed<SpannerSession_Session>;
}

export interface SpannerSessionAttrs {
  /** Output only. The approximate timestamp when the session is last used. It's typically earlier than the actual last use time. */
  approximateLastUseTime: string;
  /** Output only. The timestamp when the session is created. */
  createTime: string;
  /** The database role which created this session. */
  creatorRole: string;
  /** The labels for the session. * Label keys must be between 1 and 63 characters long and must conform to the following regular expression: `[a-z]([-a-z0-9]*[a-z0-9])?`. * Label values must be between 0 and 63 characters long and must conform to the regular expression `([a-z]([-a-z0-9]*[a-z0-9])?)?`. * No more than 64 labels can be associated with a given session. See https://goo.gl/xmQnxf for more information on and examples of labels. */
  labels: Record<string, string>;
  /** Optional. If `true`, specifies a multiplexed session. Use a multiplexed session for multiple, concurrent operations including any combination of read-only and read-write transactions. Use `sessions.create` to create multiplexed sessions. Don't use BatchCreateSessions to create a multiplexed session. You can't delete or list multiplexed sessions. */
  multiplexed: boolean;
  /** Output only. The name of the session. This is always system-assigned. */
  name: string;
  /** A session in the Cloud Spanner API. */
  session: SpannerSession_Session;
}

export const SpannerSession: ResourceBinding<SpannerSessionConfig, SpannerSessionAttrs> = {
  wireType: "google_spanner_spanner_session",
  fields: {
    session: {
      wireName: "session",
      kind: "object",
      fields: SpannerSession_SessionFields,
    },
  },
};
