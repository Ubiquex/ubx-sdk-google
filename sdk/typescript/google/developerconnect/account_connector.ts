// Code generated by `ubx sdk gen` from google_developerconnect_account_connector. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface AccountConnector_CustomOauthConfig_ServiceDirectoryConfig {
  /** Required. The Service Directory service name. Format: projects/{project}/locations/{location}/namespaces/{namespace}/services/{service}. */
  service?: string | Computed<string>;
}

export interface AccountConnector_CustomOauthConfig {
  /** Required. Immutable. The OAuth2 authorization server URL. */
  authUri?: string | Computed<string>;
  /** Required. The client ID of the OAuth application. */
  clientId?: string | Computed<string>;
  /** Required. Input only. The client secret of the OAuth application. It will be provided as plain text, but encrypted and stored in developer connect. As INPUT_ONLY field, it will not be included in the output. */
  clientSecret?: string | Computed<string>;
  /** Required. The host URI of the OAuth application. */
  hostUri?: string | Computed<string>;
  /** Optional. Disable PKCE for this OAuth config. PKCE is enabled by default. */
  pkceDisabled?: boolean | Computed<boolean>;
  /** Required. The type of the SCM provider. */
  scmProvider?: string | Computed<string>;
  /** Required. The scopes to be requested during OAuth. */
  scopes?: string[] | Computed<string[]>;
  /** Output only. SCM server version installed at the host URI. */
  serverVersion?: string | Computed<string>;
  /** ServiceDirectoryConfig represents Service Directory configuration for a connection. */
  serviceDirectoryConfig?: AccountConnector_CustomOauthConfig_ServiceDirectoryConfig | Computed<AccountConnector_CustomOauthConfig_ServiceDirectoryConfig>;
  /** Optional. SSL certificate to use for requests to a private service. */
  sslCaCertificate?: string | Computed<string>;
  /** Required. Immutable. The OAuth2 token request URL. */
  tokenUri?: string | Computed<string>;
}

export interface AccountConnector_ProviderOauthConfig {
  /** Required. User selected scopes to apply to the Oauth config In the event of changing scopes, user records under AccountConnector will be deleted and users will re-auth again. */
  scopes?: string[] | Computed<string[]>;
  /** Optional. Immutable. Developer Connect provided OAuth. */
  systemProviderId?: string | Computed<string>;
}

export interface AccountConnector_ProxyConfig {
  /** Optional. Setting this to true allows the git and http proxies to perform actions on behalf of the user configured under the account connector. */
  enabled?: boolean | Computed<boolean>;
  /** Output only. The base URI for the HTTP proxy endpoint. Has the format `https://{generatedID}-a-h-{shortRegion}.developerconnect.dev` Populated only when `enabled` is set to `true`. This endpoint is used by other Google services that integrate with Developer Connect. */
  httpProxyBaseUri?: string | Computed<string>;
}

const AccountConnector_CustomOauthConfig_ServiceDirectoryConfigFields: FieldMap = {
  service: "service",
};

const AccountConnector_CustomOauthConfigFields: FieldMap = {
  authUri: "auth_uri",
  clientId: "client_id",
  clientSecret: "client_secret",
  hostUri: "host_uri",
  pkceDisabled: "pkce_disabled",
  scmProvider: "scm_provider",
  scopes: "scopes",
  serverVersion: "server_version",
  serviceDirectoryConfig: {
    wireName: "service_directory_config",
    kind: "object",
    fields: AccountConnector_CustomOauthConfig_ServiceDirectoryConfigFields,
  },
  sslCaCertificate: "ssl_ca_certificate",
  tokenUri: "token_uri",
};

const AccountConnector_ProviderOauthConfigFields: FieldMap = {
  scopes: "scopes",
  systemProviderId: "system_provider_id",
};

const AccountConnector_ProxyConfigFields: FieldMap = {
  enabled: "enabled",
  httpProxyBaseUri: "http_proxy_base_uri",
};

export interface AccountConnectorConfig {
  /** Optional. Allows users to store small amounts of arbitrary data. */
  annotations?: Record<string, string> | Computed<Record<string, string>>;
  /** Message for a customized OAuth config. */
  customOauthConfig?: AccountConnector_CustomOauthConfig | Computed<AccountConnector_CustomOauthConfig>;
  /** Optional. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. */
  etag?: string | Computed<string>;
  /** Optional. Labels as key value pairs */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Identifier. The resource name of the accountConnector, in the format `projects/{project}/locations/{location}/accountConnectors/{account_connector_id}`. */
  name?: string | Computed<string>;
  /** ProviderOAuthConfig is the OAuth config for a provider. */
  providerOauthConfig?: AccountConnector_ProviderOauthConfig | Computed<AccountConnector_ProviderOauthConfig>;
  /** The proxy configuration. */
  proxyConfig?: AccountConnector_ProxyConfig | Computed<AccountConnector_ProxyConfig>;
}

export interface AccountConnectorAttrs {
  /** Optional. Allows users to store small amounts of arbitrary data. */
  annotations: Record<string, string>;
  /** Output only. The timestamp when the accountConnector was created. */
  createTime: string;
  /** Message for a customized OAuth config. */
  customOauthConfig: AccountConnector_CustomOauthConfig;
  /** Optional. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. */
  etag: string;
  /** Optional. Labels as key value pairs */
  labels: Record<string, string>;
  /** Identifier. The resource name of the accountConnector, in the format `projects/{project}/locations/{location}/accountConnectors/{account_connector_id}`. */
  name: string;
  /** Output only. Start OAuth flow by clicking on this URL. */
  oauthStartUri: string;
  /** ProviderOAuthConfig is the OAuth config for a provider. */
  providerOauthConfig: AccountConnector_ProviderOauthConfig;
  /** The proxy configuration. */
  proxyConfig: AccountConnector_ProxyConfig;
  /** Output only. A system-assigned unique identifier for the Account Connector. */
  uid: string;
  /** Output only. The timestamp when the accountConnector was updated. */
  updateTime: string;
}

export const AccountConnector: ResourceBinding<AccountConnectorConfig, AccountConnectorAttrs> = {
  wireType: "google_developerconnect_account_connector",
  fields: {
    annotations: "annotations",
    customOauthConfig: {
      wireName: "custom_oauth_config",
      kind: "object",
      fields: AccountConnector_CustomOauthConfigFields,
    },
    etag: "etag",
    labels: "labels",
    name: "name",
    providerOauthConfig: {
      wireName: "provider_oauth_config",
      kind: "object",
      fields: AccountConnector_ProviderOauthConfigFields,
    },
    proxyConfig: {
      wireName: "proxy_config",
      kind: "object",
      fields: AccountConnector_ProxyConfigFields,
    },
  },
};
