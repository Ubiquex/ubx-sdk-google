// Code generated by `ubx sdk gen` from google_firebasedataconnect_schema. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Schema_Datasources_HttpGraphql {
  timeout?: string | Computed<string>;
  uri?: string | Computed<string>;
}

export interface Schema_Datasources_Postgresql_CloudSql {
  edition?: string | Computed<string>;
  instance?: string | Computed<string>;
}

export interface Schema_Datasources_Postgresql {
  cloudSql?: Schema_Datasources_Postgresql_CloudSql | Computed<Schema_Datasources_Postgresql_CloudSql>;
  database?: string | Computed<string>;
  ephemeral?: boolean | Computed<boolean>;
  schema?: string | Computed<string>;
  schemaMigration?: string | Computed<string>;
  schemaValidation?: string | Computed<string>;
  unlinked?: boolean | Computed<boolean>;
}

export interface Schema_Datasources {
  httpGraphql?: Schema_Datasources_HttpGraphql | Computed<Schema_Datasources_HttpGraphql>;
  postgresql?: Schema_Datasources_Postgresql | Computed<Schema_Datasources_Postgresql>;
}

export interface Schema_Source_Files {
  content?: string | Computed<string>;
  path?: string | Computed<string>;
}

export interface Schema_Source {
  /** Required. The files that comprise the source set. */
  files?: Schema_Source_Files[] | Computed<Schema_Source_Files[]>;
}

const Schema_Datasources_HttpGraphqlFields: FieldMap = {
  timeout: "timeout",
  uri: "uri",
};

const Schema_Datasources_Postgresql_CloudSqlFields: FieldMap = {
  edition: "edition",
  instance: "instance",
};

const Schema_Datasources_PostgresqlFields: FieldMap = {
  cloudSql: {
    wireName: "cloud_sql",
    kind: "object",
    fields: Schema_Datasources_Postgresql_CloudSqlFields,
  },
  database: "database",
  ephemeral: "ephemeral",
  schema: "schema",
  schemaMigration: "schema_migration",
  schemaValidation: "schema_validation",
  unlinked: "unlinked",
};

const Schema_DatasourcesFields: FieldMap = {
  httpGraphql: {
    wireName: "http_graphql",
    kind: "object",
    fields: Schema_Datasources_HttpGraphqlFields,
  },
  postgresql: {
    wireName: "postgresql",
    kind: "object",
    fields: Schema_Datasources_PostgresqlFields,
  },
};

const Schema_Source_FilesFields: FieldMap = {
  content: "content",
  path: "path",
};

const Schema_SourceFields: FieldMap = {
  files: {
    wireName: "files",
    kind: "list",
    fields: Schema_Source_FilesFields,
  },
};

export interface SchemaConfig {
  /** Optional. Stores small amounts of arbitrary data. */
  annotations?: Record<string, string> | Computed<Record<string, string>>;
  /** Required. The data sources linked in the schema. */
  datasources?: Schema_Datasources[] | Computed<Schema_Datasources[]>;
  /** Optional. Mutable human-readable name. 63 character limit. */
  displayName?: string | Computed<string>;
  /** Optional. Labels as key value pairs. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Identifier. The relative resource name of the schema, in the format: ``` projects/{project}/locations/{location}/services/{service}/schemas/{schema} ``` Right now, the only supported schema is "main". */
  name?: string | Computed<string>;
  /** Used to represent a set of source files. */
  source?: Schema_Source | Computed<Schema_Source>;
}

export interface SchemaAttrs {
  /** Optional. Stores small amounts of arbitrary data. */
  annotations: Record<string, string>;
  /** Output only. [Output only] Create time stamp. */
  createTime: string;
  /** Required. The data sources linked in the schema. */
  datasources: Schema_Datasources[];
  /** Optional. Mutable human-readable name. 63 character limit. */
  displayName: string;
  /** Output only. This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. [AIP-154](https://google.aip.dev/154) */
  etag: string;
  /** Optional. Labels as key value pairs. */
  labels: Record<string, string>;
  /** Identifier. The relative resource name of the schema, in the format: ``` projects/{project}/locations/{location}/services/{service}/schemas/{schema} ``` Right now, the only supported schema is "main". */
  name: string;
  /** Output only. A field that if true, indicates that the system is working to compile and deploy the schema. */
  reconciling: boolean;
  /** Used to represent a set of source files. */
  source: Schema_Source;
  /** Output only. System-assigned, unique identifier. */
  uid: string;
  /** Output only. [Output only] Update time stamp. */
  updateTime: string;
}

export const Schema: ResourceBinding<SchemaConfig, SchemaAttrs> = {
  wireType: "google_firebasedataconnect_schema",
  fields: {
    annotations: "annotations",
    datasources: {
      wireName: "datasources",
      kind: "list",
      fields: Schema_DatasourcesFields,
    },
    displayName: "display_name",
    labels: "labels",
    name: "name",
    source: {
      wireName: "source",
      kind: "object",
      fields: Schema_SourceFields,
    },
  },
};
