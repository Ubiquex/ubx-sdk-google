// Code generated by `ubx sdk gen` from google_compute_beta_subnetwork. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface BetaSubnetwork_LogConfig {
  /** Can only be specified if VPC flow logging for this subnetwork is enabled. Toggles the aggregation interval for collecting flow logs. Increasing the interval time will reduce the amount of generated flow logs for long lasting connections. Default is an interval of 5 seconds per connection. */
  aggregationInterval?: string | Computed<string>;
  /** Whether to enable flow logging for this subnetwork. If this field is not explicitly set, it will not appear in get listings. If not set the default behavior is determined by the org policy, if there is no org policy specified, then it will default to disabled. Flow logging isn't supported if the subnet purpose field is set to REGIONAL_MANAGED_PROXY. */
  enable?: boolean | Computed<boolean>;
  /** Can only be specified if VPC flow logs for this subnetwork is enabled. The filter expression is used to define which VPC flow logs should be exported to Cloud Logging. */
  filterExpr?: string | Computed<string>;
  /** Can only be specified if VPC flow logging for this subnetwork is enabled. The value of the field must be in [0, 1]. Set the sampling rate of VPC flow logs within the subnetwork where 1.0 means all collected logs are reported and 0.0 means no logs are reported. Default is 0.5 unless otherwise specified by the org policy, which means half of all collected logs are reported. */
  flowSampling?: number | Computed<number>;
  /** Can only be specified if VPC flow logs for this subnetwork is enabled. Configures whether all, none or a subset of metadata fields should be added to the reported VPC flow logs. Default isEXCLUDE_ALL_METADATA. */
  metadata?: string | Computed<string>;
  /** Can only be specified if VPC flow logs for this subnetwork is enabled and "metadata" was set to CUSTOM_METADATA. */
  metadataFields?: string[] | Computed<string[]>;
}

export interface BetaSubnetwork_Params {
  /** Tag keys/values directly bound to this resource. Tag keys and values have the same definition as resource manager tags. The field is allowed for INSERT only. The keys/values to set on the resource should be specified in either ID { : } or Namespaced format { : }. For example the following are valid inputs: * {"tagKeys/333" : "tagValues/444", "tagKeys/123" : "tagValues/456"} * {"123/environment" : "production", "345/abc" : "xyz"} Note: * Invalid combinations of ID & namespaced format is not supported. For instance: {"123/environment" : "tagValues/444"} is invalid. */
  resourceManagerTags?: Record<string, string> | Computed<Record<string, string>>;
}

export interface BetaSubnetwork_SecondaryIpRanges {
  ipCidrRange?: string | Computed<string>;
  ipCollection?: string | Computed<string>;
  ipVersion?: string | Computed<string>;
  rangeName?: string | Computed<string>;
  reservedInternalRange?: string | Computed<string>;
}

export interface BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization_TotalAllocatedIp {
  high?: string | Computed<string>;
  low?: string | Computed<string>;
}

export interface BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization {
  totalAllocatedIp?: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization_TotalAllocatedIp | Computed<BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization_TotalAllocatedIp>;
  totalFreeIp?: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization_TotalAllocatedIp | Computed<BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization_TotalAllocatedIp>;
}

export interface BetaSubnetwork_UtilizationDetails_Ipv4Utilizations {
  rangeName?: string | Computed<string>;
  totalAllocatedIp?: string | Computed<string>;
  totalFreeIp?: string | Computed<string>;
}

export interface BetaSubnetwork_UtilizationDetails {
  /** The IPV6 utilization of a single IP range. */
  externalIpv6InstanceUtilization?: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization | Computed<BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization>;
  /** The IPV6 utilization of a single IP range. */
  externalIpv6LbUtilization?: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization | Computed<BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization>;
  /** The IPV6 utilization of a single IP range. */
  internalIpv6Utilization?: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization | Computed<BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization>;
  /** Utilizations of all IPV4 IP ranges. For primary ranges, the range name will be empty. */
  ipv4Utilizations?: BetaSubnetwork_UtilizationDetails_Ipv4Utilizations[] | Computed<BetaSubnetwork_UtilizationDetails_Ipv4Utilizations[]>;
}

const BetaSubnetwork_LogConfigFields: FieldMap = {
  aggregationInterval: "aggregation_interval",
  enable: "enable",
  filterExpr: "filter_expr",
  flowSampling: "flow_sampling",
  metadata: "metadata",
  metadataFields: "metadata_fields",
};

const BetaSubnetwork_ParamsFields: FieldMap = {
  resourceManagerTags: "resource_manager_tags",
};

const BetaSubnetwork_SecondaryIpRangesFields: FieldMap = {
  ipCidrRange: "ip_cidr_range",
  ipCollection: "ip_collection",
  ipVersion: "ip_version",
  rangeName: "range_name",
  reservedInternalRange: "reserved_internal_range",
};

const BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization_TotalAllocatedIpFields: FieldMap = {
  high: "high",
  low: "low",
};

const BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilizationFields: FieldMap = {
  totalAllocatedIp: {
    wireName: "total_allocated_ip",
    kind: "object",
    fields: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization_TotalAllocatedIpFields,
  },
  totalFreeIp: {
    wireName: "total_free_ip",
    kind: "object",
    fields: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilization_TotalAllocatedIpFields,
  },
};

const BetaSubnetwork_UtilizationDetails_Ipv4UtilizationsFields: FieldMap = {
  rangeName: "range_name",
  totalAllocatedIp: "total_allocated_ip",
  totalFreeIp: "total_free_ip",
};

const BetaSubnetwork_UtilizationDetailsFields: FieldMap = {
  externalIpv6InstanceUtilization: {
    wireName: "external_ipv6_instance_utilization",
    kind: "object",
    fields: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilizationFields,
  },
  externalIpv6LbUtilization: {
    wireName: "external_ipv6_lb_utilization",
    kind: "object",
    fields: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilizationFields,
  },
  internalIpv6Utilization: {
    wireName: "internal_ipv6_utilization",
    kind: "object",
    fields: BetaSubnetwork_UtilizationDetails_ExternalIpv6InstanceUtilizationFields,
  },
  ipv4Utilizations: {
    wireName: "ipv4_utilizations",
    kind: "list",
    fields: BetaSubnetwork_UtilizationDetails_Ipv4UtilizationsFields,
  },
};

export interface BetaSubnetworkConfig {
  /** Whether this subnetwork's ranges can conflict with existing custom routes. Setting this to true allows this subnetwork's primary and secondary ranges to overlap with (and contain) custom routes that have already been configured on the corresponding network. For example if a static route has range 10.1.0.0/16, a subnet range 10.0.0.0/8 could only be created if allow_conflicting_routes=true. Overlapping is only allowed on subnetwork operations; routes whose ranges conflict with this subnetwork's ranges won't be allowed unless route.allow_conflicting_subnetworks is set to true. Typically packets destined to IPs within the subnetwork (which may contain private/sensitive data) are prevented from leaving the virtual network. Setting this field to true will disable this feature. The default value is false and applies to all existing subnetworks and automatically created subnetworks. */
  allowSubnetCidrRoutesOverlap?: boolean | Computed<boolean>;
  /** Output only. [Output Only] Creation timestamp inRFC3339 text format. */
  creationTimestamp?: string | Computed<string>;
  /** An optional description of this resource. Provide this property when you create the resource. This field can be set only at resource creation time. */
  description?: string | Computed<string>;
  /** Whether to enable flow logging for this subnetwork. If this field is not explicitly set, it will not appear in get listings. If not set the default behavior is determined by the org policy, if there is no org policy specified, then it will default to disabled. This field isn't supported if the subnet purpose field is set toREGIONAL_MANAGED_PROXY. It is recommended to uselogConfig.enable field instead. */
  enableFlowLogs?: boolean | Computed<boolean>;
  /** The external IPv6 address range that is owned by this subnetwork. */
  externalIpv6Prefix?: string | Computed<string>;
  /** Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field will be ignored when inserting a Subnetwork. An up-to-date fingerprint must be provided in order to update the Subnetwork, otherwise the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a Subnetwork. */
  fingerprint?: string | Computed<string>;
  /** Output only. [Output Only] The gateway address for default routes to reach destination addresses outside this subnetwork. */
  gatewayAddress?: string | Computed<string>;
  /** Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. */
  id?: string | Computed<string>;
  /** The internal IPv6 address range that is owned by this subnetwork. */
  internalIpv6Prefix?: string | Computed<string>;
  /** The range of internal addresses that are owned by this subnetwork. Provide this property when you create the subnetwork. For example,10.0.0.0/8 or 100.64.0.0/10. Ranges must be unique and non-overlapping within a network. Only IPv4 is supported. This field is set at resource creation time. The range can be any range listed in theValid ranges list. The range can be expanded after creation usingexpandIpCidrRange. */
  ipCidrRange?: string | Computed<string>;
  /** Reference to the source of IP, like a PublicDelegatedPrefix (PDP) for BYOIP. The PDP must be a sub-PDP in EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION mode. Use one of the following formats to specify a sub-PDP when creating a dual stack or IPv6-only subnetwork with external access using BYOIP: - Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name - Partial URL, as in - projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name - regions/region/publicDelegatedPrefixes/sub-pdp-name */
  ipCollection?: string | Computed<string>;
  /** The access type of IPv6 address this subnet holds. It's immutable and can only be specified during creation or the first time the subnet is updated into IPV4_IPV6 dual stack. */
  ipv6AccessType?: string | Computed<string>;
  /** Output only. [Output Only] This field is for internal use. */
  ipv6CidrRange?: string | Computed<string>;
  /** Output only. [Output Only] Possible endpoints of this subnetwork. It can be one of the following: - VM_ONLY: The subnetwork can be used for creating instances and IPv6 addresses with VM endpoint type. Such a subnetwork gets external IPv6 ranges from a public delegated prefix and cannot be used to create NetLb. - VM_AND_FR: The subnetwork can be used for creating both VM instances and Forwarding Rules. It can also be used to reserve IPv6 addresses with both VM and FR endpoint types. Such a subnetwork gets its IPv6 range from Google IP Pool directly. */
  ipv6GceEndpoint?: string | Computed<string>;
  /** Specifies the network tier for EXTERNAL IPv6. Can only be set when ipv6_access_type is EXTERNAL. Defaults to project defaultNetworkTier if not specified during the creation of the subnetwork. This field is IMMUTABLE once set with EXTERNAL IPv6. */
  ipv6NetworkTier?: string | Computed<string>;
  /** Output only. [Output Only] Type of the resource. Always compute#subnetwork for Subnetwork resources. */
  kind?: string | Computed<string>;
  /** The available logging options for this subnetwork. */
  logConfig?: BetaSubnetwork_LogConfig | Computed<BetaSubnetwork_LogConfig>;
  /** The name of the resource, provided by the client when initially creating the resource. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. */
  name?: string | Computed<string>;
  /** The URL of the network to which this subnetwork belongs, provided by the client when initially creating the subnetwork. This field can be set only at resource creation time. */
  network?: string | Computed<string>;
  /** Additional subnetwork parameters. */
  params?: BetaSubnetwork_Params | Computed<BetaSubnetwork_Params>;
  /** Whether the VMs in this subnet can access Google services without assigned external IP addresses. This field can be both set at resource creation time and updated using setPrivateIpGoogleAccess. */
  privateIpGoogleAccess?: boolean | Computed<boolean>;
  /** This field is for internal use. This field can be both set at resource creation time and updated usingpatch. */
  privateIpv6GoogleAccess?: string | Computed<string>;
  purpose?: string | Computed<string>;
  /** URL of the region where the Subnetwork resides. This field can be set only at resource creation time. */
  region?: string | Computed<string>;
  /** The URL of the reserved internal range. */
  reservedInternalRange?: string | Computed<string>;
  /** Configures subnet mask resolution for this subnetwork. */
  resolveSubnetMask?: string | Computed<string>;
  /** The role of subnetwork. Currently, this field is only used when purpose is set to GLOBAL_MANAGED_PROXY orREGIONAL_MANAGED_PROXY. The value can be set toACTIVE or BACKUP. An ACTIVE subnetwork is one that is currently being used for Envoy-based load balancers in a region. A BACKUP subnetwork is one that is ready to be promoted to ACTIVE or is currently draining. This field can be updated with a patch request. */
  role?: string | Computed<string>;
  /** An array of configurations for secondary IP ranges for VM instances contained in this subnetwork. The primary IP of such VM must belong to the primary ipCidrRange of the subnetwork. The alias IPs may belong to either primary or secondary ranges. This field can be updated with apatch request. Supports both IPv4 and IPv6 ranges. */
  secondaryIpRanges?: BetaSubnetwork_SecondaryIpRanges[] | Computed<BetaSubnetwork_SecondaryIpRanges[]>;
  /** [Output Only] Server-defined URL for the resource. */
  selfLink?: string | Computed<string>;
  /** The stack type for the subnet. If set to IPV4_ONLY, new VMs in the subnet are assigned IPv4 addresses only. If set toIPV4_IPV6, new VMs in the subnet can be assigned both IPv4 and IPv6 addresses. If not specified, IPV4_ONLY is used. This field can be both set at resource creation time and updated usingpatch. */
  stackType?: string | Computed<string>;
  /** Output only. [Output Only] The state of the subnetwork, which can be one of the following values:READY: Subnetwork is created and ready to useDRAINING: only applicable to subnetworks that have the purpose set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that connections to the load balancer are being drained. A subnetwork that is draining cannot be used or modified until it reaches a status ofREADY */
  state?: string | Computed<string>;
  /** Output only. [Output Only] The array of external IPv6 network ranges reserved from the subnetwork's external IPv6 range for system use. */
  systemReservedExternalIpv6Ranges?: string[] | Computed<string[]>;
  /** Output only. [Output Only] The array of internal IPv6 network ranges reserved from the subnetwork's internal IPv6 range for system use. */
  systemReservedInternalIpv6Ranges?: string[] | Computed<string[]>;
  /** The current IP utilization of all subnetwork ranges. Contains the total number of allocated and free IPs in each range. */
  utilizationDetails?: BetaSubnetwork_UtilizationDetails | Computed<BetaSubnetwork_UtilizationDetails>;
}

export interface BetaSubnetworkAttrs {
  /** Whether this subnetwork's ranges can conflict with existing custom routes. Setting this to true allows this subnetwork's primary and secondary ranges to overlap with (and contain) custom routes that have already been configured on the corresponding network. For example if a static route has range 10.1.0.0/16, a subnet range 10.0.0.0/8 could only be created if allow_conflicting_routes=true. Overlapping is only allowed on subnetwork operations; routes whose ranges conflict with this subnetwork's ranges won't be allowed unless route.allow_conflicting_subnetworks is set to true. Typically packets destined to IPs within the subnetwork (which may contain private/sensitive data) are prevented from leaving the virtual network. Setting this field to true will disable this feature. The default value is false and applies to all existing subnetworks and automatically created subnetworks. */
  allowSubnetCidrRoutesOverlap: boolean;
  /** Output only. [Output Only] Creation timestamp inRFC3339 text format. */
  creationTimestamp: string;
  /** An optional description of this resource. Provide this property when you create the resource. This field can be set only at resource creation time. */
  description: string;
  /** Whether to enable flow logging for this subnetwork. If this field is not explicitly set, it will not appear in get listings. If not set the default behavior is determined by the org policy, if there is no org policy specified, then it will default to disabled. This field isn't supported if the subnet purpose field is set toREGIONAL_MANAGED_PROXY. It is recommended to uselogConfig.enable field instead. */
  enableFlowLogs: boolean;
  /** The external IPv6 address range that is owned by this subnetwork. */
  externalIpv6Prefix: string;
  /** Fingerprint of this resource. A hash of the contents stored in this object. This field is used in optimistic locking. This field will be ignored when inserting a Subnetwork. An up-to-date fingerprint must be provided in order to update the Subnetwork, otherwise the request will fail with error 412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve a Subnetwork. */
  fingerprint: string;
  /** Output only. [Output Only] The gateway address for default routes to reach destination addresses outside this subnetwork. */
  gatewayAddress: string;
  /** Output only. [Output Only] The unique identifier for the resource. This identifier is defined by the server. */
  id: string;
  /** The internal IPv6 address range that is owned by this subnetwork. */
  internalIpv6Prefix: string;
  /** The range of internal addresses that are owned by this subnetwork. Provide this property when you create the subnetwork. For example,10.0.0.0/8 or 100.64.0.0/10. Ranges must be unique and non-overlapping within a network. Only IPv4 is supported. This field is set at resource creation time. The range can be any range listed in theValid ranges list. The range can be expanded after creation usingexpandIpCidrRange. */
  ipCidrRange: string;
  /** Reference to the source of IP, like a PublicDelegatedPrefix (PDP) for BYOIP. The PDP must be a sub-PDP in EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION mode. Use one of the following formats to specify a sub-PDP when creating a dual stack or IPv6-only subnetwork with external access using BYOIP: - Full resource URL, as inhttps://www.googleapis.com/compute/v1/projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name - Partial URL, as in - projects/projectId/regions/region/publicDelegatedPrefixes/sub-pdp-name - regions/region/publicDelegatedPrefixes/sub-pdp-name */
  ipCollection: string;
  /** The access type of IPv6 address this subnet holds. It's immutable and can only be specified during creation or the first time the subnet is updated into IPV4_IPV6 dual stack. */
  ipv6AccessType: string;
  /** Output only. [Output Only] This field is for internal use. */
  ipv6CidrRange: string;
  /** Output only. [Output Only] Possible endpoints of this subnetwork. It can be one of the following: - VM_ONLY: The subnetwork can be used for creating instances and IPv6 addresses with VM endpoint type. Such a subnetwork gets external IPv6 ranges from a public delegated prefix and cannot be used to create NetLb. - VM_AND_FR: The subnetwork can be used for creating both VM instances and Forwarding Rules. It can also be used to reserve IPv6 addresses with both VM and FR endpoint types. Such a subnetwork gets its IPv6 range from Google IP Pool directly. */
  ipv6GceEndpoint: string;
  /** Specifies the network tier for EXTERNAL IPv6. Can only be set when ipv6_access_type is EXTERNAL. Defaults to project defaultNetworkTier if not specified during the creation of the subnetwork. This field is IMMUTABLE once set with EXTERNAL IPv6. */
  ipv6NetworkTier: string;
  /** Output only. [Output Only] Type of the resource. Always compute#subnetwork for Subnetwork resources. */
  kind: string;
  /** The available logging options for this subnetwork. */
  logConfig: BetaSubnetwork_LogConfig;
  /** The name of the resource, provided by the client when initially creating the resource. The name must be 1-63 characters long, and comply withRFC1035. Specifically, the name must be 1-63 characters long and match the regular expression `[a-z]([-a-z0-9]*[a-z0-9])?` which means the first character must be a lowercase letter, and all following characters must be a dash, lowercase letter, or digit, except the last character, which cannot be a dash. */
  name: string;
  /** The URL of the network to which this subnetwork belongs, provided by the client when initially creating the subnetwork. This field can be set only at resource creation time. */
  network: string;
  /** Additional subnetwork parameters. */
  params: BetaSubnetwork_Params;
  /** Whether the VMs in this subnet can access Google services without assigned external IP addresses. This field can be both set at resource creation time and updated using setPrivateIpGoogleAccess. */
  privateIpGoogleAccess: boolean;
  /** This field is for internal use. This field can be both set at resource creation time and updated usingpatch. */
  privateIpv6GoogleAccess: string;
  purpose: string;
  /** URL of the region where the Subnetwork resides. This field can be set only at resource creation time. */
  region: string;
  /** The URL of the reserved internal range. */
  reservedInternalRange: string;
  /** Configures subnet mask resolution for this subnetwork. */
  resolveSubnetMask: string;
  /** The role of subnetwork. Currently, this field is only used when purpose is set to GLOBAL_MANAGED_PROXY orREGIONAL_MANAGED_PROXY. The value can be set toACTIVE or BACKUP. An ACTIVE subnetwork is one that is currently being used for Envoy-based load balancers in a region. A BACKUP subnetwork is one that is ready to be promoted to ACTIVE or is currently draining. This field can be updated with a patch request. */
  role: string;
  /** An array of configurations for secondary IP ranges for VM instances contained in this subnetwork. The primary IP of such VM must belong to the primary ipCidrRange of the subnetwork. The alias IPs may belong to either primary or secondary ranges. This field can be updated with apatch request. Supports both IPv4 and IPv6 ranges. */
  secondaryIpRanges: BetaSubnetwork_SecondaryIpRanges[];
  /** [Output Only] Server-defined URL for the resource. */
  selfLink: string;
  /** The stack type for the subnet. If set to IPV4_ONLY, new VMs in the subnet are assigned IPv4 addresses only. If set toIPV4_IPV6, new VMs in the subnet can be assigned both IPv4 and IPv6 addresses. If not specified, IPV4_ONLY is used. This field can be both set at resource creation time and updated usingpatch. */
  stackType: string;
  /** Output only. [Output Only] The state of the subnetwork, which can be one of the following values:READY: Subnetwork is created and ready to useDRAINING: only applicable to subnetworks that have the purpose set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that connections to the load balancer are being drained. A subnetwork that is draining cannot be used or modified until it reaches a status ofREADY */
  state: string;
  /** Output only. [Output Only] The array of external IPv6 network ranges reserved from the subnetwork's external IPv6 range for system use. */
  systemReservedExternalIpv6Ranges: string[];
  /** Output only. [Output Only] The array of internal IPv6 network ranges reserved from the subnetwork's internal IPv6 range for system use. */
  systemReservedInternalIpv6Ranges: string[];
  /** The current IP utilization of all subnetwork ranges. Contains the total number of allocated and free IPs in each range. */
  utilizationDetails: BetaSubnetwork_UtilizationDetails;
}

export const BetaSubnetwork: ResourceBinding<BetaSubnetworkConfig, BetaSubnetworkAttrs> = {
  wireType: "google_compute_beta_subnetwork",
  fields: {
    allowSubnetCidrRoutesOverlap: "allow_subnet_cidr_routes_overlap",
    creationTimestamp: "creation_timestamp",
    description: "description",
    enableFlowLogs: "enable_flow_logs",
    externalIpv6Prefix: "external_ipv6_prefix",
    fingerprint: "fingerprint",
    gatewayAddress: "gateway_address",
    id: "id",
    internalIpv6Prefix: "internal_ipv6_prefix",
    ipCidrRange: "ip_cidr_range",
    ipCollection: "ip_collection",
    ipv6AccessType: "ipv6_access_type",
    ipv6CidrRange: "ipv6_cidr_range",
    ipv6GceEndpoint: "ipv6_gce_endpoint",
    ipv6NetworkTier: "ipv6_network_tier",
    kind: "kind",
    logConfig: {
      wireName: "log_config",
      kind: "object",
      fields: BetaSubnetwork_LogConfigFields,
    },
    name: "name",
    network: "network",
    params: {
      wireName: "params",
      kind: "object",
      fields: BetaSubnetwork_ParamsFields,
    },
    privateIpGoogleAccess: "private_ip_google_access",
    privateIpv6GoogleAccess: "private_ipv6_google_access",
    purpose: "purpose",
    region: "region",
    reservedInternalRange: "reserved_internal_range",
    resolveSubnetMask: "resolve_subnet_mask",
    role: "role",
    secondaryIpRanges: {
      wireName: "secondary_ip_ranges",
      kind: "list",
      fields: BetaSubnetwork_SecondaryIpRangesFields,
    },
    selfLink: "self_link",
    stackType: "stack_type",
    state: "state",
    systemReservedExternalIpv6Ranges: "system_reserved_external_ipv6_ranges",
    systemReservedInternalIpv6Ranges: "system_reserved_internal_ipv6_ranges",
    utilizationDetails: {
      wireName: "utilization_details",
      kind: "object",
      fields: BetaSubnetwork_UtilizationDetailsFields,
    },
  },
};
