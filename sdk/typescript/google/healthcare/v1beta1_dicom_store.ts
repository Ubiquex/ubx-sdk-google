// Code generated by `ubx sdk gen` from google_healthcare_v1beta1_dicom_store. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1beta1DicomStore_NotificationConfig {
  /** The [Pub/Sub](https://cloud.google.com/pubsub/docs/) topic that notifications of changes are published on. Supplied by the client. PubsubMessage.Data contains the resource name. PubsubMessage.MessageId is the ID of this message. It is guaranteed to be unique within the topic. PubsubMessage.PublishTime is the time at which the message was published. Notifications are only sent if the topic is non-empty. [Topic names](https://cloud.google.com/pubsub/docs/overview#names) must be scoped to a project. Cloud Healthcare API service account must have publisher permissions on the given Pub/Sub topic. Not having adequate permissions causes the calls that send notifications to fail. If a notification can't be published to Pub/Sub, errors are logged to Cloud Logging (see [Viewing error logs in Cloud Logging](https://cloud.google.com/healthcare/docs/how-tos/logging)). If the number of errors exceeds a certain rate, some aren't submitted. Note that not all operations trigger notifications, see [Configuring Pub/Sub notifications](https://cloud.google.com/healthcare/docs/how-tos/pubsub) for specific details. */
  pubsubTopic?: string | Computed<string>;
  /** Indicates whether or not to send Pub/Sub notifications on bulk import. Only supported for DICOM imports. */
  sendForBulkImport?: boolean | Computed<boolean>;
}

export interface V1beta1DicomStore_NotificationConfigs {
  pubsubTopic?: string | Computed<string>;
}

export interface V1beta1DicomStore_StreamConfigs_BigqueryDestination {
  changeDataCaptureConfig?: unknown | Computed<unknown>;
  force?: boolean | Computed<boolean>;
  includeSourceStore?: boolean | Computed<boolean>;
  schemaFlattened?: unknown | Computed<unknown>;
  schemaJson?: unknown | Computed<unknown>;
  tableUri?: string | Computed<string>;
  writeDisposition?: string | Computed<string>;
}

export interface V1beta1DicomStore_StreamConfigs {
  bigqueryDestination?: V1beta1DicomStore_StreamConfigs_BigqueryDestination | Computed<V1beta1DicomStore_StreamConfigs_BigqueryDestination>;
}

const V1beta1DicomStore_NotificationConfigFields: FieldMap = {
  pubsubTopic: "pubsub_topic",
  sendForBulkImport: "send_for_bulk_import",
};

const V1beta1DicomStore_NotificationConfigsFields: FieldMap = {
  pubsubTopic: "pubsub_topic",
};

const V1beta1DicomStore_StreamConfigs_BigqueryDestinationFields: FieldMap = {
  changeDataCaptureConfig: "change_data_capture_config",
  force: "force",
  includeSourceStore: "include_source_store",
  schemaFlattened: "schema_flattened",
  schemaJson: "schema_json",
  tableUri: "table_uri",
  writeDisposition: "write_disposition",
};

const V1beta1DicomStore_StreamConfigsFields: FieldMap = {
  bigqueryDestination: {
    wireName: "bigquery_destination",
    kind: "object",
    fields: V1beta1DicomStore_StreamConfigs_BigqueryDestinationFields,
  },
};

export interface V1beta1DicomStoreConfig {
  /** User-supplied key-value pairs used to organize DICOM stores. Label keys must be between 1 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes, and must conform to the following PCRE regular expression: \p{Ll}\p{Lo}{0,62} Label values are optional, must be between 1 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes, and must conform to the following PCRE regular expression: [\p{Ll}\p{Lo}\p{N}_-]{0,63} No more than 64 labels can be associated with a given store. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Identifier. Resource name of the DICOM store, of the form `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/dicomStores/{dicom_store_id}`. */
  name?: string | Computed<string>;
  /** Specifies where to send notifications upon changes to a data store. */
  notificationConfig?: V1beta1DicomStore_NotificationConfig | Computed<V1beta1DicomStore_NotificationConfig>;
  /** Optional. Specifies where and whether to send notifications upon changes to a DICOM store. */
  notificationConfigs?: V1beta1DicomStore_NotificationConfigs[] | Computed<V1beta1DicomStore_NotificationConfigs[]>;
  /** Optional. A list of streaming configs used to configure the destination of streaming exports for every DICOM instance insertion in this DICOM store. After a new config is added to `stream_configs`, DICOM instance insertions are streamed to the new destination. When a config is removed from `stream_configs`, the server stops streaming to that destination. Each config must contain a unique destination. */
  streamConfigs?: V1beta1DicomStore_StreamConfigs[] | Computed<V1beta1DicomStore_StreamConfigs[]>;
}

export interface V1beta1DicomStoreAttrs {
  /** User-supplied key-value pairs used to organize DICOM stores. Label keys must be between 1 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes, and must conform to the following PCRE regular expression: \p{Ll}\p{Lo}{0,62} Label values are optional, must be between 1 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes, and must conform to the following PCRE regular expression: [\p{Ll}\p{Lo}\p{N}_-]{0,63} No more than 64 labels can be associated with a given store. */
  labels: Record<string, string>;
  /** Identifier. Resource name of the DICOM store, of the form `projects/{project_id}/locations/{location_id}/datasets/{dataset_id}/dicomStores/{dicom_store_id}`. */
  name: string;
  /** Specifies where to send notifications upon changes to a data store. */
  notificationConfig: V1beta1DicomStore_NotificationConfig;
  /** Optional. Specifies where and whether to send notifications upon changes to a DICOM store. */
  notificationConfigs: V1beta1DicomStore_NotificationConfigs[];
  /** Optional. A list of streaming configs used to configure the destination of streaming exports for every DICOM instance insertion in this DICOM store. After a new config is added to `stream_configs`, DICOM instance insertions are streamed to the new destination. When a config is removed from `stream_configs`, the server stops streaming to that destination. Each config must contain a unique destination. */
  streamConfigs: V1beta1DicomStore_StreamConfigs[];
}

export const V1beta1DicomStore: ResourceBinding<V1beta1DicomStoreConfig, V1beta1DicomStoreAttrs> = {
  wireType: "google_healthcare_v1beta1_dicom_store",
  fields: {
    labels: "labels",
    name: "name",
    notificationConfig: {
      wireName: "notification_config",
      kind: "object",
      fields: V1beta1DicomStore_NotificationConfigFields,
    },
    notificationConfigs: {
      wireName: "notification_configs",
      kind: "list",
      fields: V1beta1DicomStore_NotificationConfigsFields,
    },
    streamConfigs: {
      wireName: "stream_configs",
      kind: "list",
      fields: V1beta1DicomStore_StreamConfigsFields,
    },
  },
};
