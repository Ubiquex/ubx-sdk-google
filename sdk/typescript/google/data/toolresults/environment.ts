// Code generated by `ubx sdk gen` from google_toolresults_environment. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface Environment_CompletionTime {
  /** Non-negative fractions of a second at nanosecond resolution. Negative second values with fractions must still have non-negative nanos values that count forward in time. Must be from 0 to 999,999,999 inclusive. */
  nanos?: number | Computed<number>;
  /** Represents seconds of UTC time since Unix epoch 1970-01-01T00:00:00Z. Must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive. */
  seconds?: string | Computed<string>;
}

export interface Environment_DimensionValue {
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface Environment_EnvironmentResult_Outcome_FailureDetail {
  /** If the failure was severe because the system (app) under test crashed. */
  crashed?: boolean | Computed<boolean>;
  /** If the device ran out of memory during a test, causing the test to crash. */
  deviceOutOfMemory?: boolean | Computed<boolean>;
  /** If the Roboscript failed to complete successfully, e.g., because a Roboscript action or assertion failed or a Roboscript action could not be matched during the entire crawl. */
  failedRoboscript?: boolean | Computed<boolean>;
  /** If an app is not installed and thus no test can be run with the app. This might be caused by trying to run a test on an unsupported platform. */
  notInstalled?: boolean | Computed<boolean>;
  /** If a native process (including any other than the app) crashed. */
  otherNativeCrash?: boolean | Computed<boolean>;
  /** If the test overran some time limit, and that is why it failed. */
  timedOut?: boolean | Computed<boolean>;
  /** If the robo was unable to crawl the app; perhaps because the app did not start. */
  unableToCrawl?: boolean | Computed<boolean>;
}

export interface Environment_EnvironmentResult_Outcome_InconclusiveDetail {
  /** If the end user aborted the test execution before a pass or fail could be determined. For example, the user pressed ctrl-c which sent a kill signal to the test runner while the test was running. */
  abortedByUser?: boolean | Computed<boolean>;
  /** If results are being provided to the user in certain cases of infrastructure failures */
  hasErrorLogs?: boolean | Computed<boolean>;
  /** If the test runner could not determine success or failure because the test depends on a component other than the system under test which failed. For example, a mobile test requires provisioning a device where the test executes, and that provisioning can fail. */
  infrastructureFailure?: boolean | Computed<boolean>;
}

export interface Environment_EnvironmentResult_Outcome_SkippedDetail {
  /** If the App doesn't support the specific API level. */
  incompatibleAppVersion?: boolean | Computed<boolean>;
  /** If the App doesn't run on the specific architecture, for example, x86. */
  incompatibleArchitecture?: boolean | Computed<boolean>;
  /** If the requested OS version doesn't run on the specific device model. */
  incompatibleDevice?: boolean | Computed<boolean>;
  /** Indicates that the test could not be scheduled in the requested time because no suitable device was available. */
  pendingTimeout?: boolean | Computed<boolean>;
}

export interface Environment_EnvironmentResult_Outcome_SuccessDetail {
  /** If a native process other than the app crashed. */
  otherNativeCrash?: boolean | Computed<boolean>;
}

export interface Environment_EnvironmentResult_Outcome {
  /** Details for an outcome with a FAILURE outcome summary. */
  failureDetail?: Environment_EnvironmentResult_Outcome_FailureDetail | Computed<Environment_EnvironmentResult_Outcome_FailureDetail>;
  /** Details for an outcome with an INCONCLUSIVE outcome summary. */
  inconclusiveDetail?: Environment_EnvironmentResult_Outcome_InconclusiveDetail | Computed<Environment_EnvironmentResult_Outcome_InconclusiveDetail>;
  /** Details for an outcome with a SKIPPED outcome summary. */
  skippedDetail?: Environment_EnvironmentResult_Outcome_SkippedDetail | Computed<Environment_EnvironmentResult_Outcome_SkippedDetail>;
  /** Details for an outcome with a SUCCESS outcome summary. LINT.IfChange */
  successDetail?: Environment_EnvironmentResult_Outcome_SuccessDetail | Computed<Environment_EnvironmentResult_Outcome_SuccessDetail>;
  /** The simplest way to interpret a result. Required */
  summary?: string | Computed<string>;
}

export interface Environment_EnvironmentResult_TestSuiteOverviews_XmlSource {
  fileUri?: string | Computed<string>;
}

export interface Environment_EnvironmentResult_TestSuiteOverviews {
  elapsedTime?: Environment_CompletionTime | Computed<Environment_CompletionTime>;
  errorCount?: number | Computed<number>;
  failureCount?: number | Computed<number>;
  flakyCount?: number | Computed<number>;
  name?: string | Computed<string>;
  skippedCount?: number | Computed<number>;
  totalCount?: number | Computed<number>;
  xmlSource?: Environment_EnvironmentResult_TestSuiteOverviews_XmlSource | Computed<Environment_EnvironmentResult_TestSuiteOverviews_XmlSource>;
}

export interface Environment_EnvironmentResult {
  /** Interprets a result so that humans and machines can act on it. */
  outcome?: Environment_EnvironmentResult_Outcome | Computed<Environment_EnvironmentResult_Outcome>;
  /** State of the resource */
  state?: string | Computed<string>;
  /** The combined and rolled-up result of each test suite that was run as part of this environment. Combining: When the test cases from a suite are run in different steps (sharding), the results are added back together in one overview. (e.g., if shard1 has 2 failures and shard2 has 1 failure than the overview failure_count = 3). Rollup: When test cases from the same suite are run multiple times (flaky), the results are combined (e.g., if testcase1.run1 fails, testcase1.run2 passes, and both testcase2.run1 and testcase2.run2 fail then the overview flaky_count = 1 and failure_count = 1). */
  testSuiteOverviews?: Environment_EnvironmentResult_TestSuiteOverviews[] | Computed<Environment_EnvironmentResult_TestSuiteOverviews[]>;
}

export interface Environment_ResultsStorage {
  /** A reference to a file. */
  resultsStoragePath?: Environment_EnvironmentResult_TestSuiteOverviews_XmlSource | Computed<Environment_EnvironmentResult_TestSuiteOverviews_XmlSource>;
  /** A reference to a file. */
  xunitXmlFile?: Environment_EnvironmentResult_TestSuiteOverviews_XmlSource | Computed<Environment_EnvironmentResult_TestSuiteOverviews_XmlSource>;
}

export interface Environment_ShardSummaries {
  runs?: unknown[] | Computed<unknown[]>;
  shardResult?: Environment_EnvironmentResult | Computed<Environment_EnvironmentResult>;
}

export interface EnvironmentConfig {
  environmentId: string | Computed<string>;
  executionId: string | Computed<string>;
  historyId: string | Computed<string>;
  projectId: string | Computed<string>;
}

export interface EnvironmentAttrs {
  /** A Timestamp represents a point in time independent of any time zone or local calendar, encoded as a count of seconds and fractions of seconds at nanosecond resolution. The count is relative to an epoch at UTC midnight on January 1, 1970, in the proleptic Gregorian calendar which extends the Gregorian calendar backwards to year one. All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap second table is needed for interpretation, using a [24-hour linear smear](https://developers.google.com/time/smear). The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By restricting to that range, we ensure that we can convert to and from [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) date strings. */
  completionTime: Environment_CompletionTime;
  /** A Timestamp represents a point in time independent of any time zone or local calendar, encoded as a count of seconds and fractions of seconds at nanosecond resolution. The count is relative to an epoch at UTC midnight on January 1, 1970, in the proleptic Gregorian calendar which extends the Gregorian calendar backwards to year one. All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap second table is needed for interpretation, using a [24-hour linear smear](https://developers.google.com/time/smear). The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By restricting to that range, we ensure that we can convert to and from [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) date strings. */
  creationTime: Environment_CompletionTime;
  /** Dimension values describing the environment. Dimension values always consist of "Model", "Version", "Locale", and "Orientation". - In response: always set - In create request: always set - In update request: never set */
  dimensionValue: Environment_DimensionValue[];
  /** A short human-readable name to display in the UI. Maximum of 100 characters. For example: Nexus 5, API 27. */
  displayName: string;
  environmentId: string;
  /** Merged test result for environment. If the environment has only one step (no reruns or shards), then the merged result is the same as the step result. If the environment has multiple shards and/or reruns, then the results of shards and reruns that belong to the same environment are merged into one environment result. */
  environmentResult: Environment_EnvironmentResult;
  executionId: string;
  historyId: string;
  projectId: string;
  /** The storage for test results. */
  resultsStorage: Environment_ResultsStorage;
  /** Output only. Summaries of shards. Only one shard will present unless sharding feature is enabled in TestExecutionService. */
  shardSummaries: Environment_ShardSummaries[];
}

export const Environment: DataSourceBinding<EnvironmentConfig, EnvironmentAttrs> = {
  wireType: "google_toolresults_environment",
  fields: {
    environmentId: "environment_id",
    executionId: "execution_id",
    historyId: "history_id",
    projectId: "project_id",
  },
};
