// Code generated by `ubx sdk gen` from google_apigee_deployment. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface Deployment_Errors {
  code?: number | Computed<number>;
  details?: Record<string, unknown>[] | Computed<Record<string, unknown>[]>;
  message?: string | Computed<string>;
}

export interface Deployment_Instances_DeployedRevisions {
  percentage?: number | Computed<number>;
  revision?: string | Computed<string>;
}

export interface Deployment_Instances_DeployedRoutes {
  basepath?: string | Computed<string>;
  envgroup?: string | Computed<string>;
  environment?: string | Computed<string>;
  percentage?: number | Computed<number>;
}

export interface Deployment_Instances {
  deployedRevisions?: Deployment_Instances_DeployedRevisions[] | Computed<Deployment_Instances_DeployedRevisions[]>;
  deployedRoutes?: Deployment_Instances_DeployedRoutes[] | Computed<Deployment_Instances_DeployedRoutes[]>;
  instance?: string | Computed<string>;
}

export interface Deployment_Pods {
  appVersion?: string | Computed<string>;
  deploymentStatus?: string | Computed<string>;
  deploymentStatusTime?: string | Computed<string>;
  deploymentTime?: string | Computed<string>;
  podName?: string | Computed<string>;
  podStatus?: string | Computed<string>;
  podStatusTime?: string | Computed<string>;
  statusCode?: string | Computed<string>;
  statusCodeDetails?: string | Computed<string>;
}

export interface Deployment_RouteConflicts_ConflictingDeployment {
  apiProxy?: string | Computed<string>;
  basepath?: string | Computed<string>;
  environment?: string | Computed<string>;
  revision?: string | Computed<string>;
}

export interface Deployment_RouteConflicts {
  conflictingDeployment?: Deployment_RouteConflicts_ConflictingDeployment | Computed<Deployment_RouteConflicts_ConflictingDeployment>;
  description?: string | Computed<string>;
  environmentGroup?: string | Computed<string>;
}

export interface DeploymentConfig {
  name: string | Computed<string>;
}

export interface DeploymentAttrs {
  /** API proxy. */
  apiProxy: string;
  /** Time the API proxy was marked `deployed` in the control plane in millisconds since epoch. */
  deployStartTime: string;
  /** Environment. */
  environment: string;
  /** Errors reported for this deployment. Populated only when state == ERROR. **Note**: This field is displayed only when viewing deployment status. */
  errors: Deployment_Errors[];
  /** Status reported by each runtime instance. **Note**: This field is displayed only when viewing deployment status. */
  instances: Deployment_Instances[];
  name: string;
  /** Status reported by runtime pods. **Note**: **This field is deprecated**. Runtime versions 1.3 and above report instance level status rather than pod status. */
  pods: Deployment_Pods[];
  /** Output only. The type of the deployment (standard or extensible) Deployed proxy revision will be marked as extensible in following 2 cases. 1. The deployed proxy revision uses extensible policies. 2. If a environment supports flowhooks and flow hook is configured. */
  proxyDeploymentType: string;
  /** API proxy revision. */
  revision: string;
  /** Conflicts in the desired state routing configuration. The presence of conflicts does not cause the state to be `ERROR`, but it will mean that some of the deployment's base paths are not routed to its environment. If the conflicts change, the state will transition to `PROGRESSING` until the latest configuration is rolled out to all instances. **Note**: This field is displayed only when viewing deployment status. */
  routeConflicts: Deployment_RouteConflicts[];
  /** The full resource name of Cloud IAM Service Account that this deployment is using, eg, `projects/-/serviceAccounts/{email}`. */
  serviceAccount: string;
  /** Current state of the deployment. **Note**: This field is displayed only when viewing deployment status. */
  state: string;
}

export const Deployment: DataSourceBinding<DeploymentConfig, DeploymentAttrs> = {
  wireType: "google_apigee_deployment",
  fields: {
    name: "name",
  },
};
