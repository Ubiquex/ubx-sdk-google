// Code generated by `ubx sdk gen` from google_websecurityscanner_v1beta_finding. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface V1betaFinding_Form {
  /** ! The URI where to send the form when it's submitted. */
  actionUri?: string | Computed<string>;
  /** ! The names of form fields related to the vulnerability. */
  fields?: string[] | Computed<string[]>;
}

export interface V1betaFinding_OutdatedLibrary {
  /** URLs to learn more information about the vulnerabilities in the library. */
  learnMoreUrls?: string[] | Computed<string[]>;
  /** The name of the outdated library. */
  libraryName?: string | Computed<string>;
  /** The version number. */
  version?: string | Computed<string>;
}

export interface V1betaFinding_ViolatingResource {
  /** The MIME type of this resource. */
  contentType?: string | Computed<string>;
  /** URL of this violating resource. */
  resourceUrl?: string | Computed<string>;
}

export interface V1betaFinding_VulnerableHeaders_Headers {
  name?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface V1betaFinding_VulnerableHeaders {
  /** List of vulnerable headers. */
  headers?: V1betaFinding_VulnerableHeaders_Headers[] | Computed<V1betaFinding_VulnerableHeaders_Headers[]>;
  /** List of missing headers. */
  missingHeaders?: V1betaFinding_VulnerableHeaders_Headers[] | Computed<V1betaFinding_VulnerableHeaders_Headers[]>;
}

export interface V1betaFinding_VulnerableParameters {
  /** The vulnerable parameter names. */
  parameterNames?: string[] | Computed<string[]>;
}

export interface V1betaFinding_Xss {
  /** An error message generated by a javascript breakage. */
  errorMessage?: string | Computed<string>;
  /** Stack traces leading to the point where the XSS occurred. */
  stackTraces?: string[] | Computed<string[]>;
}

export interface V1betaFinding_Xxe {
  /** Location within the request where the payload was placed. */
  payloadLocation?: string | Computed<string>;
  /** The XML string that triggered the XXE vulnerability. Non-payload values might be redacted. */
  payloadValue?: string | Computed<string>;
}

export interface V1betaFindingConfig {
  name: string | Computed<string>;
}

export interface V1betaFindingAttrs {
  /** The body of the request that triggered the vulnerability. */
  body: string;
  /** The description of the vulnerability. */
  description: string;
  /** The URL where the browser lands when the vulnerability is detected. */
  finalUrl: string;
  /** The type of the Finding. Detailed and up-to-date information on findings can be found here: https://cloud.google.com/security-command-center/docs/how-to-remediate-web-security-scanner */
  findingType: string;
  /** ! Information about a vulnerability with an HTML. */
  form: V1betaFinding_Form;
  /** If the vulnerability was originated from nested IFrame, the immediate parent IFrame is reported. */
  frameUrl: string;
  /** The URL produced by the server-side fuzzer and used in the request that triggered the vulnerability. */
  fuzzedUrl: string;
  /** The http method of the request that triggered the vulnerability, in uppercase. */
  httpMethod: string;
  name: string;
  /** Information reported for an outdated library. */
  outdatedLibrary: V1betaFinding_OutdatedLibrary;
  /** The URL containing human-readable payload that user can leverage to reproduce the vulnerability. */
  reproductionUrl: string;
  /** The severity level of the reported vulnerability. */
  severity: string;
  /** The tracking ID uniquely identifies a vulnerability instance across multiple ScanRuns. */
  trackingId: string;
  /** Information regarding any resource causing the vulnerability such as JavaScript sources, image, audio files, etc. */
  violatingResource: V1betaFinding_ViolatingResource;
  /** Information about vulnerable or missing HTTP Headers. */
  vulnerableHeaders: V1betaFinding_VulnerableHeaders;
  /** Information about vulnerable request parameters. */
  vulnerableParameters: V1betaFinding_VulnerableParameters;
  /** Information reported for an XSS. */
  xss: V1betaFinding_Xss;
  /** Information reported for an XXE. */
  xxe: V1betaFinding_Xxe;
}

export const V1betaFinding: DataSourceBinding<V1betaFindingConfig, V1betaFindingAttrs> = {
  wireType: "google_websecurityscanner_v1beta_finding",
  fields: {
    name: "name",
  },
};
