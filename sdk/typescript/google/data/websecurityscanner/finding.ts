// Code generated by `ubx sdk gen` from google_websecurityscanner_finding. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface Finding_Form {
  /** ! The URI where to send the form when it's submitted. */
  actionUri?: string | Computed<string>;
  /** ! The names of form fields related to the vulnerability. */
  fields?: string[] | Computed<string[]>;
}

export interface Finding_OutdatedLibrary {
  /** URLs to learn more information about the vulnerabilities in the library. */
  learnMoreUrls?: string[] | Computed<string[]>;
  /** The name of the outdated library. */
  libraryName?: string | Computed<string>;
  /** The version number. */
  version?: string | Computed<string>;
}

export interface Finding_ViolatingResource {
  /** The MIME type of this resource. */
  contentType?: string | Computed<string>;
  /** URL of this violating resource. */
  resourceUrl?: string | Computed<string>;
}

export interface Finding_VulnerableHeaders_Headers {
  name?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface Finding_VulnerableHeaders {
  /** List of vulnerable headers. */
  headers?: Finding_VulnerableHeaders_Headers[] | Computed<Finding_VulnerableHeaders_Headers[]>;
  /** List of missing headers. */
  missingHeaders?: Finding_VulnerableHeaders_Headers[] | Computed<Finding_VulnerableHeaders_Headers[]>;
}

export interface Finding_VulnerableParameters {
  /** The vulnerable parameter names. */
  parameterNames?: string[] | Computed<string[]>;
}

export interface Finding_Xss {
  /** The attack vector of the payload triggering this XSS. */
  attackVector?: string | Computed<string>;
  /** An error message generated by a javascript breakage. */
  errorMessage?: string | Computed<string>;
  /** Stack traces leading to the point where the XSS occurred. */
  stackTraces?: string[] | Computed<string[]>;
  /** The reproduction url for the seeding POST request of a Stored XSS. */
  storedXssSeedingUrl?: string | Computed<string>;
}

export interface Finding_Xxe {
  /** Location within the request where the payload was placed. */
  payloadLocation?: string | Computed<string>;
  /** The XML string that triggered the XXE vulnerability. Non-payload values might be redacted. */
  payloadValue?: string | Computed<string>;
}

export interface FindingConfig {
  name: string | Computed<string>;
}

export interface FindingAttrs {
  /** Output only. The body of the request that triggered the vulnerability. */
  body: string;
  /** Output only. The description of the vulnerability. */
  description: string;
  /** Output only. The URL where the browser lands when the vulnerability is detected. */
  finalUrl: string;
  /** Output only. The type of the Finding. Detailed and up-to-date information on findings can be found here: https://cloud.google.com/security-command-center/docs/how-to-remediate-web-security-scanner-findings */
  findingType: string;
  /** ! Information about a vulnerability with an HTML. */
  form: Finding_Form;
  /** Output only. If the vulnerability was originated from nested IFrame, the immediate parent IFrame is reported. */
  frameUrl: string;
  /** Output only. The URL produced by the server-side fuzzer and used in the request that triggered the vulnerability. */
  fuzzedUrl: string;
  /** Output only. The http method of the request that triggered the vulnerability, in uppercase. */
  httpMethod: string;
  name: string;
  /** Information reported for an outdated library. */
  outdatedLibrary: Finding_OutdatedLibrary;
  /** Output only. The URL containing human-readable payload that user can leverage to reproduce the vulnerability. */
  reproductionUrl: string;
  /** Output only. The severity level of the reported vulnerability. */
  severity: string;
  /** Output only. The tracking ID uniquely identifies a vulnerability instance across multiple ScanRuns. */
  trackingId: string;
  /** Information regarding any resource causing the vulnerability such as JavaScript sources, image, audio files, etc. */
  violatingResource: Finding_ViolatingResource;
  /** Information about vulnerable or missing HTTP Headers. */
  vulnerableHeaders: Finding_VulnerableHeaders;
  /** Information about vulnerable request parameters. */
  vulnerableParameters: Finding_VulnerableParameters;
  /** Information reported for an XSS. */
  xss: Finding_Xss;
  /** Information reported for an XXE. */
  xxe: Finding_Xxe;
}

export const Finding: DataSourceBinding<FindingConfig, FindingAttrs> = {
  wireType: "google_websecurityscanner_finding",
  fields: {
    name: "name",
  },
};
