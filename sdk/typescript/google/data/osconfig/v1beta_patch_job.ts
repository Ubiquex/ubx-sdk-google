// Code generated by `ubx sdk gen` from google_osconfig_v1beta_patch_job. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface V1betaPatchJob_InstanceDetailsSummary {
  /** Number of instances that have acked and will start shortly. */
  ackedInstanceCount?: string | Computed<string>;
  /** Number of instances that are applying patches. */
  applyingPatchesInstanceCount?: string | Computed<string>;
  /** Number of instances that are downloading patches. */
  downloadingPatchesInstanceCount?: string | Computed<string>;
  /** Number of instances that failed. */
  failedInstanceCount?: string | Computed<string>;
  /** Number of instances that are inactive. */
  inactiveInstanceCount?: string | Computed<string>;
  /** Number of instances that do not appear to be running the agent. Check to ensure that the agent is installed, running, and able to communicate with the service. */
  noAgentDetectedInstanceCount?: string | Computed<string>;
  /** Number of instances notified about patch job. */
  notifiedInstanceCount?: string | Computed<string>;
  /** Number of instances pending patch job. */
  pendingInstanceCount?: string | Computed<string>;
  /** Number of instances that are running the post-patch step. */
  postPatchStepInstanceCount?: string | Computed<string>;
  /** Number of instances that are running the pre-patch step. */
  prePatchStepInstanceCount?: string | Computed<string>;
  /** Number of instances rebooting. */
  rebootingInstanceCount?: string | Computed<string>;
  /** Number of instances that were skipped during patching. */
  skippedInstanceCount?: string | Computed<string>;
  /** Number of instances that have started. */
  startedInstanceCount?: string | Computed<string>;
  /** Number of instances that have completed successfully. */
  succeededInstanceCount?: string | Computed<string>;
  /** Number of instances that require reboot. */
  succeededRebootRequiredInstanceCount?: string | Computed<string>;
  /** Number of instances that exceeded the time out while applying the patch. */
  timedOutInstanceCount?: string | Computed<string>;
}

export interface V1betaPatchJob_InstanceFilter_GroupLabels {
  labels?: Record<string, string> | Computed<Record<string, string>>;
}

export interface V1betaPatchJob_InstanceFilter {
  /** Target all VM instances in the project. If true, no other criteria is permitted. */
  all?: boolean | Computed<boolean>;
  /** Targets VM instances matching at least one of these label sets. This allows targeting of disparate groups, for example "env=prod or env=staging". */
  groupLabels?: V1betaPatchJob_InstanceFilter_GroupLabels[] | Computed<V1betaPatchJob_InstanceFilter_GroupLabels[]>;
  /** Targets VMs whose name starts with one of these prefixes. Similar to labels, this is another way to group VMs when targeting configs, for example prefix="prod-". */
  instanceNamePrefixes?: string[] | Computed<string[]>;
  /** Targets any of the VM instances specified. Instances are specified by their URI in the form `zones/[ZONE]/instances/[INSTANCE_NAME]`, `projects/[PROJECT_ID]/zones/[ZONE]/instances/[INSTANCE_NAME]`, or `https://www.googleapis.com/compute/v1/projects/[PROJECT_ID]/zones/[ZONE]/instances/[INSTANCE_NAME]` */
  instances?: string[] | Computed<string[]>;
  /** Targets VM instances in ANY of these zones. Leave empty to target VM instances in any zone. */
  zones?: string[] | Computed<string[]>;
}

export interface V1betaPatchJob_PatchConfig_Apt {
  /** List of packages to exclude from update. These packages will be excluded */
  excludes?: string[] | Computed<string[]>;
  /** An exclusive list of packages to be updated. These are the only packages that will be updated. If these packages are not installed, they will be ignored. This field cannot be specified with any other patch configuration fields. */
  exclusivePackages?: string[] | Computed<string[]>;
  /** By changing the type to DIST, the patching is performed using `apt-get dist-upgrade` instead. */
  type?: string | Computed<string>;
}

export interface V1betaPatchJob_PatchConfig_PostStep_LinuxExecStepConfig_GcsObject {
  /** Required. Bucket of the Google Cloud Storage object. */
  bucket?: string | Computed<string>;
  /** Required. Generation number of the Google Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change. */
  generationNumber?: string | Computed<string>;
  /** Required. Name of the Google Cloud Storage object. */
  object?: string | Computed<string>;
}

export interface V1betaPatchJob_PatchConfig_PostStep_LinuxExecStepConfig {
  /** Defaults to [0]. A list of possible return values that the execution can return to indicate a success. */
  allowedSuccessCodes?: number[] | Computed<number[]>;
  /** Google Cloud Storage object representation. */
  gcsObject?: V1betaPatchJob_PatchConfig_PostStep_LinuxExecStepConfig_GcsObject | Computed<V1betaPatchJob_PatchConfig_PostStep_LinuxExecStepConfig_GcsObject>;
  /** The script interpreter to use to run the script. If no interpreter is specified the script will be executed directly, which will likely only succeed for scripts with [shebang lines] (https://en.wikipedia.org/wiki/Shebang_\(Unix\)). */
  interpreter?: string | Computed<string>;
  /** An absolute path to the executable on the VM. */
  localPath?: string | Computed<string>;
}

export interface V1betaPatchJob_PatchConfig_PostStep {
  /** Common configurations for an ExecStep. */
  linuxExecStepConfig?: V1betaPatchJob_PatchConfig_PostStep_LinuxExecStepConfig | Computed<V1betaPatchJob_PatchConfig_PostStep_LinuxExecStepConfig>;
  /** Common configurations for an ExecStep. */
  windowsExecStepConfig?: V1betaPatchJob_PatchConfig_PostStep_LinuxExecStepConfig | Computed<V1betaPatchJob_PatchConfig_PostStep_LinuxExecStepConfig>;
}

export interface V1betaPatchJob_PatchConfig_WindowsUpdate {
  /** Only apply updates of these windows update classifications. If empty, all updates are applied. */
  classifications?: string[] | Computed<string[]>;
  /** List of KBs to exclude from update. */
  excludes?: string[] | Computed<string[]>;
  /** An exclusive list of kbs to be updated. These are the only patches that will be updated. This field must not be used with other patch configurations. */
  exclusivePatches?: string[] | Computed<string[]>;
}

export interface V1betaPatchJob_PatchConfig_Yum {
  /** List of packages to exclude from update. These packages are excluded by using the yum `--exclude` flag. */
  excludes?: string[] | Computed<string[]>;
  /** An exclusive list of packages to be updated. These are the only packages that will be updated. If these packages are not installed, they will be ignored. This field must not be specified with any other patch configuration fields. */
  exclusivePackages?: string[] | Computed<string[]>;
  /** Will cause patch to run `yum update-minimal` instead. */
  minimal?: boolean | Computed<boolean>;
  /** Adds the `--security` flag to `yum update`. Not supported on all platforms. */
  security?: boolean | Computed<boolean>;
}

export interface V1betaPatchJob_PatchConfig_Zypper {
  /** Install only patches with these categories. Common categories include security, recommended, and feature. */
  categories?: string[] | Computed<string[]>;
  /** List of patches to exclude from update. */
  excludes?: string[] | Computed<string[]>;
  /** An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command. This field must not be used with any other patch configuration fields. */
  exclusivePatches?: string[] | Computed<string[]>;
  /** Install only patches with these severities. Common severities include critical, important, moderate, and low. */
  severities?: string[] | Computed<string[]>;
  /** Adds the `--with-optional` flag to `zypper patch`. */
  withOptional?: boolean | Computed<boolean>;
  /** Adds the `--with-update` flag, to `zypper patch`. */
  withUpdate?: boolean | Computed<boolean>;
}

export interface V1betaPatchJob_PatchConfig {
  /** Apt patching is completed by executing `apt-get update && apt-get upgrade`. Additional options can be set to control how this is executed. */
  apt?: V1betaPatchJob_PatchConfig_Apt | Computed<V1betaPatchJob_PatchConfig_Apt>;
  /** Googet patching is performed by running `googet update`. */
  goo?: unknown | Computed<unknown>;
  /** Allows the patch job to run on Managed instance groups (MIGs). */
  migInstancesAllowed?: boolean | Computed<boolean>;
  /** A step that runs an executable for a PatchJob. */
  postStep?: V1betaPatchJob_PatchConfig_PostStep | Computed<V1betaPatchJob_PatchConfig_PostStep>;
  /** A step that runs an executable for a PatchJob. */
  preStep?: V1betaPatchJob_PatchConfig_PostStep | Computed<V1betaPatchJob_PatchConfig_PostStep>;
  /** Post-patch reboot settings. */
  rebootConfig?: string | Computed<string>;
  /** Optional. Enables enhanced reporting for the patch job: 1. The patch job skips instances that cannot be patched and reports them as `SKIPPED`. An instance cannot be patched for two reasons: 1. The instance runs Container-Optimized OS (COS), which cannot be patched. 2. The instance is part of a managed instance group (MIG), and patching MIG instances is disabled in the patch job's configuration (PatchConfig.migInstancesAllowed is `false`). 2. The patch job is reported as `SUCCEEDED` if it completes without errors, even if some instances are `SKIPPED`. 3. The patch job is reported as `COMPLETED_WITH_INACTIVE_VMS` if it completes without errors, but does not patch instances that are `INACTIVE`. */
  skipUnpatchableVms?: boolean | Computed<boolean>;
  /** Windows patching is performed using the Windows Update Agent. */
  windowsUpdate?: V1betaPatchJob_PatchConfig_WindowsUpdate | Computed<V1betaPatchJob_PatchConfig_WindowsUpdate>;
  /** Yum patching is performed by executing `yum update`. Additional options can be set to control how this is executed. Note that not all settings are supported on all platforms. */
  yum?: V1betaPatchJob_PatchConfig_Yum | Computed<V1betaPatchJob_PatchConfig_Yum>;
  /** Zypper patching is performed by running `zypper patch`. See also https://en.opensuse.org/SDB:Zypper_manual. */
  zypper?: V1betaPatchJob_PatchConfig_Zypper | Computed<V1betaPatchJob_PatchConfig_Zypper>;
}

export interface V1betaPatchJob_Rollout_DisruptionBudget {
  /** Specifies a fixed value. */
  fixed?: number | Computed<number>;
  /** Specifies the relative value defined as a percentage, which will be multiplied by a reference value. */
  percent?: number | Computed<number>;
}

export interface V1betaPatchJob_Rollout {
  /** Message encapsulating a value that can be either absolute ("fixed") or relative ("percent") to a value. */
  disruptionBudget?: V1betaPatchJob_Rollout_DisruptionBudget | Computed<V1betaPatchJob_Rollout_DisruptionBudget>;
  /** Mode of the patch rollout. */
  mode?: string | Computed<string>;
}

export interface V1betaPatchJobConfig {
  name: string | Computed<string>;
}

export interface V1betaPatchJobAttrs {
  /** Time this patch job was created. */
  createTime: string;
  /** Description of the patch job. Length of the description is limited to 1024 characters. */
  description: string;
  /** Display name for this patch job. This is not a unique identifier. */
  displayName: string;
  /** If this patch job is a dry run, the agent reports that it has finished without running any updates on the VM instance. */
  dryRun: boolean;
  /** Duration of the patch job. After the duration ends, the patch job times out. */
  duration: string;
  /** If this patch job failed, this message provides information about the failure. */
  errorMessage: string;
  /** A summary of the current patch state across all instances that this patch job affects. Contains counts of instances in different states. These states map to `InstancePatchState`. List patch job instance details to see the specific states of each instance. */
  instanceDetailsSummary: V1betaPatchJob_InstanceDetailsSummary;
  /** A filter to target VM instances for patching. The targeted VMs must meet all criteria specified. So if both labels and zones are specified, the patch job targets only VMs with those labels and in those zones. */
  instanceFilter: V1betaPatchJob_InstanceFilter;
  name: string;
  /** Patch configuration specifications. Contains details on how to apply the patch(es) to a VM instance. */
  patchConfig: V1betaPatchJob_PatchConfig;
  /** Output only. Name of the patch deployment that created this patch job. */
  patchDeployment: string;
  /** Reflects the overall progress of the patch job in the range of 0.0 being no progress to 100.0 being complete. */
  percentComplete: number;
  /** Patch rollout configuration specifications. Contains details on the concurrency control when applying patch(es) to all targeted VMs. */
  rollout: V1betaPatchJob_Rollout;
  /** The current state of the PatchJob. */
  state: string;
  /** Last time this patch job was updated. */
  updateTime: string;
}

export const V1betaPatchJob: DataSourceBinding<V1betaPatchJobConfig, V1betaPatchJobAttrs> = {
  wireType: "google_osconfig_v1beta_patch_job",
  fields: {
    name: "name",
  },
};
