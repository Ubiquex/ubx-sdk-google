// Code generated by `ubx sdk gen` from google_apihub_addon. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface Addon_Config_AllDataAddonConfig {
  /** Required. If true, the addon is enabled for all data in the API hub. */
  enabled?: boolean | Computed<boolean>;
}

export interface Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig_EnvironmentFilter {
  allEnvironments?: boolean | Computed<boolean>;
  environments?: string[] | Computed<string[]>;
}

export interface Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig {
  environmentFilter?: Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig_EnvironmentFilter | Computed<Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig_EnvironmentFilter>;
}

export interface Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs {
  apigeeEdgeConfig?: Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig | Computed<Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig>;
  apigeeOpdkConfig?: Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig | Computed<Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig>;
  apigeeXhybridConfig?: Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig | Computed<Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs_ApigeeEdgeConfig>;
  pluginInstance?: string | Computed<string>;
}

export interface Addon_Config_GatewayPluginAddonConfig {
  /** Required. The list of gateway plugin configs for which the addon is enabled. Each gateway plugin config should have a unique plugin instance. */
  gatewayPluginConfigs?: Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs[] | Computed<Addon_Config_GatewayPluginAddonConfig_GatewayPluginConfigs[]>;
}

export interface Addon_Config {
  /** Configuration for addons which act on all data in the API hub. This is used to specify if the addon is enabled for all data in the API hub. */
  allDataAddonConfig?: Addon_Config_AllDataAddonConfig | Computed<Addon_Config_AllDataAddonConfig>;
  /** Configuration for gateway plugin addons. This is used to specify the list of gateway plugin configs for which the addon is enabled. */
  gatewayPluginAddonConfig?: Addon_Config_GatewayPluginAddonConfig | Computed<Addon_Config_GatewayPluginAddonConfig>;
}

export interface AddonConfig {
  name: string | Computed<string>;
}

export interface AddonAttrs {
  /** Output only. The Vertex AI region where the BoostSpec Gemini model calls run for this API Hub instance. Populated only for the SpecGen addon (`system-spec-generation`); other addons leave this field empty. `gemini-2.5-flash` is not available in every API Hub region, so the effective region may differ from the API Hub instance's own region. The value follows these semantics: - `""`: BoostSpec is disabled in this region (the addon is not SpecGen, or the API Hub instance region has no configured Gemini endpoint or fallback). - Equal to the API Hub instance region: BoostSpec calls run in-region. - Differs from the API Hub instance region: BoostSpec calls run in the specified fallback region. Callers rendering this field can derive the three display states from this single field combined with the API Hub instance region. */
  boostSpecGeminiRegionId: string;
  /** Configuration for the addon. */
  config: Addon_Config;
  /** Output only. The time at which the addon was created. */
  createTime: string;
  /** Required. The data source on which the addon operates. This determines which field in the `config` oneof is used. */
  dataSource: string;
  /** Optional. The description of the addon. */
  description: string;
  /** Required. The display name of the addon. */
  displayName: string;
  name: string;
  /** Output only. The state of the addon. */
  state: string;
  /** Output only. The time at which the addon was last updated. */
  updateTime: string;
}

export const Addon: DataSourceBinding<AddonConfig, AddonAttrs> = {
  wireType: "google_apihub_addon",
  fields: {
    name: "name",
  },
};
