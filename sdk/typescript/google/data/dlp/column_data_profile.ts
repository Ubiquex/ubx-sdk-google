// Code generated by `ubx sdk gen` from google_dlp_column_data_profile. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface ColumnDataProfile_ColumnInfoType_InfoType_SensitivityScore {
  /** The sensitivity score applied to the resource. */
  score?: string | Computed<string>;
}

export interface ColumnDataProfile_ColumnInfoType_InfoType {
  /** Name of the information type. Either a name of your choosing when creating a CustomInfoType, or one of the names listed at https://docs.cloud.google.com/sensitive-data-protection/docs/infotypes-reference when specifying a built-in type. When sending Cloud DLP results to Data Catalog, infoType names should conform to the pattern `[A-Za-z0-9$_-]{1,64}`. */
  name?: string | Computed<string>;
  /** Score is calculated from of all elements in the data profile. A higher level means the data is more sensitive. */
  sensitivityScore?: ColumnDataProfile_ColumnInfoType_InfoType_SensitivityScore | Computed<ColumnDataProfile_ColumnInfoType_InfoType_SensitivityScore>;
  /** Optional version name for this InfoType. */
  version?: string | Computed<string>;
}

export interface ColumnDataProfile_ColumnInfoType {
  /** Not populated for predicted infotypes. */
  estimatedPrevalence?: number | Computed<number>;
  /** Type of information detected by the API. */
  infoType?: ColumnDataProfile_ColumnInfoType_InfoType | Computed<ColumnDataProfile_ColumnInfoType_InfoType>;
}

export interface ColumnDataProfile_OtherMatches {
  estimatedPrevalence?: number | Computed<number>;
  excludedFromAnalysis?: boolean | Computed<boolean>;
  infoType?: ColumnDataProfile_ColumnInfoType_InfoType | Computed<ColumnDataProfile_ColumnInfoType_InfoType>;
}

export interface ColumnDataProfile_ProfileStatus_Status {
  /** The status code, which should be an enum value of google.rpc.Code. */
  code?: number | Computed<number>;
  /** A list of messages that carry the error details. There is a common set of message types for APIs to use. */
  details?: Record<string, unknown>[] | Computed<Record<string, unknown>[]>;
  /** A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client. */
  message?: string | Computed<string>;
}

export interface ColumnDataProfile_ProfileStatus {
  /** The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors). */
  status?: ColumnDataProfile_ProfileStatus_Status | Computed<ColumnDataProfile_ProfileStatus_Status>;
  /** Time when the profile generation status was updated */
  timestamp?: string | Computed<string>;
}

export interface ColumnDataProfileConfig {
  name: string | Computed<string>;
}

export interface ColumnDataProfileAttrs {
  /** The name of the column. */
  column: string;
  /** The infoType details for this column. */
  columnInfoType: ColumnDataProfile_ColumnInfoType;
  /** The data type of a given column. */
  columnType: string;
  /** Score is a summary of all elements in the data profile. A higher number means more risk. */
  dataRiskLevel: ColumnDataProfile_ColumnInfoType_InfoType_SensitivityScore;
  /** The BigQuery dataset ID, if the resource profiled is a BigQuery table. */
  datasetId: string;
  /** If supported, the location where the dataset's data is stored. See https://docs.cloud.google.com/bigquery/docs/locations for supported BigQuery locations. */
  datasetLocation: string;
  /** The Google Cloud project ID that owns the profiled resource. */
  datasetProjectId: string;
  /** Approximate percentage of entries being null in the column. */
  estimatedNullPercentage: string;
  /** Approximate uniqueness of the column. */
  estimatedUniquenessScore: string;
  /** The likelihood that this column contains free-form text. A value close to 1 may indicate the column is likely to contain free-form or natural language text. Range in 0-1. */
  freeTextScore: number;
  name: string;
  /** Other types found within this column. List will be unordered. */
  otherMatches: ColumnDataProfile_OtherMatches[];
  /** Indicates if a policy tag has been applied to the column. */
  policyState: string;
  /** The last time the profile was generated. */
  profileLastGenerated: string;
  /** Success or errors for the profile generation. */
  profileStatus: ColumnDataProfile_ProfileStatus;
  /** Score is calculated from of all elements in the data profile. A higher level means the data is more sensitive. */
  sensitivityScore: ColumnDataProfile_ColumnInfoType_InfoType_SensitivityScore;
  /** State of a profile. */
  state: string;
  /** The resource name of the table data profile. */
  tableDataProfile: string;
  /** The resource name of the resource this column is within. */
  tableFullResource: string;
  /** The table ID. */
  tableId: string;
}

export const ColumnDataProfile: DataSourceBinding<ColumnDataProfileConfig, ColumnDataProfileAttrs> = {
  wireType: "google_dlp_column_data_profile",
  fields: {
    name: "name",
  },
};
