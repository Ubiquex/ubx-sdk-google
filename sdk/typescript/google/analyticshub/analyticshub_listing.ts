// Code generated by `ubx sdk gen` from google_analyticshub_analyticshub_listing. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface AnalyticshubListing_BigqueryDataset_EffectiveReplicas {
  location?: string | Computed<string>;
  primaryState?: string | Computed<string>;
  replicaState?: string | Computed<string>;
}

export interface AnalyticshubListing_BigqueryDataset_RestrictedExportPolicy {
  /** Optional. If true, enable restricted export. */
  enabled?: boolean | Computed<boolean>;
  /** Optional. If true, restrict direct table access (read api/tabledata.list) on linked table. */
  restrictDirectTableAccess?: boolean | Computed<boolean>;
  /** Optional. If true, restrict export of query result derived from restricted linked dataset table. */
  restrictQueryResult?: boolean | Computed<boolean>;
}

export interface AnalyticshubListing_BigqueryDataset_SelectedResources {
  routine?: string | Computed<string>;
  table?: string | Computed<string>;
}

export interface AnalyticshubListing_BigqueryDataset {
  /** Optional. Resource name of the dataset source for this listing. e.g. `projects/myproject/datasets/123` */
  dataset?: string | Computed<string>;
  /** Output only. Server-owned effective state of replicas. Contains both primary and secondary replicas. Each replica includes a system-computed (output-only) state and primary designation. */
  effectiveReplicas?: AnalyticshubListing_BigqueryDataset_EffectiveReplicas[] | Computed<AnalyticshubListing_BigqueryDataset_EffectiveReplicas[]>;
  /** Optional. A list of regions where the publisher has created shared dataset replicas. */
  replicaLocations?: string[] | Computed<string[]>;
  /** Restricted export policy used to configure restricted export on linked dataset. */
  restrictedExportPolicy?: AnalyticshubListing_BigqueryDataset_RestrictedExportPolicy | Computed<AnalyticshubListing_BigqueryDataset_RestrictedExportPolicy>;
  /** Optional. Resource in this dataset that is selectively shared. This field is required for data clean room exchanges. */
  selectedResources?: AnalyticshubListing_BigqueryDataset_SelectedResources[] | Computed<AnalyticshubListing_BigqueryDataset_SelectedResources[]>;
}

export interface AnalyticshubListing_CommercialInfo_CloudMarketplace {
  /** Output only. Commercial state of the Marketplace Data Product. */
  commercialState?: string | Computed<string>;
  /** Output only. Resource name of the commercial service associated with the Marketplace Data Product. e.g. example.com */
  service?: string | Computed<string>;
}

export interface AnalyticshubListing_CommercialInfo {
  /** Specifies the details of the Marketplace Data Product associated with the Listing. */
  cloudMarketplace?: AnalyticshubListing_CommercialInfo_CloudMarketplace | Computed<AnalyticshubListing_CommercialInfo_CloudMarketplace>;
}

export interface AnalyticshubListing_DataProvider {
  /** Optional. Name of the data provider. */
  name?: string | Computed<string>;
  /** Optional. Email or URL of the data provider. Max Length: 1000 bytes. */
  primaryContact?: string | Computed<string>;
}

export interface AnalyticshubListing_PubsubTopic {
  /** Optional. Region hint on where the data might be published. Data affinity regions are modifiable. See https://cloud.google.com/about/locations for full listing of possible Cloud regions. */
  dataAffinityRegions?: string[] | Computed<string[]>;
  /** Required. Resource name of the Pub/Sub topic source for this listing. e.g. projects/myproject/topics/topicId */
  topic?: string | Computed<string>;
}

export interface AnalyticshubListing_StoredProcedureConfig {
  /** Output only. Types of stored procedure supported to share. */
  allowedStoredProcedureTypes?: string[] | Computed<string[]>;
  /** Optional. If true, enable sharing of stored procedure. */
  enabled?: boolean | Computed<boolean>;
}

const AnalyticshubListing_BigqueryDataset_EffectiveReplicasFields: FieldMap = {
  location: "location",
  primaryState: "primary_state",
  replicaState: "replica_state",
};

const AnalyticshubListing_BigqueryDataset_RestrictedExportPolicyFields: FieldMap = {
  enabled: "enabled",
  restrictDirectTableAccess: "restrict_direct_table_access",
  restrictQueryResult: "restrict_query_result",
};

const AnalyticshubListing_BigqueryDataset_SelectedResourcesFields: FieldMap = {
  routine: "routine",
  table: "table",
};

const AnalyticshubListing_BigqueryDatasetFields: FieldMap = {
  dataset: "dataset",
  effectiveReplicas: {
    wireName: "effective_replicas",
    kind: "list",
    fields: AnalyticshubListing_BigqueryDataset_EffectiveReplicasFields,
  },
  replicaLocations: "replica_locations",
  restrictedExportPolicy: {
    wireName: "restricted_export_policy",
    kind: "object",
    fields: AnalyticshubListing_BigqueryDataset_RestrictedExportPolicyFields,
  },
  selectedResources: {
    wireName: "selected_resources",
    kind: "list",
    fields: AnalyticshubListing_BigqueryDataset_SelectedResourcesFields,
  },
};

const AnalyticshubListing_CommercialInfo_CloudMarketplaceFields: FieldMap = {
  commercialState: "commercial_state",
  service: "service",
};

const AnalyticshubListing_CommercialInfoFields: FieldMap = {
  cloudMarketplace: {
    wireName: "cloud_marketplace",
    kind: "object",
    fields: AnalyticshubListing_CommercialInfo_CloudMarketplaceFields,
  },
};

const AnalyticshubListing_DataProviderFields: FieldMap = {
  name: "name",
  primaryContact: "primary_contact",
};

const AnalyticshubListing_PubsubTopicFields: FieldMap = {
  dataAffinityRegions: "data_affinity_regions",
  topic: "topic",
};

const AnalyticshubListing_StoredProcedureConfigFields: FieldMap = {
  allowedStoredProcedureTypes: "allowed_stored_procedure_types",
  enabled: "enabled",
};

export interface AnalyticshubListingConfig {
  /** Optional. If true, the listing is only available to get the resource metadata. Listing is non subscribable. */
  allowOnlyMetadataSharing?: boolean | Computed<boolean>;
  /** A reference to a shared dataset. It is an existing BigQuery dataset with a collection of objects such as tables and views that you want to share with subscribers. When subscriber's subscribe to a listing, Analytics Hub creates a linked dataset in the subscriber's project. A Linked dataset is an opaque, read-only BigQuery dataset that serves as a _symbolic link_ to a shared dataset. */
  bigqueryDataset?: AnalyticshubListing_BigqueryDataset | Computed<AnalyticshubListing_BigqueryDataset>;
  /** Optional. Categories of the listing. Up to five categories are allowed. */
  categories?: string[] | Computed<string[]>;
  /** Commercial info contains the information about the commercial data products associated with the listing. */
  commercialInfo?: AnalyticshubListing_CommercialInfo | Computed<AnalyticshubListing_CommercialInfo>;
  /** Contains details of the data provider. */
  dataProvider?: AnalyticshubListing_DataProvider | Computed<AnalyticshubListing_DataProvider>;
  /** Optional. Short description of the listing. The description must not contain Unicode non-characters and C0 and C1 control codes except tabs (HT), new lines (LF), carriage returns (CR), and page breaks (FF). Default value is an empty string. Max length: 2000 bytes. */
  description?: string | Computed<string>;
  /** Optional. Type of discovery of the listing on the discovery page. */
  discoveryType?: string | Computed<string>;
  /** Required. Human-readable display name of the listing. The display name must contain only Unicode letters, numbers (0-9), underscores (_), dashes (-), spaces ( ), ampersands (&) and can't start or end with spaces. Default value is an empty string. Max length: 63 bytes. */
  displayName?: string | Computed<string>;
  /** Optional. Documentation describing the listing. */
  documentation?: string | Computed<string>;
  /** Optional. Base64 encoded image representing the listing. Max Size: 3.0MiB Expected image dimensions are 512x512 pixels, however the API only performs validation on size of the encoded data. Note: For byte fields, the contents of the field are base64-encoded (which increases the size of the data by 33-36%) when using JSON on the wire. */
  icon?: string | Computed<string>;
  /** Optional. By default, false. If true, the Listing has an email sharing mandate enabled. */
  logLinkedDatasetQueryUserEmail?: boolean | Computed<boolean>;
  /** Output only. The resource name of the listing. e.g. `projects/myproject/locations/us/dataExchanges/123/listings/456` */
  name?: string | Computed<string>;
  /** Optional. Email or URL of the primary point of contact of the listing. Max Length: 1000 bytes. */
  primaryContact?: string | Computed<string>;
  /** Contains details of the listing publisher. */
  publisher?: AnalyticshubListing_DataProvider | Computed<AnalyticshubListing_DataProvider>;
  /** Pub/Sub topic source. */
  pubsubTopic?: AnalyticshubListing_PubsubTopic | Computed<AnalyticshubListing_PubsubTopic>;
  /** Optional. Email or URL of the request access of the listing. Subscribers can use this reference to request access. Max Length: 1000 bytes. */
  requestAccess?: string | Computed<string>;
  /** Output only. Listing shared asset type. */
  resourceType?: string | Computed<string>;
  /** Restricted export config, used to configure restricted export on linked dataset. */
  restrictedExportConfig?: AnalyticshubListing_BigqueryDataset_RestrictedExportPolicy | Computed<AnalyticshubListing_BigqueryDataset_RestrictedExportPolicy>;
  /** Output only. Current state of the listing. */
  state?: string | Computed<string>;
  /** Stored procedure configuration, used to configure stored procedure sharing on linked dataset. */
  storedProcedureConfig?: AnalyticshubListing_StoredProcedureConfig | Computed<AnalyticshubListing_StoredProcedureConfig>;
}

export interface AnalyticshubListingAttrs {
  /** Optional. If true, the listing is only available to get the resource metadata. Listing is non subscribable. */
  allowOnlyMetadataSharing: boolean;
  /** A reference to a shared dataset. It is an existing BigQuery dataset with a collection of objects such as tables and views that you want to share with subscribers. When subscriber's subscribe to a listing, Analytics Hub creates a linked dataset in the subscriber's project. A Linked dataset is an opaque, read-only BigQuery dataset that serves as a _symbolic link_ to a shared dataset. */
  bigqueryDataset: AnalyticshubListing_BigqueryDataset;
  /** Optional. Categories of the listing. Up to five categories are allowed. */
  categories: string[];
  /** Commercial info contains the information about the commercial data products associated with the listing. */
  commercialInfo: AnalyticshubListing_CommercialInfo;
  /** Contains details of the data provider. */
  dataProvider: AnalyticshubListing_DataProvider;
  /** Optional. Short description of the listing. The description must not contain Unicode non-characters and C0 and C1 control codes except tabs (HT), new lines (LF), carriage returns (CR), and page breaks (FF). Default value is an empty string. Max length: 2000 bytes. */
  description: string;
  /** Optional. Type of discovery of the listing on the discovery page. */
  discoveryType: string;
  /** Required. Human-readable display name of the listing. The display name must contain only Unicode letters, numbers (0-9), underscores (_), dashes (-), spaces ( ), ampersands (&) and can't start or end with spaces. Default value is an empty string. Max length: 63 bytes. */
  displayName: string;
  /** Optional. Documentation describing the listing. */
  documentation: string;
  /** Optional. Base64 encoded image representing the listing. Max Size: 3.0MiB Expected image dimensions are 512x512 pixels, however the API only performs validation on size of the encoded data. Note: For byte fields, the contents of the field are base64-encoded (which increases the size of the data by 33-36%) when using JSON on the wire. */
  icon: string;
  /** Optional. By default, false. If true, the Listing has an email sharing mandate enabled. */
  logLinkedDatasetQueryUserEmail: boolean;
  /** Output only. The resource name of the listing. e.g. `projects/myproject/locations/us/dataExchanges/123/listings/456` */
  name: string;
  /** Optional. Email or URL of the primary point of contact of the listing. Max Length: 1000 bytes. */
  primaryContact: string;
  /** Contains details of the listing publisher. */
  publisher: AnalyticshubListing_DataProvider;
  /** Pub/Sub topic source. */
  pubsubTopic: AnalyticshubListing_PubsubTopic;
  /** Optional. Email or URL of the request access of the listing. Subscribers can use this reference to request access. Max Length: 1000 bytes. */
  requestAccess: string;
  /** Output only. Listing shared asset type. */
  resourceType: string;
  /** Restricted export config, used to configure restricted export on linked dataset. */
  restrictedExportConfig: AnalyticshubListing_BigqueryDataset_RestrictedExportPolicy;
  /** Output only. Current state of the listing. */
  state: string;
  /** Stored procedure configuration, used to configure stored procedure sharing on linked dataset. */
  storedProcedureConfig: AnalyticshubListing_StoredProcedureConfig;
}

export const AnalyticshubListing: ResourceBinding<AnalyticshubListingConfig, AnalyticshubListingAttrs> = {
  wireType: "google_analyticshub_analyticshub_listing",
  fields: {
    allowOnlyMetadataSharing: "allow_only_metadata_sharing",
    bigqueryDataset: {
      wireName: "bigquery_dataset",
      kind: "object",
      fields: AnalyticshubListing_BigqueryDatasetFields,
    },
    categories: "categories",
    commercialInfo: {
      wireName: "commercial_info",
      kind: "object",
      fields: AnalyticshubListing_CommercialInfoFields,
    },
    dataProvider: {
      wireName: "data_provider",
      kind: "object",
      fields: AnalyticshubListing_DataProviderFields,
    },
    description: "description",
    discoveryType: "discovery_type",
    displayName: "display_name",
    documentation: "documentation",
    icon: "icon",
    logLinkedDatasetQueryUserEmail: "log_linked_dataset_query_user_email",
    name: "name",
    primaryContact: "primary_contact",
    publisher: {
      wireName: "publisher",
      kind: "object",
      fields: AnalyticshubListing_DataProviderFields,
    },
    pubsubTopic: {
      wireName: "pubsub_topic",
      kind: "object",
      fields: AnalyticshubListing_PubsubTopicFields,
    },
    requestAccess: "request_access",
    resourceType: "resource_type",
    restrictedExportConfig: {
      wireName: "restricted_export_config",
      kind: "object",
      fields: AnalyticshubListing_BigqueryDataset_RestrictedExportPolicyFields,
    },
    state: "state",
    storedProcedureConfig: {
      wireName: "stored_procedure_config",
      kind: "object",
      fields: AnalyticshubListing_StoredProcedureConfigFields,
    },
  },
};
