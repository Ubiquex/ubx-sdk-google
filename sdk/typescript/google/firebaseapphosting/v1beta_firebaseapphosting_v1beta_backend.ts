// Code generated by `ubx sdk gen` from google_firebaseapphosting_v1beta_firebaseapphosting_v1beta_backend. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1betaFirebaseapphostingV1betaBackend_Codebase {
  /** Required. The resource name for the Developer Connect [`gitRepositoryLink`](https://cloud.google.com/developer-connect/docs/api/reference/rest/v1/projects.locations.connections.gitRepositoryLinks) connected to this backend, in the format: `projects/{project}/locations/{location}/connections/{connection}/gitRepositoryLinks/{repositoryLink}` The connection for the `gitRepositoryLink` must made be using the Firebase App Hosting GitHub App via the Firebase Console. */
  repository?: string | Computed<string>;
  /** Optional. If `repository` is provided, the directory relative to the root of the repository to use as the root for the deployed web app. Defaults to use the root of the repository if not provided. If deploying a [monorepo](https://firebase.google.com/docs/app-hosting/monorepos), this should be the directory that contains the `package.json` or `apphosting.yaml` file. */
  rootDirectory?: string | Computed<string>;
}

export interface V1betaFirebaseapphostingV1betaBackend_ManagedResources_RunService {
  service?: string | Computed<string>;
}

export interface V1betaFirebaseapphostingV1betaBackend_ManagedResources {
  runService?: V1betaFirebaseapphostingV1betaBackend_ManagedResources_RunService | Computed<V1betaFirebaseapphostingV1betaBackend_ManagedResources_RunService>;
}

export interface V1betaFirebaseapphostingV1betaBackend_OverrideEnv {
  availability?: string[] | Computed<string[]>;
  origin?: string | Computed<string>;
  originFileName?: string | Computed<string>;
  secret?: string | Computed<string>;
  value?: string | Computed<string>;
  variable?: string | Computed<string>;
}

export interface V1betaFirebaseapphostingV1betaBackend_Runtime {
  /** Optional. The value of the runtime. */
  value?: string | Computed<string>;
}

const V1betaFirebaseapphostingV1betaBackend_CodebaseFields: FieldMap = {
  repository: "repository",
  rootDirectory: "root_directory",
};

const V1betaFirebaseapphostingV1betaBackend_ManagedResources_RunServiceFields: FieldMap = {
  service: "service",
};

const V1betaFirebaseapphostingV1betaBackend_ManagedResourcesFields: FieldMap = {
  runService: {
    wireName: "run_service",
    kind: "object",
    fields: V1betaFirebaseapphostingV1betaBackend_ManagedResources_RunServiceFields,
  },
};

const V1betaFirebaseapphostingV1betaBackend_OverrideEnvFields: FieldMap = {
  availability: "availability",
  origin: "origin",
  originFileName: "origin_file_name",
  secret: "secret",
  value: "value",
  variable: "variable",
};

const V1betaFirebaseapphostingV1betaBackend_RuntimeFields: FieldMap = {
  value: "value",
};

export interface V1betaFirebaseapphostingV1betaBackendConfig {
  /** Optional. Unstructured key value map that may be set by external tools to store and arbitrary metadata. They are not queryable and should be preserved when modifying objects. */
  annotations?: Record<string, string> | Computed<Record<string, string>>;
  /** Optional. The [ID of a Web App](https://firebase.google.com/docs/reference/firebase-management/rest/v1beta1/projects.webApps#WebApp.FIELDS.app_id) associated with the backend. */
  appId?: string | Computed<string>;
  /** Optional. automatic_base_image_updates_disabled acts as a way for users to opt out of ABIU. */
  automaticBaseImageUpdatesDisabled?: boolean | Computed<boolean>;
  /** The connection to an external source repository to watch for event-driven updates to the backend. */
  codebase?: V1betaFirebaseapphostingV1betaBackend_Codebase | Computed<V1betaFirebaseapphostingV1betaBackend_Codebase>;
  /** Output only. Time at which the backend was created. */
  createTime?: string | Computed<string>;
  /** Output only. Time at which the backend was deleted. */
  deleteTime?: string | Computed<string>;
  /** Optional. Human-readable name. 63 character limit. */
  displayName?: string | Computed<string>;
  /** Optional. The environment name of the backend, used to load environment variables from environment specific configuration. */
  environment?: string | Computed<string>;
  /** Output only. Server-computed checksum based on other values; may be sent on update or delete to ensure operation is done on expected resource. */
  etag?: string | Computed<string>;
  /** Optional. Unstructured key value map that can be used to organize and categorize objects. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Output only. A list of the resources managed by this backend. */
  managedResources?: V1betaFirebaseapphostingV1betaBackend_ManagedResources[] | Computed<V1betaFirebaseapphostingV1betaBackend_ManagedResources[]>;
  /** Optional. Deprecated: Use `environment` instead. */
  mode?: string | Computed<string>;
  /** Identifier. The resource name of the backend. Format: `projects/{project}/locations/{locationId}/backends/{backendId}`. */
  name?: string | Computed<string>;
  /** Optional. Override environment variables for this Backend. */
  overrideEnv?: V1betaFirebaseapphostingV1betaBackend_OverrideEnv[] | Computed<V1betaFirebaseapphostingV1betaBackend_OverrideEnv[]>;
  /** Output only. A field that, if true, indicates that the system is working to make adjustments to the backend during a LRO. */
  reconciling?: boolean | Computed<boolean>;
  /** Optional. A field that, if true, indicates that incoming request logs are disabled for this backend. Incoming request logs are enabled by default. */
  requestLogsDisabled?: boolean | Computed<boolean>;
  /** Runtime is a string that represents the runtime that is used to build the backend. Users can specify one of the following runtimes: nodejs20, nodejs22, nodejs24, nodejs. Runtime "nodejs" means that nodejs version will be determined at build time. If not specified or specified with a value that is not in the list above, the default runtime `nodejs` will be used and Automatic Base Image Updates will be disabled. See [Firebase documentation](https://firebase.google.com/docs/app-hosting/frameworks-tooling#managing_runtime_versions) for more details. */
  runtime?: V1betaFirebaseapphostingV1betaBackend_Runtime | Computed<V1betaFirebaseapphostingV1betaBackend_Runtime>;
  /** Required. The name of the service account used for Cloud Build and Cloud Run. Should have the role roles/firebaseapphosting.computeRunner or equivalent permissions. */
  serviceAccount?: string | Computed<string>;
  /** Required. Immutable. Specifies how App Hosting will serve the content for this backend. It will either be contained to a single region (REGIONAL_STRICT) or allowed to use App Hosting's global-replicated serving infrastructure (GLOBAL_ACCESS). */
  servingLocality?: string | Computed<string>;
  /** Output only. System-assigned, unique identifier. */
  uid?: string | Computed<string>;
  /** Output only. Time at which the backend was last updated. */
  updateTime?: string | Computed<string>;
  /** Output only. The primary URI to communicate with the backend. */
  uri?: string | Computed<string>;
}

export interface V1betaFirebaseapphostingV1betaBackendAttrs {
  /** Optional. Unstructured key value map that may be set by external tools to store and arbitrary metadata. They are not queryable and should be preserved when modifying objects. */
  annotations: Record<string, string>;
  /** Optional. The [ID of a Web App](https://firebase.google.com/docs/reference/firebase-management/rest/v1beta1/projects.webApps#WebApp.FIELDS.app_id) associated with the backend. */
  appId: string;
  /** Optional. automatic_base_image_updates_disabled acts as a way for users to opt out of ABIU. */
  automaticBaseImageUpdatesDisabled: boolean;
  /** The connection to an external source repository to watch for event-driven updates to the backend. */
  codebase: V1betaFirebaseapphostingV1betaBackend_Codebase;
  /** Output only. Time at which the backend was created. */
  createTime: string;
  /** Output only. Time at which the backend was deleted. */
  deleteTime: string;
  /** Optional. Human-readable name. 63 character limit. */
  displayName: string;
  /** Optional. The environment name of the backend, used to load environment variables from environment specific configuration. */
  environment: string;
  /** Output only. Server-computed checksum based on other values; may be sent on update or delete to ensure operation is done on expected resource. */
  etag: string;
  /** Optional. Unstructured key value map that can be used to organize and categorize objects. */
  labels: Record<string, string>;
  /** Output only. A list of the resources managed by this backend. */
  managedResources: V1betaFirebaseapphostingV1betaBackend_ManagedResources[];
  /** Optional. Deprecated: Use `environment` instead. */
  mode: string;
  /** Identifier. The resource name of the backend. Format: `projects/{project}/locations/{locationId}/backends/{backendId}`. */
  name: string;
  /** Optional. Override environment variables for this Backend. */
  overrideEnv: V1betaFirebaseapphostingV1betaBackend_OverrideEnv[];
  /** Output only. A field that, if true, indicates that the system is working to make adjustments to the backend during a LRO. */
  reconciling: boolean;
  /** Optional. A field that, if true, indicates that incoming request logs are disabled for this backend. Incoming request logs are enabled by default. */
  requestLogsDisabled: boolean;
  /** Runtime is a string that represents the runtime that is used to build the backend. Users can specify one of the following runtimes: nodejs20, nodejs22, nodejs24, nodejs. Runtime "nodejs" means that nodejs version will be determined at build time. If not specified or specified with a value that is not in the list above, the default runtime `nodejs` will be used and Automatic Base Image Updates will be disabled. See [Firebase documentation](https://firebase.google.com/docs/app-hosting/frameworks-tooling#managing_runtime_versions) for more details. */
  runtime: V1betaFirebaseapphostingV1betaBackend_Runtime;
  /** Required. The name of the service account used for Cloud Build and Cloud Run. Should have the role roles/firebaseapphosting.computeRunner or equivalent permissions. */
  serviceAccount: string;
  /** Required. Immutable. Specifies how App Hosting will serve the content for this backend. It will either be contained to a single region (REGIONAL_STRICT) or allowed to use App Hosting's global-replicated serving infrastructure (GLOBAL_ACCESS). */
  servingLocality: string;
  /** Output only. System-assigned, unique identifier. */
  uid: string;
  /** Output only. Time at which the backend was last updated. */
  updateTime: string;
  /** Output only. The primary URI to communicate with the backend. */
  uri: string;
}

export const V1betaFirebaseapphostingV1betaBackend: ResourceBinding<V1betaFirebaseapphostingV1betaBackendConfig, V1betaFirebaseapphostingV1betaBackendAttrs> = {
  wireType: "google_firebaseapphosting_v1beta_firebaseapphosting_v1beta_backend",
  fields: {
    annotations: "annotations",
    appId: "app_id",
    automaticBaseImageUpdatesDisabled: "automatic_base_image_updates_disabled",
    codebase: {
      wireName: "codebase",
      kind: "object",
      fields: V1betaFirebaseapphostingV1betaBackend_CodebaseFields,
    },
    createTime: "create_time",
    deleteTime: "delete_time",
    displayName: "display_name",
    environment: "environment",
    etag: "etag",
    labels: "labels",
    managedResources: {
      wireName: "managed_resources",
      kind: "list",
      fields: V1betaFirebaseapphostingV1betaBackend_ManagedResourcesFields,
    },
    mode: "mode",
    name: "name",
    overrideEnv: {
      wireName: "override_env",
      kind: "list",
      fields: V1betaFirebaseapphostingV1betaBackend_OverrideEnvFields,
    },
    reconciling: "reconciling",
    requestLogsDisabled: "request_logs_disabled",
    runtime: {
      wireName: "runtime",
      kind: "object",
      fields: V1betaFirebaseapphostingV1betaBackend_RuntimeFields,
    },
    serviceAccount: "service_account",
    servingLocality: "serving_locality",
    uid: "uid",
    updateTime: "update_time",
    uri: "uri",
  },
};
