// Code generated by `ubx sdk gen` from google_alloydb_v1alpha_backup. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1alphaBackup_EncryptionConfig {
  /** The fully-qualified resource name of the KMS key. Each Cloud KMS key is regionalized and has the following format: projects/[PROJECT]/locations/[REGION]/keyRings/[RING]/cryptoKeys/[KEY_NAME] */
  kmsKeyName?: string | Computed<string>;
}

export interface V1alphaBackup_EncryptionInfo {
  /** Output only. Type of encryption. */
  encryptionType?: string | Computed<string>;
  /** Output only. Cloud KMS key versions that are being used to protect the database or the backup. */
  kmsKeyVersions?: string[] | Computed<string[]>;
}

export interface V1alphaBackup_ExpiryQuantity {
  /** Output only. The backup's position among its backups with the same source cluster and type, by descending chronological order create time(i.e. newest first). */
  retentionCount?: number | Computed<number>;
  /** Output only. The length of the quantity-based queue, specified by the backup's retention policy. */
  totalRetentionCount?: number | Computed<number>;
}

const V1alphaBackup_EncryptionConfigFields: FieldMap = {
  kmsKeyName: "kms_key_name",
};

const V1alphaBackup_EncryptionInfoFields: FieldMap = {
  encryptionType: "encryption_type",
  kmsKeyVersions: "kms_key_versions",
};

const V1alphaBackup_ExpiryQuantityFields: FieldMap = {
  retentionCount: "retention_count",
  totalRetentionCount: "total_retention_count",
};

export interface V1alphaBackupConfig {
  /** Annotations to allow client tools to store small amount of arbitrary data. This is distinct from labels. https://google.aip.dev/128 */
  annotations?: Record<string, string> | Computed<Record<string, string>>;
  /** Required. The full resource name of the backup source cluster (e.g., projects/{project}/locations/{region}/clusters/{cluster_id}). */
  clusterName?: string | Computed<string>;
  /** User-provided description of the backup. */
  description?: string | Computed<string>;
  /** User-settable and human-readable display name for the Backup. */
  displayName?: string | Computed<string>;
  /** EncryptionConfig describes the encryption config of a cluster or a backup that is encrypted with a CMEK (customer-managed encryption key). */
  encryptionConfig?: V1alphaBackup_EncryptionConfig | Computed<V1alphaBackup_EncryptionConfig>;
  /** EncryptionInfo describes the encryption information of a cluster or a backup. */
  encryptionInfo?: V1alphaBackup_EncryptionInfo | Computed<V1alphaBackup_EncryptionInfo>;
  /** For Resource freshness validation (https://google.aip.dev/154) */
  etag?: string | Computed<string>;
  /** A backup's position in a quantity-based retention queue, of backups with the same source cluster and type, with length, retention, specified by the backup's retention policy. Once the position is greater than the retention, the backup is eligible to be garbage collected. Example: 5 backups from the same source cluster and type with a quantity-based retention of 3 and denoted by backup_id (position, retention). Safe: backup_5 (1, 3), backup_4, (2, 3), backup_3 (3, 3). Awaiting garbage collection: backup_2 (4, 3), backup_1 (5, 3) */
  expiryQuantity?: V1alphaBackup_ExpiryQuantity | Computed<V1alphaBackup_ExpiryQuantity>;
  /** Labels as key value pairs */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: ``` "123/environment": "production", "123/costCenter": "marketing" ``` */
  tags?: Record<string, string> | Computed<Record<string, string>>;
  /** The backup type, which suggests the trigger for the backup. */
  type?: string | Computed<string>;
}

export interface V1alphaBackupAttrs {
  /** Annotations to allow client tools to store small amount of arbitrary data. This is distinct from labels. https://google.aip.dev/128 */
  annotations: Record<string, string>;
  /** Output only. Set to true if the cluster corresponding to this backup is deleted. This field is only populated for when using the BACKUP_VIEW_CLUSTER_DELETED view. */
  clusterDeleted: boolean;
  /** Required. The full resource name of the backup source cluster (e.g., projects/{project}/locations/{region}/clusters/{cluster_id}). */
  clusterName: string;
  /** Output only. The system-generated UID of the cluster which was used to create this resource. */
  clusterUid: string;
  /** Output only. Timestamp when the resource finished being created. */
  createCompletionTime: string;
  /** Output only. Create time stamp */
  createTime: string;
  /** Output only. The database engine major version of the cluster this backup was created from. Any restored cluster created from this backup will have the same database version. */
  databaseVersion: string;
  /** Output only. Delete time stamp */
  deleteTime: string;
  /** User-provided description of the backup. */
  description: string;
  /** User-settable and human-readable display name for the Backup. */
  displayName: string;
  /** EncryptionConfig describes the encryption config of a cluster or a backup that is encrypted with a CMEK (customer-managed encryption key). */
  encryptionConfig: V1alphaBackup_EncryptionConfig;
  /** EncryptionInfo describes the encryption information of a cluster or a backup. */
  encryptionInfo: V1alphaBackup_EncryptionInfo;
  /** For Resource freshness validation (https://google.aip.dev/154) */
  etag: string;
  /** A backup's position in a quantity-based retention queue, of backups with the same source cluster and type, with length, retention, specified by the backup's retention policy. Once the position is greater than the retention, the backup is eligible to be garbage collected. Example: 5 backups from the same source cluster and type with a quantity-based retention of 3 and denoted by backup_id (position, retention). Safe: backup_5 (1, 3), backup_4, (2, 3), backup_3 (3, 3). Awaiting garbage collection: backup_2 (4, 3), backup_1 (5, 3) */
  expiryQuantity: V1alphaBackup_ExpiryQuantity;
  /** Output only. The time at which after the backup is eligible to be garbage collected. It is the duration specified by the backup's retention policy, added to the backup's create_time. */
  expiryTime: string;
  /** Labels as key value pairs */
  labels: Record<string, string>;
  /** Output only. The name of the backup resource with the format: * projects/{project}/locations/{region}/backups/{backup_id} where the cluster and backup ID segments should satisfy the regex expression `[a-z]([a-z0-9-]{0,61}[a-z0-9])?`, e.g. 1-63 characters of lowercase letters, numbers, and dashes, starting with a letter, and ending with a letter or number. For more details see https://google.aip.dev/122. The prefix of the backup resource name is the name of the parent resource: * projects/{project}/locations/{region} */
  name: string;
  /** Output only. Reconciling (https://google.aip.dev/128#reconciliation), if true, indicates that the service is actively updating the resource. This can happen due to user-triggered updates or system actions like failover or maintenance. */
  reconciling: boolean;
  /** Output only. Reserved for future use. */
  satisfiesPzi: boolean;
  /** Output only. Reserved for future use. */
  satisfiesPzs: boolean;
  /** Output only. The size of the backup in bytes. */
  sizeBytes: string;
  /** Output only. The current state of the backup. */
  state: string;
  /** Optional. Input only. Immutable. Tag keys/values directly bound to this resource. For example: ``` "123/environment": "production", "123/costCenter": "marketing" ``` */
  tags: Record<string, string>;
  /** The backup type, which suggests the trigger for the backup. */
  type: string;
  /** Output only. The system-generated UID of the resource. The UID is assigned when the resource is created, and it is retained until it is deleted. */
  uid: string;
  /** Output only. Update time stamp Users should not infer any meaning from this field. Its value is generally unrelated to the timing of the backup creation operation. */
  updateTime: string;
}

export const V1alphaBackup: ResourceBinding<V1alphaBackupConfig, V1alphaBackupAttrs> = {
  wireType: "google_alloydb_v1alpha_backup",
  fields: {
    annotations: "annotations",
    clusterName: "cluster_name",
    description: "description",
    displayName: "display_name",
    encryptionConfig: {
      wireName: "encryption_config",
      kind: "object",
      fields: V1alphaBackup_EncryptionConfigFields,
    },
    encryptionInfo: {
      wireName: "encryption_info",
      kind: "object",
      fields: V1alphaBackup_EncryptionInfoFields,
    },
    etag: "etag",
    expiryQuantity: {
      wireName: "expiry_quantity",
      kind: "object",
      fields: V1alphaBackup_ExpiryQuantityFields,
    },
    labels: "labels",
    tags: "tags",
    type: "type",
  },
};
