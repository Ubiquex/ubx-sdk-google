// Code generated by `ubx sdk gen` from google_sqladmin_user. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface User_PasswordPolicy_Status {
  /** If true, user does not have login privileges. */
  locked?: boolean | Computed<boolean>;
  /** The expiration time of the current password. */
  passwordExpirationTime?: string | Computed<string>;
}

export interface User_PasswordPolicy {
  /** Number of failed login attempts allowed before user get locked. */
  allowedFailedAttempts?: number | Computed<number>;
  /** If true, failed login attempts check will be enabled. */
  enableFailedAttemptsCheck?: boolean | Computed<boolean>;
  /** If true, the user must specify the current password before changing the password. This flag is supported only for MySQL. */
  enablePasswordVerification?: boolean | Computed<boolean>;
  /** Expiration duration after password is updated. */
  passwordExpirationDuration?: string | Computed<string>;
  /** Read-only password status. */
  status?: User_PasswordPolicy_Status | Computed<User_PasswordPolicy_Status>;
}

export interface User_SqlserverUserDetails {
  /** If the user has been disabled */
  disabled?: boolean | Computed<boolean>;
  /** The server roles for this user */
  serverRoles?: string[] | Computed<string[]>;
}

const User_PasswordPolicy_StatusFields: FieldMap = {
  locked: "locked",
  passwordExpirationTime: "password_expiration_time",
};

const User_PasswordPolicyFields: FieldMap = {
  allowedFailedAttempts: "allowed_failed_attempts",
  enableFailedAttemptsCheck: "enable_failed_attempts_check",
  enablePasswordVerification: "enable_password_verification",
  passwordExpirationDuration: "password_expiration_duration",
  status: {
    wireName: "status",
    kind: "object",
    fields: User_PasswordPolicy_StatusFields,
  },
};

const User_SqlserverUserDetailsFields: FieldMap = {
  disabled: "disabled",
  serverRoles: "server_roles",
};

export interface UserConfig {
  /** Optional. Role memberships of the user */
  databaseRoles?: string[] | Computed<string[]>;
  /** Dual password status for the user. */
  dualPasswordType?: string | Computed<string>;
  /** This field is deprecated and will be removed from a future version of the API. */
  etag?: string | Computed<string>;
  /** Optional. The host from which the user can connect. For `insert` operations, host defaults to an empty string. For `update` operations, host is specified as part of the request URL. The host name cannot be updated after insertion. For a MySQL instance, it's required; for a PostgreSQL or SQL Server instance, it's optional. */
  host?: string | Computed<string>;
  /** Optional. The full email for an IAM user. For normal database users, this will not be filled. Only applicable to MySQL database users. */
  iamEmail?: string | Computed<string>;
  /** Indicates if a group is active or inactive for IAM database authentication. */
  iamStatus?: string | Computed<string>;
  /** The name of the Cloud SQL instance. This does not include the project ID. Can be omitted for `update` because it is already specified on the URL. */
  instance?: string | Computed<string>;
  /** This is always `sql#user`. */
  kind?: string | Computed<string>;
  /** The name of the user in the Cloud SQL instance. Can be omitted for `update` because it is already specified in the URL. */
  name?: string | Computed<string>;
  /** The password for the user. */
  password?: string | Computed<string>;
  /** User level password validation policy. */
  passwordPolicy?: User_PasswordPolicy | Computed<User_PasswordPolicy>;
  /** The project ID of the project containing the Cloud SQL database. The Google apps domain is prefixed if applicable. Can be omitted for `update` because it is already specified on the URL. */
  project?: string | Computed<string>;
  /** Optional. The server roles for the SQL Server login. */
  serverRoles?: string[] | Computed<string[]>;
  /** Represents a Sql Server user on the Cloud SQL instance. */
  sqlserverUserDetails?: User_SqlserverUserDetails | Computed<User_SqlserverUserDetails>;
  /** The user type. It determines the method to authenticate the user during login. The default is the database's built-in user type. */
  type?: string | Computed<string>;
}

export interface UserAttrs {
  /** Optional. Role memberships of the user */
  databaseRoles: string[];
  /** Dual password status for the user. */
  dualPasswordType: string;
  /** This field is deprecated and will be removed from a future version of the API. */
  etag: string;
  /** Optional. The host from which the user can connect. For `insert` operations, host defaults to an empty string. For `update` operations, host is specified as part of the request URL. The host name cannot be updated after insertion. For a MySQL instance, it's required; for a PostgreSQL or SQL Server instance, it's optional. */
  host: string;
  /** Optional. The full email for an IAM user. For normal database users, this will not be filled. Only applicable to MySQL database users. */
  iamEmail: string;
  /** Indicates if a group is active or inactive for IAM database authentication. */
  iamStatus: string;
  /** The name of the Cloud SQL instance. This does not include the project ID. Can be omitted for `update` because it is already specified on the URL. */
  instance: string;
  /** This is always `sql#user`. */
  kind: string;
  /** The name of the user in the Cloud SQL instance. Can be omitted for `update` because it is already specified in the URL. */
  name: string;
  /** The password for the user. */
  password: string;
  /** User level password validation policy. */
  passwordPolicy: User_PasswordPolicy;
  /** The project ID of the project containing the Cloud SQL database. The Google apps domain is prefixed if applicable. Can be omitted for `update` because it is already specified on the URL. */
  project: string;
  /** Optional. The server roles for the SQL Server login. */
  serverRoles: string[];
  /** Represents a Sql Server user on the Cloud SQL instance. */
  sqlserverUserDetails: User_SqlserverUserDetails;
  /** The user type. It determines the method to authenticate the user during login. The default is the database's built-in user type. */
  type: string;
}

export const User: ResourceBinding<UserConfig, UserAttrs> = {
  wireType: "google_sqladmin_user",
  fields: {
    databaseRoles: "database_roles",
    dualPasswordType: "dual_password_type",
    etag: "etag",
    host: "host",
    iamEmail: "iam_email",
    iamStatus: "iam_status",
    instance: "instance",
    kind: "kind",
    name: "name",
    password: "password",
    passwordPolicy: {
      wireName: "password_policy",
      kind: "object",
      fields: User_PasswordPolicyFields,
    },
    project: "project",
    serverRoles: "server_roles",
    sqlserverUserDetails: {
      wireName: "sqlserver_user_details",
      kind: "object",
      fields: User_SqlserverUserDetailsFields,
    },
    type: "type",
  },
};
