// Code generated by `ubx sdk gen` from google_cloudidentity_cloudidentity_group. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface CloudidentityGroup_AdditionalGroupKeys {
  id?: string | Computed<string>;
  namespace?: string | Computed<string>;
}

export interface CloudidentityGroup_DynamicGroupMetadata_Queries {
  query?: string | Computed<string>;
  resourceType?: string | Computed<string>;
}

export interface CloudidentityGroup_DynamicGroupMetadata_Status {
  /** Status of the dynamic group. */
  status?: string | Computed<string>;
  /** The latest time at which the dynamic group is guaranteed to be in the given status. If status is `UP_TO_DATE`, the latest time at which the dynamic group was confirmed to be up-to-date. If status is `UPDATING_MEMBERSHIPS`, the time at which dynamic group was created. */
  statusTime?: string | Computed<string>;
}

export interface CloudidentityGroup_DynamicGroupMetadata {
  /** Memberships will be the union of all queries. Only one entry with USER resource is currently supported. Customers can create up to 500 dynamic groups. */
  queries?: CloudidentityGroup_DynamicGroupMetadata_Queries[] | Computed<CloudidentityGroup_DynamicGroupMetadata_Queries[]>;
  /** The current status of a dynamic group along with timestamp. */
  status?: CloudidentityGroup_DynamicGroupMetadata_Status | Computed<CloudidentityGroup_DynamicGroupMetadata_Status>;
}

const CloudidentityGroup_AdditionalGroupKeysFields: FieldMap = {
  id: "id",
  namespace: "namespace",
};

const CloudidentityGroup_DynamicGroupMetadata_QueriesFields: FieldMap = {
  query: "query",
  resourceType: "resource_type",
};

const CloudidentityGroup_DynamicGroupMetadata_StatusFields: FieldMap = {
  status: "status",
  statusTime: "status_time",
};

const CloudidentityGroup_DynamicGroupMetadataFields: FieldMap = {
  queries: {
    wireName: "queries",
    kind: "list",
    fields: CloudidentityGroup_DynamicGroupMetadata_QueriesFields,
  },
  status: {
    wireName: "status",
    kind: "object",
    fields: CloudidentityGroup_DynamicGroupMetadata_StatusFields,
  },
};

export interface CloudidentityGroupConfig {
  /** Output only. Additional group keys associated with the Group. */
  additionalGroupKeys?: CloudidentityGroup_AdditionalGroupKeys[] | Computed<CloudidentityGroup_AdditionalGroupKeys[]>;
  /** Output only. The time when the `Group` was created. */
  createTime?: string | Computed<string>;
  /** An extended description to help users determine the purpose of a `Group`. Must not be longer than 4,096 characters. */
  description?: string | Computed<string>;
  /** The display name of the `Group`. */
  displayName?: string | Computed<string>;
  /** Dynamic group metadata like queries and status. */
  dynamicGroupMetadata?: CloudidentityGroup_DynamicGroupMetadata | Computed<CloudidentityGroup_DynamicGroupMetadata>;
  /** A unique identifier for an entity in the Cloud Identity Groups API. An entity can represent either a group with an optional `namespace` or a user without a `namespace`. The combination of `id` and `namespace` must be unique; however, the same `id` can be used with different `namespace`s. */
  groupKey?: CloudidentityGroup_AdditionalGroupKeys | Computed<CloudidentityGroup_AdditionalGroupKeys>;
  /** Required. One or more label entries that apply to the Group. Labels contain a key with an empty value. Google Groups are the default type of group and have a label with a key of `cloudidentity.googleapis.com/groups.discussion_forum` and an empty value. Existing Google Groups can have an additional label with a key of `cloudidentity.googleapis.com/groups.security` and an empty value added to them. **This is an immutable change and the security label cannot be removed once added.** Dynamic groups have a label with a key of `cloudidentity.googleapis.com/groups.dynamic`. Identity-mapped groups for Cloud Search have a label with a key of `system/groups/external` and an empty value. Google Groups can be [locked](https://support.google.com/a?p=locked-groups). To lock a group, add a label with a key of `cloudidentity.googleapis.com/groups.locked` and an empty value. Doing so locks the group. To unlock the group, remove this label. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Output only. The [resource name](https://cloud.google.com/apis/design/resource_names) of the `Group`. Shall be of the form `groups/{group}`. */
  name?: string | Computed<string>;
  /** Required. Immutable. The resource name of the entity under which this `Group` resides in the Cloud Identity resource hierarchy. Must be of the form `identitysources/{identity_source}` for external [identity-mapped groups](https://support.google.com/a/answer/9039510) or `customers/{customer_id}` for Google Groups. The `customer_id` must begin with "C" (for example, 'C046psxkn'). [Find your customer ID.] (https://support.google.com/cloudidentity/answer/10070793) */
  parent?: string | Computed<string>;
  /** Output only. The time when the `Group` was last updated. */
  updateTime?: string | Computed<string>;
}

export interface CloudidentityGroupAttrs {
  /** Output only. Additional group keys associated with the Group. */
  additionalGroupKeys: CloudidentityGroup_AdditionalGroupKeys[];
  /** Output only. The time when the `Group` was created. */
  createTime: string;
  /** An extended description to help users determine the purpose of a `Group`. Must not be longer than 4,096 characters. */
  description: string;
  /** The display name of the `Group`. */
  displayName: string;
  /** Dynamic group metadata like queries and status. */
  dynamicGroupMetadata: CloudidentityGroup_DynamicGroupMetadata;
  /** A unique identifier for an entity in the Cloud Identity Groups API. An entity can represent either a group with an optional `namespace` or a user without a `namespace`. The combination of `id` and `namespace` must be unique; however, the same `id` can be used with different `namespace`s. */
  groupKey: CloudidentityGroup_AdditionalGroupKeys;
  /** Required. One or more label entries that apply to the Group. Labels contain a key with an empty value. Google Groups are the default type of group and have a label with a key of `cloudidentity.googleapis.com/groups.discussion_forum` and an empty value. Existing Google Groups can have an additional label with a key of `cloudidentity.googleapis.com/groups.security` and an empty value added to them. **This is an immutable change and the security label cannot be removed once added.** Dynamic groups have a label with a key of `cloudidentity.googleapis.com/groups.dynamic`. Identity-mapped groups for Cloud Search have a label with a key of `system/groups/external` and an empty value. Google Groups can be [locked](https://support.google.com/a?p=locked-groups). To lock a group, add a label with a key of `cloudidentity.googleapis.com/groups.locked` and an empty value. Doing so locks the group. To unlock the group, remove this label. */
  labels: Record<string, string>;
  /** Output only. The [resource name](https://cloud.google.com/apis/design/resource_names) of the `Group`. Shall be of the form `groups/{group}`. */
  name: string;
  /** Required. Immutable. The resource name of the entity under which this `Group` resides in the Cloud Identity resource hierarchy. Must be of the form `identitysources/{identity_source}` for external [identity-mapped groups](https://support.google.com/a/answer/9039510) or `customers/{customer_id}` for Google Groups. The `customer_id` must begin with "C" (for example, 'C046psxkn'). [Find your customer ID.] (https://support.google.com/cloudidentity/answer/10070793) */
  parent: string;
  /** Output only. The time when the `Group` was last updated. */
  updateTime: string;
}

export const CloudidentityGroup: ResourceBinding<CloudidentityGroupConfig, CloudidentityGroupAttrs> = {
  wireType: "google_cloudidentity_cloudidentity_group",
  fields: {
    additionalGroupKeys: {
      wireName: "additional_group_keys",
      kind: "list",
      fields: CloudidentityGroup_AdditionalGroupKeysFields,
    },
    createTime: "create_time",
    description: "description",
    displayName: "display_name",
    dynamicGroupMetadata: {
      wireName: "dynamic_group_metadata",
      kind: "object",
      fields: CloudidentityGroup_DynamicGroupMetadataFields,
    },
    groupKey: {
      wireName: "group_key",
      kind: "object",
      fields: CloudidentityGroup_AdditionalGroupKeysFields,
    },
    labels: "labels",
    name: "name",
    parent: "parent",
    updateTime: "update_time",
  },
};
