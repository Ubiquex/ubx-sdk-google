// Code generated by `ubx sdk gen` from google_cloudidentity_inbound_saml_sso_profile. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface InboundSamlSsoProfile_IdpConfig {
  /** The **Change Password URL** of the identity provider. Users will be sent to this URL when changing their passwords at `myaccount.google.com`. This takes precedence over the change password URL configured at customer-level. Must use `HTTPS`. */
  changePasswordUri?: string | Computed<string>;
  /** Required. The SAML **Entity ID** of the identity provider. */
  entityId?: string | Computed<string>;
  /** The **Logout Redirect URL** (sign-out page URL) of the identity provider. When a user clicks the sign-out link on a Google page, they will be redirected to this URL. This is a pure redirect with no attached SAML `LogoutRequest` i.e. SAML single logout is not supported. Must use `HTTPS`. */
  logoutRedirectUri?: string | Computed<string>;
  /** Required. The `SingleSignOnService` endpoint location (sign-in page URL) of the identity provider. This is the URL where the `AuthnRequest` will be sent. Must use `HTTPS`. Assumed to accept the `HTTP-Redirect` binding. */
  singleSignOnServiceUri?: string | Computed<string>;
}

export interface InboundSamlSsoProfile_SpConfig {
  /** Output only. The SAML **Assertion Consumer Service (ACS) URL** to be used for the IDP-initiated login. Assumed to accept response messages via the `HTTP-POST` binding. */
  assertionConsumerServiceUri?: string | Computed<string>;
  /** Output only. The SAML **Entity ID** for this service provider. */
  entityId?: string | Computed<string>;
}

const InboundSamlSsoProfile_IdpConfigFields: FieldMap = {
  changePasswordUri: "change_password_uri",
  entityId: "entity_id",
  logoutRedirectUri: "logout_redirect_uri",
  singleSignOnServiceUri: "single_sign_on_service_uri",
};

const InboundSamlSsoProfile_SpConfigFields: FieldMap = {
  assertionConsumerServiceUri: "assertion_consumer_service_uri",
  entityId: "entity_id",
};

export interface InboundSamlSsoProfileConfig {
  /** Immutable. The customer. For example: `customers/C0123abc`. */
  customer?: string | Computed<string>;
  /** Human-readable name of the SAML SSO profile. */
  displayName?: string | Computed<string>;
  /** SAML IDP (identity provider) configuration. */
  idpConfig?: InboundSamlSsoProfile_IdpConfig | Computed<InboundSamlSsoProfile_IdpConfig>;
  /** SAML SP (service provider) configuration. */
  spConfig?: InboundSamlSsoProfile_SpConfig | Computed<InboundSamlSsoProfile_SpConfig>;
}

export interface InboundSamlSsoProfileAttrs {
  /** Immutable. The customer. For example: `customers/C0123abc`. */
  customer: string;
  /** Human-readable name of the SAML SSO profile. */
  displayName: string;
  /** SAML IDP (identity provider) configuration. */
  idpConfig: InboundSamlSsoProfile_IdpConfig;
  /** Output only. [Resource name](https://cloud.google.com/apis/design/resource_names) of the SAML SSO profile. */
  name: string;
  /** SAML SP (service provider) configuration. */
  spConfig: InboundSamlSsoProfile_SpConfig;
}

export const InboundSamlSsoProfile: ResourceBinding<InboundSamlSsoProfileConfig, InboundSamlSsoProfileAttrs> = {
  wireType: "google_cloudidentity_inbound_saml_sso_profile",
  fields: {
    customer: "customer",
    displayName: "display_name",
    idpConfig: {
      wireName: "idp_config",
      kind: "object",
      fields: InboundSamlSsoProfile_IdpConfigFields,
    },
    spConfig: {
      wireName: "sp_config",
      kind: "object",
      fields: InboundSamlSsoProfile_SpConfigFields,
    },
  },
};
