// Code generated by `ubx sdk gen` from google_firestore_v1beta1_document. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1beta1Document_Fields_ArrayValue {
  values?: unknown[] | Computed<unknown[]>;
}

export interface V1beta1Document_Fields_FunctionValue {
  args?: unknown[] | Computed<unknown[]>;
  name?: string | Computed<string>;
  options?: Record<string, unknown> | Computed<Record<string, unknown>>;
}

export interface V1beta1Document_Fields_GeoPointValue {
  latitude?: number | Computed<number>;
  longitude?: number | Computed<number>;
}

export interface V1beta1Document_Fields_MapValue {
  fields?: Record<string, unknown> | Computed<Record<string, unknown>>;
}

export interface V1beta1Document_Fields_PipelineValue {
  stages?: V1beta1Document_Fields_FunctionValue[] | Computed<V1beta1Document_Fields_FunctionValue[]>;
}

export interface V1beta1Document_Fields {
  arrayValue?: V1beta1Document_Fields_ArrayValue | Computed<V1beta1Document_Fields_ArrayValue>;
  booleanValue?: boolean | Computed<boolean>;
  bytesValue?: string | Computed<string>;
  doubleValue?: number | Computed<number>;
  fieldReferenceValue?: string | Computed<string>;
  functionValue?: V1beta1Document_Fields_FunctionValue | Computed<V1beta1Document_Fields_FunctionValue>;
  geoPointValue?: V1beta1Document_Fields_GeoPointValue | Computed<V1beta1Document_Fields_GeoPointValue>;
  integerValue?: string | Computed<string>;
  mapValue?: V1beta1Document_Fields_MapValue | Computed<V1beta1Document_Fields_MapValue>;
  nullValue?: string | Computed<string>;
  pipelineValue?: V1beta1Document_Fields_PipelineValue | Computed<V1beta1Document_Fields_PipelineValue>;
  referenceValue?: string | Computed<string>;
  stringValue?: string | Computed<string>;
  timestampValue?: string | Computed<string>;
  variableReferenceValue?: string | Computed<string>;
}

const V1beta1Document_Fields_ArrayValueFields: FieldMap = {
  values: "values",
};

const V1beta1Document_Fields_FunctionValueFields: FieldMap = {
  args: "args",
  name: "name",
  options: "options",
};

const V1beta1Document_Fields_GeoPointValueFields: FieldMap = {
  latitude: "latitude",
  longitude: "longitude",
};

const V1beta1Document_Fields_MapValueFields: FieldMap = {
  fields: "fields",
};

const V1beta1Document_Fields_PipelineValueFields: FieldMap = {
  stages: {
    wireName: "stages",
    kind: "list",
    fields: V1beta1Document_Fields_FunctionValueFields,
  },
};

const V1beta1Document_FieldsFields: FieldMap = {
  arrayValue: {
    wireName: "array_value",
    kind: "object",
    fields: V1beta1Document_Fields_ArrayValueFields,
  },
  booleanValue: "boolean_value",
  bytesValue: "bytes_value",
  doubleValue: "double_value",
  fieldReferenceValue: "field_reference_value",
  functionValue: {
    wireName: "function_value",
    kind: "object",
    fields: V1beta1Document_Fields_FunctionValueFields,
  },
  geoPointValue: {
    wireName: "geo_point_value",
    kind: "object",
    fields: V1beta1Document_Fields_GeoPointValueFields,
  },
  integerValue: "integer_value",
  mapValue: {
    wireName: "map_value",
    kind: "object",
    fields: V1beta1Document_Fields_MapValueFields,
  },
  nullValue: "null_value",
  pipelineValue: {
    wireName: "pipeline_value",
    kind: "object",
    fields: V1beta1Document_Fields_PipelineValueFields,
  },
  referenceValue: "reference_value",
  stringValue: "string_value",
  timestampValue: "timestamp_value",
  variableReferenceValue: "variable_reference_value",
};

export interface V1beta1DocumentConfig {
  /** Output only. The time at which the document was created. This value increases monotonically when a document is deleted then recreated. It can also be compared to values from other documents and the `read_time` of a query. */
  createTime?: string | Computed<string>;
  /** The document's fields. The map keys represent field names. Field names matching the regular expression `__.*__` are reserved. Reserved field names are forbidden except in certain documented contexts. The field names, represented as UTF-8, must not exceed 1,500 bytes and cannot be empty. Field paths may be used in other contexts to refer to structured fields defined here. For `map_value`, the field path is represented by a dot-delimited (`.`) string of segments. Each segment is either a simple field name (defined below) or a quoted field name. For example, the structured field `"foo" : { map_value: { "x&y" : { string_value: "hello" }}}` would be represented by the field path `` foo.`x&y` ``. A simple field name contains only characters `a` to `z`, `A` to `Z`, `0` to `9`, or `_`, and must not start with `0` to `9`. For example, `foo_bar_17`. A quoted field name starts and ends with `` ` `` and may contain any character. Some characters, including `` ` ``, must be escaped using a `\`. For example, `` `x&y` `` represents `x&y` and `` `bak\`tik` `` represents `` bak`tik ``. */
  fields?: Record<string, V1beta1Document_Fields> | Computed<Record<string, V1beta1Document_Fields>>;
  /** The resource name of the document, for example `projects/{project_id}/databases/{database_id}/documents/{document_path}`. */
  name?: string | Computed<string>;
  /** Output only. The time at which the document was last changed. This value is initially set to the `create_time` then increases monotonically with each change to the document. It can also be compared to values from other documents and the `read_time` of a query. */
  updateTime?: string | Computed<string>;
}

export interface V1beta1DocumentAttrs {
  /** Output only. The time at which the document was created. This value increases monotonically when a document is deleted then recreated. It can also be compared to values from other documents and the `read_time` of a query. */
  createTime: string;
  /** The document's fields. The map keys represent field names. Field names matching the regular expression `__.*__` are reserved. Reserved field names are forbidden except in certain documented contexts. The field names, represented as UTF-8, must not exceed 1,500 bytes and cannot be empty. Field paths may be used in other contexts to refer to structured fields defined here. For `map_value`, the field path is represented by a dot-delimited (`.`) string of segments. Each segment is either a simple field name (defined below) or a quoted field name. For example, the structured field `"foo" : { map_value: { "x&y" : { string_value: "hello" }}}` would be represented by the field path `` foo.`x&y` ``. A simple field name contains only characters `a` to `z`, `A` to `Z`, `0` to `9`, or `_`, and must not start with `0` to `9`. For example, `foo_bar_17`. A quoted field name starts and ends with `` ` `` and may contain any character. Some characters, including `` ` ``, must be escaped using a `\`. For example, `` `x&y` `` represents `x&y` and `` `bak\`tik` `` represents `` bak`tik ``. */
  fields: Record<string, V1beta1Document_Fields>;
  /** The resource name of the document, for example `projects/{project_id}/databases/{database_id}/documents/{document_path}`. */
  name: string;
  /** Output only. The time at which the document was last changed. This value is initially set to the `create_time` then increases monotonically with each change to the document. It can also be compared to values from other documents and the `read_time` of a query. */
  updateTime: string;
}

export const V1beta1Document: ResourceBinding<V1beta1DocumentConfig, V1beta1DocumentAttrs> = {
  wireType: "google_firestore_v1beta1_document",
  fields: {
    createTime: "create_time",
    fields: {
      wireName: "fields",
      kind: "map",
      fields: V1beta1Document_FieldsFields,
    },
    name: "name",
    updateTime: "update_time",
  },
};
