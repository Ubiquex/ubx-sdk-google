// Code generated by `ubx sdk gen` from google_dataform_workflow_invocation. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface WorkflowInvocation_DataEncryptionState {
  /** Required. The KMS key version name with which data of a resource is encrypted. */
  kmsKeyVersionName?: string | Computed<string>;
}

export interface WorkflowInvocation_InvocationConfig_IncludedTargets {
  /** The database (BigQuery project) this included target table belongs to. (AI-inferred) */
  database?: string | Computed<string>;
  /** The name of this included target table. (AI-inferred) */
  name?: string | Computed<string>;
  /** The schema (BigQuery dataset) this included target table belongs to. (AI-inferred) */
  schema?: string | Computed<string>;
}

export interface WorkflowInvocation_InvocationConfig {
  /** Optional. When set to true, any incremental tables will be fully refreshed. */
  fullyRefreshIncrementalTablesEnabled?: boolean | Computed<boolean>;
  /** Optional. The set of tags to include. */
  includedTags?: string[] | Computed<string[]>;
  /** Optional. The set of action identifiers to include. */
  includedTargets?: WorkflowInvocation_InvocationConfig_IncludedTargets[] | Computed<WorkflowInvocation_InvocationConfig_IncludedTargets[]>;
  /** Optional. Specifies the priority for query execution in BigQuery. More information can be found at https://cloud.google.com/bigquery/docs/running-queries#queries. */
  queryPriority?: string | Computed<string>;
  /** Optional. The service account to run workflow invocations under. */
  serviceAccount?: string | Computed<string>;
  /** Optional. When set to true, transitive dependencies of included actions will be executed. */
  transitiveDependenciesIncluded?: boolean | Computed<boolean>;
  /** Optional. When set to true, transitive dependents of included actions will be executed. */
  transitiveDependentsIncluded?: boolean | Computed<boolean>;
}

export interface WorkflowInvocation_InvocationTiming {
  /** Optional. Exclusive end of the interval. If specified, a Timestamp matching this interval will have to be before the end. */
  endTime?: string | Computed<string>;
  /** Optional. Inclusive start of the interval. If specified, a Timestamp matching this interval will have to be the same or after the start. */
  startTime?: string | Computed<string>;
}

export interface WorkflowInvocation_PipelineConfig {
  /** Required. The relative path within the Git repository where the pipeline is defined. For example, for a Dataform pipeline, it is a path to the folder where `workflow_settings.yaml` or `dataform.json` is located. */
  path?: string | Computed<string>;
  /** Required. The type of the pipeline. */
  pipelineType?: string | Computed<string>;
}

export interface WorkflowInvocation_PrivateResourceMetadata {
  /** Output only. If true, this resource is user-scoped, meaning it is either a workspace or sourced from a workspace. */
  userScoped?: boolean | Computed<boolean>;
}

const WorkflowInvocation_DataEncryptionStateFields: FieldMap = {
  kmsKeyVersionName: "kms_key_version_name",
};

const WorkflowInvocation_InvocationConfig_IncludedTargetsFields: FieldMap = {
  database: "database",
  name: "name",
  schema: "schema",
};

const WorkflowInvocation_InvocationConfigFields: FieldMap = {
  fullyRefreshIncrementalTablesEnabled: "fully_refresh_incremental_tables_enabled",
  includedTags: "included_tags",
  includedTargets: {
    wireName: "included_targets",
    kind: "list",
    fields: WorkflowInvocation_InvocationConfig_IncludedTargetsFields,
  },
  queryPriority: "query_priority",
  serviceAccount: "service_account",
  transitiveDependenciesIncluded: "transitive_dependencies_included",
  transitiveDependentsIncluded: "transitive_dependents_included",
};

const WorkflowInvocation_InvocationTimingFields: FieldMap = {
  endTime: "end_time",
  startTime: "start_time",
};

const WorkflowInvocation_PipelineConfigFields: FieldMap = {
  path: "path",
  pipelineType: "pipeline_type",
};

const WorkflowInvocation_PrivateResourceMetadataFields: FieldMap = {
  userScoped: "user_scoped",
};

export interface WorkflowInvocationConfig {
  /** Immutable. The name of the compilation result to use for this invocation. Must be in the format `projects/* /locations/* /repositories/* /compilationResults/*`. */
  compilationResult?: string | Computed<string>;
  /** Describes encryption state of a resource. */
  dataEncryptionState?: WorkflowInvocation_DataEncryptionState | Computed<WorkflowInvocation_DataEncryptionState>;
  /** Includes various configuration options for a workflow invocation. If both `included_targets` and `included_tags` are unset, all actions will be included. */
  invocationConfig?: WorkflowInvocation_InvocationConfig | Computed<WorkflowInvocation_InvocationConfig>;
  /** Represents a time interval, encoded as a Timestamp start (inclusive) and a Timestamp end (exclusive). The start must be less than or equal to the end. When the start equals the end, the interval is empty (matches no time). When both start and end are unspecified, the interval matches any time. */
  invocationTiming?: WorkflowInvocation_InvocationTiming | Computed<WorkflowInvocation_InvocationTiming>;
  /** Defines the pipeline type and path within the Git repository. */
  pipelineConfig?: WorkflowInvocation_PipelineConfig | Computed<WorkflowInvocation_PipelineConfig>;
  /** Metadata used to identify if a resource is user scoped. */
  privateResourceMetadata?: WorkflowInvocation_PrivateResourceMetadata | Computed<WorkflowInvocation_PrivateResourceMetadata>;
  /** Immutable. The name of the workflow config to invoke. Must be in the format `projects/* /locations/* /repositories/* /workflowConfigs/*`. */
  workflowConfig?: string | Computed<string>;
}

export interface WorkflowInvocationAttrs {
  /** Immutable. The name of the compilation result to use for this invocation. Must be in the format `projects/* /locations/* /repositories/* /compilationResults/*`. */
  compilationResult: string;
  /** Describes encryption state of a resource. */
  dataEncryptionState: WorkflowInvocation_DataEncryptionState;
  /** Output only. All the metadata information that is used internally to serve the resource. For example: timestamps, flags, status fields, etc. The format of this field is a JSON string. */
  internalMetadata: string;
  /** Includes various configuration options for a workflow invocation. If both `included_targets` and `included_tags` are unset, all actions will be included. */
  invocationConfig: WorkflowInvocation_InvocationConfig;
  /** Represents a time interval, encoded as a Timestamp start (inclusive) and a Timestamp end (exclusive). The start must be less than or equal to the end. When the start equals the end, the interval is empty (matches no time). When both start and end are unspecified, the interval matches any time. */
  invocationTiming: WorkflowInvocation_InvocationTiming;
  /** Output only. The workflow invocation's name. */
  name: string;
  /** Defines the pipeline type and path within the Git repository. */
  pipelineConfig: WorkflowInvocation_PipelineConfig;
  /** Metadata used to identify if a resource is user scoped. */
  privateResourceMetadata: WorkflowInvocation_PrivateResourceMetadata;
  /** Output only. The resolved compilation result that was used to create this invocation. Will be in the format `projects/* /locations/* /repositories/* /compilationResults/*`. */
  resolvedCompilationResult: string;
  /** Output only. This workflow invocation's current state. */
  state: string;
  /** Immutable. The name of the workflow config to invoke. Must be in the format `projects/* /locations/* /repositories/* /workflowConfigs/*`. */
  workflowConfig: string;
}

export const WorkflowInvocation: ResourceBinding<WorkflowInvocationConfig, WorkflowInvocationAttrs> = {
  wireType: "google_dataform_workflow_invocation",
  fields: {
    compilationResult: "compilation_result",
    dataEncryptionState: {
      wireName: "data_encryption_state",
      kind: "object",
      fields: WorkflowInvocation_DataEncryptionStateFields,
    },
    invocationConfig: {
      wireName: "invocation_config",
      kind: "object",
      fields: WorkflowInvocation_InvocationConfigFields,
    },
    invocationTiming: {
      wireName: "invocation_timing",
      kind: "object",
      fields: WorkflowInvocation_InvocationTimingFields,
    },
    pipelineConfig: {
      wireName: "pipeline_config",
      kind: "object",
      fields: WorkflowInvocation_PipelineConfigFields,
    },
    privateResourceMetadata: {
      wireName: "private_resource_metadata",
      kind: "object",
      fields: WorkflowInvocation_PrivateResourceMetadataFields,
    },
    workflowConfig: "workflow_config",
  },
};
