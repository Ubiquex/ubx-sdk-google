// Code generated by `ubx sdk gen` from google_cloudkms_crypto_key_version. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface CryptoKeyVersion_Attestation_CertChains {
  /** Cavium certificate chain corresponding to the attestation. */
  caviumCerts?: string[] | Computed<string[]>;
  /** Google card certificate chain corresponding to the attestation. */
  googleCardCerts?: string[] | Computed<string[]>;
  /** Google partition certificate chain corresponding to the attestation. */
  googlePartitionCerts?: string[] | Computed<string[]>;
}

export interface CryptoKeyVersion_Attestation {
  /** Certificate chains needed to verify the attestation. Certificates in chains are PEM-encoded and are ordered based on https://tools.ietf.org/html/rfc5246#section-7.4.2. */
  certChains?: CryptoKeyVersion_Attestation_CertChains | Computed<CryptoKeyVersion_Attestation_CertChains>;
  /** Output only. The attestation data provided by the HSM when the key operation was performed. */
  content?: string | Computed<string>;
  /** Output only. The format of the attestation data. */
  format?: string | Computed<string>;
}

export interface CryptoKeyVersion_ExternalProtectionLevelOptions {
  /** Optional. The resource name of the backend environment where the key material of CryptoKeyVersions is associated with. Setting this field overrides the crypto_key_backend. This field may be set when CryptoKeyVersions is set to EXTERNAL_VPC. Format: `projects/* /locations/* /ekmConnections/*`. */
  ekmConnectionBackendOverride?: string | Computed<string>;
  /** Optional. The path to the external key material on the EKM when using EkmConnection e.g., "v0/my/key". Set this field instead of external_key_uri when using an EkmConnection. */
  ekmConnectionKeyPath?: string | Computed<string>;
  /** Optional. The URI for an external resource that this CryptoKeyVersion represents. */
  externalKeyUri?: string | Computed<string>;
}

const CryptoKeyVersion_Attestation_CertChainsFields: FieldMap = {
  caviumCerts: "cavium_certs",
  googleCardCerts: "google_card_certs",
  googlePartitionCerts: "google_partition_certs",
};

const CryptoKeyVersion_AttestationFields: FieldMap = {
  certChains: {
    wireName: "cert_chains",
    kind: "object",
    fields: CryptoKeyVersion_Attestation_CertChainsFields,
  },
  content: "content",
  format: "format",
};

const CryptoKeyVersion_ExternalProtectionLevelOptionsFields: FieldMap = {
  ekmConnectionBackendOverride: "ekm_connection_backend_override",
  ekmConnectionKeyPath: "ekm_connection_key_path",
  externalKeyUri: "external_key_uri",
};

export interface CryptoKeyVersionConfig {
  /** Contains an HSM-generated attestation about a key operation. For more information, see [Verifying attestations] (https://cloud.google.com/kms/docs/attest-key). */
  attestation?: CryptoKeyVersion_Attestation | Computed<CryptoKeyVersion_Attestation>;
  /** ExternalProtectionLevelOptions stores a group of additional fields for configuring a CryptoKeyVersion that are specific to the EXTERNAL protection level and EXTERNAL_VPC protection levels. */
  externalProtectionLevelOptions?: CryptoKeyVersion_ExternalProtectionLevelOptions | Computed<CryptoKeyVersion_ExternalProtectionLevelOptions>;
  /** The current state of the CryptoKeyVersion. */
  state?: string | Computed<string>;
  /** Immutable. Field indicating that the key may be wrapped by a trusted key. This field can be set for all key purposes except ENCRYPT_DECRYPT, and is only valid for keys with protection level HSM_SINGLE_TENANT. This field can only be set at creation or import time via CreateCryptoKeyVersion, or ImportCryptoKeyVersion. */
  trustedWrappingEnabled?: boolean | Computed<boolean>;
}

export interface CryptoKeyVersionAttrs {
  /** Output only. The CryptoKeyVersionAlgorithm that this CryptoKeyVersion supports. */
  algorithm: string;
  /** Contains an HSM-generated attestation about a key operation. For more information, see [Verifying attestations] (https://cloud.google.com/kms/docs/attest-key). */
  attestation: CryptoKeyVersion_Attestation;
  /** Output only. The time at which this CryptoKeyVersion was created. */
  createTime: string;
  /** Output only. The time this CryptoKeyVersion's key material was destroyed. Only present if state is DESTROYED. */
  destroyEventTime: string;
  /** Output only. The time this CryptoKeyVersion's key material is scheduled for destruction. Only present if state is DESTROY_SCHEDULED. */
  destroyTime: string;
  /** Output only. The root cause of the most recent external destruction failure. Only present if state is EXTERNAL_DESTRUCTION_FAILED. */
  externalDestructionFailureReason: string;
  /** ExternalProtectionLevelOptions stores a group of additional fields for configuring a CryptoKeyVersion that are specific to the EXTERNAL protection level and EXTERNAL_VPC protection levels. */
  externalProtectionLevelOptions: CryptoKeyVersion_ExternalProtectionLevelOptions;
  /** Output only. The time this CryptoKeyVersion's key material was generated. */
  generateTime: string;
  /** Output only. The root cause of the most recent generation failure. Only present if state is GENERATION_FAILED. */
  generationFailureReason: string;
  /** Output only. Field indicating that the key wrapping key is trusted. This field is only valid for key purpose AES_256_WRAPPING, and protection level HSM_SINGLE_TENANT. */
  hsmTrusted: boolean;
  /** Output only. The root cause of the most recent import failure. Only present if state is IMPORT_FAILED. */
  importFailureReason: string;
  /** Output only. The name of the ImportJob used in the most recent import of this CryptoKeyVersion. Only present if the underlying key material was imported. */
  importJob: string;
  /** Output only. The time at which this CryptoKeyVersion's key material was most recently imported. */
  importTime: string;
  /** Output only. The resource name for this CryptoKeyVersion in the format `projects/* /locations/* /keyRings/* /cryptoKeys/* /cryptoKeyVersions/*`. */
  name: string;
  /** Output only. The ProtectionLevel describing how crypto operations are performed with this CryptoKeyVersion. */
  protectionLevel: string;
  /** Output only. Whether or not this key version is eligible for reimport, by being specified as a target in ImportCryptoKeyVersionRequest.crypto_key_version. */
  reimportEligible: boolean;
  /** The current state of the CryptoKeyVersion. */
  state: string;
  /** Immutable. Field indicating that the key may be wrapped by a trusted key. This field can be set for all key purposes except ENCRYPT_DECRYPT, and is only valid for keys with protection level HSM_SINGLE_TENANT. This field can only be set at creation or import time via CreateCryptoKeyVersion, or ImportCryptoKeyVersion. */
  trustedWrappingEnabled: boolean;
}

export const CryptoKeyVersion: ResourceBinding<CryptoKeyVersionConfig, CryptoKeyVersionAttrs> = {
  wireType: "google_cloudkms_crypto_key_version",
  fields: {
    attestation: {
      wireName: "attestation",
      kind: "object",
      fields: CryptoKeyVersion_AttestationFields,
    },
    externalProtectionLevelOptions: {
      wireName: "external_protection_level_options",
      kind: "object",
      fields: CryptoKeyVersion_ExternalProtectionLevelOptionsFields,
    },
    state: "state",
    trustedWrappingEnabled: "trusted_wrapping_enabled",
  },
};
