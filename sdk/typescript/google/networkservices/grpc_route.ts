// Code generated by `ubx sdk gen` from google_networkservices_grpc_route. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface GrpcRoute_Rules_Action_Destinations {
  serviceName?: string | Computed<string>;
  weight?: number | Computed<number>;
}

export interface GrpcRoute_Rules_Action_FaultInjectionPolicy_Abort {
  httpStatus?: number | Computed<number>;
  percentage?: number | Computed<number>;
}

export interface GrpcRoute_Rules_Action_FaultInjectionPolicy_Delay {
  fixedDelay?: string | Computed<string>;
  percentage?: number | Computed<number>;
}

export interface GrpcRoute_Rules_Action_FaultInjectionPolicy {
  abort?: GrpcRoute_Rules_Action_FaultInjectionPolicy_Abort | Computed<GrpcRoute_Rules_Action_FaultInjectionPolicy_Abort>;
  delay?: GrpcRoute_Rules_Action_FaultInjectionPolicy_Delay | Computed<GrpcRoute_Rules_Action_FaultInjectionPolicy_Delay>;
}

export interface GrpcRoute_Rules_Action_RetryPolicy {
  numRetries?: number | Computed<number>;
  retryConditions?: string[] | Computed<string[]>;
}

export interface GrpcRoute_Rules_Action_StatefulSessionAffinity {
  cookieTtl?: string | Computed<string>;
}

export interface GrpcRoute_Rules_Action {
  destinations?: GrpcRoute_Rules_Action_Destinations[] | Computed<GrpcRoute_Rules_Action_Destinations[]>;
  faultInjectionPolicy?: GrpcRoute_Rules_Action_FaultInjectionPolicy | Computed<GrpcRoute_Rules_Action_FaultInjectionPolicy>;
  idleTimeout?: string | Computed<string>;
  retryPolicy?: GrpcRoute_Rules_Action_RetryPolicy | Computed<GrpcRoute_Rules_Action_RetryPolicy>;
  statefulSessionAffinity?: GrpcRoute_Rules_Action_StatefulSessionAffinity | Computed<GrpcRoute_Rules_Action_StatefulSessionAffinity>;
  timeout?: string | Computed<string>;
}

export interface GrpcRoute_Rules_Matches_Headers {
  key?: string | Computed<string>;
  type?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface GrpcRoute_Rules_Matches_Method {
  caseSensitive?: boolean | Computed<boolean>;
  grpcMethod?: string | Computed<string>;
  grpcService?: string | Computed<string>;
  type?: string | Computed<string>;
}

export interface GrpcRoute_Rules_Matches {
  headers?: GrpcRoute_Rules_Matches_Headers[] | Computed<GrpcRoute_Rules_Matches_Headers[]>;
  method?: GrpcRoute_Rules_Matches_Method | Computed<GrpcRoute_Rules_Matches_Method>;
}

export interface GrpcRoute_Rules {
  action?: GrpcRoute_Rules_Action | Computed<GrpcRoute_Rules_Action>;
  matches?: GrpcRoute_Rules_Matches[] | Computed<GrpcRoute_Rules_Matches[]>;
}

const GrpcRoute_Rules_Action_DestinationsFields: FieldMap = {
  serviceName: "service_name",
  weight: "weight",
};

const GrpcRoute_Rules_Action_FaultInjectionPolicy_AbortFields: FieldMap = {
  httpStatus: "http_status",
  percentage: "percentage",
};

const GrpcRoute_Rules_Action_FaultInjectionPolicy_DelayFields: FieldMap = {
  fixedDelay: "fixed_delay",
  percentage: "percentage",
};

const GrpcRoute_Rules_Action_FaultInjectionPolicyFields: FieldMap = {
  abort: {
    wireName: "abort",
    kind: "object",
    fields: GrpcRoute_Rules_Action_FaultInjectionPolicy_AbortFields,
  },
  delay: {
    wireName: "delay",
    kind: "object",
    fields: GrpcRoute_Rules_Action_FaultInjectionPolicy_DelayFields,
  },
};

const GrpcRoute_Rules_Action_RetryPolicyFields: FieldMap = {
  numRetries: "num_retries",
  retryConditions: "retry_conditions",
};

const GrpcRoute_Rules_Action_StatefulSessionAffinityFields: FieldMap = {
  cookieTtl: "cookie_ttl",
};

const GrpcRoute_Rules_ActionFields: FieldMap = {
  destinations: {
    wireName: "destinations",
    kind: "list",
    fields: GrpcRoute_Rules_Action_DestinationsFields,
  },
  faultInjectionPolicy: {
    wireName: "fault_injection_policy",
    kind: "object",
    fields: GrpcRoute_Rules_Action_FaultInjectionPolicyFields,
  },
  idleTimeout: "idle_timeout",
  retryPolicy: {
    wireName: "retry_policy",
    kind: "object",
    fields: GrpcRoute_Rules_Action_RetryPolicyFields,
  },
  statefulSessionAffinity: {
    wireName: "stateful_session_affinity",
    kind: "object",
    fields: GrpcRoute_Rules_Action_StatefulSessionAffinityFields,
  },
  timeout: "timeout",
};

const GrpcRoute_Rules_Matches_HeadersFields: FieldMap = {
  key: "key",
  type: "type",
  value: "value",
};

const GrpcRoute_Rules_Matches_MethodFields: FieldMap = {
  caseSensitive: "case_sensitive",
  grpcMethod: "grpc_method",
  grpcService: "grpc_service",
  type: "type",
};

const GrpcRoute_Rules_MatchesFields: FieldMap = {
  headers: {
    wireName: "headers",
    kind: "list",
    fields: GrpcRoute_Rules_Matches_HeadersFields,
  },
  method: {
    wireName: "method",
    kind: "object",
    fields: GrpcRoute_Rules_Matches_MethodFields,
  },
};

const GrpcRoute_RulesFields: FieldMap = {
  action: {
    wireName: "action",
    kind: "object",
    fields: GrpcRoute_Rules_ActionFields,
  },
  matches: {
    wireName: "matches",
    kind: "list",
    fields: GrpcRoute_Rules_MatchesFields,
  },
};

export interface GrpcRouteConfig {
  /** Output only. The timestamp when the resource was created. */
  createTime?: string | Computed<string>;
  /** Optional. A free-text description of the resource. Max length 1024 characters. */
  description?: string | Computed<string>;
  /** Optional. Gateways defines a list of gateways this GrpcRoute is attached to, as one of the routing rules to route the requests served by the gateway. Each gateway reference should match the pattern: `projects/* /locations/* /gateways/` */
  gateways?: string[] | Computed<string[]>;
  /** Required. Service hostnames with an optional port for which this route describes traffic. Format: [:] Hostname is the fully qualified domain name of a network host. This matches the RFC 1123 definition of a hostname with 2 notable exceptions: - IPs are not allowed. - A hostname may be prefixed with a wildcard label (`*.`). The wildcard label must appear by itself as the first label. Hostname can be "precise" which is a domain name without the terminating dot of a network host (e.g. `foo.example.com`) or "wildcard", which is a domain name prefixed with a single wildcard label (e.g. `*.example.com`). Note that as per RFC1035 and RFC1123, a label must consist of lower case alphanumeric characters or '-', and must start and end with an alphanumeric character. No other punctuation is allowed. The routes associated with a Mesh or Gateway must have unique hostnames. If you attempt to attach multiple routes with conflicting hostnames, the configuration will be rejected. For example, while it is acceptable for routes for the hostnames `*.foo.bar.com` and `*.bar.com` to be associated with the same route, it is not possible to associate two routes both with `*.bar.com` or both with `bar.com`. If a port is specified, then gRPC clients must use the channel URI with the port to match this rule (i.e. "xds:///service:123"), otherwise they must supply the URI without a port (i.e. "xds:///service"). */
  hostnames?: string[] | Computed<string[]>;
  /** Optional. Set of label tags associated with the GrpcRoute resource. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Optional. Meshes defines a list of meshes this GrpcRoute is attached to, as one of the routing rules to route the requests served by the mesh. Each mesh reference should match the pattern: `projects/* /locations/* /meshes/` */
  meshes?: string[] | Computed<string[]>;
  /** Identifier. Name of the GrpcRoute resource. It matches pattern `projects/* /locations/* /grpcRoutes/` */
  name?: string | Computed<string>;
  /** Required. A list of detailed rules defining how to route traffic. Within a single GrpcRoute, the GrpcRoute.RouteAction associated with the first matching GrpcRoute.RouteRule will be executed. At least one rule must be supplied. */
  rules?: GrpcRoute_Rules[] | Computed<GrpcRoute_Rules[]>;
  /** Output only. Server-defined URL of this resource */
  selfLink?: string | Computed<string>;
  /** Output only. The timestamp when the resource was updated. */
  updateTime?: string | Computed<string>;
}

export interface GrpcRouteAttrs {
  /** Output only. The timestamp when the resource was created. */
  createTime: string;
  /** Optional. A free-text description of the resource. Max length 1024 characters. */
  description: string;
  /** Optional. Gateways defines a list of gateways this GrpcRoute is attached to, as one of the routing rules to route the requests served by the gateway. Each gateway reference should match the pattern: `projects/* /locations/* /gateways/` */
  gateways: string[];
  /** Required. Service hostnames with an optional port for which this route describes traffic. Format: [:] Hostname is the fully qualified domain name of a network host. This matches the RFC 1123 definition of a hostname with 2 notable exceptions: - IPs are not allowed. - A hostname may be prefixed with a wildcard label (`*.`). The wildcard label must appear by itself as the first label. Hostname can be "precise" which is a domain name without the terminating dot of a network host (e.g. `foo.example.com`) or "wildcard", which is a domain name prefixed with a single wildcard label (e.g. `*.example.com`). Note that as per RFC1035 and RFC1123, a label must consist of lower case alphanumeric characters or '-', and must start and end with an alphanumeric character. No other punctuation is allowed. The routes associated with a Mesh or Gateway must have unique hostnames. If you attempt to attach multiple routes with conflicting hostnames, the configuration will be rejected. For example, while it is acceptable for routes for the hostnames `*.foo.bar.com` and `*.bar.com` to be associated with the same route, it is not possible to associate two routes both with `*.bar.com` or both with `bar.com`. If a port is specified, then gRPC clients must use the channel URI with the port to match this rule (i.e. "xds:///service:123"), otherwise they must supply the URI without a port (i.e. "xds:///service"). */
  hostnames: string[];
  /** Optional. Set of label tags associated with the GrpcRoute resource. */
  labels: Record<string, string>;
  /** Optional. Meshes defines a list of meshes this GrpcRoute is attached to, as one of the routing rules to route the requests served by the mesh. Each mesh reference should match the pattern: `projects/* /locations/* /meshes/` */
  meshes: string[];
  /** Identifier. Name of the GrpcRoute resource. It matches pattern `projects/* /locations/* /grpcRoutes/` */
  name: string;
  /** Required. A list of detailed rules defining how to route traffic. Within a single GrpcRoute, the GrpcRoute.RouteAction associated with the first matching GrpcRoute.RouteRule will be executed. At least one rule must be supplied. */
  rules: GrpcRoute_Rules[];
  /** Output only. Server-defined URL of this resource */
  selfLink: string;
  /** Output only. The timestamp when the resource was updated. */
  updateTime: string;
}

export const GrpcRoute: ResourceBinding<GrpcRouteConfig, GrpcRouteAttrs> = {
  wireType: "google_networkservices_grpc_route",
  fields: {
    createTime: "create_time",
    description: "description",
    gateways: "gateways",
    hostnames: "hostnames",
    labels: "labels",
    meshes: "meshes",
    name: "name",
    rules: {
      wireName: "rules",
      kind: "list",
      fields: GrpcRoute_RulesFields,
    },
    selfLink: "self_link",
    updateTime: "update_time",
  },
};
