// Code generated by `ubx sdk gen` from google_discoveryengine_document. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Document_AclInfo_Readers_Principals {
  /** An identifier for this entity in an external system. (AI-inferred) */
  externalEntityId?: string | Computed<string>;
  /** An identifier for the group this belongs to. (AI-inferred) */
  groupId?: string | Computed<string>;
  /** An identifier for the end user this applies to. (AI-inferred) */
  userId?: string | Computed<string>;
}

export interface Document_AclInfo_Readers {
  /** Whether this configuration applies across the whole identity provider, rather than a specific scope. (AI-inferred) */
  idpWide?: boolean | Computed<boolean>;
  /** The identity/identities this permission applies to. (AI-inferred) */
  principals?: Document_AclInfo_Readers_Principals[] | Computed<Document_AclInfo_Readers_Principals[]>;
}

export interface Document_AclInfo {
  /** Readers of the document. */
  readers?: Document_AclInfo_Readers[] | Computed<Document_AclInfo_Readers[]>;
}

export interface Document_Content {
  /** The MIME type of the content. Supported types: * `application/pdf` (PDF, only native PDFs are supported for now) * `text/html` (HTML) * `text/plain` (TXT) * `application/xml` or `text/xml` (XML) * `application/json` (JSON) * `application/vnd.openxmlformats-officedocument.wordprocessingml.document` (DOCX) * `application/vnd.openxmlformats-officedocument.presentationml.presentation` (PPTX) * `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` (XLSX) * `application/vnd.ms-excel.sheet.macroenabled.12` (XLSM) The following types are supported only if layout parser is enabled in the data store: * `image/bmp` (BMP) * `image/gif` (GIF) * `image/jpeg` (JPEG) * `image/png` (PNG) * `image/tiff` (TIFF) See https://www.iana.org/assignments/media-types/media-types.xhtml. */
  mimeType?: string | Computed<string>;
  /** The content represented as a stream of bytes. The maximum length is 1,000,000 bytes (1 MB / ~0.95 MiB). Note: As with all `bytes` fields, this field is represented as pure binary in Protocol Buffers and base64-encoded string in JSON. For example, `abc123!?$*&()'-=@~` should be represented as `YWJjMTIzIT8kKiYoKSctPUB+` in JSON. See https://developers.google.com/protocol-buffers/docs/proto3#json. */
  rawBytes?: string | Computed<string>;
  /** The URI of the content. Only Cloud Storage URIs (e.g. `gs://bucket-name/path/to/file`) are supported. The maximum file size is 2.5 MB for text-based formats, 200 MB for other formats. */
  uri?: string | Computed<string>;
}

export interface Document_IndexStatus_ErrorSamples {
  /** A machine-readable code identifying this result. (AI-inferred) */
  code?: number | Computed<number>;
  /** Additional detail about this result. (AI-inferred) */
  details?: Record<string, unknown>[] | Computed<Record<string, unknown>[]>;
  /** A human-readable description of this result. (AI-inferred) */
  message?: string | Computed<string>;
}

export interface Document_IndexStatus {
  /** A sample of errors encountered while indexing the document. If this field is populated, the document is not indexed due to errors. */
  errorSamples?: Document_IndexStatus_ErrorSamples[] | Computed<Document_IndexStatus_ErrorSamples[]>;
  /** The time when the document was indexed. If this field is populated, it means the document has been indexed. While documents typically become searchable within seconds of indexing, it can sometimes take up to a few hours. */
  indexTime?: string | Computed<string>;
  /** Immutable. The message indicates the document index is in progress. If this field is populated, the document index is pending. */
  pendingMessage?: string | Computed<string>;
}

const Document_AclInfo_Readers_PrincipalsFields: FieldMap = {
  externalEntityId: "external_entity_id",
  groupId: "group_id",
  userId: "user_id",
};

const Document_AclInfo_ReadersFields: FieldMap = {
  idpWide: "idp_wide",
  principals: {
    wireName: "principals",
    kind: "list",
    fields: Document_AclInfo_Readers_PrincipalsFields,
  },
};

const Document_AclInfoFields: FieldMap = {
  readers: {
    wireName: "readers",
    kind: "list",
    fields: Document_AclInfo_ReadersFields,
  },
};

const Document_ContentFields: FieldMap = {
  mimeType: "mime_type",
  rawBytes: "raw_bytes",
  uri: "uri",
};

const Document_IndexStatus_ErrorSamplesFields: FieldMap = {
  code: "code",
  details: "details",
  message: "message",
};

const Document_IndexStatusFields: FieldMap = {
  errorSamples: {
    wireName: "error_samples",
    kind: "list",
    fields: Document_IndexStatus_ErrorSamplesFields,
  },
  indexTime: "index_time",
  pendingMessage: "pending_message",
};

export interface DocumentConfig {
  /** ACL Information of the Document. */
  aclInfo?: Document_AclInfo | Computed<Document_AclInfo>;
  /** Unstructured data linked to this document. */
  content?: Document_Content | Computed<Document_Content>;
  /** Immutable. The identifier of the document. Id should conform to [RFC-1034](https://tools.ietf.org/html/rfc1034) standard with a length limit of 128 characters. */
  id?: string | Computed<string>;
  /** Index status of the document. */
  indexStatus?: Document_IndexStatus | Computed<Document_IndexStatus>;
  /** The JSON string representation of the document. It should conform to the registered Schema or an `INVALID_ARGUMENT` error is thrown. */
  jsonData?: string | Computed<string>;
  /** Immutable. The full resource name of the document. Format: `projects/{project}/locations/{location}/collections/{collection}/dataStores/{data_store}/branches/{branch}/documents/{document_id}`. This field must be a UTF-8 encoded string with a length limit of 1024 characters. */
  name?: string | Computed<string>;
  /** The identifier of the parent document. Currently supports at most two level document hierarchy. Id should conform to [RFC-1034](https://tools.ietf.org/html/rfc1034) standard with a length limit of 63 characters. */
  parentDocumentId?: string | Computed<string>;
  /** The identifier of the schema located in the same data store. */
  schemaId?: string | Computed<string>;
  /** The structured JSON data for the document. It should conform to the registered Schema or an `INVALID_ARGUMENT` error is thrown. */
  structData?: Record<string, unknown> | Computed<Record<string, unknown>>;
}

export interface DocumentAttrs {
  /** ACL Information of the Document. */
  aclInfo: Document_AclInfo;
  /** Unstructured data linked to this document. */
  content: Document_Content;
  /** Output only. This field is OUTPUT_ONLY. It contains derived data that are not in the original input document. */
  derivedStructData: Record<string, unknown>;
  /** Immutable. The identifier of the document. Id should conform to [RFC-1034](https://tools.ietf.org/html/rfc1034) standard with a length limit of 128 characters. */
  id: string;
  /** Index status of the document. */
  indexStatus: Document_IndexStatus;
  /** Output only. The time when the document was last indexed. If this field is populated, it means the document has been indexed. While documents typically become searchable within seconds of indexing, it can sometimes take up to a few hours. If this field is not populated, it means the document has never been indexed. */
  indexTime: string;
  /** The JSON string representation of the document. It should conform to the registered Schema or an `INVALID_ARGUMENT` error is thrown. */
  jsonData: string;
  /** Immutable. The full resource name of the document. Format: `projects/{project}/locations/{location}/collections/{collection}/dataStores/{data_store}/branches/{branch}/documents/{document_id}`. This field must be a UTF-8 encoded string with a length limit of 1024 characters. */
  name: string;
  /** The identifier of the parent document. Currently supports at most two level document hierarchy. Id should conform to [RFC-1034](https://tools.ietf.org/html/rfc1034) standard with a length limit of 63 characters. */
  parentDocumentId: string;
  /** The identifier of the schema located in the same data store. */
  schemaId: string;
  /** The structured JSON data for the document. It should conform to the registered Schema or an `INVALID_ARGUMENT` error is thrown. */
  structData: Record<string, unknown>;
}

export const Document: ResourceBinding<DocumentConfig, DocumentAttrs> = {
  wireType: "google_discoveryengine_document",
  fields: {
    aclInfo: {
      wireName: "acl_info",
      kind: "object",
      fields: Document_AclInfoFields,
    },
    content: {
      wireName: "content",
      kind: "object",
      fields: Document_ContentFields,
    },
    id: "id",
    indexStatus: {
      wireName: "index_status",
      kind: "object",
      fields: Document_IndexStatusFields,
    },
    jsonData: "json_data",
    name: "name",
    parentDocumentId: "parent_document_id",
    schemaId: "schema_id",
    structData: "struct_data",
  },
};
