// Code generated by `ubx sdk gen` from google_discoveryengine_v1beta_control. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1betaControl_BoostAction_InterpolationBoostSpec_ControlPoints {
  attributeValue?: string | Computed<string>;
  boostAmount?: number | Computed<number>;
}

export interface V1betaControl_BoostAction_InterpolationBoostSpec {
  /** Optional. The attribute type to be used to determine the boost amount. The attribute value can be derived from the field value of the specified field_name. In the case of numerical it is straightforward i.e. attribute_value = numerical_field_value. In the case of freshness however, attribute_value = (time.now() - datetime_field_value). */
  attributeType?: string | Computed<string>;
  /** Optional. The control points used to define the curve. The monotonic function (defined through the interpolation_type above) passes through the control points listed here. */
  controlPoints?: V1betaControl_BoostAction_InterpolationBoostSpec_ControlPoints[] | Computed<V1betaControl_BoostAction_InterpolationBoostSpec_ControlPoints[]>;
  /** Optional. The name of the field whose value will be used to determine the boost amount. */
  fieldName?: string | Computed<string>;
  /** Optional. The interpolation type to be applied to connect the control points listed below. */
  interpolationType?: string | Computed<string>;
}

export interface V1betaControl_BoostAction {
  /** Strength of the boost, which should be in [-1, 1]. Negative boost means demotion. Default is 0.0 (No-op). */
  boost?: number | Computed<number>;
  /** Required. Specifies which data store's documents can be boosted by this control. Full data store name e.g. projects/123/locations/global/collections/default_collection/dataStores/default_data_store */
  dataStore?: string | Computed<string>;
  /** Required. Specifies which products to apply the boost to. If no filter is provided all products will be boosted (No-op). Syntax documentation: https://cloud.google.com/retail/docs/filter-and-order Maximum length is 5000 characters. Otherwise an INVALID ARGUMENT error is thrown. */
  filter?: string | Computed<string>;
  /** Optional. Strength of the boost, which should be in [-1, 1]. Negative boost means demotion. Default is 0.0 (No-op). */
  fixedBoost?: number | Computed<number>;
  /** Specification for custom ranking based on customer specified attribute value. It provides more controls for customized ranking than the simple (condition, boost) combination above. */
  interpolationBoostSpec?: V1betaControl_BoostAction_InterpolationBoostSpec | Computed<V1betaControl_BoostAction_InterpolationBoostSpec>;
}

export interface V1betaControl_Conditions_ActiveTimeRange {
  endTime?: string | Computed<string>;
  startTime?: string | Computed<string>;
}

export interface V1betaControl_Conditions_QueryTerms {
  fullMatch?: boolean | Computed<boolean>;
  value?: string | Computed<string>;
}

export interface V1betaControl_Conditions {
  activeTimeRange?: V1betaControl_Conditions_ActiveTimeRange[] | Computed<V1betaControl_Conditions_ActiveTimeRange[]>;
  queryRegex?: string | Computed<string>;
  queryTerms?: V1betaControl_Conditions_QueryTerms[] | Computed<V1betaControl_Conditions_QueryTerms[]>;
}

export interface V1betaControl_FilterAction {
  /** Required. Specifies which data store's documents can be filtered by this control. Full data store name e.g. projects/123/locations/global/collections/default_collection/dataStores/default_data_store */
  dataStore?: string | Computed<string>;
  /** Required. A filter to apply on the matching condition results. Required Syntax documentation: https://cloud.google.com/retail/docs/filter-and-order Maximum length is 5000 characters. Otherwise an INVALID ARGUMENT error is thrown. */
  filter?: string | Computed<string>;
}

export interface V1betaControl_PromoteAction_SearchLinkPromotion {
  /** Optional. The Promotion description. Maximum length: 200 characters. */
  description?: string | Computed<string>;
  /** Optional. The Document the user wants to promote. For site search, leave unset and only populate uri. Can be set along with uri. */
  document?: string | Computed<string>;
  /** Optional. The enabled promotion will be returned for any serving configs associated with the parent of the control this promotion is attached to. This flag is used for basic site search only. */
  enabled?: boolean | Computed<boolean>;
  /** Optional. The promotion thumbnail image url. */
  imageUri?: string | Computed<string>;
  /** Required. The title of the promotion. Maximum length: 160 characters. */
  title?: string | Computed<string>;
  /** Optional. The URL for the page the user wants to promote. Must be set for site search. For other verticals, this is optional. */
  uri?: string | Computed<string>;
}

export interface V1betaControl_PromoteAction {
  /** Required. Data store with which this promotion is attached to. */
  dataStore?: string | Computed<string>;
  /** Promotion proto includes uri and other helping information to display the promotion. */
  searchLinkPromotion?: V1betaControl_PromoteAction_SearchLinkPromotion | Computed<V1betaControl_PromoteAction_SearchLinkPromotion>;
}

export interface V1betaControl_RedirectAction {
  /** Required. The URI to which the shopper will be redirected. Required. URI must have length equal or less than 2000 characters. Otherwise an INVALID ARGUMENT error is thrown. */
  redirectUri?: string | Computed<string>;
}

export interface V1betaControl_SynonymsAction {
  /** Defines a set of synonyms. Can specify up to 100 synonyms. Must specify at least 2 synonyms. Otherwise an INVALID ARGUMENT error is thrown. */
  synonyms?: string[] | Computed<string[]>;
}

const V1betaControl_BoostAction_InterpolationBoostSpec_ControlPointsFields: FieldMap = {
  attributeValue: "attribute_value",
  boostAmount: "boost_amount",
};

const V1betaControl_BoostAction_InterpolationBoostSpecFields: FieldMap = {
  attributeType: "attribute_type",
  controlPoints: {
    wireName: "control_points",
    kind: "list",
    fields: V1betaControl_BoostAction_InterpolationBoostSpec_ControlPointsFields,
  },
  fieldName: "field_name",
  interpolationType: "interpolation_type",
};

const V1betaControl_BoostActionFields: FieldMap = {
  boost: "boost",
  dataStore: "data_store",
  filter: "filter",
  fixedBoost: "fixed_boost",
  interpolationBoostSpec: {
    wireName: "interpolation_boost_spec",
    kind: "object",
    fields: V1betaControl_BoostAction_InterpolationBoostSpecFields,
  },
};

const V1betaControl_Conditions_ActiveTimeRangeFields: FieldMap = {
  endTime: "end_time",
  startTime: "start_time",
};

const V1betaControl_Conditions_QueryTermsFields: FieldMap = {
  fullMatch: "full_match",
  value: "value",
};

const V1betaControl_ConditionsFields: FieldMap = {
  activeTimeRange: {
    wireName: "active_time_range",
    kind: "list",
    fields: V1betaControl_Conditions_ActiveTimeRangeFields,
  },
  queryRegex: "query_regex",
  queryTerms: {
    wireName: "query_terms",
    kind: "list",
    fields: V1betaControl_Conditions_QueryTermsFields,
  },
};

const V1betaControl_FilterActionFields: FieldMap = {
  dataStore: "data_store",
  filter: "filter",
};

const V1betaControl_PromoteAction_SearchLinkPromotionFields: FieldMap = {
  description: "description",
  document: "document",
  enabled: "enabled",
  imageUri: "image_uri",
  title: "title",
  uri: "uri",
};

const V1betaControl_PromoteActionFields: FieldMap = {
  dataStore: "data_store",
  searchLinkPromotion: {
    wireName: "search_link_promotion",
    kind: "object",
    fields: V1betaControl_PromoteAction_SearchLinkPromotionFields,
  },
};

const V1betaControl_RedirectActionFields: FieldMap = {
  redirectUri: "redirect_uri",
};

const V1betaControl_SynonymsActionFields: FieldMap = {
  synonyms: "synonyms",
};

export interface V1betaControlConfig {
  /** Output only. List of all ServingConfig IDs this control is attached to. May take up to 10 minutes to update after changes. */
  associatedServingConfigIds?: string[] | Computed<string[]>;
  /** Adjusts order of products in returned list. */
  boostAction?: V1betaControl_BoostAction | Computed<V1betaControl_BoostAction>;
  /** Determines when the associated action will trigger. Omit to always apply the action. Currently only a single condition may be specified. Otherwise an INVALID ARGUMENT error is thrown. */
  conditions?: V1betaControl_Conditions[] | Computed<V1betaControl_Conditions[]>;
  /** Required. Human readable name. The identifier used in UI views. Must be UTF-8 encoded string. Length limit is 128 characters. Otherwise an INVALID ARGUMENT error is thrown. */
  displayName?: string | Computed<string>;
  /** Specified which products may be included in results. Uses same filter as boost. */
  filterAction?: V1betaControl_FilterAction | Computed<V1betaControl_FilterAction>;
  /** Immutable. Fully qualified name `projects/* /locations/global/dataStore/* /controls/*` */
  name?: string | Computed<string>;
  /** Promote certain links based on some trigger queries. Example: Promote shoe store link when searching for `shoe` keyword. The link can be outside of associated data store. */
  promoteAction?: V1betaControl_PromoteAction | Computed<V1betaControl_PromoteAction>;
  /** Redirects a shopper to the provided URI. */
  redirectAction?: V1betaControl_RedirectAction | Computed<V1betaControl_RedirectAction>;
  /** Required. Immutable. What solution the control belongs to. Must be compatible with vertical of resource. Otherwise an INVALID ARGUMENT error is thrown. */
  solutionType?: string | Computed<string>;
  /** Creates a set of terms that will act as synonyms of one another. Example: "happy" will also be considered as "glad", "glad" will also be considered as "happy". */
  synonymsAction?: V1betaControl_SynonymsAction | Computed<V1betaControl_SynonymsAction>;
  /** Specifies the use case for the control. Affects what condition fields can be set. Only applies to SOLUTION_TYPE_SEARCH. Currently only allow one use case per control. Must be set when solution_type is SolutionType.SOLUTION_TYPE_SEARCH. */
  useCases?: string[] | Computed<string[]>;
}

export interface V1betaControlAttrs {
  /** Output only. List of all ServingConfig IDs this control is attached to. May take up to 10 minutes to update after changes. */
  associatedServingConfigIds: string[];
  /** Adjusts order of products in returned list. */
  boostAction: V1betaControl_BoostAction;
  /** Determines when the associated action will trigger. Omit to always apply the action. Currently only a single condition may be specified. Otherwise an INVALID ARGUMENT error is thrown. */
  conditions: V1betaControl_Conditions[];
  /** Required. Human readable name. The identifier used in UI views. Must be UTF-8 encoded string. Length limit is 128 characters. Otherwise an INVALID ARGUMENT error is thrown. */
  displayName: string;
  /** Specified which products may be included in results. Uses same filter as boost. */
  filterAction: V1betaControl_FilterAction;
  /** Immutable. Fully qualified name `projects/* /locations/global/dataStore/* /controls/*` */
  name: string;
  /** Promote certain links based on some trigger queries. Example: Promote shoe store link when searching for `shoe` keyword. The link can be outside of associated data store. */
  promoteAction: V1betaControl_PromoteAction;
  /** Redirects a shopper to the provided URI. */
  redirectAction: V1betaControl_RedirectAction;
  /** Required. Immutable. What solution the control belongs to. Must be compatible with vertical of resource. Otherwise an INVALID ARGUMENT error is thrown. */
  solutionType: string;
  /** Creates a set of terms that will act as synonyms of one another. Example: "happy" will also be considered as "glad", "glad" will also be considered as "happy". */
  synonymsAction: V1betaControl_SynonymsAction;
  /** Specifies the use case for the control. Affects what condition fields can be set. Only applies to SOLUTION_TYPE_SEARCH. Currently only allow one use case per control. Must be set when solution_type is SolutionType.SOLUTION_TYPE_SEARCH. */
  useCases: string[];
}

export const V1betaControl: ResourceBinding<V1betaControlConfig, V1betaControlAttrs> = {
  wireType: "google_discoveryengine_v1beta_control",
  fields: {
    associatedServingConfigIds: "associated_serving_config_ids",
    boostAction: {
      wireName: "boost_action",
      kind: "object",
      fields: V1betaControl_BoostActionFields,
    },
    conditions: {
      wireName: "conditions",
      kind: "list",
      fields: V1betaControl_ConditionsFields,
    },
    displayName: "display_name",
    filterAction: {
      wireName: "filter_action",
      kind: "object",
      fields: V1betaControl_FilterActionFields,
    },
    name: "name",
    promoteAction: {
      wireName: "promote_action",
      kind: "object",
      fields: V1betaControl_PromoteActionFields,
    },
    redirectAction: {
      wireName: "redirect_action",
      kind: "object",
      fields: V1betaControl_RedirectActionFields,
    },
    solutionType: "solution_type",
    synonymsAction: {
      wireName: "synonyms_action",
      kind: "object",
      fields: V1betaControl_SynonymsActionFields,
    },
    useCases: "use_cases",
  },
};
