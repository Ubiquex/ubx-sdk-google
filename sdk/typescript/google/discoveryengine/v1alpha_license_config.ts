// Code generated by `ubx sdk gen` from google_discoveryengine_v1alpha_license_config. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1alphaLicenseConfig_AlertPolicyResourceConfig_AlertEnrollments {
  alertId?: string | Computed<string>;
  enrollState?: string | Computed<string>;
  notificationParams?: Record<string, string> | Computed<Record<string, string>>;
}

export interface V1alphaLicenseConfig_AlertPolicyResourceConfig_ContactDetails {
  emailAddress?: string | Computed<string>;
}

export interface V1alphaLicenseConfig_AlertPolicyResourceConfig {
  /** Optional. The enrollment state of each alert. */
  alertEnrollments?: V1alphaLicenseConfig_AlertPolicyResourceConfig_AlertEnrollments[] | Computed<V1alphaLicenseConfig_AlertPolicyResourceConfig_AlertEnrollments[]>;
  /** Immutable. The fully qualified resource name of the AlertPolicy. */
  alertPolicy?: string | Computed<string>;
  /** Optional. The contact details for each alert policy. */
  contactDetails?: V1alphaLicenseConfig_AlertPolicyResourceConfig_ContactDetails[] | Computed<V1alphaLicenseConfig_AlertPolicyResourceConfig_ContactDetails[]>;
  /** Optional. The language code used for notifications */
  languageCode?: string | Computed<string>;
  /** Optional. The region code used of the user that subscribed to the alert policy. */
  regionCode?: string | Computed<string>;
}

export interface V1alphaLicenseConfig_EarlyTerminationDate {
  /** Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant. */
  day?: number | Computed<number>;
  /** Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day. */
  month?: number | Computed<number>;
  /** Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year. */
  year?: number | Computed<number>;
}

const V1alphaLicenseConfig_AlertPolicyResourceConfig_AlertEnrollmentsFields: FieldMap = {
  alertId: "alert_id",
  enrollState: "enroll_state",
  notificationParams: "notification_params",
};

const V1alphaLicenseConfig_AlertPolicyResourceConfig_ContactDetailsFields: FieldMap = {
  emailAddress: "email_address",
};

const V1alphaLicenseConfig_AlertPolicyResourceConfigFields: FieldMap = {
  alertEnrollments: {
    wireName: "alert_enrollments",
    kind: "list",
    fields: V1alphaLicenseConfig_AlertPolicyResourceConfig_AlertEnrollmentsFields,
  },
  alertPolicy: "alert_policy",
  contactDetails: {
    wireName: "contact_details",
    kind: "list",
    fields: V1alphaLicenseConfig_AlertPolicyResourceConfig_ContactDetailsFields,
  },
  languageCode: "language_code",
  regionCode: "region_code",
};

const V1alphaLicenseConfig_EarlyTerminationDateFields: FieldMap = {
  day: "day",
  month: "month",
  year: "year",
};

export interface V1alphaLicenseConfigConfig {
  /** The resource level alert config. Used in: * UserLicense * EngineUserData The AlertPolicyConfig in data connector is of same usage. No easy way to migrate. */
  alertPolicyResourceConfig?: V1alphaLicenseConfig_AlertPolicyResourceConfig | Computed<V1alphaLicenseConfig_AlertPolicyResourceConfig>;
  /** Optional. Whether the license config should be auto renewed when it reaches the end date. */
  autoRenew?: boolean | Computed<boolean>;
  /** Output only. Indication of whether the subscription is terminated earlier than the expiration date. This is usually terminated by pipeline once the subscription gets terminated from subsv3. */
  earlyTerminated?: boolean | Computed<boolean>;
  /** Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp */
  earlyTerminationDate?: V1alphaLicenseConfig_EarlyTerminationDate | Computed<V1alphaLicenseConfig_EarlyTerminationDate>;
  /** Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp */
  endDate?: V1alphaLicenseConfig_EarlyTerminationDate | Computed<V1alphaLicenseConfig_EarlyTerminationDate>;
  /** Optional. Whether the license config is for free trial. */
  freeTrial?: boolean | Computed<boolean>;
  /** Output only. Whether the license config is for Gemini bundle. */
  geminiBundle?: boolean | Computed<boolean>;
  /** Optional. Timestamp of the most recent user-initiated update (seat count change or subscription term change). Unlike `update_time`, this field is only stamped when a customer explicitly updates the license (e.g. via the UI), and is not touched by system-driven writes (subscription pipeline, BALC propagation, etc.). */
  lastUserUpdateTime?: string | Computed<string>;
  /** Required. Number of licenses purchased. */
  licenseCount?: string | Computed<string>;
  /** Immutable. Identifier. The fully qualified resource name of the license config. Format: `projects/{project}/locations/{location}/licenseConfigs/{license_config}` */
  name?: string | Computed<string>;
  /** Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp */
  startDate?: V1alphaLicenseConfig_EarlyTerminationDate | Computed<V1alphaLicenseConfig_EarlyTerminationDate>;
  /** Output only. The state of the license config. */
  state?: string | Computed<string>;
  /** Required. Subscription term. */
  subscriptionTerm?: string | Computed<string>;
  /** Required. Subscription tier information for the license config. */
  subscriptionTier?: string | Computed<string>;
}

export interface V1alphaLicenseConfigAttrs {
  /** The resource level alert config. Used in: * UserLicense * EngineUserData The AlertPolicyConfig in data connector is of same usage. No easy way to migrate. */
  alertPolicyResourceConfig: V1alphaLicenseConfig_AlertPolicyResourceConfig;
  /** Optional. Whether the license config should be auto renewed when it reaches the end date. */
  autoRenew: boolean;
  /** Output only. Indication of whether the subscription is terminated earlier than the expiration date. This is usually terminated by pipeline once the subscription gets terminated from subsv3. */
  earlyTerminated: boolean;
  /** Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp */
  earlyTerminationDate: V1alphaLicenseConfig_EarlyTerminationDate;
  /** Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp */
  endDate: V1alphaLicenseConfig_EarlyTerminationDate;
  /** Optional. Whether the license config is for free trial. */
  freeTrial: boolean;
  /** Output only. Whether the license config is for Gemini bundle. */
  geminiBundle: boolean;
  /** Optional. Timestamp of the most recent user-initiated update (seat count change or subscription term change). Unlike `update_time`, this field is only stamped when a customer explicitly updates the license (e.g. via the UI), and is not touched by system-driven writes (subscription pipeline, BALC propagation, etc.). */
  lastUserUpdateTime: string;
  /** Required. Number of licenses purchased. */
  licenseCount: string;
  /** Immutable. Identifier. The fully qualified resource name of the license config. Format: `projects/{project}/locations/{location}/licenseConfigs/{license_config}` */
  name: string;
  /** Represents a whole or partial calendar date, such as a birthday. The time of day and time zone are either specified elsewhere or are insignificant. The date is relative to the Gregorian Calendar. This can represent one of the following: * A full date, with non-zero year, month, and day values. * A month and day, with a zero year (for example, an anniversary). * A year on its own, with a zero month and a zero day. * A year and month, with a zero day (for example, a credit card expiration date). Related types: * google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp */
  startDate: V1alphaLicenseConfig_EarlyTerminationDate;
  /** Output only. The state of the license config. */
  state: string;
  /** Required. Subscription term. */
  subscriptionTerm: string;
  /** Required. Subscription tier information for the license config. */
  subscriptionTier: string;
}

export const V1alphaLicenseConfig: ResourceBinding<V1alphaLicenseConfigConfig, V1alphaLicenseConfigAttrs> = {
  wireType: "google_discoveryengine_v1alpha_license_config",
  fields: {
    alertPolicyResourceConfig: {
      wireName: "alert_policy_resource_config",
      kind: "object",
      fields: V1alphaLicenseConfig_AlertPolicyResourceConfigFields,
    },
    autoRenew: "auto_renew",
    earlyTerminated: "early_terminated",
    earlyTerminationDate: {
      wireName: "early_termination_date",
      kind: "object",
      fields: V1alphaLicenseConfig_EarlyTerminationDateFields,
    },
    endDate: {
      wireName: "end_date",
      kind: "object",
      fields: V1alphaLicenseConfig_EarlyTerminationDateFields,
    },
    freeTrial: "free_trial",
    geminiBundle: "gemini_bundle",
    lastUserUpdateTime: "last_user_update_time",
    licenseCount: "license_count",
    name: "name",
    startDate: {
      wireName: "start_date",
      kind: "object",
      fields: V1alphaLicenseConfig_EarlyTerminationDateFields,
    },
    state: "state",
    subscriptionTerm: "subscription_term",
    subscriptionTier: "subscription_tier",
  },
};
