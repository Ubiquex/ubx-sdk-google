// Code generated by `ubx sdk gen` from google_identitytoolkit_oauth_idp_config. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface OauthIdpConfig_ResponseType {
  /** If true, authorization code is returned from IdP's authorization endpoint. */
  code?: boolean | Computed<boolean>;
  /** If true, ID token is returned from IdP's authorization endpoint. */
  idToken?: boolean | Computed<boolean>;
  /** Do not use. The `token` response type is not supported at the moment. */
  token?: boolean | Computed<boolean>;
}

const OauthIdpConfig_ResponseTypeFields: FieldMap = {
  code: "code",
  idToken: "id_token",
  token: "token",
};

export interface OauthIdpConfigConfig {
  /** The client id of an OAuth client. */
  clientId?: string | Computed<string>;
  /** The client secret of the OAuth client, to enable OIDC code flow. */
  clientSecret?: string | Computed<string>;
  /** The config's display name set by developers. */
  displayName?: string | Computed<string>;
  /** True if allows the user to sign in with the provider. */
  enabled?: boolean | Computed<boolean>;
  /** For OIDC Idps, the issuer identifier. */
  issuer?: string | Computed<string>;
  /** The name of the OAuthIdpConfig resource, for example: 'projects/my-awesome-project/oauthIdpConfigs/oauth-config-id'. Ignored during create requests. */
  name?: string | Computed<string>;
  /** The response type to request for in the OAuth authorization flow. You can set either `id_token` or `code` to true, but not both. Setting both types to be simultaneously true (`{code: true, id_token: true}`) is not yet supported. See https://openid.net/specs/openid-connect-core-1_0.html#Authentication for a mapping of response type to OAuth 2.0 flow. */
  responseType?: OauthIdpConfig_ResponseType | Computed<OauthIdpConfig_ResponseType>;
}

export interface OauthIdpConfigAttrs {
  /** The client id of an OAuth client. */
  clientId: string;
  /** The client secret of the OAuth client, to enable OIDC code flow. */
  clientSecret: string;
  /** The config's display name set by developers. */
  displayName: string;
  /** True if allows the user to sign in with the provider. */
  enabled: boolean;
  /** For OIDC Idps, the issuer identifier. */
  issuer: string;
  /** The name of the OAuthIdpConfig resource, for example: 'projects/my-awesome-project/oauthIdpConfigs/oauth-config-id'. Ignored during create requests. */
  name: string;
  /** The response type to request for in the OAuth authorization flow. You can set either `id_token` or `code` to true, but not both. Setting both types to be simultaneously true (`{code: true, id_token: true}`) is not yet supported. See https://openid.net/specs/openid-connect-core-1_0.html#Authentication for a mapping of response type to OAuth 2.0 flow. */
  responseType: OauthIdpConfig_ResponseType;
}

export const OauthIdpConfig: ResourceBinding<OauthIdpConfigConfig, OauthIdpConfigAttrs> = {
  wireType: "google_identitytoolkit_oauth_idp_config",
  fields: {
    clientId: "client_id",
    clientSecret: "client_secret",
    displayName: "display_name",
    enabled: "enabled",
    issuer: "issuer",
    name: "name",
    responseType: {
      wireName: "response_type",
      kind: "object",
      fields: OauthIdpConfig_ResponseTypeFields,
    },
  },
};
