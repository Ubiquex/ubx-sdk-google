// Code generated by `ubx sdk gen` from google_gkehub_v1beta_membership. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1betaMembership_Authority {
  /** Output only. An identity provider that reflects the `issuer` in the workload identity pool. */
  identityProvider?: string | Computed<string>;
  /** Optional. A JSON Web Token (JWT) issuer URI. `issuer` must start with `https://` and be a valid URL with length <2000 characters, it must use `location` rather than `zone` for GKE clusters. If set, then Google will allow valid OIDC tokens from this issuer to authenticate within the workload_identity_pool. OIDC discovery will be performed on this URI to validate tokens from the issuer. Clearing `issuer` disables Workload Identity. `issuer` cannot be directly modified; it must be cleared (and Workload Identity disabled) before using a new issuer (and re-enabling Workload Identity). */
  issuer?: string | Computed<string>;
  /** Optional. OIDC verification keys for this Membership in JWKS format (RFC 7517). When this field is set, OIDC discovery will NOT be performed on `issuer`, and instead OIDC tokens will be validated using this field. */
  oidcJwks?: string | Computed<string>;
  /** Optional. Output only. The identity provider for the scope-tenancy workload identity pool. */
  scopeTenancyIdentityProvider?: string | Computed<string>;
  /** Optional. Output only. The name of the scope-tenancy workload identity pool. This pool is set in the fleet-level feature. */
  scopeTenancyWorkloadIdentityPool?: string | Computed<string>;
  /** Output only. The name of the workload identity pool in which `issuer` will be recognized. There is a single Workload Identity Pool per Hub that is shared between all Memberships that belong to that Hub. For a Hub hosted in {PROJECT_ID}, the workload pool format is `{PROJECT_ID}.hub.id.goog`, although this is subject to change in newer versions of this API. */
  workloadIdentityPool?: string | Computed<string>;
}

export interface V1betaMembership_Endpoint_ApplianceCluster {
  /** Immutable. Self-link of the Google Cloud resource for the Appliance Cluster. For example: //transferappliance.googleapis.com/projects/my-project/locations/us-west1-a/appliances/my-appliance */
  resourceLink?: string | Computed<string>;
}

export interface V1betaMembership_Endpoint_GkeCluster {
  /** Output only. If cluster_missing is set then it denotes that the GKE cluster no longer exists in the GKE Control Plane. */
  clusterMissing?: boolean | Computed<boolean>;
  /** Immutable. Self-link of the Google Cloud resource for the GKE cluster. For example: //container.googleapis.com/projects/my-project/locations/us-west1-a/clusters/my-cluster Zonal clusters are also supported. */
  resourceLink?: string | Computed<string>;
}

export interface V1betaMembership_Endpoint_KubernetesMetadata {
  /** Output only. Kubernetes API server version string as reported by `/version`. */
  kubernetesApiServerVersion?: string | Computed<string>;
  /** Output only. The total memory capacity as reported by the sum of all Kubernetes nodes resources, defined in MB. */
  memoryMb?: number | Computed<number>;
  /** Output only. Node count as reported by Kubernetes nodes resources. */
  nodeCount?: number | Computed<number>;
  /** Output only. Node providerID as reported by the first node in the list of nodes on the Kubernetes endpoint. On Kubernetes platforms that support zero-node clusters (like GKE on Google Cloud), the node_count will be zero and the node_provider_id will be empty. */
  nodeProviderId?: string | Computed<string>;
  /** Output only. The time at which these details were last updated. This update_time is different from the Membership-level update_time since EndpointDetails are updated internally for API consumers. */
  updateTime?: string | Computed<string>;
  /** Output only. vCPU count as reported by Kubernetes nodes resources. */
  vcpuCount?: number | Computed<number>;
}

export interface V1betaMembership_Endpoint_KubernetesResource_ConnectResources {
  clusterScoped?: boolean | Computed<boolean>;
  manifest?: string | Computed<string>;
}

export interface V1betaMembership_Endpoint_KubernetesResource_ResourceOptions {
  /** Optional. The Connect agent version to use for connect_resources. Defaults to the latest GKE Connect version. The version must be a currently supported version, obsolete versions will be rejected. */
  connectVersion?: string | Computed<string>;
  /** Optional. Git version of the Kubernetes cluster. This is only used to gate the Connect Agent migration to svc.id.goog on GDC-SO 1.33.100 patch and above. */
  k8sGitVersion?: string | Computed<string>;
  /** Optional. Major and minor version of the Kubernetes cluster. This is only used to determine which version to use for the CustomResourceDefinition resources, `apiextensions/v1beta1` or`apiextensions/v1`. */
  k8sVersion?: string | Computed<string>;
  /** Optional. Use `apiextensions/v1beta1` instead of `apiextensions/v1` for CustomResourceDefinition resources. This option should be set for clusters with Kubernetes apiserver versions <1.16. */
  v1beta1Crd?: boolean | Computed<boolean>;
}

export interface V1betaMembership_Endpoint_KubernetesResource {
  /** Output only. The Kubernetes resources for installing the GKE Connect agent This field is only populated in the Membership returned from a successful long-running operation from CreateMembership or UpdateMembership. It is not populated during normal GetMembership or ListMemberships requests. To get the resource manifest after the initial registration, the caller should make a UpdateMembership call with an empty field mask. */
  connectResources?: V1betaMembership_Endpoint_KubernetesResource_ConnectResources[] | Computed<V1betaMembership_Endpoint_KubernetesResource_ConnectResources[]>;
  /** Input only. The YAML representation of the Membership CR. This field is ignored for GKE clusters where Hub can read the CR directly. Callers should provide the CR that is currently present in the cluster during CreateMembership or UpdateMembership, or leave this field empty if none exists. The CR manifest is used to validate the cluster has not been registered with another Membership. */
  membershipCrManifest?: string | Computed<string>;
  /** Output only. Additional Kubernetes resources that need to be applied to the cluster after Membership creation, and after every update. This field is only populated in the Membership returned from a successful long-running operation from CreateMembership or UpdateMembership. It is not populated during normal GetMembership or ListMemberships requests. To get the resource manifest after the initial registration, the caller should make a UpdateMembership call with an empty field mask. */
  membershipResources?: V1betaMembership_Endpoint_KubernetesResource_ConnectResources[] | Computed<V1betaMembership_Endpoint_KubernetesResource_ConnectResources[]>;
  /** ResourceOptions represent options for Kubernetes resource generation. */
  resourceOptions?: V1betaMembership_Endpoint_KubernetesResource_ResourceOptions | Computed<V1betaMembership_Endpoint_KubernetesResource_ResourceOptions>;
}

export interface V1betaMembership_Endpoint_OnPremCluster {
  /** Immutable. Whether the cluster is an admin cluster. */
  adminCluster?: boolean | Computed<boolean>;
  /** Output only. If cluster_missing is set then it denotes that API(gkeonprem.googleapis.com) resource for this GKE On-Prem cluster no longer exists. */
  clusterMissing?: boolean | Computed<boolean>;
  /** Immutable. The on prem cluster's type. */
  clusterType?: string | Computed<string>;
  /** Immutable. Self-link of the Google Cloud resource for the GKE On-Prem cluster. For example: //gkeonprem.googleapis.com/projects/my-project/locations/us-west1-a/vmwareClusters/my-cluster //gkeonprem.googleapis.com/projects/my-project/locations/us-west1-a/bareMetalClusters/my-cluster */
  resourceLink?: string | Computed<string>;
}

export interface V1betaMembership_Endpoint {
  /** ApplianceCluster contains information specific to GDC Edge Appliance Clusters. */
  applianceCluster?: V1betaMembership_Endpoint_ApplianceCluster | Computed<V1betaMembership_Endpoint_ApplianceCluster>;
  /** EdgeCluster contains information specific to Google Edge Clusters. */
  edgeCluster?: V1betaMembership_Endpoint_ApplianceCluster | Computed<V1betaMembership_Endpoint_ApplianceCluster>;
  /** GkeCluster contains information specific to GKE clusters. */
  gkeCluster?: V1betaMembership_Endpoint_GkeCluster | Computed<V1betaMembership_Endpoint_GkeCluster>;
  /** Output only. Whether the lifecycle of this membership is managed by a google cluster platform service. */
  googleManaged?: boolean | Computed<boolean>;
  /** KubernetesMetadata provides informational metadata for Memberships representing Kubernetes clusters. */
  kubernetesMetadata?: V1betaMembership_Endpoint_KubernetesMetadata | Computed<V1betaMembership_Endpoint_KubernetesMetadata>;
  /** KubernetesResource contains the YAML manifests and configuration for Membership Kubernetes resources in the cluster. After CreateMembership or UpdateMembership, these resources should be re-applied in the cluster. */
  kubernetesResource?: V1betaMembership_Endpoint_KubernetesResource | Computed<V1betaMembership_Endpoint_KubernetesResource>;
  /** MultiCloudCluster contains information specific to GKE Multi-Cloud clusters. */
  multiCloudCluster?: V1betaMembership_Endpoint_GkeCluster | Computed<V1betaMembership_Endpoint_GkeCluster>;
  /** OnPremCluster contains information specific to GKE On-Prem clusters. */
  onPremCluster?: V1betaMembership_Endpoint_OnPremCluster | Computed<V1betaMembership_Endpoint_OnPremCluster>;
}

export interface V1betaMembership_MonitoringConfig {
  /** Optional. Cluster name used to report metrics. For Anthos on VMWare/Baremetal/MultiCloud clusters, it would be in format {cluster_type}/{cluster_name}, e.g., "awsClusters/cluster_1". */
  cluster?: string | Computed<string>;
  /** Optional. For GKE and Multicloud clusters, this is the UUID of the cluster resource. For VMWare and Baremetal clusters, this is the kube-system UID. */
  clusterHash?: string | Computed<string>;
  /** Optional. Kubernetes system metrics, if available, are written to this prefix. This defaults to kubernetes.io for GKE, and kubernetes.io/anthos for Anthos eventually. Noted: Anthos MultiCloud will have kubernetes.io prefix today but will migration to be under kubernetes.io/anthos. */
  kubernetesMetricsPrefix?: string | Computed<string>;
  /** Optional. Location used to report Metrics */
  location?: string | Computed<string>;
  /** Optional. Project used to report Metrics */
  projectId?: string | Computed<string>;
}

export interface V1betaMembership_State {
  /** Output only. The current state of the Membership resource. */
  code?: string | Computed<string>;
}

const V1betaMembership_AuthorityFields: FieldMap = {
  identityProvider: "identity_provider",
  issuer: "issuer",
  oidcJwks: "oidc_jwks",
  scopeTenancyIdentityProvider: "scope_tenancy_identity_provider",
  scopeTenancyWorkloadIdentityPool: "scope_tenancy_workload_identity_pool",
  workloadIdentityPool: "workload_identity_pool",
};

const V1betaMembership_Endpoint_ApplianceClusterFields: FieldMap = {
  resourceLink: "resource_link",
};

const V1betaMembership_Endpoint_GkeClusterFields: FieldMap = {
  clusterMissing: "cluster_missing",
  resourceLink: "resource_link",
};

const V1betaMembership_Endpoint_KubernetesMetadataFields: FieldMap = {
  kubernetesApiServerVersion: "kubernetes_api_server_version",
  memoryMb: "memory_mb",
  nodeCount: "node_count",
  nodeProviderId: "node_provider_id",
  updateTime: "update_time",
  vcpuCount: "vcpu_count",
};

const V1betaMembership_Endpoint_KubernetesResource_ConnectResourcesFields: FieldMap = {
  clusterScoped: "cluster_scoped",
  manifest: "manifest",
};

const V1betaMembership_Endpoint_KubernetesResource_ResourceOptionsFields: FieldMap = {
  connectVersion: "connect_version",
  k8sGitVersion: "k8s_git_version",
  k8sVersion: "k8s_version",
  v1beta1Crd: "v1beta1_crd",
};

const V1betaMembership_Endpoint_KubernetesResourceFields: FieldMap = {
  connectResources: {
    wireName: "connect_resources",
    kind: "list",
    fields: V1betaMembership_Endpoint_KubernetesResource_ConnectResourcesFields,
  },
  membershipCrManifest: "membership_cr_manifest",
  membershipResources: {
    wireName: "membership_resources",
    kind: "list",
    fields: V1betaMembership_Endpoint_KubernetesResource_ConnectResourcesFields,
  },
  resourceOptions: {
    wireName: "resource_options",
    kind: "object",
    fields: V1betaMembership_Endpoint_KubernetesResource_ResourceOptionsFields,
  },
};

const V1betaMembership_Endpoint_OnPremClusterFields: FieldMap = {
  adminCluster: "admin_cluster",
  clusterMissing: "cluster_missing",
  clusterType: "cluster_type",
  resourceLink: "resource_link",
};

const V1betaMembership_EndpointFields: FieldMap = {
  applianceCluster: {
    wireName: "appliance_cluster",
    kind: "object",
    fields: V1betaMembership_Endpoint_ApplianceClusterFields,
  },
  edgeCluster: {
    wireName: "edge_cluster",
    kind: "object",
    fields: V1betaMembership_Endpoint_ApplianceClusterFields,
  },
  gkeCluster: {
    wireName: "gke_cluster",
    kind: "object",
    fields: V1betaMembership_Endpoint_GkeClusterFields,
  },
  googleManaged: "google_managed",
  kubernetesMetadata: {
    wireName: "kubernetes_metadata",
    kind: "object",
    fields: V1betaMembership_Endpoint_KubernetesMetadataFields,
  },
  kubernetesResource: {
    wireName: "kubernetes_resource",
    kind: "object",
    fields: V1betaMembership_Endpoint_KubernetesResourceFields,
  },
  multiCloudCluster: {
    wireName: "multi_cloud_cluster",
    kind: "object",
    fields: V1betaMembership_Endpoint_GkeClusterFields,
  },
  onPremCluster: {
    wireName: "on_prem_cluster",
    kind: "object",
    fields: V1betaMembership_Endpoint_OnPremClusterFields,
  },
};

const V1betaMembership_MonitoringConfigFields: FieldMap = {
  cluster: "cluster",
  clusterHash: "cluster_hash",
  kubernetesMetricsPrefix: "kubernetes_metrics_prefix",
  location: "location",
  projectId: "project_id",
};

const V1betaMembership_StateFields: FieldMap = {
  code: "code",
};

export interface V1betaMembershipConfig {
  /** Authority encodes how Google will recognize identities from this Membership. See the workload identity documentation for more details: https://cloud.google.com/kubernetes-engine/docs/how-to/workload-identity */
  authority?: V1betaMembership_Authority | Computed<V1betaMembership_Authority>;
  /** MembershipEndpoint contains information needed to contact a Kubernetes API, endpoint and any additional Kubernetes metadata. */
  endpoint?: V1betaMembership_Endpoint | Computed<V1betaMembership_Endpoint>;
  /** Optional. An externally-generated and managed ID for this Membership. This ID may be modified after creation, but this is not recommended. The ID must match the regex: `a-zA-Z0-9*` If this Membership represents a Kubernetes cluster, this value should be set to the UID of the `kube-system` namespace object. */
  externalId?: string | Computed<string>;
  /** Optional. Labels for this membership. These labels are not leveraged by multi-cluster features, instead, we prefer cluster labels, which can be set on GKE cluster or other cluster types. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** MonitoringConfig informs Fleet-based applications/services/UIs how the metrics for the underlying cluster is reported to cloud monitoring services. It can be set from empty to non-empty, but can't be mutated directly to prevent accidentally breaking the constinousty of metrics. */
  monitoringConfig?: V1betaMembership_MonitoringConfig | Computed<V1betaMembership_MonitoringConfig>;
  /** MembershipState describes the state of a Membership resource. */
  state?: V1betaMembership_State | Computed<V1betaMembership_State>;
}

export interface V1betaMembershipAttrs {
  /** Authority encodes how Google will recognize identities from this Membership. See the workload identity documentation for more details: https://cloud.google.com/kubernetes-engine/docs/how-to/workload-identity */
  authority: V1betaMembership_Authority;
  /** Output only. The tier of the cluster. */
  clusterTier: string;
  /** Output only. When the Membership was created. */
  createTime: string;
  /** Output only. When the Membership was deleted. */
  deleteTime: string;
  /** Output only. Description of this membership, limited to 63 characters. Must match the regex: `a-zA-Z0-9*` This field is present for legacy purposes. */
  description: string;
  /** MembershipEndpoint contains information needed to contact a Kubernetes API, endpoint and any additional Kubernetes metadata. */
  endpoint: V1betaMembership_Endpoint;
  /** Optional. An externally-generated and managed ID for this Membership. This ID may be modified after creation, but this is not recommended. The ID must match the regex: `a-zA-Z0-9*` If this Membership represents a Kubernetes cluster, this value should be set to the UID of the `kube-system` namespace object. */
  externalId: string;
  /** Optional. Labels for this membership. These labels are not leveraged by multi-cluster features, instead, we prefer cluster labels, which can be set on GKE cluster or other cluster types. */
  labels: Record<string, string>;
  /** Output only. For clusters using Connect, the timestamp of the most recent connection established with Google Cloud. This time is updated every several minutes, not continuously. For clusters that do not use GKE Connect, or that have never connected successfully, this field will be unset. */
  lastConnectionTime: string;
  /** Output only. The type of the membership. */
  membershipType: string;
  /** MonitoringConfig informs Fleet-based applications/services/UIs how the metrics for the underlying cluster is reported to cloud monitoring services. It can be set from empty to non-empty, but can't be mutated directly to prevent accidentally breaking the constinousty of metrics. */
  monitoringConfig: V1betaMembership_MonitoringConfig;
  /** Output only. The full, unique name of this Membership resource in the format `projects/* /locations/* /memberships/{membership_id}`, set during creation. `membership_id` must be a valid RFC 1123 compliant DNS label: 1. At most 63 characters in length 2. It must consist of lower case alphanumeric characters or `-` 3. It must start and end with an alphanumeric character Which can be expressed as the regex: `[a-z0-9]([-a-z0-9]*[a-z0-9])?`, with a maximum length of 63 characters. */
  name: string;
  /** MembershipState describes the state of a Membership resource. */
  state: V1betaMembership_State;
  /** Output only. Google-generated UUID for this resource. This is unique across all Membership resources. If a Membership resource is deleted and another resource with the same name is created, it gets a different unique_id. */
  uniqueId: string;
  /** Output only. When the Membership was last updated. */
  updateTime: string;
}

export const V1betaMembership: ResourceBinding<V1betaMembershipConfig, V1betaMembershipAttrs> = {
  wireType: "google_gkehub_v1beta_membership",
  fields: {
    authority: {
      wireName: "authority",
      kind: "object",
      fields: V1betaMembership_AuthorityFields,
    },
    endpoint: {
      wireName: "endpoint",
      kind: "object",
      fields: V1betaMembership_EndpointFields,
    },
    externalId: "external_id",
    labels: "labels",
    monitoringConfig: {
      wireName: "monitoring_config",
      kind: "object",
      fields: V1betaMembership_MonitoringConfigFields,
    },
    state: {
      wireName: "state",
      kind: "object",
      fields: V1betaMembership_StateFields,
    },
  },
};
