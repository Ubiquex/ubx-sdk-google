// Code generated by `ubx sdk gen` from google_apigateway_v1beta_config. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface V1betaConfig_GatewayConfig_BackendConfig {
  /** Google Cloud IAM service account used to sign OIDC tokens for backends that have authentication configured (https://cloud.google.com/service-infrastructure/docs/service-management/reference/rest/v1/services.configs#backend). This may either be the Service Account's email (i.e. "{ACCOUNT_ID}@{PROJECT}.iam.gserviceaccount.com") or its full resource name (i.e. "projects/{PROJECT}/accounts/{UNIQUE_ID}"). This is most often used when the backend is a GCP resource such as a Cloud Run Service or an IAP-secured service. Note that this token is always sent as an authorization header bearer token. The audience of the OIDC token is configured in the associated Service Config in the BackendRule option (https://github.com/googleapis/googleapis/blob/master/google/api/backend.proto#L125). */
  googleServiceAccount?: string | Computed<string>;
}

export interface V1betaConfig_GatewayConfig {
  /** Configuration for all backends. */
  backendConfig?: V1betaConfig_GatewayConfig_BackendConfig | Computed<V1betaConfig_GatewayConfig_BackendConfig>;
}

export interface V1betaConfig_GrpcServices_FileDescriptorSet {
  contents?: string | Computed<string>;
  path?: string | Computed<string>;
}

export interface V1betaConfig_GrpcServices {
  fileDescriptorSet?: V1betaConfig_GrpcServices_FileDescriptorSet | Computed<V1betaConfig_GrpcServices_FileDescriptorSet>;
  source?: V1betaConfig_GrpcServices_FileDescriptorSet[] | Computed<V1betaConfig_GrpcServices_FileDescriptorSet[]>;
}

export interface V1betaConfig_OpenapiDocuments {
  document?: V1betaConfig_GrpcServices_FileDescriptorSet | Computed<V1betaConfig_GrpcServices_FileDescriptorSet>;
}

const V1betaConfig_GatewayConfig_BackendConfigFields: FieldMap = {
  googleServiceAccount: "google_service_account",
};

const V1betaConfig_GatewayConfigFields: FieldMap = {
  backendConfig: {
    wireName: "backend_config",
    kind: "object",
    fields: V1betaConfig_GatewayConfig_BackendConfigFields,
  },
};

const V1betaConfig_GrpcServices_FileDescriptorSetFields: FieldMap = {
  contents: "contents",
  path: "path",
};

const V1betaConfig_GrpcServicesFields: FieldMap = {
  fileDescriptorSet: {
    wireName: "file_descriptor_set",
    kind: "object",
    fields: V1betaConfig_GrpcServices_FileDescriptorSetFields,
  },
  source: {
    wireName: "source",
    kind: "list",
    fields: V1betaConfig_GrpcServices_FileDescriptorSetFields,
  },
};

const V1betaConfig_OpenapiDocumentsFields: FieldMap = {
  document: {
    wireName: "document",
    kind: "object",
    fields: V1betaConfig_GrpcServices_FileDescriptorSetFields,
  },
};

export interface V1betaConfigConfig {
  /** Optional. Display name. */
  displayName?: string | Computed<string>;
  /** Configuration settings for Gateways. */
  gatewayConfig?: V1betaConfig_GatewayConfig | Computed<V1betaConfig_GatewayConfig>;
  /** Immutable. The Google Cloud IAM Service Account that Gateways serving this config should use to authenticate to other services. This may either be the Service Account's email (`{ACCOUNT_ID}@{PROJECT}.iam.gserviceaccount.com`) or its full resource name (`projects/{PROJECT}/accounts/{UNIQUE_ID}`). This is most often used when the service is a GCP resource such as a Cloud Run Service or an IAP-secured service. */
  gatewayServiceAccount?: string | Computed<string>;
  /** Optional. gRPC service definition files. If specified, openapi_documents must not be included. */
  grpcServices?: V1betaConfig_GrpcServices[] | Computed<V1betaConfig_GrpcServices[]>;
  /** Optional. Resource labels to represent user-provided metadata. Refer to cloud documentation on labels for more details. https://cloud.google.com/compute/docs/labeling-resources */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Optional. Service Configuration files. At least one must be included when using gRPC service definitions. See https://cloud.google.com/endpoints/docs/grpc/grpc-service-config#service_configuration_overview for the expected file contents. If multiple files are specified, the files are merged with the following rules: * All singular scalar fields are merged using "last one wins" semantics in the order of the files uploaded. * Repeated fields are concatenated. * Singular embedded messages are merged using these rules for nested fields. */
  managedServiceConfigs?: V1betaConfig_GrpcServices_FileDescriptorSet[] | Computed<V1betaConfig_GrpcServices_FileDescriptorSet[]>;
  /** Optional. OpenAPI specification documents. If specified, grpc_services and managed_service_configs must not be included. */
  openapiDocuments?: V1betaConfig_OpenapiDocuments[] | Computed<V1betaConfig_OpenapiDocuments[]>;
}

export interface V1betaConfigAttrs {
  /** Output only. Created time. */
  createTime: string;
  /** Optional. Display name. */
  displayName: string;
  /** Configuration settings for Gateways. */
  gatewayConfig: V1betaConfig_GatewayConfig;
  /** Immutable. The Google Cloud IAM Service Account that Gateways serving this config should use to authenticate to other services. This may either be the Service Account's email (`{ACCOUNT_ID}@{PROJECT}.iam.gserviceaccount.com`) or its full resource name (`projects/{PROJECT}/accounts/{UNIQUE_ID}`). This is most often used when the service is a GCP resource such as a Cloud Run Service or an IAP-secured service. */
  gatewayServiceAccount: string;
  /** Optional. gRPC service definition files. If specified, openapi_documents must not be included. */
  grpcServices: V1betaConfig_GrpcServices[];
  /** Optional. Resource labels to represent user-provided metadata. Refer to cloud documentation on labels for more details. https://cloud.google.com/compute/docs/labeling-resources */
  labels: Record<string, string>;
  /** Optional. Service Configuration files. At least one must be included when using gRPC service definitions. See https://cloud.google.com/endpoints/docs/grpc/grpc-service-config#service_configuration_overview for the expected file contents. If multiple files are specified, the files are merged with the following rules: * All singular scalar fields are merged using "last one wins" semantics in the order of the files uploaded. * Repeated fields are concatenated. * Singular embedded messages are merged using these rules for nested fields. */
  managedServiceConfigs: V1betaConfig_GrpcServices_FileDescriptorSet[];
  /** Output only. Resource name of the API Config. Format: projects/{project}/locations/global/apis/{api}/configs/{api_config} */
  name: string;
  /** Optional. OpenAPI specification documents. If specified, grpc_services and managed_service_configs must not be included. */
  openapiDocuments: V1betaConfig_OpenapiDocuments[];
  /** Output only. The ID of the associated Service Config ( https://cloud.google.com/service-infrastructure/docs/glossary#config). */
  serviceConfigId: string;
  /** Output only. State of the API Config. */
  state: string;
  /** Output only. Updated time. */
  updateTime: string;
}

export const V1betaConfig: ResourceBinding<V1betaConfigConfig, V1betaConfigAttrs> = {
  wireType: "google_apigateway_v1beta_config",
  fields: {
    displayName: "display_name",
    gatewayConfig: {
      wireName: "gateway_config",
      kind: "object",
      fields: V1betaConfig_GatewayConfigFields,
    },
    gatewayServiceAccount: "gateway_service_account",
    grpcServices: {
      wireName: "grpc_services",
      kind: "list",
      fields: V1betaConfig_GrpcServicesFields,
    },
    labels: "labels",
    managedServiceConfigs: {
      wireName: "managed_service_configs",
      kind: "list",
      fields: V1betaConfig_GrpcServices_FileDescriptorSetFields,
    },
    openapiDocuments: {
      wireName: "openapi_documents",
      kind: "list",
      fields: V1betaConfig_OpenapiDocumentsFields,
    },
  },
};
