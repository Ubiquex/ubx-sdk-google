// Code generated by `ubx sdk gen` from google_bigquery_bigquery_dataset. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface BigqueryDataset_Access_Condition {
  description?: string | Computed<string>;
  expression?: string | Computed<string>;
  location?: string | Computed<string>;
  title?: string | Computed<string>;
}

export interface BigqueryDataset_Access_Dataset_Dataset {
  datasetId?: string | Computed<string>;
  projectId?: string | Computed<string>;
}

export interface BigqueryDataset_Access_Dataset {
  dataset?: BigqueryDataset_Access_Dataset_Dataset | Computed<BigqueryDataset_Access_Dataset_Dataset>;
  targetTypes?: string[] | Computed<string[]>;
}

export interface BigqueryDataset_Access_Routine {
  datasetId?: string | Computed<string>;
  projectId?: string | Computed<string>;
  routineId?: string | Computed<string>;
}

export interface BigqueryDataset_Access_View {
  datasetId?: string | Computed<string>;
  projectId?: string | Computed<string>;
  tableId?: string | Computed<string>;
}

export interface BigqueryDataset_Access {
  condition?: BigqueryDataset_Access_Condition | Computed<BigqueryDataset_Access_Condition>;
  dataset?: BigqueryDataset_Access_Dataset | Computed<BigqueryDataset_Access_Dataset>;
  domain?: string | Computed<string>;
  groupByEmail?: string | Computed<string>;
  iamMember?: string | Computed<string>;
  role?: string | Computed<string>;
  routine?: BigqueryDataset_Access_Routine | Computed<BigqueryDataset_Access_Routine>;
  specialGroup?: string | Computed<string>;
  userByEmail?: string | Computed<string>;
  view?: BigqueryDataset_Access_View | Computed<BigqueryDataset_Access_View>;
}

export interface BigqueryDataset_DefaultEncryptionConfiguration {
  /** Optional. Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table. The BigQuery Service Account associated with your project requires access to this encryption key. */
  kmsKeyName?: string | Computed<string>;
}

export interface BigqueryDataset_ExternalCatalogDatasetOptions {
  /** Optional. The storage location URI for all tables in the dataset. Equivalent to hive metastore's database locationUri. Maximum length of 1024 characters. */
  defaultStorageLocationUri?: string | Computed<string>;
  /** Optional. A map of key value pairs defining the parameters and properties of the open source schema. Maximum size of 2MiB. */
  parameters?: Record<string, string> | Computed<Record<string, string>>;
}

export interface BigqueryDataset_ExternalDatasetReference {
  /** Required. The connection id that is used to access the external_source. Format: projects/{project_id}/locations/{location_id}/connections/{connection_id} */
  connection?: string | Computed<string>;
  /** Required. External source that backs this dataset. */
  externalSource?: string | Computed<string>;
}

export interface BigqueryDataset_LinkedDatasetMetadata {
  /** Output only. Specifies whether Linked Dataset is currently in a linked state or not. */
  linkState?: string | Computed<string>;
}

export interface BigqueryDataset_LinkedDatasetSource {
  /** Identifier for a dataset. */
  sourceDataset?: BigqueryDataset_Access_Dataset_Dataset | Computed<BigqueryDataset_Access_Dataset_Dataset>;
}

export interface BigqueryDataset_Restrictions {
  /** Output only. Specifies the type of dataset/table restriction. */
  type?: string | Computed<string>;
}

export interface BigqueryDataset_Tags {
  tagKey?: string | Computed<string>;
  tagValue?: string | Computed<string>;
}

const BigqueryDataset_Access_ConditionFields: FieldMap = {
  description: "description",
  expression: "expression",
  location: "location",
  title: "title",
};

const BigqueryDataset_Access_Dataset_DatasetFields: FieldMap = {
  datasetId: "dataset_id",
  projectId: "project_id",
};

const BigqueryDataset_Access_DatasetFields: FieldMap = {
  dataset: {
    wireName: "dataset",
    kind: "object",
    fields: BigqueryDataset_Access_Dataset_DatasetFields,
  },
  targetTypes: "target_types",
};

const BigqueryDataset_Access_RoutineFields: FieldMap = {
  datasetId: "dataset_id",
  projectId: "project_id",
  routineId: "routine_id",
};

const BigqueryDataset_Access_ViewFields: FieldMap = {
  datasetId: "dataset_id",
  projectId: "project_id",
  tableId: "table_id",
};

const BigqueryDataset_AccessFields: FieldMap = {
  condition: {
    wireName: "condition",
    kind: "object",
    fields: BigqueryDataset_Access_ConditionFields,
  },
  dataset: {
    wireName: "dataset",
    kind: "object",
    fields: BigqueryDataset_Access_DatasetFields,
  },
  domain: "domain",
  groupByEmail: "group_by_email",
  iamMember: "iam_member",
  role: "role",
  routine: {
    wireName: "routine",
    kind: "object",
    fields: BigqueryDataset_Access_RoutineFields,
  },
  specialGroup: "special_group",
  userByEmail: "user_by_email",
  view: {
    wireName: "view",
    kind: "object",
    fields: BigqueryDataset_Access_ViewFields,
  },
};

const BigqueryDataset_DefaultEncryptionConfigurationFields: FieldMap = {
  kmsKeyName: "kms_key_name",
};

const BigqueryDataset_ExternalCatalogDatasetOptionsFields: FieldMap = {
  defaultStorageLocationUri: "default_storage_location_uri",
  parameters: "parameters",
};

const BigqueryDataset_ExternalDatasetReferenceFields: FieldMap = {
  connection: "connection",
  externalSource: "external_source",
};

const BigqueryDataset_LinkedDatasetMetadataFields: FieldMap = {
  linkState: "link_state",
};

const BigqueryDataset_LinkedDatasetSourceFields: FieldMap = {
  sourceDataset: {
    wireName: "source_dataset",
    kind: "object",
    fields: BigqueryDataset_Access_Dataset_DatasetFields,
  },
};

const BigqueryDataset_RestrictionsFields: FieldMap = {
  type: "type",
};

const BigqueryDataset_TagsFields: FieldMap = {
  tagKey: "tag_key",
  tagValue: "tag_value",
};

export interface BigqueryDatasetConfig {
  /** Optional. An array of objects that define dataset access for one or more entities. You can set this property when inserting or updating a dataset in order to control who is allowed to access the data. If unspecified at dataset creation time, BigQuery adds default dataset access for the following entities: access.specialGroup: projectReaders; access.role: READER; access.specialGroup: projectWriters; access.role: WRITER; access.specialGroup: projectOwners; access.role: OWNER; access.userByEmail: [dataset creator email]; access.role: OWNER; If you patch a dataset, then this field is overwritten by the patched dataset's access field. To add entities, you must supply the entire existing access array in addition to any new entities that you want to add. */
  access?: BigqueryDataset_Access[] | Computed<BigqueryDataset_Access[]>;
  /** Output only. The origin of the dataset, one of: * (Unset) - Native BigQuery Dataset * BIGLAKE - Dataset is backed by a namespace stored natively in Biglake */
  catalogSource?: string | Computed<string>;
  /** Output only. The time when this dataset was created, in milliseconds since the epoch. */
  creationTime?: string | Computed<string>;
  /** Identifier for a dataset. */
  datasetReference?: BigqueryDataset_Access_Dataset_Dataset | Computed<BigqueryDataset_Access_Dataset_Dataset>;
  /** Optional. Defines the default collation specification of future tables created in the dataset. If a table is created in this dataset without table-level default collation, then the table inherits the dataset default collation, which is applied to the string fields that do not have explicit collation specified. A change to this field affects only tables created afterwards, and does not alter the existing tables. The following values are supported: * 'und:ci': undetermined locale, case insensitive. * '': empty string. Default to case-sensitive behavior. */
  defaultCollation?: string | Computed<string>;
  /** Configuration for Cloud KMS encryption settings. */
  defaultEncryptionConfiguration?: BigqueryDataset_DefaultEncryptionConfiguration | Computed<BigqueryDataset_DefaultEncryptionConfiguration>;
  /** This default partition expiration, expressed in milliseconds. When new time-partitioned tables are created in a dataset where this property is set, the table will inherit this value, propagated as the `TimePartitioning.expirationMs` property on the new table. If you set `TimePartitioning.expirationMs` explicitly when creating a table, the `defaultPartitionExpirationMs` of the containing dataset is ignored. When creating a partitioned table, if `defaultPartitionExpirationMs` is set, the `defaultTableExpirationMs` value is ignored and the table will not be inherit a table expiration deadline. */
  defaultPartitionExpirationMs?: string | Computed<string>;
  /** Optional. Defines the default rounding mode specification of new tables created within this dataset. During table creation, if this field is specified, the table within this dataset will inherit the default rounding mode of the dataset. Setting the default rounding mode on a table overrides this option. Existing tables in the dataset are unaffected. If columns are defined during that table creation, they will immediately inherit the table's default rounding mode, unless otherwise specified. */
  defaultRoundingMode?: string | Computed<string>;
  /** Optional. The default lifetime of all tables in the dataset, in milliseconds. The minimum lifetime value is 3600000 milliseconds (one hour). To clear an existing default expiration with a PATCH request, set to 0. Once this property is set, all newly-created tables in the dataset will have an expirationTime property set to the creation time plus the value in this property, and changing the value will only affect new tables, not existing ones. When the expirationTime for a given table is reached, that table will be deleted automatically. If a table's expirationTime is modified or removed before the table expires, or if you provide an explicit expirationTime when creating a table, that value takes precedence over the default expiration time indicated by this property. */
  defaultTableExpirationMs?: string | Computed<string>;
  /** Optional. A user-friendly description of the dataset. */
  description?: string | Computed<string>;
  /** Output only. A hash of the resource. */
  etag?: string | Computed<string>;
  /** Options defining open source compatible datasets living in the BigQuery catalog. Contains metadata of open source database, schema, or namespace represented by the current dataset. */
  externalCatalogDatasetOptions?: BigqueryDataset_ExternalCatalogDatasetOptions | Computed<BigqueryDataset_ExternalCatalogDatasetOptions>;
  /** Configures the access a dataset defined in an external metadata storage. */
  externalDatasetReference?: BigqueryDataset_ExternalDatasetReference | Computed<BigqueryDataset_ExternalDatasetReference>;
  /** Optional. A descriptive name for the dataset. */
  friendlyName?: string | Computed<string>;
  /** Output only. The fully-qualified unique name of the dataset in the format projectId:datasetId. The dataset name without the project name is given in the datasetId field. When creating a new dataset, leave this field blank, and instead specify the datasetId field. */
  id?: string | Computed<string>;
  /** Optional. TRUE if the dataset and its table names are case-insensitive, otherwise FALSE. By default, this is FALSE, which means the dataset and its table names are case-sensitive. This field does not affect routine references. */
  isCaseInsensitive?: boolean | Computed<boolean>;
  /** Output only. The resource type. */
  kind?: string | Computed<string>;
  /** The labels associated with this dataset. You can use these to organize and group your datasets. You can set this property when inserting or updating a dataset. See [Creating and Updating Dataset Labels](https://cloud.google.com/bigquery/docs/creating-managing-labels#creating_and_updating_dataset_labels) for more information. */
  labels?: Record<string, string> | Computed<Record<string, string>>;
  /** Output only. The date when this dataset was last modified, in milliseconds since the epoch. */
  lastModifiedTime?: string | Computed<string>;
  /** Metadata about the Linked Dataset. */
  linkedDatasetMetadata?: BigqueryDataset_LinkedDatasetMetadata | Computed<BigqueryDataset_LinkedDatasetMetadata>;
  /** A dataset source type which refers to another BigQuery dataset. */
  linkedDatasetSource?: BigqueryDataset_LinkedDatasetSource | Computed<BigqueryDataset_LinkedDatasetSource>;
  /** The geographic location where the dataset should reside. See https://cloud.google.com/bigquery/docs/locations for supported locations. */
  location?: string | Computed<string>;
  /** Optional. Defines the time travel window in hours. The value can be from 48 to 168 hours (2 to 7 days). The default value is 168 hours if this is not set. */
  maxTimeTravelHours?: string | Computed<string>;
  /** Optional. The [tags](https://cloud.google.com/bigquery/docs/tags) attached to this dataset. Tag keys are globally unique. Tag key is expected to be in the namespaced format, for example "123456789012/environment" where 123456789012 is the ID of the parent organization or project resource for this tag key. Tag value is expected to be the short name, for example "Production". See [Tag definitions](https://cloud.google.com/iam/docs/tags-access-control#definitions) for more details. */
  resourceTags?: Record<string, string> | Computed<Record<string, string>>;
  restrictions?: BigqueryDataset_Restrictions | Computed<BigqueryDataset_Restrictions>;
  /** Output only. Reserved for future use. */
  satisfiesPzi?: boolean | Computed<boolean>;
  /** Output only. Reserved for future use. */
  satisfiesPzs?: boolean | Computed<boolean>;
  /** Output only. A URL that can be used to access the resource again. You can use this URL in Get or Update requests to the resource. */
  selfLink?: string | Computed<string>;
  /** Optional. Updates storage_billing_model for the dataset. */
  storageBillingModel?: string | Computed<string>;
  /** Output only. Tags for the dataset. To provide tags as inputs, use the `resourceTags` field. */
  tags?: BigqueryDataset_Tags[] | Computed<BigqueryDataset_Tags[]>;
  /** Output only. Same as `type` in `ListFormatDataset`. The type of the dataset, one of: * DEFAULT - only accessible by owner and authorized accounts, * PUBLIC - accessible by everyone, * LINKED - linked dataset, * EXTERNAL - dataset with definition in external metadata catalog, * BIGLAKE_ICEBERG - a Biglake dataset accessible through the Iceberg API, * BIGLAKE_HIVE - a Biglake dataset accessible through the Hive API. */
  type?: string | Computed<string>;
}

export interface BigqueryDatasetAttrs {
  /** Optional. An array of objects that define dataset access for one or more entities. You can set this property when inserting or updating a dataset in order to control who is allowed to access the data. If unspecified at dataset creation time, BigQuery adds default dataset access for the following entities: access.specialGroup: projectReaders; access.role: READER; access.specialGroup: projectWriters; access.role: WRITER; access.specialGroup: projectOwners; access.role: OWNER; access.userByEmail: [dataset creator email]; access.role: OWNER; If you patch a dataset, then this field is overwritten by the patched dataset's access field. To add entities, you must supply the entire existing access array in addition to any new entities that you want to add. */
  access: BigqueryDataset_Access[];
  /** Output only. The origin of the dataset, one of: * (Unset) - Native BigQuery Dataset * BIGLAKE - Dataset is backed by a namespace stored natively in Biglake */
  catalogSource: string;
  /** Output only. The time when this dataset was created, in milliseconds since the epoch. */
  creationTime: string;
  /** Identifier for a dataset. */
  datasetReference: BigqueryDataset_Access_Dataset_Dataset;
  /** Optional. Defines the default collation specification of future tables created in the dataset. If a table is created in this dataset without table-level default collation, then the table inherits the dataset default collation, which is applied to the string fields that do not have explicit collation specified. A change to this field affects only tables created afterwards, and does not alter the existing tables. The following values are supported: * 'und:ci': undetermined locale, case insensitive. * '': empty string. Default to case-sensitive behavior. */
  defaultCollation: string;
  /** Configuration for Cloud KMS encryption settings. */
  defaultEncryptionConfiguration: BigqueryDataset_DefaultEncryptionConfiguration;
  /** This default partition expiration, expressed in milliseconds. When new time-partitioned tables are created in a dataset where this property is set, the table will inherit this value, propagated as the `TimePartitioning.expirationMs` property on the new table. If you set `TimePartitioning.expirationMs` explicitly when creating a table, the `defaultPartitionExpirationMs` of the containing dataset is ignored. When creating a partitioned table, if `defaultPartitionExpirationMs` is set, the `defaultTableExpirationMs` value is ignored and the table will not be inherit a table expiration deadline. */
  defaultPartitionExpirationMs: string;
  /** Optional. Defines the default rounding mode specification of new tables created within this dataset. During table creation, if this field is specified, the table within this dataset will inherit the default rounding mode of the dataset. Setting the default rounding mode on a table overrides this option. Existing tables in the dataset are unaffected. If columns are defined during that table creation, they will immediately inherit the table's default rounding mode, unless otherwise specified. */
  defaultRoundingMode: string;
  /** Optional. The default lifetime of all tables in the dataset, in milliseconds. The minimum lifetime value is 3600000 milliseconds (one hour). To clear an existing default expiration with a PATCH request, set to 0. Once this property is set, all newly-created tables in the dataset will have an expirationTime property set to the creation time plus the value in this property, and changing the value will only affect new tables, not existing ones. When the expirationTime for a given table is reached, that table will be deleted automatically. If a table's expirationTime is modified or removed before the table expires, or if you provide an explicit expirationTime when creating a table, that value takes precedence over the default expiration time indicated by this property. */
  defaultTableExpirationMs: string;
  /** Optional. A user-friendly description of the dataset. */
  description: string;
  /** Output only. A hash of the resource. */
  etag: string;
  /** Options defining open source compatible datasets living in the BigQuery catalog. Contains metadata of open source database, schema, or namespace represented by the current dataset. */
  externalCatalogDatasetOptions: BigqueryDataset_ExternalCatalogDatasetOptions;
  /** Configures the access a dataset defined in an external metadata storage. */
  externalDatasetReference: BigqueryDataset_ExternalDatasetReference;
  /** Optional. A descriptive name for the dataset. */
  friendlyName: string;
  /** Output only. The fully-qualified unique name of the dataset in the format projectId:datasetId. The dataset name without the project name is given in the datasetId field. When creating a new dataset, leave this field blank, and instead specify the datasetId field. */
  id: string;
  /** Optional. TRUE if the dataset and its table names are case-insensitive, otherwise FALSE. By default, this is FALSE, which means the dataset and its table names are case-sensitive. This field does not affect routine references. */
  isCaseInsensitive: boolean;
  /** Output only. The resource type. */
  kind: string;
  /** The labels associated with this dataset. You can use these to organize and group your datasets. You can set this property when inserting or updating a dataset. See [Creating and Updating Dataset Labels](https://cloud.google.com/bigquery/docs/creating-managing-labels#creating_and_updating_dataset_labels) for more information. */
  labels: Record<string, string>;
  /** Output only. The date when this dataset was last modified, in milliseconds since the epoch. */
  lastModifiedTime: string;
  /** Metadata about the Linked Dataset. */
  linkedDatasetMetadata: BigqueryDataset_LinkedDatasetMetadata;
  /** A dataset source type which refers to another BigQuery dataset. */
  linkedDatasetSource: BigqueryDataset_LinkedDatasetSource;
  /** The geographic location where the dataset should reside. See https://cloud.google.com/bigquery/docs/locations for supported locations. */
  location: string;
  /** Optional. Defines the time travel window in hours. The value can be from 48 to 168 hours (2 to 7 days). The default value is 168 hours if this is not set. */
  maxTimeTravelHours: string;
  /** Optional. The [tags](https://cloud.google.com/bigquery/docs/tags) attached to this dataset. Tag keys are globally unique. Tag key is expected to be in the namespaced format, for example "123456789012/environment" where 123456789012 is the ID of the parent organization or project resource for this tag key. Tag value is expected to be the short name, for example "Production". See [Tag definitions](https://cloud.google.com/iam/docs/tags-access-control#definitions) for more details. */
  resourceTags: Record<string, string>;
  restrictions: BigqueryDataset_Restrictions;
  /** Output only. Reserved for future use. */
  satisfiesPzi: boolean;
  /** Output only. Reserved for future use. */
  satisfiesPzs: boolean;
  /** Output only. A URL that can be used to access the resource again. You can use this URL in Get or Update requests to the resource. */
  selfLink: string;
  /** Optional. Updates storage_billing_model for the dataset. */
  storageBillingModel: string;
  /** Output only. Tags for the dataset. To provide tags as inputs, use the `resourceTags` field. */
  tags: BigqueryDataset_Tags[];
  /** Output only. Same as `type` in `ListFormatDataset`. The type of the dataset, one of: * DEFAULT - only accessible by owner and authorized accounts, * PUBLIC - accessible by everyone, * LINKED - linked dataset, * EXTERNAL - dataset with definition in external metadata catalog, * BIGLAKE_ICEBERG - a Biglake dataset accessible through the Iceberg API, * BIGLAKE_HIVE - a Biglake dataset accessible through the Hive API. */
  type: string;
}

export const BigqueryDataset: ResourceBinding<BigqueryDatasetConfig, BigqueryDatasetAttrs> = {
  wireType: "google_bigquery_bigquery_dataset",
  fields: {
    access: {
      wireName: "access",
      kind: "list",
      fields: BigqueryDataset_AccessFields,
    },
    catalogSource: "catalog_source",
    creationTime: "creation_time",
    datasetReference: {
      wireName: "dataset_reference",
      kind: "object",
      fields: BigqueryDataset_Access_Dataset_DatasetFields,
    },
    defaultCollation: "default_collation",
    defaultEncryptionConfiguration: {
      wireName: "default_encryption_configuration",
      kind: "object",
      fields: BigqueryDataset_DefaultEncryptionConfigurationFields,
    },
    defaultPartitionExpirationMs: "default_partition_expiration_ms",
    defaultRoundingMode: "default_rounding_mode",
    defaultTableExpirationMs: "default_table_expiration_ms",
    description: "description",
    etag: "etag",
    externalCatalogDatasetOptions: {
      wireName: "external_catalog_dataset_options",
      kind: "object",
      fields: BigqueryDataset_ExternalCatalogDatasetOptionsFields,
    },
    externalDatasetReference: {
      wireName: "external_dataset_reference",
      kind: "object",
      fields: BigqueryDataset_ExternalDatasetReferenceFields,
    },
    friendlyName: "friendly_name",
    id: "id",
    isCaseInsensitive: "is_case_insensitive",
    kind: "kind",
    labels: "labels",
    lastModifiedTime: "last_modified_time",
    linkedDatasetMetadata: {
      wireName: "linked_dataset_metadata",
      kind: "object",
      fields: BigqueryDataset_LinkedDatasetMetadataFields,
    },
    linkedDatasetSource: {
      wireName: "linked_dataset_source",
      kind: "object",
      fields: BigqueryDataset_LinkedDatasetSourceFields,
    },
    location: "location",
    maxTimeTravelHours: "max_time_travel_hours",
    resourceTags: "resource_tags",
    restrictions: {
      wireName: "restrictions",
      kind: "object",
      fields: BigqueryDataset_RestrictionsFields,
    },
    satisfiesPzi: "satisfies_pzi",
    satisfiesPzs: "satisfies_pzs",
    selfLink: "self_link",
    storageBillingModel: "storage_billing_model",
    tags: {
      wireName: "tags",
      kind: "list",
      fields: BigqueryDataset_TagsFields,
    },
    type: "type",
  },
};
